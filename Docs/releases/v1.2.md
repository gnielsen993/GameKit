# v1.2

Date: in progress (2026-05-12 → )
Type: minor

## Summary
- Video Mode milestone — optional layout adaptation that keeps GameDrawer
  playable while a floating PiP video sits on screen. User manually picks
  one of six PiP positions (Large top/bottom + 4 corners); every game
  screen reflows accordingly. Design-first build: Phase 8 locked the
  visual language against real screenshots before any code shipped.

## User-facing changes
- **Phase 9 (Video Mode foundation).** Settings → VIDEO MODE card with
  an Off/On toggle (default Off, persisted across launches). When On,
  a "Video location" link reveals a visual iPhone-outline picker — tap
  any of 6 zones (Large top, Large bottom, Small top-left / top-right /
  bottom-left / bottom-right) to select where your video appears on
  screen. The selected zone highlights with the active theme's accent
  color and shows "Your video will go here". A short paragraph below
  the picker explains that GameDrawer can't detect your video
  automatically — the manual pick keeps the board and controls clear.
- No game layout changes in this phase. Mines, Merge, and Nonogram
  render byte-identical to v1.1 (Phase 10–12 introduce per-game
  reflow).
- **Phase 10 (Video Mode layout primitives).** No user-facing change.
  Internal-only — ships the `.videoModeAware()` modifier + slot-router
  helper consumed by Phase 11+. Mines / Merge / Nonogram still render
  byte-identical to v1.1 with Video Mode toggled either Off (default)
  or On.
- **Phase 11 (Minesweeper adoption).** Minesweeper now adopts Video Mode.
  When Video Mode is On and the PiP is positioned at Large-top or
  Large-bottom, the game presents a compact control row at the edge
  opposite the reserved video band: mines-remaining chip on the left,
  centered Reveal/Flag mode pill, timer chip on the right, restart
  button with an embedded difficulty picker menu. Small zones inherit
  the v1.0 layout with toolbar items re-anchored per the PiP location.
  Hard 16×30 fits cleanly via a Video-Mode-aware cell-size floor
  (locked at 12pt after the Phase 8 ADR §8.12 audit on Dracula +
  Voltage). Video Mode Off renders the v1.0 Minesweeper layout
  byte-identical — pinch-zoom, Reveal/Flag toggle, win/loss animations,
  and the HeaderBar all unchanged.
- **Phase 12 (Merge + Nonogram Video Mode adoption).** Both v1.1 games
  now reflow when Video Mode is On. On Large PiP zones (top/bottom),
  their HeaderBar + ModePill + nav-bar toolbar collapse into a single
  compact control row positioned opposite the reserved video band —
  each game's slot closures vary, but the shared `VideoCompactControlRow`
  is consumed verbatim (SC5).
  - **Merge:** Back · Score chip · Win/Infinite picker · Best chip ·
    Restart (with Change-mode submenu). No standalone settings gear
    (the Mode picker covers that role per D-MG-01). Swipe-driven
    merge interaction stays gesture-clean across Large PiP zones
    (Classic + Dracula audited at largeBottom worst-case row).
  - **Nonogram:** Back · Size-or-Lives chip (single-slot conditional
    swap based on game mode — D-NG-01) · Place/Mark picker · Time
    chip · Restart (with Change-size + Change-mode submenus). On
    Large PiP zones, Hard 15×15 renders at a smaller cell-size floor
    (12pt vs the v1.0 14pt) so hint digits + super-cell rules stay
    readable inside the squeezed available area. Off-path is
    byte-identical to v1.1 (14pt floor preserved).
  - Off-restore (Video Mode → Off) returns both games to byte-identical
    v1.1 layouts per VIDEO-13.
  - **Known gap (carryforward from Phase 11):** Small PiP corners
    keep the v1.1 layout but currently do NOT reposition the
    HeaderBar chips or ModePill out from under the PiP overlay —
    on Top L/R the Score/Best (Merge), Size/Lives/Timer (Nonogram),
    and MinesRemaining/Timer (Mines) chips get covered; on Bottom L/R
    the bottom-center ModePill gets covered. `VideoModeSlotRouter`
    returns the correct `anchors.picker` value but no game wires it
    in yet. Tracked for Phase 12.1 gap-closure.

## Internal changes
- **VideoModeStore** (`Core/VideoModeStore.swift`) — @Observable
  @MainActor user-preference store mirroring the Phase 4 SettingsStore
  pattern verbatim; backs `isEnabled` + `location` to UserDefaults.
- **VideoModeLocation** enum (`Core/VideoModeLocation.swift`) — 6-case
  raw-string-stable vocabulary.
- **VideoCompactControlRow** (`Core/VideoCompactControlRow.swift`) —
  generic `@ViewBuilder` shared compact control row component
  (Back | primary info | picker | secondary info | settings); reads
  Phase 8 token anchors (`theme.radii.button`, `theme.spacing.xl`,
  `theme.spacing.s`); #Preview block shows the 3 game slot mappings
  (Mines / Merge / Nonogram) — Phase 11/12 will adopt it.
- **VideoLocationPickerView** (`Screens/VideoMode/`) — visual iPhone-
  outline picker using GeometryReader + 9:19.5 aspect ratio; 6 zones
  laid out per Phase 8 VIDEO-MODE-LAYOUTS.md vocabulary; A11Y per zone
  (label + isButton trait + selection value).
- **GameKitApp.swift** — adds the VideoModeStore to the 5-store
  injection chain (no impact on existing 4 stores).
- **Localizable.xcstrings** — 12 new `videoMode.*` keys including the
  VIDEO-14 verbatim manual-selection copy.
- **Tests** — 7 new test files (14 @Test funcs) covering store round-
  trip, environment injection, Settings binding, picker tap+a11y,
  catalog key existence, and the SC5 off-state contract.
- **VideoModeAware** (`Core/VideoModeAware.swift`) — `ViewModifier`
  conformance shipping the v1.2 layout contract:
  - Off-path hard short-circuit (`if !store.isEnabled { return AnyView(content) }`)
    — byte-identical to un-wrapped view per CONTEXT D-05 / VIDEO-13.
  - Large-PiP band reservation via `.safeAreaInset(edge: .top/.bottom)`
    at `geometry.size.height * 0.32` (measured from
    `Docs/screenshots/v1.2-design/home-classic-pip-large-bottom.png`,
    `private static let largeBandFraction` per D-09 / D-10).
  - Compactness publication via custom `EnvironmentKey`
    (`\.videoModeCompactness` — 3 levels: `.normal` / `.collapsedSettings`
    / `.reducedTime`) computed from available board height vs
    `minBoardHeight` parameter per D-13 / D-14 0.85× threshold.
  - 12 named `#Preview` blocks (6 PiP zones × Classic Chrome Diner +
    Dracula) for SC5 manual legibility audit per D-16.
- **VideoModeSlotRouter** (`Core/VideoModeSlotRouter.swift`) — pure
  Foundation-only helper. `static func anchors(for: VideoModeLocation)
  -> SlotAnchorMap` returns the 4-slot anchor map (back / settings /
  picker / fab) per the locked switch table — data sourced from Phase 8
  `VIDEO-MODE-LAYOUTS.md`. Phase 11/12 game views read this in their
  slot reposition logic (P10 ships zero adoption).
- **Adoption API** — `View.videoModeAware(minBoardHeight: CGFloat = 320)`
  extension. One-line call site for every Phase 11/12 game view per
  CONTEXT D-01 (wraps the game view at the outermost layer; D-15 keeps
  `MinesweeperBoardView`'s `MagnifyGesture` + auto-scale stack
  UNTOUCHED).
- **Tests (Phase 10)** — 2 new test files: `VideoModeAwareTests.swift`
  (4 @Test funcs covering off-state byte-identical + 3 compactness
  levels) + `VideoModeSlotRouterTests.swift` (7 @Test funcs / 25
  #expect covering 24 slot anchors + 1 exhaustiveness loop). RED-gate
  in commits `fcdbafd` / `7e97b7e`; GREEN flip in `c1c5830` (router) +
  `e2bbc18` (modifier).
- **Phase 11-01: Chip extraction.** Carved `MinesRemainingChip` (50
  LOC) and `TimerChip` (77 LOC) out of `MinesweeperHeaderBar` as
  props-only sibling subviews; thinned `MinesweeperHeaderBar` from
  133 to 35 lines. Public init signature preserved — off-path
  call site byte-identical. Round-2 polish later added a
  `compact: Bool = false` parameter for the Video-Mode slot variant.
- **Phase 11-03: Wrap site + layout branch.** Added
  `VideoModeLocation.isLarge` classifier; wrapped Mines
  `NavigationLink` destination in `.videoModeAware(minBoardHeight: 480)`;
  refactored `MinesweeperGameView` body into a three-way layout
  branch (off-path / Small-zone slot-routed / Large-zone compact-row).
  Sibling extension `MinesweeperGameView+VideoMode.swift` hosts the
  Video-Mode-specific helpers so the main view file stays under the
  500-LOC §8.5 cap. `MinesweeperBoardView.swift` SHA unchanged
  (D-15 / D-17 untouched contract).
- **Phase 11-04: Large-zone compact-row composition.** Composed
  `VideoCompactControlRow` for the Large-zone branch with Mines
  chip, centered Reveal/Flag pill, Timer chip, and a restart
  button hosting an embedded difficulty-picker menu. Four rounds
  of user-feedback polish landed inline: (1) drop the standalone
  settings gear and always-collapse the difficulty menu; (2) add
  compact chip + ModePill variants with theme-token sizing; (3)
  split the stacked chip slot into a symmetric two-chip layout with
  the picker center-anchored via Spacers; (4) tighten the picker
  Spacers to pull the side info slots toward the centerline. Phase 8
  D-06 (stacked chip in slot 2) intentionally superseded for Mines;
  Phase 9 D-12 5-slot contract softened to 5-or-4 via nullable
  `onSettings`.
- **Phase 11-05: Hard cell-size floor.** Locked
  `MinesweeperBoardView.minCellSizeVideoMode = 12pt` after the
  Phase 8 ADR §8.12 audit on Dracula + Voltage. Single-gate on
  `videoModeStore.isEnabled` per CONTEXT D-12 (no location.isLarge,
  no difficulty conditioning); off-path 18pt floor preserved
  verbatim. D-17 byte-identity verified via grep-vs-git-HEAD on
  the `MagnifyGesture` / `.scaleEffect` / `clampZoomScale` /
  `LongPressGesture` lines. ADR §Rollback did NOT fire.
- **Phase 11-06: A2 NavStack carry-forward.** Empirical measurement
  on iPhone 17 Pro Max confirmed Hard 16×30 fits inside the real
  NavigationStack chrome at the locked 12pt floor on both
  `largeTop` and `largeBottom` — no `safeAreaInsets.top` widening
  needed. P10 `10-VERIFICATION.md` A2 carry-forward CLOSED with
  zero code change in `VideoModeAware.swift`.
- **Phase 11-07: Manual verification matrix.** Authored
  `.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` —
  18-row matrix (3 difficulties × 6 PiP zones) with locked-floor
  references for the Hard rows. Filled in Phase 11-08 with row 14
  PASS (verified during execution) and 17 rows DEFERRED to the
  TestFlight sweep.
- **Phase 12-01: Merge chip extraction + shared timer chip rename.**
  `MergeScoreChip` + `MergeBestChip` extracted from `MergeHeaderBar`
  as props-only sibling subviews with `compact: Bool = false` API
  (off-path defaults to false → byte-identical to v1.1 inline chips).
  `Games/Minesweeper/TimerChip.swift` RENAMED to
  `Core/VideoModeTimerChip.swift` so all 3 games can consume the same
  primitive without duplicating the TimelineView freeze invariant.
  Mines's 2 call sites (HeaderBar + GameView+VideoMode.compactRowComposed)
  updated; off-path render byte-identical. See
  `.planning/phases/12-merge-nonogram-adoption/12-01-SUMMARY.md`.
- **Phase 12-02: MergeGameView three-way layout branch.** Body now
  reads `@Environment(\.videoModeStore)` + `@Environment(\.videoModeCompactness)`.
  Off path: v1.1 verbatim. Large path: `VideoCompactControlRow` with
  D-MG-01 slot order (Back | MergeScoreChip | Win/Infinite picker |
  MergeBestChip | Restart-w-overflow-menu); HeaderBar + ModePill + toolbar
  hidden. Small path: existing layout + toolbar items repositioned per
  `VideoModeSlotRouter.anchors(for:)`. `MergeModePill` gained
  `compact: Bool = false` API. `MergeBoardView.swift` SHA UNCHANGED
  (D-MG-17 untouched contract verified by grep against git HEAD).
  Sibling extension `MergeGameView+VideoMode.swift` created to keep
  host under §8.5 cap. HomeView Merge destination wrapped with
  `.videoModeAware(minBoardHeight: 480)`. See
  `.planning/phases/12-merge-nonogram-adoption/12-02-SUMMARY.md`.
- **Phase 12-03: Nonogram chip extraction.** `NonogramSizeChip` +
  `NonogramLivesChip` extracted from `NonogramHeaderBar` as props-only
  sibling subviews with `compact: Bool = false` API. HeaderBar
  refactored to consume the new chips + the shared `VideoModeTimerChip`
  (created in Plan 12-01) with `compact: false` defaults → off-path
  byte-identical. HeaderBar shrank from 128 → ~40 lines. See
  `.planning/phases/12-merge-nonogram-adoption/12-03-SUMMARY.md`.
- **Phase 12-04: NonogramGameView three-way layout branch.** Same
  shape as Merge's, adapted for D-NG-01: slot 2 hosts a single-slot
  conditional swap between `NonogramSizeChip` (Free mode) and
  `NonogramLivesChip` (Lives mode) — NOT a stacked composite (D-06
  supersession preserved). `restartWithOverflowMenu` folds BOTH
  Change-size + Change-mode submenus. `NonogramModePill` gained
  `compact: Bool = false` API. Sibling extension
  `NonogramGameView+VideoMode.swift` MANDATORY because GameView was
  already at 281 LOC pre-refactor. HomeView Nonogram destination
  wrapped with `.videoModeAware(minBoardHeight: 480)`. See
  `.planning/phases/12-merge-nonogram-adoption/12-04-SUMMARY.md`.
- **Phase 12-05: NonogramBoardView Video-Mode-aware floor seam.**
  New `static let minCellSizeVideoMode: CGFloat = 12` +
  `static func minCellSize(videoModeOn:) -> CGFloat` helper. Single
  gate (`videoModeStore.isEnabled`) per CONTEXT D-NG-15 verbatim — no
  `location.isLarge` or `difficulty.*` conditioning. `slideGesture` /
  `superCellRules` / hint geometry constants / drag state names
  byte-identical (D-NG-17 untouched contract verified via grep against
  git HEAD). Audit-locked at 12pt on Dracula + Voltage per CLAUDE.md
  §8.12. See `.planning/phases/12-merge-nonogram-adoption/12-05-SUMMARY.md`.
- **Phase 12-06: Phase close + manual verification matrix.** Authored
  `.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md`
  — 24-row matrix (2 games × 2 representative difficulties × 6 PiP
  zones per D-12-MATRIX). Row 2 (Merge × winMode × largeBottom) and
  Row 20 (Nonogram × Hard 15×15 × largeBottom) verified during
  execution; 22 other rows DEFERRED to TestFlight per P11 precedent.
  Top-level status: PARTIAL — SC2 + SC4 + SC5 PASS; SC1 + SC3 FAIL
  on small-zone routing (P11 carryforward defect, see Risks/notes).

## Fixes
- (none — this is a feature phase)

## Risks / notes
- VideoMode is **Off by default** and no game view reads the store in
  Phase 9, so the off-path is automatic byte-equivalence with v1.1
  (CONTEXT D-15). Phase 11/12 introduce the actual reads.
- Theme audit on the picker sub-screen confirmed legible on Classic
  (Chrome Diner) + at least one Loud preset (Voltage or Dracula) per
  CLAUDE.md §8.12. The selected-zone fill uses
  `theme.colors.accentPrimary.opacity(0.25)` with the "Your video will
  go here" label set to `theme.colors.textPrimary` — pre-empts the
  accent-on-accent legibility failure mode flagged in Phase 9
  research.
- MARKETING_VERSION remains 1.1; bumped to 1.2 in a later Phase 13
  ship plan.
- **`AnyView` short-circuit cost (off-path, Phase 10)** — accepted per
  CONTEXT D-05. Toggling Video Mode on→off triggers a clean SwiftUI
  rebuild of the wrapped game view (no leftover insets, no env values).
  If Phase 11 reveals a state-loss regression (e.g., timer reset on
  mid-game toggle), the remediation is to promote state UP one level
  above the modifier, NOT to eliminate the AnyView short-circuit (the
  byte-identical-off-restore guarantee is the SC3 contract).
- **Hard-Mines `MagnifyGesture` UNTOUCHED (CONTEXT D-15, Phase 10)** —
  Phase 10 wraps `MinesweeperGameView` (not `MinesweeperBoardView`) in
  Phase 11 adoption. The 06.1-03 / A11Y-05 `MagnifyGesture` + auto-scale
  `cellSize(forWidth:...)` stack stays byte-identical. The only
  Video-Mode-aware seam in Mines is `MinesweeperBoardView.Self.minCellSize`,
  gated on `videoModeStore.isEnabled` in Phase 11 per
  `08-HARD-MINES-ADR.md` (smaller-cells / Variant 1).
- **Screenshot README device-label drift (Pitfall 5, Phase 10)** —
  `Docs/screenshots/v1.2-design/README.md` claims iPhone 17 Pro Max but
  pixel dimensions are 17 Pro / 16 Pro (1179×2556px = 393×852pt). The
  `largeBandFraction = 0.32` measurement is device-portable (it's a
  fraction of `geometry.size.height`), so this has no code impact — but
  the README annotation can be corrected in a future docs commit.
- **SC5 audit-helper iteration (Phase 10)** — Plan 10-03 initial preview
  helpers had two audit blockers (Dracula resolved in light scheme; no
  visual PiP indicator on Small zones). Fixed in Plan 10-04 follow-up
  (commit `9bc50ab`) — preview-only changes, no production logic
  touched. Pattern (translucent corner box / edge band labelled "PiP")
  is reusable for the Phase 13 win/loss banner audit.
- **Phase 11 — design-doc divergences (intentional, user-feedback driven).**
  Phase 8 D-06 (stacked chip in slot 2) and Phase 9 D-12 (5-slot row
  contract) were both superseded for Mines after the user reviewed
  the rendered Large-bottom Hard layout on Voltage. Mines now uses
  a symmetric two-chip layout (Mines-left, Time-right) with a
  center-anchored Reveal/Flag pill and no standalone settings gear
  (difficulty picker folded into the restart button). The shared
  `VideoCompactControlRow` was extended with a nullable `onSettings`
  closure and center-anchoring spacers — backward-compatible for
  Merge / Nonogram Phase 12 adoption, which can still pass a non-nil
  closure to keep slot 6, or inherit the same symmetric shape.
- **Phase 11 — Phase 8 D-05 slot order still holds at slot identity.**
  Mines and Timer chips are both present; they're no longer stacked.
  The slot 4+5 collapse (difficulty menu into restart) is now
  unconditional on Large zones (no threshold reaction).
- **Phase 11 — D-17 / off-path byte-identity preserved.**
  `MinesweeperBoardView.swift` SHA unchanged across the phase
  (only the `minCellSize` floor lookup gained a Video-Mode-aware seam
  in Plan 11-05; gesture/scale/clamp/long-press lines are
  byte-identical via grep-vs-git-HEAD).
  `MinesweeperHeaderBar.swift` SHA also unchanged across the polish
  rounds — the new `compact: Bool = false` chip parameter defaults
  preserve the off-path call shape.
- **Phase 11 — manual sweep DEFERRED to TestFlight.**
  `11-VIDEO-MANUAL-CHECK.md` row 14 (Hard × largeBottom × Voltage)
  PASS during execution; 17 other rows DEFERRED. SC1/SC3/SC4 status
  is PARTIAL; SC5 (off-restore byte-identity) is DEFERRED but
  guaranteed by acceptance criteria. Full 18-row coverage runs in the
  v1.0 carry-over screenshot sweep (PF-06).
- **Phase 12 — Nonogram cell-size floor lock (Plan 12-05).** Hard 15×15
  renders at `minCellSizeVideoMode = 12pt` when Video Mode is On (vs
  v1.0 `minCellSize = 14pt`). Locked 2026-05-13 after auditing
  10/11/12/13/14pt on Dracula + Voltage per CLAUDE.md §8.12 (hint
  digits + super-cell rules + fill/X marks). Rollback condition per
  CONTEXT D-NG-15 line 92: if hint legibility regresses on Dracula
  post-TestFlight, fall back to `existingLayout` on Large zones
  (modify Plan 12-04's `largeZoneLayout` to render the existing
  HeaderBar + Board + ModePill layout with the compact row hidden,
  instead of dropping the floor below the audit-locked value). Merge
  does NOT use a floor seam (D-MG-17) — the 4×4 grid fits at any
  reasonable cell size on any PiP zone.
- **Phase 12 — D-MG-17 / D-NG-17 off-path byte-identity preserved.**
  `MergeBoardView.swift` and `NonogramBoardView.swift`'s gesture /
  scale / drag-state lines all byte-identical via grep-vs-git-HEAD
  (only NonogramBoardView gained the `minCellSizeVideoMode` constant
  + `minCellSize(videoModeOn:)` helper in Plan 12-05; existing
  `minCellSize = 14` preserved verbatim for off-path).
- **Phase 12 — small-zone picker + chip routing gap (P11 carryforward).**
  Surfaced by Plan 12-06's small-zone audit: `VideoModeSlotRouter.anchors(for:)`
  returns the correct `anchors.picker` value for all 6 zones, but none
  of the 3 adopter games (Mines, Merge, Nonogram) wire it into
  `existingLayout` on the Small-zone branch. Result:
  - On Bottom L/R PiP zones, the bottom-center ModePill (Reveal/Flag
    in Mines, Win/Infinite in Merge, Place/Mark in Nonogram) gets
    covered by the Small PiP overlay.
  - On Top L/R PiP zones, the top-center HeaderBar chips (MinesRemaining
    + Timer in Mines, Score + Best in Merge, Size/Lives + Timer in
    Nonogram) get covered by the Small PiP overlay.

  SC1 (Merge 6-zone playability) and SC3 (legibility across both
  games × 6 zones) consequently fail on 4 of 6 zones per game (the
  Small L/R corners; Small zones top/bottom unaffected if there's
  no top-or-bottom-center conflict, but in practice all 4 small
  corners are affected). SC2 (Nonogram Hard hint legibility in Large
  zones), SC4 (Off-restore byte-identity), and SC5 (compact-row
  consumed verbatim) all PASS — those operate on Large zones, Off-path,
  or shared-component identity respectively.

  This gap existed in Phase 11 (Mines) but was not caught at 11-08
  sign-off because the P11 matrix's worst-case row (Hard × largeBottom)
  was a Large-zone row that did not exercise the Small-zone branch.
  12-06's tighter audit caught it across all 3 games at once.

  **Gap-closure plan:** Phase 12.1 (via `/gsd-plan-phase 12.1 --gaps`)
  authors the Small-zone reposition seam for ModePill + HeaderBar
  chips across all 3 adopter games and re-audits the 4 small-zone
  rows per game. Phase 12 does not close until 12.1 lands; v1.2 cannot
  ship without 12.1.
- **Phase 12 — MARKETING_VERSION remains 1.1.** Bump deferred to
  Phase 13 / Phase 12.1 ship plan per CONTEXT line 141 lock.
- **Phase 12.1 (Small-Zone Routing Gap Closure).** Closes the SC1 + SC3
  gap surfaced by 12-06's audit. Small-zone layout is now zone-aware:
  Top L/R uses the v1.1 layout shape rendered compact (chips packed
  opposite the covered PiP corner, board fills middle, ModePill below);
  Bot L/R uses a Spacer-centered chrome cluster (chips above picker)
  anchored in the bottom corner opposite the covered PiP — board fills
  the upper area. Mines difficulty toolbar menu drops its inline
  "Easy/Medium/Hard" text and renders icon-only for Small zones so the
  toolbar fits next to the PiP corner. Merge drops its body ModePill on
  Bot zones (the toolbar menu already covers Win/Infinite — mode is
  settings-ish, changed infrequently); Nonogram keeps the picker
  full-size at top (frequently used Fill/Mark tap target) but compact
  in the Bot chrome cluster to match Mines's pill size. Off-path and
  Large-zone path render byte-identical to pre-12.1.
- **Phase 12.1 — MARKETING_VERSION remains 1.1.** Bump still deferred
  to v1.2 ship.

## Internal changes (12.1)
- `VideoModeSlotRouter.SlotAnchorMap` extended with `headerBar: SlotAnchor`
  field (Plan 12.1-01) — compile-time exhaustive routing of the chip
  cluster anchor, mirroring `picker` / `back` / `settings`.
- `VideoModeLocation.isTopSmall` boolean partition added so adopter
  body branches can route the 4 Small zones into Top L/R vs Bot L/R
  layout variants.
- New sibling files: `MinesweeperGameView+SmallZone.swift` (216 LOC) +
  `NonogramGameView+SmallZone.swift` (170 LOC) — keeps host
  `+VideoMode.swift` files under CLAUDE.md §8.5 ≤500-line cap.
- `MinesweeperToolbarMenu` gains `compact: Bool = false` — icon-only
  variant used in Small-zone toolbar.

## Internal changes (13)
- Plan 13-01 lands the shared `VideoModeBanner` primitive in `Core/` —
  3-game-consumer threshold met (Mines / Merge / Nonogram) per CLAUDE
  §2. Foundation-only `VideoModeBannerAnchor.swift` exposes
  `VideoModeBannerEdge` + `VideoModeBannerAlignment` +
  `VideoModeBannerRouter.anchor(for:)` with an exhaustive 6-zone
  switch encoding the 08-BANNER-PLACEMENT D-09 anchor table.
  `VideoModeBannerContent.swift` ships the C-02 LOCKED PoD struct
  (title + subtitle + primary CTA + a11y label + outcome). The
  SwiftUI banner view honors D-13-HAPTICS (FIRST-guard
  `hapticsEnabled` mirrors v1.0 05-03 D-10), D-13-ANIM
  (`.transition` collapses to `.identity` on Reduce Motion /
  animationsEnabled=off), and the D-11 single-`DKButton`
  tap-anywhere-FORBIDDEN lock. Wave 2 (13-02/03/04) wires per-game
  adoption.
- **Phase 13 Wave 2 (per-game banner adoption, 2026-05-14).** All 3
  games (Minesweeper / Merge / Nonogram) now render the shared
  `VideoModeBanner` on win/loss instead of the v1.x full-screen
  `EndStateCard`. The banner is a centered card with a 2-1 button
  layout: top row = primary CTA + game-specific change-X
  (Mines "Change difficulty" / Merge "Restart" on win / Nonogram
  "Change size"); below = a low-weight "View board" button that
  dismisses the banner so the player can inspect the final board
  (revealed mines, completed 2048 tile chain, solved Nonogram
  picture) for as long as they want. The banner ships in BOTH
  Video Mode On AND Off (user override during execution —
  deprecates the v1.x full-screen EndStateCard from the render
  path; card files retained on disk for reference / rollback).
  Per-game files: `MinesweeperGameView+EndBanner.swift`,
  `MergeGameView+EndBanner.swift`, `NonogramGameView+EndBanner.swift`.
- **Phase 13 design deviations from 08-BANNER-PLACEMENT.md** —
  the original "edge-docked pill opposite PiP corner" placement
  spec was overridden during execution per live-sim user feedback.
  The banner is now CENTERED in all 6 PiP zones (the 6-row D-09
  anchor table is no longer consumed for placement; the
  `VideoModeBannerRouter` helper + 2-zone unit tests remain on
  disk as forward-compat infrastructure). The single-action D-02
  lock was relaxed to support the 2-1 button layout. The
  off-path byte-identity D-13-OFFPATH lock was relaxed to
  unify the win/loss surface across all modes.

## Internal changes (16)
- **Phase 16 — Sudoku stats integration.** New `SudokuStatsCard`
  surfaces per-difficulty Games / Wins / Avg / Best in `StatsView`
  (4th game-stats panel; section order matches drawer order). Card
  is data-driven props-only per §8.2 — reads from shared
  `GameRecord` + `BestTime` SwiftData models, no new model. New
  `GameStats.wonPuzzleIDs(gameKind:difficulty:)` adds the first
  read-side method to `GameStats` (write-only until now), backing
  the `SudokuPuzzlePool`'s played-puzzle skip logic via the
  ViewModel — Phase 15's TODO stub replaced. No SwiftData schema
  bump; adding the `.sudoku` GameKind rawValue is additive per §1.
  Tests: `SudokuStatsIntegrationTests` (5 cases) cover record-on-win,
  no-best-on-loss, faster-only best updates, per-difficulty filter,
  and game-kind isolation — all green.

## Internal changes (15)
- **Phase 15 — Sudoku game vertical slice (user-visible).** Adds
  Sudoku as the 4th drawer game. New `Games/Sudoku/` folder ships
  ~17 source files mirroring the Nonogram pattern: pure-model layer
  (Board, Cell, Difficulty, GameMode, GameState, InteractionMode,
  UndoSnapshot, PuzzleEntry, PuzzlePack, PuzzlePool actor), the
  `@Observable @MainActor` `SudokuViewModel` orchestrator, and the
  SwiftUI layer (BoardView, CellView, NumberPad, HeaderBar, LivesChip,
  ModePill, EndStateCard, ToolbarMenu, GameView + VideoMode extension).
  Game runs end-to-end: 9×9 grid with thick box borders, peer +
  same-number + selected highlights, pencil notes mode (auto-clear on
  value commit), single-step undo, 1-second timer, both `.free` and
  `.lives` modes (3 mistakes → game over). Win/loss recorded via
  existing `GameStats.record(gameKind: .sudoku, ...)` — no new
  SwiftData model. Drawer wired: `GameKind.sudoku` + `GameRoute.sudoku` +
  `GameDescriptor` entry + `HomeView.destination(for:)` arm. Theme
  audited under Classic + Voltage + Dracula presets per CLAUDE §8.12.

## Internal changes (14)
- **Phase 14 — Sudoku engine vendored (infrastructure-only).**
  Imported the `cxnielsen/sudokuplus` SudokuCore engine at SHA
  `b02c848` as a local Swift Package under `Packages/SudokuCore/`.
  Engine sources lifted verbatim from upstream; SQLite-backed
  puzzle repository dropped entirely (GameKit ships puzzles from
  a bundled JSON pack via the runtime `SudokuPuzzlePool` to land
  in Phase 15 — no on-device DB). New `InMemoryPuzzleRepository`
  actor added (~85 lines) for the offline pack-generation
  use case (dedup-by-hash only; other `PuzzleRepository` methods
  throw `notSupported`). Upstream's `SeedGenerator` executable
  dropped — we ship our own `tools/GenerateSudokuPack` Swift CLI
  with `--per-difficulty`, `--difficulties`, `--output`,
  `--append` (resumable), and `--time-budget` (clean exit for
  batched generation). Placeholder pack of 40 puzzles (10 per
  difficulty) shipped at `Resources/SudokuPuzzles.json` so the
  app target has a real bundle resource to load; the full
  1500-per-difficulty pack is generated in Phase 17 by repeated
  `--append --time-budget 20` runs of the CLI. Vendored test
  suite green (38 tests run, 7 skipped — SQLite-specific
  behaviors documented in `Packages/SudokuCore/README.md`).
  SudokuCore wired as a local SPM dependency on the gamekit
  app target; `xcodebuild build` succeeds. No user-facing
  change yet — Sudoku does not appear in The Drawer until
  Phase 15 (game vertical slice).

## QA checklist
- [ ] Core user flow sanity checked
- [ ] Crash-free startup on clean install
- [ ] Theme/preset spot-check (Classic + one Loud/Moody preset per CLAUDE §8.12)
