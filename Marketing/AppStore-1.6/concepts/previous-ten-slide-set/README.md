# Preserved previous selection

[Open all twenty prior iPhone/iPad exports](index.html). Archived before selecting the two separate Sudoku Video Mode screenshots for the current App Store upload sequence.

The PNGs are unchanged, checked against the copied `manifest.json` export hashes. `slides.js` records their original order and copy. Original native sources remain in the campaign's `../../raw/` directory; the historical manifest's source paths are relative to that campaign root. Full reproduction of this previous composition is also preserved in commit `6db407c`.

Use these as alternates. The current numbered upload folders and selected sequence are [two levels up](../../index.html).
