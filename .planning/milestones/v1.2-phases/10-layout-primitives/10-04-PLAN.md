---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-02, 10-03]
files_modified:
  - Docs/releases/v1.2.md
  - .planning/phases/10-layout-primitives/10-VERIFICATION.md
autonomous: false
requirements: [VIDEO-05, VIDEO-06, VIDEO-13]
tags: [verification, theme-audit, release-log, manual-checkpoint, video-mode]

must_haves:
  truths:
    - "All 5 Phase 10 Success Criteria signed off (SC1 small-router 24 anchors, SC2 large-band reservation, SC3 off-restore byte-identical, SC4 adoption API compile, SC5 #Preview legibility audit on Classic + Dracula across 6 zones)."
    - "12-tile #Preview matrix in VideoModeAware.swift visually verified in Xcode canvas: chips legible, picker non-clipped, no covered corner on Small zones, board within reserved band on Large zones — per CLAUDE.md §8.12."
    - "Docs/releases/v1.2.md has a Phase 10 entry under the existing 'User-facing changes' / 'Internal changes' / 'Risks / notes' sections per CLAUDE.md §8.14."
  artifacts:
    - path: ".planning/phases/10-layout-primitives/10-VERIFICATION.md"
      provides: "Manual SC sign-off doc — 5 SCs checked, theme-audit findings, A6 fallback notes from 10-03, any Plan 11 carry-forward items (e.g. A2 NavigationStack height)"
      min_lines: 60
    - path: "Docs/releases/v1.2.md"
      provides: "Phase 10 entry appended under existing v1.2 sections — what shipped (.videoModeAware modifier + VideoModeSlotRouter helper + #Preview matrix), what did NOT ship (zero per-game adoption)"
      min_lines: 90
  key_links:
    - from: "10-VERIFICATION.md"
      to: "All 3 v1.2 requirements VIDEO-05 / VIDEO-06 / VIDEO-13"
      via: "explicit per-SC sign-off checklist + automated test result references"
      pattern: "VIDEO-05|VIDEO-06|VIDEO-13"
    - from: "Docs/releases/v1.2.md"
      to: "Plan 10-02 + Plan 10-03 ship artifacts"
      via: "Internal changes section bullets citing each new file"
      pattern: "VideoModeAware\\.swift|VideoModeSlotRouter\\.swift"
---

<objective>
Wave 2 — close the phase. Two artifacts:

1. **Manual SC sign-off** in `10-VERIFICATION.md` — SC1 / SC2 / SC3 / SC4 mapped
   to automated test results from Plans 10-02 + 10-03 (pull from those SUMMARYs);
   SC5 covered by a 12-tile `#Preview` legibility audit in Xcode canvas (manual
   visual sign-off per CLAUDE.md §8.12).

2. **Release log entry** in `Docs/releases/v1.2.md` — append a Phase 10 entry
   under the existing v1.2 milestone sections per CLAUDE.md §8.14. Note that
   no per-game adoption ships in this phase (that's P11/P12).

This plan has 1 checkpoint task (visual audit) — `autonomous: false`. The
release-log task is fully autonomous.

Output:
- `.planning/phases/10-layout-primitives/10-VERIFICATION.md` (new doc with sign-off + theme audit table)
- `Docs/releases/v1.2.md` (existing — append Phase 10 entry; do NOT rewrite Phase 9 content)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/10-layout-primitives/10-CONTEXT.md
@.planning/phases/10-layout-primitives/10-RESEARCH.md
@.planning/phases/10-layout-primitives/10-VALIDATION.md
@.planning/phases/10-layout-primitives/10-01-SUMMARY.md
@.planning/phases/10-layout-primitives/10-02-SUMMARY.md
@.planning/phases/10-layout-primitives/10-03-SUMMARY.md
@Docs/releases/v1.2.md

<analogs>
<!-- Pattern source for the verification doc + release-log entry shape. -->

Verification doc pattern source:
.planning/phases/09-video-mode-foundation/ (search for *VERIFICATION.md if exists;
otherwise mirror the SC-by-SC table format from ROADMAP.md Phase 10 Success Criteria
section)

Release-log append pattern source:
Docs/releases/v1.2.md (lines 1-72 — currently contains the Phase 9 entry; append
a new "Phase 10" section under "User-facing changes" / "Internal changes" /
"Risks / notes"; do NOT mutate Phase 9 content per CLAUDE.md §0.3 immutability
rule for shipped sections)
</analogs>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: SC5 #Preview legibility audit (Classic + Dracula across 6 zones)</name>
  <read_first>
    - gamekit/gamekit/Core/VideoModeAware.swift (the 12 #Preview blocks shipped by Plan 10-03)
    - .planning/phases/10-layout-primitives/10-CONTEXT.md (D-16 — #Preview-only SC5 stub; no DEBUG screen)
    - .planning/phases/10-layout-primitives/10-VALIDATION.md (manual SC5 row — "Open VideoModeAware.swift in Xcode → Canvas → step through all 12 named previews")
    - CLAUDE.md (§8.12 — Classic + 1 Loud preset visual audit gate for game-screen changes)
  </read_first>
  <what-built>
    Plan 10-03 shipped 12 named `#Preview` blocks in `VideoModeAware.swift`:
    - "Classic — Large top", "Classic — Large bottom", "Classic — Small TL", "Classic — Small TR", "Classic — Small BL", "Classic — Small BR"
    - "Dracula — Large top", "Dracula — Large bottom", "Dracula — Small TL", "Dracula — Small TR", "Dracula — Small BL", "Dracula — Small BR"

    Each tile renders a `StubGameContent` (board placeholder + `VideoCompactControlRow`)
    wrapped with `.videoModeAware(minBoardHeight: 480)` with the corresponding
    `VideoModeStore` zone + preset injected via env.
  </what-built>
  <how-to-verify>
    **Step 1 — Open the file in Xcode:**

    Open `gamekit/gamekit/gamekit.xcodeproj` in Xcode. In the project navigator,
    open `gamekit/Core/VideoModeAware.swift`. Open the Canvas (Editor → Canvas, or
    Option-Cmd-Return).

    **Step 2 — Render every preview tile (12 total):**

    Xcode shows a list of named previews in the canvas pane. Click each named
    preview entry and let it render (resume/pin if needed). Confirm none report
    "no preview available" or a runtime error.

    If a tile fails: confirm whether Plan 10-03 applied the A6 fallback
    (static-shared `StubStores` pattern from 10-03-SUMMARY.md). If not yet
    applied AND tiles fail with "VideoModeStore construction error" or similar,
    surface the failure to the user — Plan 10-03 needs a follow-up edit
    applying the documented fallback. Do NOT silently work around it here.

    **Step 3 — Per-tile legibility check (CLAUDE.md §8.12):**

    For each tile, confirm visually:

    | Property | Pass criterion |
    |----------|----------------|
    | **Large top tile (both presets)** | Board placeholder fills middle/bottom; reserved band visible at top occupying ~32% of tile height; VideoCompactControlRow visible at bottom; "primary"/"picker"/"secondary" labels legible |
    | **Large bottom tile (both presets)** | Board placeholder fills top/middle; reserved band visible at bottom occupying ~32% of tile height; VideoCompactControlRow visible at bottom (above the band's reserved space); labels legible |
    | **Small TL / TR / BL / BR tiles (both presets)** | No reserved band visible; board placeholder fills the entire content area above the compact row; VideoCompactControlRow visible at bottom; labels legible. The small-zone routing is the GAME VIEW's concern in P11 — this audit only confirms the modifier passes through unchanged. |
    | **Dracula contrast (all 6 zones)** | Background is dark; the "primary"/"picker"/"secondary" labels render in `theme.colors.textPrimary` against `theme.colors.background` — confirm ≥ WCAG AA contrast by eye (no near-invisible text) |
    | **Classic Chrome Diner (all 6 zones)** | Cream background + brushed-grey row surfaces + diner-red accent — confirm the same labels remain legible on the lighter palette |

    **Step 4 — Sign off in `10-VERIFICATION.md`:**

    Create `.planning/phases/10-layout-primitives/10-VERIFICATION.md` with the
    template below (subsequent Task 2 will append the SC1/SC2/SC3/SC4
    automated-test references; this task fills in the SC5 row + any preset/zone
    issues found).

    ```markdown
    # Phase 10 — Verification

    **Verified:** <YYYY-MM-DD>
    **Verifier:** <Gabe / claude-with-checkpoint>
    **Phase artifacts:**
    - `gamekit/gamekit/Core/VideoModeAware.swift` (Plan 10-03)
    - `gamekit/gamekit/Core/VideoModeSlotRouter.swift` (Plan 10-02)
    - `gamekit/gamekitTests/Core/VideoModeAwareTests.swift` (Plan 10-01)
    - `gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift` (Plan 10-01)

    ## SC1 — Small-PiP slot reposition (VIDEO-05)
    Test result: 7/7 @Test funcs GREEN (24 anchor + 1 count #expect) — see `10-02-SUMMARY.md`.
    Status: ✅ PASS

    ## SC2 — Large-PiP reserved band + compromise order (VIDEO-06)
    Test result: 4/4 @Test funcs GREEN (off + normal + collapsedSettings + reducedTime) — see `10-03-SUMMARY.md`.
    Status: ✅ PASS

    ## SC3 — Off-restore byte-identical (VIDEO-13)
    Test result: `test_offState_doesNotPublishCompactness` GREEN — see `10-03-SUMMARY.md`.
    Status: ✅ PASS (structural — manual mid-game toggle deferred to P11 adoption per VALIDATION.md)

    ## SC4 — Adoption API compile-time gate
    Build result: `xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` PASSES with `.videoModeAware(minBoardHeight:)` extension present — see `10-03-SUMMARY.md`.
    Status: ✅ PASS

    ## SC5 — #Preview legibility on Classic + Dracula across 6 zones
    Method: Xcode canvas inspection of 12 named #Preview blocks in `VideoModeAware.swift`.

    | Zone × Preset | Band visible? | Compact row legible? | Labels legible? | Notes |
    |---------------|---------------|----------------------|-----------------|-------|
    | Large top / Classic | yes (~32% top) | yes | yes | |
    | Large bottom / Classic | yes (~32% bottom) | yes | yes | |
    | Small TL / Classic | no (correct — D-11) | yes | yes | |
    | Small TR / Classic | no | yes | yes | |
    | Small BL / Classic | no | yes | yes | |
    | Small BR / Classic | no | yes | yes | |
    | Large top / Dracula | yes (~32% top) | yes | yes | |
    | Large bottom / Dracula | yes (~32% bottom) | yes | yes | |
    | Small TL / Dracula | no | yes | yes | |
    | Small TR / Dracula | no | yes | yes | |
    | Small BL / Dracula | no | yes | yes | |
    | Small BR / Dracula | no | yes | yes | |

    Status: ✅ PASS / ⚠️ ISSUES (describe below)

    ## A6 fallback (10-RESEARCH.md Assumption A6)
    Was the static-shared `StubStores` fallback applied in 10-03?: <yes / no>
    If yes: <one-sentence rationale>

    ## Carry-forward to Phase 11 / 12
    - A2 (NavigationStack height adjustment) — <observed in any preview? / deferred to P11 adoption>
    - <any other Plan 11/12 dependency item>

    ## Sign-off
    Phase 10 ships: <yes / no>
    Next phase: Phase 11 (Minesweeper adoption) — consumes `.videoModeAware(minBoardHeight: 480)`
    on `MinesweeperGameView` outermost layer per D-15 untouched contract.
    ```

    **Step 5 — Report back to the user:**

    Once the 12 tiles render and pass the per-row criteria above, respond with
    `approved` and the verification doc path. If any tile fails legibility OR
    fails to render at all, describe the failing tile(s) precisely (zone +
    preset + specific failure mode) so Plan 10-03 can be amended (likely via
    A6 fallback or a `theme.colors.textPrimary` adjustment).
  </how-to-verify>
  <resume-signal>Reply with "approved" (theme-audit passed; no changes needed) OR describe specific failures (e.g., "Dracula — Small BR: 'secondary' label illegible on dark bg" so 10-03 follow-up can adjust).</resume-signal>
  <files>.planning/phases/10-layout-primitives/10-VERIFICATION.md</files>
  <action>Execute the 5 steps in the &lt;how-to-verify&gt; block above: open the file in Xcode, render all 12 #Preview tiles, run the per-tile legibility check (Classic + Dracula × 6 zones), create `.planning/phases/10-layout-primitives/10-VERIFICATION.md` from the template embedded in step 4, and report back to the user with `approved` or specific failure descriptions per step 5. This is a checkpoint:human-verify task — the human (Gabe) signs off on SC5; the assistant fills the verification doc with that sign-off + the automated SC1/SC2/SC3/SC4 results from prior plan SUMMARYs.</action>
  <verify>
    <automated>test -f .planning/phases/10-layout-primitives/10-VERIFICATION.md &amp;&amp; grep -q "SC5 — #Preview legibility" .planning/phases/10-layout-primitives/10-VERIFICATION.md &amp;&amp; grep -q "Phase 10 ships:" .planning/phases/10-layout-primitives/10-VERIFICATION.md</automated>
  </verify>
  <done>`.planning/phases/10-layout-primitives/10-VERIFICATION.md` exists, contains all 5 SC sign-off sections with the 12-row legibility table populated for Classic + Dracula × 6 zones, the A6 fallback status recorded, and the "Phase 10 ships: yes" sign-off line filled in. User has responded "approved" (or follow-up issues have been logged and resolved before this task's `done` state is reached).</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 10 entry to Docs/releases/v1.2.md</name>
  <files>Docs/releases/v1.2.md</files>
  <read_first>
    - Docs/releases/v1.2.md (existing — lines 1-72, the Phase 9 entry; do NOT modify Phase 9 content)
    - CLAUDE.md (§0.3 release-log convention, §8.14 release-log append rule, §8.13 status table — Phase 10 ships NO user-facing change so §0.1 update is N/A)
    - .planning/phases/10-layout-primitives/10-VERIFICATION.md (the doc Task 1 just produced — pull SC sign-off status to inform "Risks / notes" section)
    - .planning/phases/10-layout-primitives/10-02-SUMMARY.md (Plan 10-02 ship artifacts — VideoModeSlotRouter.swift summary)
    - .planning/phases/10-layout-primitives/10-03-SUMMARY.md (Plan 10-03 ship artifacts — VideoModeAware.swift summary)
  </read_first>
  <action>
Open `Docs/releases/v1.2.md` and APPEND content to existing sections — do NOT
mutate the existing Phase 9 bullets per CLAUDE.md §0.3 ("A new file is opened
when MARKETING_VERSION is bumped — never mutate a shipped version's file" —
v1.2 is still in progress, so we append within the same v1.2 file).

**Section: User-facing changes** — append a new bullet at the bottom of the
existing "Phase 9 (Video Mode foundation)" bullet:

```markdown
- **Phase 10 (Video Mode layout primitives).** No user-facing change.
  Internal-only — ships the `.videoModeAware()` modifier + slot-router helper
  consumed by Phase 11+. Mines / Merge / Nonogram still render byte-identical
  to v1.1 with Video Mode toggled either Off (default) or On.
```

**Section: Internal changes** — append BELOW the existing Phase 9 bullets:

```markdown
- **VideoModeAware** (`Core/VideoModeAware.swift`) — `ViewModifier`
  conformance shipping the v1.2 layout contract:
  - Off-path hard short-circuit (`if !store.isEnabled { return AnyView(content) }`)
    — byte-identical to un-wrapped view per CONTEXT D-05 / VIDEO-13.
  - Large-PiP band reservation via `.safeAreaInset(edge: .top/.bottom)` at
    `geometry.size.height * 0.32` (measured from
    `Docs/screenshots/v1.2-design/home-classic-pip-large-bottom.png`,
    `private static let largeBandFraction` per D-09 / D-10).
  - Compactness publication via custom `EnvironmentKey`
    (`\.videoModeCompactness` — 3 levels: `.normal` / `.collapsedSettings` /
    `.reducedTime`) computed from available board height vs `minBoardHeight`
    parameter per D-13 / D-14 0.85× threshold.
  - 12 named `#Preview` blocks (6 PiP zones × Classic Chrome Diner + Dracula)
    for SC5 manual legibility audit per D-16.
- **VideoModeSlotRouter** (`Core/VideoModeSlotRouter.swift`) — pure
  Foundation-only helper. `static func anchors(for: VideoModeLocation) ->
  SlotAnchorMap` returns the 4-slot anchor map (back / settings / picker /
  fab) per the locked switch table — data sourced from Phase 8
  `VIDEO-MODE-LAYOUTS.md`. Phase 11/12 game views read this in their slot
  reposition logic (P10 ships zero adoption).
- **Adoption API** — `View.videoModeAware(minBoardHeight: CGFloat = 320)`
  extension. One-line call site for every Phase 11/12 game view per CONTEXT
  D-01 (wraps the game view at the outermost layer; D-15 keeps
  `MinesweeperBoardView`'s `MagnifyGesture` + auto-scale stack UNTOUCHED).
- **Tests** — 2 new test files: `VideoModeAwareTests.swift` (4 @Test funcs
  covering off-state byte-identical + 3 compactness levels) +
  `VideoModeSlotRouterTests.swift` (7 @Test funcs / 25 #expect covering 24
  slot anchors + 1 exhaustiveness loop).
```

**Section: Risks / notes** — append BELOW the existing Phase 9 risks:

```markdown
- **`AnyView` short-circuit cost (off-path)** — accepted per CONTEXT D-05.
  Toggling Video Mode on→off triggers a clean SwiftUI rebuild of the wrapped
  game view (no leftover insets, no env values). If Phase 11 reveals a
  state-loss regression (e.g., timer reset on mid-game toggle), the
  remediation is to promote state UP one level above the modifier, NOT to
  eliminate the AnyView short-circuit (the byte-identical-off-restore
  guarantee is the SC3 contract).
- **Hard-Mines `MagnifyGesture` UNTOUCHED (CONTEXT D-15)** — Phase 10 wraps
  `MinesweeperGameView` (not `MinesweeperBoardView`) in Phase 11 adoption.
  The 06.1-03 / A11Y-05 `MagnifyGesture` + auto-scale `cellSize(forWidth:...)`
  stack stays byte-identical. The only Video-Mode-aware seam in Mines is
  `MinesweeperBoardView.Self.minCellSize`, gated on `videoModeStore.isEnabled`
  in Phase 11 per `08-HARD-MINES-ADR.md` (smaller-cells / Variant 1).
- **Screenshot README device-label drift (Pitfall 5)** — `Docs/screenshots/
  v1.2-design/README.md` claims iPhone 17 Pro Max but pixel dimensions are
  17 Pro / 16 Pro (1179×2556px = 393×852pt). The `largeBandFraction = 0.32`
  measurement is device-portable (it's a fraction of `geometry.size.height`),
  so this has no code impact — but the README annotation can be corrected in
  a future docs commit.
- **No `MARKETING_VERSION` bump in this phase** — still 1.1; bumped to 1.2
  in the Phase 13 ship plan per the v1.2 release-log line 65.
```

**Do NOT:**
- Modify the existing "Summary" or "Phase 9 (Video Mode foundation)" content.
- Update `CLAUDE.md §0.1` — Phase 10 ships zero user-facing change, so the
  status table is unaffected per §8.13. Verified by the planning-context
  hint "P10 ships no user-facing change so likely N/A".
- Change `MARKETING_VERSION` in `pbxproj`.
- Touch any source code file.
- Add a `### Phase 10` heading — the existing structure uses inline bullets
  with "**Phase N**" inside the standard sections (Summary / User-facing
  changes / Internal changes / Fixes / Risks/notes / QA checklist). Mirror
  that shape exactly.
  </action>
  <verify>
    <automated>
    # Phase 10 bullet present in each target section
    grep -q "Phase 10 (Video Mode layout primitives)" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "VideoModeAware\\*\\* (\`Core/VideoModeAware.swift\`)" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "VideoModeSlotRouter\\*\\* (\`Core/VideoModeSlotRouter.swift\`)" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "View.videoModeAware(minBoardHeight: CGFloat = 320)" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "AnyView.*short-circuit" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "Hard-Mines.*MagnifyGesture.*UNTOUCHED" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "0.32" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "VideoModeAwareTests\\.swift" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "VideoModeSlotRouterTests\\.swift" Docs/releases/v1.2.md

    # Phase 9 content NOT mutated — Phase 9 first bullet still present verbatim
    grep -q "Phase 9 (Video Mode foundation)" Docs/releases/v1.2.md &amp;&amp; \
    grep -q "VideoModeStore\\*\\* (\`Core/VideoModeStore.swift\`)" Docs/releases/v1.2.md

    # File length growth roughly tracks ~30-40 lines of appended content
    wc -l Docs/releases/v1.2.md | awk '{exit !($1&gt;=100 &amp;&amp; $1&lt;180)}'
    </automated>
  </verify>
  <done>
    - `Docs/releases/v1.2.md` exists with the Phase 9 content intact AND new Phase 10 bullets appended in the User-facing changes / Internal changes / Risks-notes sections.
    - All 9 grep checks pass (Phase 10 entries + Phase 9 preserved + size sanity).
    - No modification to `CLAUDE.md §0.1` (P10 has no user-facing changes).
    - No modification to `MARKETING_VERSION` in `pbxproj`.
    - No modification to any source file under `gamekit/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation + manual verification — no code shipped. No auth / input / crypto / network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-info | N/A | Verification doc + release-log entry | accept | No applicable threat surface — manual sign-off doc + release-log append. ASVS V2/V3/V4/V5/V6 all N/A. |

**Informational note:** This plan creates documentation only; no code paths added.
</threat_model>

<verification>
**Phase-level checks for this plan:**
- `.planning/phases/10-layout-primitives/10-VERIFICATION.md` exists with 5 SCs marked PASS (or PASS-with-notes), the 12-row Classic+Dracula × 6-zone table populated, A6 fallback status recorded, and Phase 11/12 carry-forward items listed.
- `Docs/releases/v1.2.md` shows Phase 10 entries in the 3 affected sections (User-facing changes / Internal changes / Risks-notes), with Phase 9 content unchanged.
- User has reviewed the 12 #Preview tiles in Xcode canvas and either approved OR flagged specific failures for a 10-03 follow-up.
- No source code touched, no `pbxproj` change, no `CLAUDE.md §0.1` row change.
</verification>

<success_criteria>
- **All 5 SCs sign-off complete** in `10-VERIFICATION.md` with the per-zone × per-preset legibility table populated.
- **`Docs/releases/v1.2.md` updated** per CLAUDE.md §0.3 + §8.14 — Phase 10 ship artifacts documented under existing v1.2 sections; Phase 9 content immutable.
- **User checkpoint passed** for SC5 visual audit (the only manual SC for Phase 10).
- **No drift** — no §0.1 status-table edit, no `MARKETING_VERSION` bump, no source modification.
- **Phase 11 unblock confirmed** — verification doc states Phase 11 (Minesweeper adoption) is ready to plan; the `.videoModeAware(minBoardHeight: 480)` adoption surface is shipped and the D-15 untouched contract for `MinesweeperBoardView` is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/10-layout-primitives/10-04-SUMMARY.md` with:
- SC5 audit outcome (approved / issues — quote user's resume signal)
- Confirmation of v1.2.md Phase 10 append + line-count delta
- Confirmation that no §0.1 update was needed (Phase 10 ships no user-facing change)
- "Phase 10 closed — Phase 11 ready to plan" status line
- Total Phase 10 plans complete: 4/4 (10-01 RED gate + 10-02 slot router GREEN + 10-03 modifier GREEN + 10-04 audit + release log)
</output>
</content>
</invoke>