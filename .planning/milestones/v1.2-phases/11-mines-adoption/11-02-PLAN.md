---
phase: 11-mines-adoption
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md
  - .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md
autonomous: true
requirements: [VIDEO-07]
requirements_addressed: [VIDEO-07]
tags: [docs, supersession, video-mode, slot-order]

must_haves:
  truths:
    - "Easy (9×9/10) + Medium (16×16/40) playable across all 6 PiP locations — first-tap, reveal, long-press flag, restart, win, loss all complete; manual recipe doc per-location quick-check."
    - "Hard (16×30/99) matches Phase 8 Hard-Mines ADR exactly — smaller-cells implemented, ADR referenced by name in plan body, rejected alternatives NOT re-debated."
    - "Hard validated against ADR screenshots — final render parity for Large-top, Large-bottom, ≥1 Small."
    - "Legibility regression: Classic + Voltage/Dracula × Easy/Medium/Hard × 6 zones (CLAUDE.md §8.12)."
    - "Video Mode Off byte-identical to v1.0/v1.0.6.1 — MagnifyGesture (A11Y-05), Reveal/Flag toggle (MINES-12), animations (MINES-08) unchanged (VIDEO-13 spot-check)."
  artifacts:
    - path: ".planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md"
      provides: "Updated Minesweeper slot-row rows reflect the D-05 revised order"
      contains: "Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart"
    - path: ".planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md"
      provides: "Updated Minesweeper §Per-Game Slot Mapping entry reflects D-05"
      contains: "Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart"
  key_links:
    - from: "VIDEO-MODE-LAYOUTS.md updated Minesweeper rows"
      to: ".planning/phases/11-mines-adoption/11-CONTEXT.md#D-05"
      via: "inline supersession note backlinking the new slot order's source decision"
      pattern: "supersed.*11-CONTEXT|D-05"
    - from: "08-COMPACT-ROW-TOKENS.md updated Minesweeper entry"
      to: ".planning/phases/11-mines-adoption/11-CONTEXT.md#D-05"
      via: "inline supersession note backlinking the new slot order's source decision"
      pattern: "supersed.*11-CONTEXT|D-05"
---

<objective>
Update the two Phase 8 design docs whose locked Minesweeper slot order is
superseded by Phase 11 CONTEXT D-05. Doc-only — no app code touched in this
plan. Lands in Wave 1 because plan 11-04 (compact-row composition) references
the new slot order as already-authoritative.

The supersession (per 11-CONTEXT D-05):
- **Before:** `Back | Flags/mines | Reveal/Flag picker | Time | Settings`
- **After:**  `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart`

Rationale (also captured inline in the doc edits):
- Restart is the most-tapped in-flight Minesweeper action under stress; promoting
  it into the compact row keeps it reachable when `MinesweeperHeaderBar` +
  toolbar are hidden on Large PiP zones (D-01).
- Time + Mines remaining are co-located in a single vertical-stack subview
  (slot 2) so the 5-slot `VideoCompactControlRow` contract (P9 D-12) is
  preserved — D-07 contract intact, no API change to the shared component,
  Merge + Nonogram unaffected in P12.
- Settings slot (slot 4) opens `MinesweeperToolbarMenu` (difficulty change menu
  only) per D-08. Global app Settings stays reachable from HomeView.

Purpose: Lock the doc surface so downstream Plan 11-04 can implement
against an unambiguous slot order. Single source of truth — without this
update, the 08 docs would contradict the implementation.

Output: 2 doc edits (no code, no test changes, no localization changes).

This plan is the doc-supersession commit per CLAUDE.md §8.10. Lands as
its own commit; the chip-extraction commit (Plan 11-01) and this commit
run in parallel within Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mines-adoption/11-CONTEXT.md
@.planning/phases/11-mines-adoption/11-PATTERNS.md

# Target docs (read in full before editing)
@.planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md
@.planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md

# Cross-references that MUST stay valid after the edit
# - 11-CONTEXT.md §canonical_refs §"Locked design from Phase 8" references both docs by path; this plan preserves both paths.
# - 08-HARD-MINES-ADR.md §"Consumed by" references the LAYOUTS doc by name; this plan does not rename it.
# - Plan 11-04 will reference the D-05 slot order assuming both docs are aligned.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update VIDEO-MODE-LAYOUTS.md Minesweeper slot-order rows</name>
  <read_first>
    - .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md (lines 75-180 cover the Minesweeper §Easy / §Medium / §Hard tables — Mines slot rows live at lines 96, 97, 129, 130 and equivalent positions for Hard)
    - .planning/phases/11-mines-adoption/11-CONTEXT.md §"D-05" (lines 73-86) — verbatim source of the new slot order
    - .planning/phases/11-mines-adoption/11-PATTERNS.md §"VIDEO-MODE-LAYOUTS.md (MODIFIED · D-05 slot row supersession)" — the supersession instruction
  </read_first>
  <files>.planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md</files>
  <behavior>
    - Every occurrence of the old Mines compact-row slot string `Back | Flags/mines | Reveal/Flag picker | Time | Settings` inside the §Minesweeper sections (Easy / Medium / Hard, all Large-top + Large-bottom rows) is replaced with the new D-05 string `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart`.
    - A one-line supersession note is inserted at the top of the Minesweeper section (between the §Minesweeper heading and the per-difficulty tables) backlinking 11-CONTEXT D-05.
    - Merge + Nonogram slot strings are UNCHANGED (out of scope for P11; their slot rows ship as-is from Phase 8).
    - The doc's §"Hard-Mines strategy" reference to `08-HARD-MINES-ADR.md` stays intact (lines 182-184 area).
  </behavior>
  <action>
    1. Open `.planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md`.

    2. Insert this supersession note immediately AFTER the §"## Minesweeper — Easy (9x9 / 10 mines)" heading (around line 75), as a new paragraph BEFORE the existing per-zone table:

       ```markdown
       > **Slot-row supersession (2026-05-13):** the Minesweeper compact-row slot string in the Large-top / Large-bottom rows below has been revised per `11-CONTEXT.md` D-05. The new order is `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart`. Restart is rightmost (most-tapped in-flight action under stress); slot 2 is a vertical-stack subview (Mines remaining on top, Time below) so the 5-slot `VideoCompactControlRow` contract (Phase 9 D-12) stays intact. Settings (slot 4) opens `MinesweeperToolbarMenu` (difficulty change menu) per D-08; global app Settings stays reachable from HomeView. Applies to Easy / Medium / Hard alike.
       ```

    3. Replace EVERY occurrence within the Minesweeper sections (Easy / Medium / Hard tables — these strings appear at LAYOUTS lines 96, 97, 129, 130, and the Hard equivalents around lines 139-180) of the literal string:

       ```
       Back | Flags/mines | Reveal/Flag picker | Time | Settings
       ```

       with:

       ```
       Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart
       ```

       Do NOT touch:
       - The Merge slot string `Back | Score | Mode picker | Best/time | Settings` (Merge section around lines 211-212)
       - The Nonogram slot string `Back | Lives/size | Fill/Mark picker | Time | Settings` (Nonogram section around lines 243-244)

    4. The reference to `08-COMPACT-ROW-TOKENS.md` after each Mines slot string stays intact verbatim — that doc is also updated in Task 2 of this plan.

    Do NOT introduce a Phase 11 plan body that re-debates the slot order — D-05 is the locked source decision; this edit is the doc-side reflection.
  </action>
  <verify>
    <automated>grep -c "Back | Flags/mines | Reveal/Flag picker | Time | Settings" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Back | Flags/mines | Reveal/Flag picker | Time | Settings" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns `0` (old slot string fully replaced inside Minesweeper sections)
    - `grep -c "Back | \[Mines⊥Time stacked chip\] | Reveal/Flag picker | Settings | Restart" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 2 (new slot string appears in Easy, Medium, and Hard tables — at minimum 2 occurrences if Easy + Medium share one mention each plus Hard mention; total of ≥ 2 with template instances)
    - `grep -c "Back | Score | Mode picker | Best/time | Settings" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 1 (Merge slot row preserved)
    - `grep -c "Back | Lives/size | Fill/Mark picker | Time | Settings" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 1 (Nonogram slot row preserved)
    - `grep -c "Slot-row supersession" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 1 (supersession note inserted)
    - `grep -c "11-CONTEXT" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 1 (backlink to Plan 11 CONTEXT.md present)
    - `grep -c "08-HARD-MINES-ADR.md" .planning/phases/08-video-mode-design/VIDEO-MODE-LAYOUTS.md` returns ≥ 1 (the ADR reference at LAYOUTS line ~182 was not deleted)
  </acceptance_criteria>
  <done>Old Mines slot string fully replaced inside Minesweeper sections; Merge + Nonogram slot strings preserved; supersession note + backlink to 11-CONTEXT D-05 present; ADR reference intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update 08-COMPACT-ROW-TOKENS.md Minesweeper §Per-Game Slot Mapping</name>
  <read_first>
    - .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md (full file — 62 lines; the §Per-Game Slot Mapping §Minesweeper entry is on line 32)
    - .planning/phases/11-mines-adoption/11-CONTEXT.md §"D-05" (verbatim source of new order)
    - .planning/phases/11-mines-adoption/11-PATTERNS.md §"08-COMPACT-ROW-TOKENS.md (MODIFIED · D-05 slot row supersession)"
  </read_first>
  <files>.planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md</files>
  <behavior>
    - The §"### Minesweeper" entry (currently line 30-33 area) has its single-line slot string `Back | Flags/mines | Reveal/Flag picker | Time | Settings` replaced with the D-05 string.
    - A supersession note is added under the Minesweeper subheading backlinking 11-CONTEXT D-05.
    - The §Token Anchors table (lines 14-23) is NOT modified — those tokens (radii.button / spacing.xl / spacing.s / radii.chip / spacing.l) remain the Phase 9 D-13 locks and apply unchanged to the Mines compact row.
    - The §Merge and §Nonogram entries below it are NOT modified.
    - The §Consumed-by section reference to "Phase 11 — Minesweeper adoption uses the Minesweeper slot mapping" (line 61) is updated to reference the new (revised) slot mapping.
  </behavior>
  <action>
    1. Open `.planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md`.

    2. Locate the §"### Minesweeper" subheading (line 30) and the slot string on line 32 (currently: `Back | Flags/mines | Reveal/Flag picker | Time | Settings`).

    3. Replace the Minesweeper subsection content (lines 30-33 area) with:

       ```markdown
       ### Minesweeper

       > **Supersedes 2026-05-13** (per `11-CONTEXT.md` D-05). Restart is the most-tapped in-flight action under stress and joins the compact row at slot 5 (rightmost). Slot 2 becomes a vertical-stack subview hosting both `MinesRemainingChip` (top) and `TimerChip` (bottom) so the 5-slot `VideoCompactControlRow` contract from Phase 9 D-12 is preserved unchanged. Slot 4 (Settings) opens `MinesweeperToolbarMenu` (difficulty change menu only) per D-08; global app Settings stays reachable from HomeView. Token anchors above are unchanged.

       `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart`
       ```

    4. Locate the §"Consumed by" section (line 58+) — the line currently reads:

       ```
       - Phase 11 — Minesweeper adoption uses the Minesweeper slot mapping (Back | Flags/mines | Reveal/Flag picker | Time | Settings).
       ```

       Replace with:

       ```
       - Phase 11 — Minesweeper adoption uses the Minesweeper slot mapping (revised 2026-05-13 per `11-CONTEXT.md` D-05): `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart`.
       ```

    Do NOT modify the §Token Anchors table — those tokens are Phase 9 D-13 locks and remain unchanged.

    Do NOT modify the §Merge or §Nonogram entries — those slot mappings are Phase 12's source decisions and unaffected by P11.
  </action>
  <verify>
    <automated>grep -c "Back | Flags/mines | Reveal/Flag picker | Time | Settings" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Back | Flags/mines | Reveal/Flag picker | Time | Settings" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns `0` (old slot string fully removed)
    - `grep -c "Back | \[Mines⊥Time stacked chip\] | Reveal/Flag picker | Settings | Restart" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns ≥ 2 (slot string appears in both §Minesweeper subsection and §Consumed by reference)
    - `grep -c "### Merge" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns `1` (Merge subsection preserved)
    - `grep -c "### Nonogram" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns `1` (Nonogram subsection preserved)
    - `grep -c "Back | Score | Mode picker | Best/time | Settings" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns `1` (Merge slot string preserved verbatim)
    - `grep -c "Back | Lives/size | Fill/Mark picker | Time | Settings" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns `1` (Nonogram slot string preserved verbatim)
    - `grep -c "11-CONTEXT" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns ≥ 2 (backlink in both §Minesweeper and §Consumed by)
    - `grep -cE "radii.button|spacing.xl|spacing.s|radii.chip|spacing.l" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns ≥ 5 (token anchors table untouched)
    - `grep -c "Supersedes 2026-05-13" .planning/phases/08-video-mode-design/08-COMPACT-ROW-TOKENS.md` returns ≥ 1 (supersession note inserted)
  </acceptance_criteria>
  <done>Old Mines slot string removed from both §Per-Game Slot Mapping §Minesweeper entry AND §Consumed by reference; new slot string + supersession note + 11-CONTEXT D-05 backlink present; Token Anchors table, Merge, and Nonogram entries unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design doc ↔ implementation | The Phase 8 docs are Phase 11's locked specs. A mismatch between doc and code would cause downstream agents to implement the wrong slot order. |

## STRIDE Threat Register (ASVS L1, documentation-only edit)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | Slot order doc supersession | mitigate | Both files updated atomically in a single commit. Backlinks to `11-CONTEXT.md` D-05 traceable both directions. Acceptance criteria assert old string count = 0 and new string count ≥ expected, preventing partial replacement. |
| T-11-06 | Repudiation | Provenance of the change | mitigate | Supersession notes include date (`2026-05-13`) + source decision ID (D-05) + project skill backlinks. Anyone reading the docs later sees exactly when + why the slot order changed. |
| T-11-07 | Tampering | Untouched Merge/Nonogram rows | mitigate | Acceptance criteria explicitly grep that the Merge + Nonogram slot strings + subsections survive verbatim. Prevents accidental over-replace from search/replace. |
</threat_model>

<verification>
- Both files committed together as a single commit (CLAUDE.md §8.10 — doc-supersession commit; chip-extraction lands separately in Plan 11-01).
- No code files modified.
- No xcstrings entries modified.
- `git diff --stat` shows exactly two `.md` files changed under `.planning/phases/08-video-mode-design/`.
- Manual readback: open each updated doc, confirm the Minesweeper slot string reads `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker | Settings | Restart` in every Mines-section table cell.
</verification>

<success_criteria>
- VIDEO-MODE-LAYOUTS.md §Minesweeper §Easy / §Medium / §Hard rows reflect D-05.
- 08-COMPACT-ROW-TOKENS.md §Minesweeper entry and §Consumed by reference reflect D-05.
- Both docs backlink to `11-CONTEXT.md` D-05.
- Merge + Nonogram slot strings + subsections unchanged.
- Token Anchors table in 08-COMPACT-ROW-TOKENS.md unchanged.

This plan delivers a doc-side prerequisite for SC1 (slot order is correct
when Easy/Medium are verified across 6 PiP zones in Plan 11-07) and for
SC2 (the ADR-anchored Hard strategy is implemented against the same locked
slot order in Plans 11-04/11-05).
</success_criteria>

<output>
After completion, create `.planning/phases/11-mines-adoption/11-02-SUMMARY.md` listing:
- The two doc files modified
- The exact line ranges replaced
- Confirmation that Merge + Nonogram slot strings are unchanged
- The supersession-note text inserted into each doc
</output>
