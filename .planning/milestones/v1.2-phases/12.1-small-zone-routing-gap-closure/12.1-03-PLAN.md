---
phase: 12.1
plan: 03
type: execute
wave: 2
depends_on: ["12.1-01"]
files_modified:
  - gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift
  - gamekit/gamekit/Games/Merge/MergeGameView.swift
autonomous: true
requirements: [VIDEO-09]
gap_closure: true
tags: [video-mode, small-zone, merge, gap-closure]
must_haves:
  truths:
    - "MergeGameView.body's Small-zone arm (`else` arm of the 3-way branch at line 67) renders `smallZoneLayout` instead of `existingLayout`."
    - "On Top L/R Small zones (`anchors.headerBar` = `.bottomLeading` / `.bottomTrailing`) MergeHeaderBar renders at the BOTTOM of the board column, leaving the top edge clear for the top-PiP overlay (D-07)."
    - "On Bot L/R Small zones (`anchors.headerBar` = `.topLeading` / `.topTrailing`) MergeHeaderBar stays at top and MergeModePill renders directly below it, leaving the bottom edge clear for the bottom-PiP overlay (D-08 / D-09)."
    - "Off-path (`videoModeStore.isEnabled == false`) still resolves to `existingLayout` verbatim — `git diff` shows zero lines changed on the off-path branch (D-11)."
    - "`MergeBoardView.swift` SHA `4aec14161b00ac2dbd1ea00e3bebb696bea6fc26` is unchanged (D-MG-10 — `MergeBoardView` constructor call site byte-identical across all layout vars)."
  artifacts:
    - path: "gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift"
      provides: "smallZoneLayout view consuming anchors.headerBar"
      contains: "var smallZoneLayout"
    - path: "gamekit/gamekit/Games/Merge/MergeGameView.swift"
      provides: "Body's else arm calls smallZoneLayout"
      contains: "smallZoneLayout"
  key_links:
    - from: "MergeGameView.body else arm"
      to: "smallZoneLayout"
      via: "Group { if/else if/else } body branch line 66-69"
      pattern: "else.*smallZoneLayout"
    - from: "smallZoneLayout"
      to: "VideoModeSlotRouter.anchors(for: videoModeStore.location)"
      via: "let anchors = ... at top of view body"
      pattern: "VideoModeSlotRouter.anchors\\(for:"
---

<objective>
Wire `anchors.headerBar` + `anchors.picker` (from `SlotAnchorMap` extended in 12.1-01) into MergeGameView's Small-zone branch by adding a new `smallZoneLayout` view to `MergeGameView+VideoMode.swift`, and swapping the `else` arm of `MergeGameView.body`'s 3-way branch (line 67) to call it. Off-path stays on `existingLayout` (byte-identical). Large-zone path stays on `largeZoneLayout` (untouched). `MergeBoardView.swift` SHA stays at `4aec14161b00ac2dbd1ea00e3bebb696bea6fc26` per D-MG-10.

Purpose: Closes the SC1 + SC3 picker + HeaderBar small-zone routing gap for Merge (Phase 12 carryforward defect against VIDEO-09). The implementation mirrors Plan 12.1-02 (Minesweeper) structurally — same VStack-ordering flip based on `headerBarAtBottom`, same byte-identical `MergeBoardView` call site preservation, same off-path untouched contract.

Output: `smallZoneLayout` added to the extension; `MergeGameView.body` else-arm updated. Project builds. `MergeBoardView` byte-identity preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-01-PLAN.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-02-PLAN.md
@.planning/phases/12-merge-nonogram-adoption/12-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Existing structure to mirror — extracted from the codebase. -->

```swift
// MergeGameView.swift body (lines 58-69):
var body: some View {
    Group {
        if !videoModeStore.isEnabled {
            existingLayout
                .toolbar { existingToolbarContent }
        } else if videoModeStore.location.isLarge {
            largeZoneLayout
                .toolbar(.hidden, for: .navigationBar)
        } else {
            existingLayout                              // <— change this to smallZoneLayout
                .toolbar { smallZoneToolbarContent }
        }
    }
    ...
}

// existingLayout's body shape (MergeGameView+VideoMode.swift lines 45-86):
ZStack {
    theme.colors.background.ignoresSafeArea()
    VStack(spacing: theme.spacing.m) {
        MergeHeaderBar(theme:, score:, bestScore:, mode:)           // TOP
        MergeBoardView(theme:, board:, onSwipe:)                    // BOARD — D-MG-10 byte-identical call site
            .padding(.horizontal, theme.spacing.l)
            .sensoryFeedback(.impact(weight: .light), trigger: ...)
            .sensoryFeedback(.success, trigger: ...)
        MergeModePill(theme:, mode:, onSelect:)                     // BOTTOM
            .padding(.top, theme.spacing.s)
            .opacity(isTerminal ? 0 : 1)
            .allowsHitTesting(!isTerminal)
    }
    if let endState = endStateForOverlay {
        endStateOverlay(state: endState)
    }
}
```

Same D-02 anchor matrix + same `headerBarAtBottom` boolean derivation as Plan 12.1-02. Same two VStack orderings:

- **Top L/R zones (TL+TR)** — `anchors.headerBar` is bottom → order: `MergeBoardView` (with its 2 sensoryFeedbacks + padding chain byte-identical) → `MergeHeaderBar` → `MergeModePill` (with its full modifier chain).
- **Bottom L/R zones (BL+BR)** — `anchors.headerBar` is top → order: `MergeHeaderBar` → `MergeModePill` → `MergeBoardView`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `smallZoneLayout` view to MergeGameView+VideoMode.swift (consumes anchors.headerBar + anchors.picker)</name>
  <files>gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift (entire file — 308 LOC; read existingLayout lines 44-86 and existing smallZoneToolbarContent lines 141-159)
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift (post-12.1-01)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md (D-04 / D-05 / D-07 / D-08 / D-09 / D-MG-10 / D-11)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-02-PLAN.md (mirror Mines wiring shape — same VStack-flip pattern)
    - /Users/gabrielnielsen/Desktop/GameKit/CLAUDE.md (§8.5 500-LOC cap — Merge is 308 LOC, plenty of headroom)
  </read_first>
  <action>
Add a new `@ViewBuilder var smallZoneLayout: some View` right after `var existingLayout` (around line 87, before the `// MARK: - Toolbar items (shared button bodies)` section). The body MUST:

1. Read `let anchors = VideoModeSlotRouter.anchors(for: videoModeStore.location)` at the top of the body.
2. Derive `let headerBarAtBottom = (anchors.headerBar == .bottomLeading || anchors.headerBar == .bottomTrailing)`.
3. Use the same outer `ZStack { theme.colors.background.ignoresSafeArea(); VStack(spacing: theme.spacing.m) { ... }; <endStateOverlay> }` shell as `existingLayout` (lines 46-85). Preserve the `if let endState = endStateForOverlay { endStateOverlay(state: endState) }` overlay byte-identical.
4. Inside the VStack, order `MergeHeaderBar` / `MergeBoardView` / `MergeModePill` per `headerBarAtBottom`:
   - If `!headerBarAtBottom` (Bot L/R): `MergeHeaderBar(...)` → `MergeModePill(...)` (with full modifier chain — `.padding(.top, theme.spacing.s)` + `.opacity(...)` + `.allowsHitTesting(...)`) → `MergeBoardView(...)` (with full modifier chain — `.padding(.horizontal, theme.spacing.l)` + both `.sensoryFeedback` calls).
   - If `headerBarAtBottom` (Top L/R): `MergeBoardView(...)` (full chain) → `MergeHeaderBar(...)` → `MergeModePill(...)` (full chain).
5. **D-MG-10 byte-identity:** `MergeBoardView(theme: theme, board: viewModel.board, onSwipe: { viewModel.handleSwipe($0) })` constructor call MUST be IDENTICAL to its `existingLayout` invocation at lines 57-61 (same labels, same order: `theme:`, `board:`, `onSwipe:`). The 3-line modifier chain (`.padding(.horizontal, theme.spacing.l)` + 2× `.sensoryFeedback`) chained to it (lines 62-70) also stays byte-identical.
6. `MergeHeaderBar(theme: theme, score: viewModel.score, bestScore: viewModel.bestScore, mode: viewModel.mode)` byte-identical to lines 50-55.
7. `MergeModePill(theme: theme, mode: viewModel.mode, onSelect: { viewModel.requestModeChange($0) })` plus its `.padding(.top, theme.spacing.s)` + `.opacity(isTerminal ? 0 : 1)` + `.allowsHitTesting(!isTerminal)` modifier chain byte-identical to lines 72-79.

Add a doc-comment above `var smallZoneLayout`: "Phase 12.1 (Plan 12.1-03) — Small-zone branch for Merge consuming `anchors.headerBar` + `anchors.picker` per CONTEXT D-04 / D-05 / D-07 / D-08 / D-09. Ordering of MergeHeaderBar / MergeBoardView / MergeModePill flips based on whether `anchors.headerBar` is a top or bottom anchor. `MergeBoardView` call site is byte-identical to `existingLayout` per D-MG-10 (SHA `4aec14161b00ac2dbd1ea00e3bebb696bea6fc26` unchanged across this phase)."

Do NOT modify `existingLayout`, `largeZoneLayout`, `compactRowComposed`, `smallZoneToolbarContent`, or any other existing view/function. Do NOT touch `MergeBoardView.swift`. Do NOT touch `MergeHeaderBar.swift` (D-11: off-path HeaderBar consumer with NO `compact:` arg must remain unchanged so the v1.1 byte-identical render is preserved on the off-path).

`anchors.picker` is implicitly consumed via `headerBarAtBottom` (the boolean unifies the 4 small zones into 2 orderings; D-09 + D-08 already encoded the cross-product). May add an explicit `_ = anchors.picker` line or a comment referencing `anchors.picker` for documentation/readability, but no behavior branches on it independently.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var smallZoneLayout" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift` returns `1`
    - `grep -c "VideoModeSlotRouter.anchors(for:" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift` returns `>= 2`
    - `grep -c "headerBarAtBottom" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift` returns `>= 1`
    - `grep -c "MergeBoardView(" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift` returns `>= 2` (existingLayout + smallZoneLayout — note: Merge has no separate `largeZoneLayout` board call; the compact row owns the Large path differently. Verify against actual codebase post-Task-1 — value MAY be 2 or 3 depending on whether largeZoneLayout invokes MergeBoardView directly. Read the file to confirm and adjust this acceptance.)
    - **D-MG-10 SHA byte-identity:** `git diff -- gamekit/gamekit/Games/Merge/MergeBoardView.swift | wc -l` returns `0`. Cross-check: `shasum gamekit/gamekit/Games/Merge/MergeBoardView.swift | awk '{print $1}'` returns the same SHA recorded in 12-VERIFICATION.md (`4aec14161b00ac2dbd1ea00e3bebb696bea6fc26`).
    - `git diff -- gamekit/gamekit/Games/Merge/MergeHeaderBar.swift | wc -l` returns `0` (off-path consumer untouched per D-11)
    - `wc -l /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift` returns a value < 400 (was 308, expected delta ~30-40 LOC for a clean inline-conditional implementation)
    - `xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max"` exits 0
  </acceptance_criteria>
  <done>
`smallZoneLayout` exists; consumes `anchors.headerBar`; VStack ordering flips correctly; `MergeBoardView.swift` + `MergeHeaderBar.swift` have zero diff; file < 400 LOC; project builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap `MergeGameView.body` else arm from `existingLayout` to `smallZoneLayout`</name>
  <files>gamekit/gamekit/Games/Merge/MergeGameView.swift</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView.swift (lines 40-70 — body's 3-way branch and surrounding doc-comment)
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView+VideoMode.swift (post-Task-1 to confirm `smallZoneLayout` is in scope)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md (D-04 / D-11)
  </read_first>
  <action>
In `MergeGameView.swift`'s body (line 66-68), locate the else arm:

```swift
} else {
    existingLayout
        .toolbar { smallZoneToolbarContent }
}
```

Change `existingLayout` to `smallZoneLayout`. KEEP the `.toolbar { smallZoneToolbarContent }` modifier exactly as-is (D-06 preserves the existing toolbar wiring; the layout swap is orthogonal to the toolbar swap).

Update the inline doc-comment line above the 3-way branch (around line 48): "- .isEnabled true + small zone → existing layout + repositioned toolbar (D-MG-02)." → "- .isEnabled true + small zone → `smallZoneLayout` (Phase 12.1, Plan 12.1-03) — chips/picker reposition away from PiP overlay corners + repositioned toolbar (D-MG-02 + CONTEXT D-04)."

Also update the file-header doc-comment block at lines 17-26 ("Small-zone: existingLayout + smallZoneToolbarContent — toolbar items reposition only") to: "Small-zone: `smallZoneLayout` (Phase 12.1) + `smallZoneToolbarContent` — Header/ModePill ordering inside the VStack flips per `anchors.headerBar`, and toolbar items reposition via the existing `smallZoneToolbarContent`."

Do NOT modify any other lines. The off-path arm (`if !videoModeStore.isEnabled { existingLayout.toolbar { existingToolbarContent } }` at lines 60-62) MUST be unchanged per D-11. The Large-zone arm (`else if videoModeStore.location.isLarge { largeZoneLayout.toolbar(.hidden, for: .navigationBar) }` at lines 63-65) MUST be unchanged.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "smallZoneLayout" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView.swift` returns `>= 1`
    - The off-path arm still resolves to `existingLayout`: `grep -B 1 "existingLayout" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView.swift | grep -c "!videoModeStore.isEnabled"` returns `>= 1`
    - The Large-zone arm still resolves to `largeZoneLayout`: `grep "largeZoneLayout" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Games/Merge/MergeGameView.swift | wc -l` returns `>= 1`
    - **D-MG-10 SHA check (re-confirm post-Task-2):** `git diff -- gamekit/gamekit/Games/Merge/MergeBoardView.swift | wc -l` returns `0`
    - **D-11 off-path byte-identity:** `git diff gamekit/gamekit/Games/Merge/MergeGameView.swift` shows the diff is scoped to ONLY (a) the doc-comment lines and (b) the single else-arm line that changed from `existingLayout` to `smallZoneLayout`. Verify with: `git diff gamekit/gamekit/Games/Merge/MergeGameView.swift | grep -E "^[+-]\s+" | grep -vE "(//|smallZoneLayout|existingLayout$)" | wc -l` returns `0` (no other code-line changes).
    - `xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max"` exits 0
    - `xcodebuild test -scheme gamekit -only-testing:gamekitTests/VideoModeSlotRouter -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -quiet 2>&1 | tail -5` shows green (12.1-01 contract intact).
  </acceptance_criteria>
  <done>
Body's else arm calls `smallZoneLayout`; off-path + Large-zone arms unchanged; `MergeBoardView.swift` zero diff; `MergeHeaderBar.swift` zero diff; project builds.
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max"` succeeds.
2. `xcodebuild test -scheme gamekit -only-testing:gamekitTests/VideoModeSlotRouter` exits 0.
3. **D-MG-10 byte-identity:** `git diff -- gamekit/gamekit/Games/Merge/MergeBoardView.swift` returns 0 lines.
4. **D-11 off-path byte-identity:** `MergeHeaderBar.swift` has 0 lines of diff; the off-path arm of `MergeGameView.body` is unchanged.
5. **Large-zone untouched:** `largeZoneLayout` + `compactRowComposed` in `MergeGameView+VideoMode.swift` have zero structural diff.
6. **File-size cap:** `wc -l MergeGameView+VideoMode.swift` < 400 (Merge has plenty of headroom, no fallback needed).
</verification>

<success_criteria>
- `smallZoneLayout` exists in `MergeGameView+VideoMode.swift`, consumes `anchors.headerBar`.
- `MergeGameView.body`'s else arm calls `smallZoneLayout`, with `.toolbar { smallZoneToolbarContent }` preserved.
- Off-path branch byte-identical; Large-zone branch byte-identical.
- `MergeBoardView.swift` SHA stays `4aec14161b00ac2dbd1ea00e3bebb696bea6fc26` (D-MG-10).
- `MergeHeaderBar.swift` zero diff (D-11).
- `xcodebuild build` + `xcodebuild test -only-testing:gamekitTests/VideoModeSlotRouter` both green.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-small-zone-routing-gap-closure/12.1-03-SUMMARY.md`. Document:
- The final `MergeBoardView` call site (paste 1-2 lines) — confirms D-MG-10 byte-identity
- `MergeBoardView.swift` SHA before/after (should match `4aec14161b00ac2dbd1ea00e3bebb696bea6fc26`)
- The VStack ordering per zone (4 zones × ordering pattern table)
- Confirmation that off-path + Large-zone branches + `MergeHeaderBar.swift` are untouched
</output>