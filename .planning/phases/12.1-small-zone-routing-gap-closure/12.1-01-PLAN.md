---
phase: 12.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Core/VideoModeSlotRouter.swift
  - gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift
autonomous: true
requirements: [VIDEO-08, VIDEO-09, VIDEO-10]
gap_closure: true
tags: [video-mode, small-zone, router, gap-closure]
must_haves:
  truths:
    - "SlotAnchorMap exposes a 5th named field `headerBar: SlotAnchor` so call sites can reposition the per-game HeaderBar away from the top-PiP overlay without re-introducing the silent-omission failure mode that caused this gap."
    - "VideoModeSlotRouter.anchors(for:) returns the per-zone headerBar anchor values locked in D-02 for all 6 VideoModeLocation cases."
    - "The 3 existing adopter call sites (Mines/Merge/Nonogram +VideoMode.swift) still compile against the new SlotAnchorMap shape — no behavior change yet (consumption happens in 12.1-02/03/04)."
    - "VideoModeSlotRouterTests asserts the new headerBar anchor values per zone so a future drift produces a test failure, not a silent regression."
  artifacts:
    - path: "gamekit/gamekit/Core/VideoModeSlotRouter.swift"
      provides: "SlotAnchorMap with headerBar field + 6 switch cases filled per D-02"
      contains: "let headerBar: SlotAnchor"
    - path: "gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift"
      provides: "headerBar anchor assertions per zone"
      contains: "map.headerBar"
  key_links:
    - from: "VideoModeSlotRouter.anchors(for:)"
      to: "SlotAnchorMap(...) initializer with 5 named fields"
      via: "switch on VideoModeLocation"
      pattern: "headerBar: \\.[a-zA-Z]+"
---

<objective>
Extend `SlotAnchorMap` (in `VideoModeSlotRouter.swift`) with a 5th named field `headerBar: SlotAnchor`, and fill the corresponding value in all 6 switch cases of `VideoModeSlotRouter.anchors(for:)` per CONTEXT D-02. Update `VideoModeSlotRouterTests` to assert the new field per zone. No behavior change at adopter call sites yet — they continue to call existing fields (back/settings/picker/fab). This plan is the shared contract that 12.1-02 / 12.1-03 / 12.1-04 consume in parallel.

Purpose: Closes the structural part of the SC1 + SC3 gap by giving the 3 game-view extensions a single typed anchor to reposition each game's HeaderBar (chips) away from the top-PiP overlay on Top L/R Small zones. Per D-01 the named-fields shape (vs. inline computation) is mandatory — it gives compile-time exhaustiveness and prevents the silent-omission failure mode that surfaced this gap.

Output: Updated `VideoModeSlotRouter.swift` + `VideoModeSlotRouterTests.swift`. `xcodebuild test` passes, the 3 adopter `+VideoMode.swift` files still compile (they don't read `headerBar` yet — that's 12.1-02/03/04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md
@.planning/phases/12-merge-nonogram-adoption/12-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Current SlotAnchor / SlotAnchorMap contract — extracted from VideoModeSlotRouter.swift -->

```swift
// Current shape (lines 43-50, 67-72):
enum SlotAnchor: Sendable, Equatable {
    case topLeading
    case topTrailing
    case bottomLeading
    case bottomTrailing
    case inCompactRow
    case hidden
}

struct SlotAnchorMap: Equatable, Sendable {
    let back: SlotAnchor
    let settings: SlotAnchor
    let picker: SlotAnchor
    let fab: SlotAnchor
}

// Existing test pattern (one @Test per zone + one allCases exhaustiveness check):
@Test("Small top-left — slots move to trailing edge (VIDEO-05 / D-11)")
func test_smallTopLeft_anchors() {
    let map = VideoModeSlotRouter.anchors(for: .smallTopLeft)
    #expect(map.back == .topTrailing)
    #expect(map.settings == .topTrailing)
    #expect(map.picker == .bottomTrailing)
    #expect(map.fab == .bottomTrailing)
}
```

D-02 locked anchor values per zone (copy verbatim into the switch):

| Zone | headerBar |
|------|-----------|
| `.largeTop` | `.inCompactRow` |
| `.largeBottom` | `.inCompactRow` |
| `.smallTopLeft` | `.bottomLeading` |
| `.smallTopRight` | `.bottomTrailing` |
| `.smallBottomLeft` | `.topLeading` |
| `.smallBottomRight` | `.topTrailing` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SlotAnchorMap with `headerBar` field + fill all 6 switch cases per D-02</name>
  <files>gamekit/gamekit/Core/VideoModeSlotRouter.swift</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift (entire file — 144 LOC, the contract being extended)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md (D-01 + D-02 locks)
    - /Users/gabrielnielsen/Desktop/GameKit/CLAUDE.md (§1 smallest change, §8.5 file-size cap)
  </read_first>
  <action>
Add a 5th named field `let headerBar: SlotAnchor` to the `SlotAnchorMap` struct, positioned between the existing `picker` and `fab` fields (per D-01). Update the struct's doc-comment "Slot semantics" block to add a `- headerBar` bullet describing it as the per-game HeaderBar reposition seam.

In `VideoModeSlotRouter.anchors(for:)`, add the `headerBar` argument to all 6 `SlotAnchorMap(...)` initializer call sites with the exact D-02 values:

- `.largeTop` → `headerBar: .inCompactRow`
- `.largeBottom` → `headerBar: .inCompactRow`
- `.smallTopLeft` → `headerBar: .bottomLeading`
- `.smallTopRight` → `headerBar: .bottomTrailing`
- `.smallBottomLeft` → `headerBar: .topLeading`
- `.smallBottomRight` → `headerBar: .topTrailing`

Place `headerBar:` between `picker:` and `fab:` in each initializer call (matches struct field order).

Update the per-case inline comments to note the headerBar anchor's intent. Example for `.smallTopLeft`: keep the existing "PiP covers TL → move all affordances toward trailing edge." comment and append: "HeaderBar moves to bottom-leading (away from top-PiP overlay; lean leading)." per CONTEXT D-02 + D-07.

Also update the file's top-level doc-comment "Phase 10 invariants" / "Phase 13 forward-compat NOTE" section to reflect that Phase 12.1 has now added `headerBar` (parallel to the speculated `banner` extension noted at lines 27-32). Do not delete the banner forward-compat note — append a "Phase 12.1 update: `headerBar` added per CONTEXT D-01 for Small-zone chip reposition." line above the banner note.

Do NOT change any existing field values (per D-03 `anchors.picker` stays as-is; `anchors.back` / `anchors.settings` / `anchors.fab` unchanged).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let headerBar: SlotAnchor" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift` returns `1`
    - `grep -c "headerBar:" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift` returns `7` (1 struct field + 6 initializer call sites)
    - `grep "headerBar: \.inCompactRow" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns `2` (largeTop + largeBottom)
    - `grep "headerBar: \.bottomLeading" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns `1` (smallTopLeft)
    - `grep "headerBar: \.bottomTrailing" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns `1` (smallTopRight)
    - `grep "headerBar: \.topLeading" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns `1` (smallBottomLeft)
    - `grep "headerBar: \.topTrailing" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns `1` (smallBottomRight)
    - File size unchanged in spirit — D-03 means `back` / `settings` / `picker` / `fab` per-case values are unchanged: `git diff gamekit/gamekit/Core/VideoModeSlotRouter.swift | grep -E "^-\s+(back|settings|picker|fab):" | wc -l` returns `0`
    - `wc -l /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift` returns `< 200`
  </acceptance_criteria>
  <done>
SlotAnchorMap has a 5th `headerBar: SlotAnchor` field; `VideoModeSlotRouter.anchors(for:)` returns the 6 D-02-locked headerBar values; existing back/settings/picker/fab values unchanged; project still builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add headerBar assertions to VideoModeSlotRouterTests (6 zones + exhaustiveness)</name>
  <files>gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift (entire file — 95 LOC, the test shape we mirror)
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Core/VideoModeSlotRouter.swift (post-Task-1, to read the D-02 values back into the test)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md (D-02 values)
  </read_first>
  <action>
For each of the 6 existing `@Test` zone methods (`test_largeTop_allInCompactRow`, `test_largeBottom_allInCompactRow`, `test_smallTopLeft_anchors`, `test_smallTopRight_anchors`, `test_smallBottomLeft_anchors`, `test_smallBottomRight_anchors`), append one `#expect(map.headerBar == ...)` line with the D-02-locked value:

- In `test_largeTop_allInCompactRow`: add `#expect(map.headerBar == .inCompactRow)`
- In `test_largeBottom_allInCompactRow`: add `#expect(map.headerBar == .inCompactRow)`
- In `test_smallTopLeft_anchors`: add `#expect(map.headerBar == .bottomLeading)`
- In `test_smallTopRight_anchors`: add `#expect(map.headerBar == .bottomTrailing)`
- In `test_smallBottomLeft_anchors`: add `#expect(map.headerBar == .topLeading)`
- In `test_smallBottomRight_anchors`: add `#expect(map.headerBar == .topTrailing)`

Place each new `#expect` immediately after the existing `map.fab` assertion in that method, so the order is back → settings → picker → fab → headerBar (matches struct field order after Task 1).

Update the file-level header doc-comment block to bump "6 VideoModeLocation cases × 4 SlotAnchorMap fields = 24 anchor assertions" to "6 VideoModeLocation cases × 5 SlotAnchorMap fields = 30 anchor assertions". Also append a line: "Phase 12.1 update: `headerBar` field added (Plan 12.1-01) — repositions per-game HeaderBar away from top-PiP overlay on Small zones per CONTEXT D-02."

Do NOT add a 7th test method — the matrix-shape pattern of one `@Test` per zone (already established by the existing tests) covers the new field exactly the same way. The existing `test_all_cases_have_mappings` test continues to validate exhaustiveness without modification.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild test -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max" -only-testing:gamekitTests/VideoModeSlotRouter -quiet 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "map.headerBar ==" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift` returns `6` (one per zone test method)
    - `grep -c "map.headerBar == .inCompactRow" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift` returns `2`
    - `grep "map.headerBar == .bottomLeading" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift | wc -l` returns `1`
    - `grep "map.headerBar == .bottomTrailing" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift | wc -l` returns `1`
    - `grep "map.headerBar == .topLeading" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift | wc -l` returns `1`
    - `grep "map.headerBar == .topTrailing" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift | wc -l` returns `1`
    - `xcodebuild test -scheme gamekit -only-testing:gamekitTests/VideoModeSlotRouter` exits 0 (all 7 existing tests + the augmented assertions PASS)
    - `wc -l /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekitTests/Core/VideoModeSlotRouterTests.swift` returns `< 120` (was 95; new assertions add ~6 lines)
  </acceptance_criteria>
  <done>
Each of the 6 zone test methods asserts the D-02 headerBar value; the suite passes under `xcodebuild test`; no behavior change at game-view call sites (they don't read `headerBar` yet — that lands in 12.1-02 / 03 / 04).
  </done>
</task>

</tasks>

<verification>
1. `xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max"` succeeds — the 3 adopter `+VideoMode.swift` files still compile because they only consume `anchors.back` / `anchors.settings` / `anchors.picker` / `anchors.fab` (Task 1 added a new field without removing any).
2. `xcodebuild test -scheme gamekit -only-testing:gamekitTests/VideoModeSlotRouter` exits 0.
3. `git diff gamekit/gamekit/Core/VideoModeSlotRouter.swift` shows only additive lines for `headerBar` (one struct-field line + 6 initializer-call-site lines + doc-comment updates) — zero removed lines under `back:` / `settings:` / `picker:` / `fab:`.
</verification>

<success_criteria>
- `SlotAnchorMap.headerBar` exists with `SlotAnchor` type.
- All 6 switch cases in `VideoModeSlotRouter.anchors(for:)` return the D-02-locked `headerBar` value.
- All existing back/settings/picker/fab values are byte-identical to pre-12.1 (D-03 lock).
- `VideoModeSlotRouterTests` assert all 6 headerBar values; suite passes.
- No game-view file (`MinesweeperGameView+VideoMode.swift`, `MergeGameView+VideoMode.swift`, `NonogramGameView+VideoMode.swift`) is modified in this plan — they pick up `headerBar` consumption in 12.1-02 / 03 / 04.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-small-zone-routing-gap-closure/12.1-01-SUMMARY.md` per the standard SUMMARY template — focus on:
- Final `SlotAnchorMap` shape (5 fields, in declared order)
- The D-02 anchor matrix (6 zones × 1 new field) as a small table
- Confirmation that no behavior change exists at adopter call sites (the consumption work is in 12.1-02 / 03 / 04)
- Test count delta (added 6 `#expect` lines; existing 7 test methods preserved)
</output>