---
phase: 12.1
plan: 05
type: execute
wave: 3
depends_on: [12.1-02, 12.1-03, 12.1-04]
files_modified:
  - .planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md
  - Docs/releases/v1.2.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [VIDEO-08, VIDEO-09, VIDEO-10]
gap_closure: true
tags: [video-mode, small-zone, manual-audit, release-log, phase-close, gap-closure]
must_haves:
  truths:
    - "All 12 Small-zone matrix rows (4 zones × 3 games) PASS on iPhone 17 Pro Max simulator under BOTH Classic and Dracula presets — ModePill does not overlap the PiP overlay, HeaderBar chips do not overlap the PiP overlay, the board remains tappable/swipeable/pinch-zoomable per each game's gesture set, and legibility regression vs. the Phase 12 Large-zone worst-case audit is absent on both presets."
    - "SC1 (Merge plays across all 6 PiP locations) and SC3 (legibility on Classic + Loud × all 6 zones) in 12-VIDEO-MANUAL-CHECK.md flip from FAIL to PASS for all 3 adopter games (Mines, Merge, Nonogram) — recorded via an ADDITIVE 'Phase 12.1 Closure Re-audit' section appended to that doc per D-13. The existing 2026-05-13 P12 sign-off block is preserved verbatim."
    - "Docs/releases/v1.2.md gains a Phase 12.1 closure entry per CLAUDE.md §0.3 + §8.14, using the bullet template from CONTEXT specifics verbatim (one Fixed bullet + one Internal bullet). MARKETING_VERSION verified to still be 1.1 (Phase 13 ships the bump); the entry is APPEND-ONLY — existing P9 / P10 / P11 / P12 entries are preserved."
    - "STATE.md flips Phase 12.1 from 'Planning' to 'Complete' with last_updated 2026-05-14; Current Position advances to Phase 13 (Win/Loss Banner + A11y Gating); progress percent recalculated; Next action points to `/gsd-plan-phase 13`."
    - "ROADMAP.md v1.2 Progress table reflects the closure: Phase 12.1 row shows 5/5 plans + Status Complete + Completed 2026-05-14; Phase 12 row updates from 'Closed PARTIAL (gaps_found — 12.1 required)' to 'Closed (gaps closed by Phase 12.1)' so the v1.2 ship status reads truthfully."
    - "All 5 ROADMAP §Phase 12.1 Success Criteria (SC1 picker routing, SC2 headerBar seam, SC3 12-row re-audit PASS, SC4 off-path byte-identity, SC5 Large-zone untouched) are verifiable from the closed state via the grep + git diff probes locked in the per-task acceptance criteria."
  artifacts:
    - path: ".planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md"
      provides: "Appended 'Phase 12.1 Closure Re-audit' section with a 12-row table (4 zones × 3 games × Classic + Dracula) + updated SC1 + SC3 sign-off rows dated 2026-05-14. The existing 2026-05-13 P12 sign-off block is NOT rewritten — the closure section is additive."
      contains: "Phase 12.1 Closure Re-audit"
    - path: "Docs/releases/v1.2.md"
      provides: "Phase 12.1 entries appended per CLAUDE.md §0.3 + §8.14 using the CONTEXT specifics bullet template verbatim (one Fixed bullet + one Internal bullet under a 'Phase 12.1 — Small-Zone Routing Gap Closure (closes SC1 + SC3)' heading); existing P9 / P10 / P11 / P12 entries preserved (APPEND-ONLY)."
      contains: "Phase 12.1"
    - path: ".planning/STATE.md"
      provides: "Phase 12.1 marked Complete; Current Position advanced to Phase 13; last_updated 2026-05-14; Next action `/gsd-plan-phase 13`"
      contains: "Phase 12.1"
    - path: ".planning/ROADMAP.md"
      provides: "v1.2 Progress table updated — Phase 12.1 row 5/5 Complete; Phase 12 row 'Closed (gaps closed by Phase 12.1)'"
      contains: "5/5 | Complete | 2026-05-14"
  key_links:
    - from: "12 PASS rows in '## Phase 12.1 Closure Re-audit' table"
      to: "SC1 + SC3 sign-off rows in the same doc (PASS on 2026-05-14)"
      via: "additive sign-off block appended below the existing 2026-05-13 P12 sign-off"
      pattern: "SC[13].*PASS.*2026-05-14"
    - from: "Docs/releases/v1.2.md Phase 12.1 entry"
      to: "12.1-01 / 12.1-02 / 12.1-03 / 12.1-04 / 12.1-05 SUMMARYs"
      via: "release-log bullets summarize the wave-1 router contract + wave-2 per-game wirings + wave-3 closure"
      pattern: "Phase 12.1"
    - from: ".planning/STATE.md `Next action`"
      to: "Phase 13 — Win/Loss Banner + A11y Gating"
      via: "STATE.md frontmatter + Current Position narrative both pointed at Phase 13"
      pattern: "/gsd-plan-phase 13"
---

<objective>
Close Phase 12.1 by manually re-auditing the 12 Small-zone matrix rows surfaced as FAIL in `12-VIDEO-MANUAL-CHECK.md`'s 2026-05-13 sign-off block, recording the PASS outcomes in an APPEND-ONLY "Phase 12.1 Closure Re-audit" section per CONTEXT D-13, and propagating the closure across `Docs/releases/v1.2.md` (per CLAUDE.md §0.3 + §8.14), `.planning/STATE.md`, and `.planning/ROADMAP.md` (v1.2 Progress table — Phase 12.1 row flips to 5/5 Complete; Phase 12 row updates from PARTIAL → Closed).

This plan is the phase-close commit per CLAUDE.md §8.10. The 12.1-01 router contract (added `anchors.headerBar`), 12.1-02 / 12.1-03 / 12.1-04 per-game `smallZoneLayout` wirings (Mines / Merge / Nonogram) have already landed in Waves 1 + 2. This Wave 3 plan verifies them against the human-eyes audit gate, then closes the docs.

Purpose: Phase 12.1 only ships when the 12 Small-zone rows actually pass on Classic + Dracula on a physical-shape simulator (iPhone 17 Pro Max), per CLAUDE.md §8.12. Manual sim audit is mandatory — `autonomous: false`. If any row FAILs, the plan halts and a `12.1-VERIFICATION.md` gap report is authored instead of flipping SC1 + SC3 to PASS.

Output: 4 doc files modified (no code changes — code work landed in 12.1-01 / 02 / 03 / 04). v1.2 ship status advances to "all 6 v1.2 phases ready; Phase 13 next."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md
@.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md
@.planning/phases/12-merge-nonogram-adoption/12-VERIFICATION.md
@CLAUDE.md

# Wave 1 + 2 prereq SUMMARYs (read for closure-summary context)
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-01-PLAN.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-02-PLAN.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-03-PLAN.md
@.planning/phases/12.1-small-zone-routing-gap-closure/12.1-04-PLAN.md

# Analog: original P12 manual-check plan — mirror its task shape
@.planning/phases/12-merge-nonogram-adoption/12-06-PLAN.md

# Files modified by this plan
@Docs/releases/v1.2.md

<interfaces>
<!-- Bullet template from CONTEXT §specifics — to be appended verbatim to Docs/releases/v1.2.md -->

```markdown
## Phase 12.1 — Small-Zone Routing Gap Closure (closes SC1 + SC3)
- Fixed: ModePill + HeaderBar no longer collide with PiP overlay on Small zones in Minesweeper, Merge, and Nonogram (closes P11 carryforward defect).
- Internal: extended SlotAnchorMap with headerBar slot; added smallZoneLayout to each adopter game view; manual re-audit appended to 12-VIDEO-MANUAL-CHECK.md.
```

<!-- Manual re-audit matrix template from CONTEXT §specifics — to be appended verbatim to 12-VIDEO-MANUAL-CHECK.md under a new "## Phase 12.1 Closure Re-audit" section -->

```markdown
| Game       | Zone              | Classic | Dracula | Sign-off |
|------------|-------------------|---------|---------|----------|
| Mines      | smallTopLeft      | TBD     | TBD     | TBD      |
| Mines      | smallTopRight     | TBD     | TBD     | TBD      |
| Mines      | smallBottomLeft   | TBD     | TBD     | TBD      |
| Mines      | smallBottomRight  | TBD     | TBD     | TBD      |
| Merge      | smallTopLeft      | TBD     | TBD     | TBD      |
| Merge      | smallTopRight     | TBD     | TBD     | TBD      |
| Merge      | smallBottomLeft   | TBD     | TBD     | TBD      |
| Merge      | smallBottomRight  | TBD     | TBD     | TBD      |
| Nonogram   | smallTopLeft      | TBD     | TBD     | TBD      |
| Nonogram   | smallTopRight     | TBD     | TBD     | TBD      |
| Nonogram   | smallBottomLeft   | TBD     | TBD     | TBD      |
| Nonogram   | smallBottomRight  | TBD     | TBD     | TBD      |
```

Per CONTEXT §specifics, Pass criteria per row:
1. ModePill does not overlap PiP overlay
2. HeaderBar chips do not overlap PiP overlay
3. Board remains tappable / swipeable / pinch-zoomable per the game's existing gesture set
4. Legibility regression check vs. Large-zone worst-case PASSES on both presets

<!-- ROADMAP Phase 12.1 Success Criteria probes (must verify from closed state) -->
- SC1 picker routing wired: `grep -l "anchors.picker" gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*+VideoMode.swift | wc -l` returns 3
- SC2 headerBar seam exists: `grep "let headerBar: SlotAnchor" gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns 1; `grep "headerBar:" gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returns 7 (1 struct field + 6 switch cases)
- SC3 12-row re-audit PASS: appended Closure Re-audit section in 12-VIDEO-MANUAL-CHECK.md has 12 PASS rows
- SC4 off-path byte-identity: `git diff HEAD~5 -- gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*GameView.swift` scoped to the off-path body branch shows 0 changed lines (existingLayout call site unchanged)
- SC5 Large-zone untouched: `git diff HEAD~5 -- gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*+VideoMode.swift` scoped to `largeZoneLayout` / `compactRowComposed` shows 0 changed lines
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual re-audit — 12 Small-zone rows (4 zones × 3 games) on Classic + Dracula on iPhone 17 Pro Max sim</name>
  <files>(observations only — no file edits yet; Task 2 records them)</files>
  <what-built>
    Waves 1 + 2 of Phase 12.1 landed:
    - 12.1-01: `SlotAnchorMap` extended with `headerBar: SlotAnchor` field; `VideoModeSlotRouter.anchors(for:)` returns the 6 D-02-locked headerBar values; `VideoModeSlotRouterTests` updated with 6 new `#expect(map.headerBar == ...)` lines per zone.
    - 12.1-02: `MinesweeperGameView+VideoMode.swift` gained a `smallZoneLayout` view that consumes BOTH `anchors.headerBar` and `anchors.picker`. `MinesweeperGameView.body`'s `else` arm now calls `smallZoneLayout` instead of `existingLayout` when Video Mode is On + the zone is a Small zone. `MinesweeperBoardView` untouched (D-MS-10 honored).
    - 12.1-03: Same pattern for `MergeGameView+VideoMode.swift`. `MergeBoardView` untouched (D-MG-10).
    - 12.1-04: Same pattern for `NonogramGameView+VideoMode.swift`. `NonogramBoardView.computeLayout` floor seam untouched (D-NG-10 — 12pt floor preserved).

    This checkpoint is the human-eyes verification that the 4 Bottom L/R + Top L/R Small zones × 3 games × Classic + Dracula now PASS the 4 pass criteria from CONTEXT §specifics. SC1 + SC3 in `12-VIDEO-MANUAL-CHECK.md` flip from FAIL to PASS only if all 12 rows PASS on both presets.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Build + run on iPhone 17 Pro Max simulator: `cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination "platform=iOS Simulator,name=iPhone 17 Pro Max"`.
    2. Settings → Theme → Classic (Chrome Diner — the default preset).
    3. Settings → Video Mode → On.

    **The 12-row audit matrix (4 zones × 3 games):**

    For EACH of the 4 Small zones (`smallTopLeft`, `smallTopRight`, `smallBottomLeft`, `smallBottomRight`) AND for EACH of the 3 games (Mines, Merge, Nonogram) — 12 rows total per preset — verify the 4 pass criteria from CONTEXT §specifics:

    1. **ModePill does not overlap PiP overlay.** The per-game ModePill (`MinesweeperModePill` = Reveal/Flag, `MergeModePill` = Win/Infinite, `NonogramModePill` = Place/Mark) renders away from the covered corner per `anchors.picker`. On Bottom L/R zones, the ModePill must NOT be in the bottom-center (where it would collide with the bottom-PiP overlay).
    2. **HeaderBar chips do not overlap PiP overlay.** The per-game HeaderBar (`MinesweeperHeaderBar` chips: MinesRemaining + Timer; `MergeHeaderBar` chips: Score + Best; `NonogramHeaderBar` chips: Size-or-Lives + Timer) renders away from the covered corner per `anchors.headerBar` (D-02). On Top L/R zones, the HeaderBar must NOT be in the top-center.
    3. **Board remains tappable / swipeable / pinch-zoomable per the game's existing gesture set:**
       - **Mines:** Tap reveals a cell; long-press flags; pinch-zoom (`MagnifyGesture` + `clampZoomScale` from 06.1-03 + A11Y-05) still works WITHOUT regression. D-MS-10 contract.
       - **Merge:** Swipe up/down/left/right merges tiles; no edge-swipe-back gesture hijack. D-MG-10 contract.
       - **Nonogram:** Tap fills/marks a cell; slide-fill drags across multiple cells; long-press marks. D-NG-10 contract.
    4. **Legibility regression vs. Large-zone worst-case is ABSENT on BOTH presets.** Compare each Small-zone render against the Phase 12 Large-zone audit screenshots (`Docs/screenshots/v1.2-phase-12/nonogram-hard-{classic,dracula}-largeBottom-locked.png` for Nonogram; Mines + Merge inherit their Large-zone PASS shapes from row 14 / row 2 of `11-VIDEO-MANUAL-CHECK.md` + `12-VIDEO-MANUAL-CHECK.md`). No chip clipped at the safe-area edge; no fill mark indistinguishable from X mark; no super-cell rule invisible; no ModePill label truncated.

    **Per-row procedure (repeat 24 times — 12 rows × 2 presets):**

    1. Settings → Video Mode → On.
    2. Settings → Video location → select the target Small zone.
    3. Home → tap the target game tile.
    4. Verify pass criteria 1 (ModePill placement) — eyeball the ModePill position vs. where the small PiP corner overlay lives. Pass = no overlap.
    5. Verify pass criteria 2 (HeaderBar placement) — eyeball the HeaderBar chips' position vs. the small PiP corner. Pass = no overlap.
    6. Verify pass criteria 3 (gesture set) — run the game-appropriate gesture; confirm it registers and the game responds.
    7. Verify pass criteria 4 (legibility vs Large-zone audit) — Pass = nothing newly clipped/illegible/regressed vs the Large-zone PASS reference.
    8. Mark the row PASS or FAIL with a one-line note per (Game, Zone, Preset) tuple.
    9. Settings → Theme → Dracula. Repeat steps 2–8 for the Dracula leg.

    **Halt rules (per `<halt_conditions>` below):**

    - If ANY of the 12 rows × 2 presets = 24 cells FAILs: STOP. Do NOT proceed to Task 2 with a partial PASS. Instead, log the FAIL details and resume-signal Task 2 to author a `12.1-VERIFICATION.md` gap report. Do not silently flip SC1 / SC3 to PASS — that re-introduces the exact failure mode that caused this gap.
    - If a Loud preset surfaces a legibility regression that was NOT present in the Phase 12 Large-zone audit (i.e., something newly broken specifically on Small zones that Large zones didn't catch): flag it explicitly. That's a NEW finding, not a closure — it needs a 12.2 gap-closure plan, not a Phase 12.1 sign-off flip.

    **Resume signal options:**
    - `All 12 rows PASS on Classic + Dracula — phase 12.1 ships` → Task 2 proceeds with the additive sign-off block.
    - `FAIL — <game> <zone> <preset>: <one-line description>` → Task 2 authors `12.1-VERIFICATION.md` gap report instead of closing.
    - `NEW FINDING — <description>` → Task 2 halts and Phase 12.2 planning is queued.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — Task 2 records the outcome. Resume-signal gates Task 2." && test -f /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md</automated>
  </verify>
  <done>
    Auditor has run all 12 rows × 2 presets = 24 audit cells on iPhone 17 Pro Max simulator and produced a per-row PASS / FAIL observation list. Resume signal received per the options above.
  </done>
  <resume-signal>Type `All 12 rows PASS on Classic + Dracula — phase 12.1 ships`, OR `FAIL — <game> <zone> <preset>: <one-line>`, OR `NEW FINDING — <description>`.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append "Phase 12.1 Closure Re-audit" section to 12-VIDEO-MANUAL-CHECK.md (additive — preserves 2026-05-13 P12 sign-off block)</name>
  <files>.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md (existing 2026-05-13 P12 sign-off block — preserve verbatim)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md §specifics "Manual re-audit matrix" + D-13
    - Task 1's per-row observations (from the auditor's resume signal + notes)
  </read_first>
  <action>
    1. Open `.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md`.

    2. APPEND (do NOT edit existing rows — additive only) a new section at the END of the file, AFTER the existing "## Gap Description (SC1 + SC3 FAIL)" block:

    ```markdown
    ---

    ## Phase 12.1 Closure Re-audit

    **Audited:** 2026-05-14
    **Auditor:** gabrielnielsen
    **Device:** iPhone 17 Pro Max simulator
    **Presets:** Classic (Chrome Diner) + Dracula (Loud canary per CLAUDE.md §8.12)
    **Source:** Phase 12.1 — closes the SC1 + SC3 gap recorded in the 2026-05-13 P12 sign-off above.

    ### Re-audit Matrix (4 Small zones × 3 games)

    Pass criteria per row (CONTEXT D-12 + §specifics):
    1. ModePill does not overlap PiP overlay.
    2. HeaderBar chips do not overlap PiP overlay.
    3. Board remains tappable / swipeable / pinch-zoomable per the game's existing gesture set (Mines: tap + long-press + MagnifyGesture; Merge: swipe; Nonogram: tap + slide-fill + long-press).
    4. Legibility regression vs. Large-zone worst-case PASSES on both presets.

    | Game       | Zone              | Classic       | Dracula       | Sign-off                        |
    |------------|-------------------|---------------|---------------|---------------------------------|
    | Mines      | smallTopLeft      | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Mines      | smallTopRight     | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Mines      | smallBottomLeft   | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Mines      | smallBottomRight  | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Merge      | smallTopLeft      | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Merge      | smallTopRight     | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Merge      | smallBottomLeft   | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Merge      | smallBottomRight  | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Nonogram   | smallTopLeft      | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Nonogram   | smallTopRight     | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Nonogram   | smallBottomLeft   | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |
    | Nonogram   | smallBottomRight  | ☑ PASS        | ☑ PASS        | gabrielnielsen 2026-05-14       |

    ### Updated SC1 + SC3 Sign-off (closes the 2026-05-13 PARTIAL)

    | Success criterion | Verifier | Date | Status |
    |-------------------|----------|------|--------|
    | SC1 — Merge plays across 6 zones (closure of 2026-05-13 FAIL via Phase 12.1) | gabrielnielsen | 2026-05-14 | ☑ PASS |
    | SC3 — Classic + Loud × both games × 6 zones legibility (closure of 2026-05-13 FAIL via Phase 12.1) | gabrielnielsen | 2026-05-14 | ☑ PASS |

    SC2 (Nonogram Hard hint legibility — Large zones), SC4 (Off-restore byte-identity), and SC5 (Compact row consumed verbatim) remain PASS as signed off 2026-05-13 — Phase 12.1 did not touch Large-zone code paths or shared-component identity. D-MS-10 / D-MG-10 / D-NG-10 untouched contracts preserved across Waves 1 + 2.

    ### Phase 12 — Final Top-level Status

    With SC1 + SC3 now PASS via Phase 12.1 closure (and SC2 / SC4 / SC5 already PASS from 2026-05-13), Phase 12's top-level status advances from PARTIAL to **PASS (closed by Phase 12.1)**.
    ```

    Use the auditor's per-row notes from Task 1 to fill the "Sign-off" column with date + verifier (default `gabrielnielsen 2026-05-14`). If the auditor recorded a non-trivial observation for a row (e.g., "ModePill renders at .topTrailing per D-02 — confirmed visually clear of small PiP overlay"), append that note to the Sign-off cell as a short parenthetical. Default to the dated sign-off if no per-row note was recorded.

    Do NOT modify the existing YAML frontmatter at the top of the file (the 2026-05-13 frontmatter records the original P12 sign-off — preserving it is part of the audit history per D-13). The new closure section is plain markdown after the existing content.

    Do NOT modify the existing matrix table rows 1-24 (the original P12 24-row matrix), the existing "## Sign-off" block (the 2026-05-13 SC1-SC5 + Top-level rows), or the existing "## Gap Description (SC1 + SC3 FAIL)" block. They are the historical record per D-13.
  </action>
  <verify>
    <automated>grep -c "Phase 12.1 Closure Re-audit" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 12.1 Closure Re-audit" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `1` (additive section appended)
    - `grep -cE "SC1.*PASS|SC3.*PASS" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns ≥ `2` (the new 2026-05-14 PASS rows under "Updated SC1 + SC3 Sign-off"; the original 2026-05-13 FAIL rows preserved verbatim — note original used `☑ FAIL` markers, not `☑ PASS`, so the PASS count grows by exactly 2)
    - `grep -c "2026-05-14" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns ≥ `2` (closure section header + at least one sign-off cell)
    - `grep -cE "^\| Mines\s+\| small" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `4` (4 Mines small-zone rows in the new closure table)
    - `grep -cE "^\| Merge\s+\| small" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `4` (4 Merge small-zone rows in the new closure table)
    - `grep -cE "^\| Nonogram\s+\| small" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `4` (4 Nonogram small-zone rows in the new closure table)
    - The original 2026-05-13 sign-off block is preserved: `grep -c "SC2 — Nonogram Hard hint legibility in Large zones | gabrielnielsen | 2026-05-13 | ☑ PASS" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `1` (untouched)
    - The original gap description block is preserved: `grep -c "## Gap Description (SC1 + SC3 FAIL)" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `1` (untouched — it's the historical record)
    - Phase 12 top-level status callout: `grep -c "PASS (closed by Phase 12.1)" /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `1`
  </acceptance_criteria>
  <done>
    `12-VIDEO-MANUAL-CHECK.md` now has a "## Phase 12.1 Closure Re-audit" section appended below the original P12 content; the 12-row matrix is fully PASS-marked (Classic + Dracula); the new SC1 + SC3 sign-off rows dated 2026-05-14 are added without rewriting the 2026-05-13 PARTIAL sign-off; the original gap-description block is preserved as the audit history.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append Phase 12.1 entries to Docs/releases/v1.2.md (verbatim CONTEXT bullet template; MARKETING_VERSION verified 1.1)</name>
  <files>Docs/releases/v1.2.md</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md (existing P9 / P10 / P11 / P12 entries — preserve verbatim; locate the Phase 12 entry block to insert after)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/phases/12.1-small-zone-routing-gap-closure/12.1-CONTEXT.md §specifics "Release log bullet template"
    - /Users/gabrielnielsen/Desktop/GameKit/CLAUDE.md §0.3 (release-log convention) + §8.14 (same-commit rule)
    - /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit.xcodeproj/project.pbxproj — confirm `MARKETING_VERSION = 1.1` still holds at HEAD (Phase 13 will bump to 1.2; Phase 12.1 is still on 1.1).
  </read_first>
  <action>
    1. Verify `MARKETING_VERSION`:

       Run: `grep "MARKETING_VERSION" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit.xcodeproj/project.pbxproj | head -1`

       Expected output: `MARKETING_VERSION = 1.1;` (Phase 13 ships the 1.1 → 1.2 bump per ROADMAP §Phase 13 + STATE.md Current Position lock). If the value is NOT `1.1`, STOP and surface the drift — do NOT append a Phase 12.1 entry to a wrong-version release log. Per CLAUDE.md §0.3, a new file is opened when MARKETING_VERSION is bumped; appending Phase 12.1 to `v1.2.md` is only correct while MARKETING_VERSION stays at `1.1` (i.e., v1.2 is in-progress).

    2. Open `Docs/releases/v1.2.md`. The Phase 12 entries already exist in three sections — locate them:
       - §User-facing changes: Phase 12 (Merge + Nonogram Video Mode adoption) — multi-line bullet around line 43-71 (includes the "Known gap (carryforward from Phase 11)" sub-bullet at the end).
       - §Internal changes: Phase 12-01 through Phase 12-06 bullet block (around lines 174-232).
       - §Risks / notes: Phase 12-related entries including the "small-zone picker + chip routing gap (P11 carryforward)" bullet (around lines 322-354).

    3. APPEND (do not insert mid-file; chronological order after Phase 12) to §User-facing changes AFTER the existing Phase 12 bullet block. Use the CONTEXT §specifics "Release log bullet template" VERBATIM — do NOT paraphrase or simplify:

       ```markdown
       ## Phase 12.1 — Small-Zone Routing Gap Closure (closes SC1 + SC3)
       - Fixed: ModePill + HeaderBar no longer collide with PiP overlay on Small zones in Minesweeper, Merge, and Nonogram (closes P11 carryforward defect).
       - Internal: extended SlotAnchorMap with headerBar slot; added smallZoneLayout to each adopter game view; manual re-audit appended to 12-VIDEO-MANUAL-CHECK.md.
       ```

       Note on placement: CLAUDE.md §0.3 expects bullets under the existing top-level section headers ("User-facing changes", "Internal changes", "Fixes", "Risks/notes"). The CONTEXT template uses a `## Phase 12.1 — ...` heading directly — this is the verbatim template per the user-locked specifics, so we keep that exact shape. It will land after the Phase 12 entries in the §User-facing changes section (it spans both user-facing + internal — the template is intentionally compact for a small gap-closure phase).

    4. APPEND a Phase 12.1 entry to §Risks / notes AFTER the existing "Phase 12 — small-zone picker + chip routing gap (P11 carryforward)" bullet to flag closure:

       ```markdown
       - **Phase 12.1 — small-zone routing gap CLOSED.** The P11 carryforward
         defect flagged above is now closed. `VideoModeSlotRouter.SlotAnchorMap`
         gained a `headerBar: SlotAnchor` field (Plan 12.1-01) wired per the
         D-02-locked anchor table; each adopter game view (`MinesweeperGameView+VideoMode.swift`
         / `MergeGameView+VideoMode.swift` / `NonogramGameView+VideoMode.swift`)
         gained a `smallZoneLayout` view that consumes both `anchors.headerBar`
         and `anchors.picker` (Plans 12.1-02 / 12.1-03 / 12.1-04). 12 Small-zone
         rows (4 zones × 3 games) re-audited on Classic + Dracula on iPhone 17
         Pro Max sim per CLAUDE.md §8.12 — all 12 PASS (Plan 12.1-05). SC1 + SC3
         in `12-VIDEO-MANUAL-CHECK.md` flipped from FAIL → PASS via an additive
         "Phase 12.1 Closure Re-audit" section dated 2026-05-14. Large-zone path
         (`largeZoneLayout` / `compactRowComposed`) untouched (SC5 preserved);
         off-path branch untouched (VIDEO-13 / SC4 preserved); board view files
         (`MinesweeperBoardView`, `MergeBoardView`, `NonogramBoardView`) untouched
         (D-MS-10 / D-MG-10 / D-NG-10 honored — the `MagnifyGesture` stack, swipe
         gesture, and 12pt Nonogram cell-size floor all preserved).
       ```

    5. Do NOT bump `MARKETING_VERSION` in pbxproj. Per CONTEXT line 141 / STATE.md / ROADMAP §Phase 13, the 1.1 → 1.2 bump ships in Phase 13.

    6. Do NOT touch §QA checklist (per-version-final, not per-phase).

    7. Do NOT modify the existing P9 / P10 / P11 / P12 entries — they are the historical record per CLAUDE.md §0.3 append-only convention. The acceptance criteria below enforce this via grep counts.
  </action>
  <verify>
    <automated>grep -c "Phase 12.1" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 12.1 — Small-Zone Routing Gap Closure (closes SC1 + SC3)" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (CONTEXT bullet template appended verbatim)
    - `grep -c "Fixed: ModePill + HeaderBar no longer collide with PiP overlay on Small zones in Minesweeper, Merge, and Nonogram" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (Fixed bullet from template)
    - `grep -c "Internal: extended SlotAnchorMap with headerBar slot; added smallZoneLayout to each adopter game view" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (Internal bullet from template)
    - `grep -c "Phase 12.1 — small-zone routing gap CLOSED" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (Risks/notes closure bullet)
    - `grep -c "Phase 12.1" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns ≥ `2` (the heading + the Risks/notes bullet; possibly more references inside body text)
    - `grep -cE "Phase 9|Phase 10|Phase 11|Phase 12" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns ≥ `12` (existing entries preserved — Phase 9 + Phase 10 + Phase 11 + Phase 12 bullets across User-facing + Internal + Risks/notes sections)
    - `grep -c "MARKETING_VERSION remains 1.1" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (existing version note preserved; Phase 13 bumps it)
    - `grep "MARKETING_VERSION = 1.1" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit.xcodeproj/project.pbxproj | wc -l` returns ≥ `1` (pbxproj still on 1.1 — Phase 13 ships the bump)
    - The original Phase 12 carryforward-gap bullet is preserved verbatim: `grep -c "Phase 12 — small-zone picker + chip routing gap (P11 carryforward)" /Users/gabrielnielsen/Desktop/GameKit/Docs/releases/v1.2.md` returns `1` (NOT deleted; new closure bullet is additive)
  </acceptance_criteria>
  <done>
    `Docs/releases/v1.2.md` has Phase 12.1 closure entries appended — one Fixed bullet + one Internal bullet under the CONTEXT-template heading (verbatim) + one closure bullet under Risks/notes; existing P9 / P10 / P11 / P12 entries preserved; `MARKETING_VERSION` still 1.1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Flip .planning/STATE.md — Phase 12.1 Complete, Current Position advances to Phase 13</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md (full file — confirm frontmatter shape + Current Position narrative + Next action)
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md §Phase 13 (for the Next action target — Win/Loss Banner + A11y Gating)
  </read_first>
  <action>
    1. Open `.planning/STATE.md`. Update the YAML frontmatter:

       - `stopped_at`: `Completed 12.1-05-PLAN.md — Phase 12.1 closed (SC1 + SC3 PASS; small-zone routing wired)`
       - `last_updated`: `"2026-05-14T<HH:MM:SS.000>Z"` (replace `<HH:MM:SS>` with the actual time at write-time using `date -u +%H:%M:%S`)
       - `last_activity`: `2026-05-14`
       - `progress.completed_phases`: bump from `12` to `13` (Phase 12.1 now counts as a completed phase per ROADMAP's v1.2 Progress table)
       - `progress.completed_plans`: bump from `78` to `83` (12.1-01 + 12.1-02 + 12.1-03 + 12.1-04 + 12.1-05 = +5 plans)
       - `progress.total_phases`: stays `14` (12.1 was inserted between 12 and 13 — see ROADMAP v1.2 Progress table where it appears as its own row)
       - `progress.total_plans`: bump from `81` to `86` (+5 from Phase 12.1 plans)
       - `progress.percent`: recalculate — `(83 / 86) * 100` rounded = `97`

    2. Update the "## Current Position" section:

       ```markdown
       ## Current Position

       Milestone: v1.2 — Video Mode
       Phase: 12.1 (small-zone-routing-gap-closure) — COMPLETE
       Plan: 5 of 5 (12.1-05 executed; SC1 + SC3 PASS via 12-row Closure Re-audit on Classic + Dracula)
       Next action: `/gsd-plan-phase 13`
       Last activity: 2026-05-14

       Progress: [█████████░] 97%

       **Next action:** `/gsd-plan-phase 13` — begin Phase 13 (Win/Loss Banner + A11y Gating). Phase 13 is the v1.2 closure phase: replaces full-screen win/loss overlays with a non-board-covering banner that avoids the selected PiP zone, routes haptics / SFX / animations through the existing Settings toggles + `accessibilityReduceMotion`, and ships the `MARKETING_VERSION` 1.1 → 1.2 bump. Depends on Phase 12 (Merge + Nonogram win/loss flows) + Phase 11 (Minesweeper win/loss flow) + Phase 12.1 (small-zone routing — closed 2026-05-14).

       **Phase 12.1 — what shipped:** Plans 12.1-01..12.1-05 closed the SC1 + SC3 small-zone routing gap. 12.1-01 extended `SlotAnchorMap` with a `headerBar: SlotAnchor` field + filled all 6 switch cases per D-02. 12.1-02 / 12.1-03 / 12.1-04 wired each adopter game view's `smallZoneLayout` to consume both `anchors.headerBar` + `anchors.picker` (Mines / Merge / Nonogram in parallel — Wave 2 had no file-overlap conflicts). 12.1-05 ran the 12-row manual re-audit (4 zones × 3 games × Classic + Dracula on iPhone 17 Pro Max sim) — all PASS — and appended a "Phase 12.1 Closure Re-audit" section to `12-VIDEO-MANUAL-CHECK.md` per D-13. Phase 12 status advanced from PARTIAL to PASS (closed by Phase 12.1). Off-path byte-identity (VIDEO-13 / SC4) and Large-zone path (SC5) both preserved — board views (`MinesweeperBoardView` / `MergeBoardView` / `NonogramBoardView`) untouched.

       **Phase 13 forward-looking:** The router contract gained `headerBar` in 12.1 — Phase 13 will likely extend `SlotAnchorMap` further with a `banner: SlotAnchor` field per the forward-compat note in `VideoModeSlotRouter.swift`. The pattern is now proven.
       ```

    3. Preserve all other existing sections in STATE.md (v1.0 Carry-Over, Performance Metrics, plan completion tables, decision logs, blockers, etc.) — they are the historical record. This task is APPEND-AND-FLIP-FRONTMATTER-ONLY for the "Current Position" + frontmatter blocks.

    4. The plan completion table near line 139 may optionally gain rows for Phase 12.1 P01..P05 (mirroring the existing Phase 12 P01..P06 row shape) — if the table is auto-generated from elsewhere, leave it untouched; if it's hand-maintained, append the 5 rows. (Inspect the table's surrounding comments to determine which.)
  </action>
  <verify>
    <automated>grep -cE "Phase 12.1|12\.1" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md && grep -c "Next action: \`/gsd-plan-phase 13\`" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "Phase: 12.1 \\(small-zone-routing-gap-closure\\) — COMPLETE" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "Plan: 5 of 5" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "Next action: \`/gsd-plan-phase 13\`" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns ≥ `1` (frontmatter + Current Position narrative both point at 13)
    - `grep -c "last_updated: \"2026-05-14T" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "stopped_at: Completed 12.1-05-PLAN.md" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "completed_phases: 13" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "completed_plans: 83" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1`
    - `grep -c "Phase 13 (Win/Loss Banner + A11y Gating)" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns ≥ `1`
    - Existing v1.0 carry-over section preserved: `grep -c "## v1.0 Carry-Over" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1` (NOT deleted)
    - Existing Performance Metrics section preserved: `grep -c "## Performance Metrics" /Users/gabrielnielsen/Desktop/GameKit/.planning/STATE.md` returns `1` (NOT deleted)
  </acceptance_criteria>
  <done>
    STATE.md frontmatter advanced to last_updated 2026-05-14; Current Position narrative points at Phase 13; progress percent recalculated to 97%; historical sections untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update .planning/ROADMAP.md v1.2 Progress table (Phase 12.1 → 5/5 Complete; Phase 12 → Closed)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md (focus on `## v1.2 Progress` table near line 378-391 + "Last updated" line near line 4 + the Phase 12.1 entry around line 352-364)
  </read_first>
  <action>
    1. Open `.planning/ROADMAP.md`. Update the document header (line ~4):

       Replace the existing "**Last updated:** 2026-05-13 (Phase 12 closes PARTIAL — 6/6 plans, gaps_found on SC1+SC3 small-zone routing; Phase 12.1 required for v1.2 ship; v1.2 progress 5/6 phases complete)" line with:

       ```markdown
       **Last updated:** 2026-05-14 (Phase 12.1 CLOSED — 5/5 plans, SC1 + SC3 PASS via 12-row re-audit on Classic + Dracula; Phase 12 advances from PARTIAL to Closed; v1.2 progress 6/7 phases complete, only Phase 13 remaining for ship)
       ```

    2. Update the §v1.2 Progress table rows for Phase 12 and Phase 12.1:

       **Phase 12 row** (currently):
       ```
       | 12. Merge + Nonogram Adoption | 6/6 | Closed PARTIAL (gaps_found — 12.1 required) | 2026-05-13 |
       ```

       Change to:
       ```
       | 12. Merge + Nonogram Adoption | 6/6 | Closed (gaps closed by Phase 12.1) | 2026-05-13 |
       ```

       **Phase 12.1 row** (currently):
       ```
       | 12.1. Small-Zone Routing Gap Closure | 0/TBD | Planning | - |
       ```

       Change to:
       ```
       | 12.1. Small-Zone Routing Gap Closure | 5/5 | Complete | 2026-05-14 |
       ```

    3. Update the §Phase 12.1 narrative (the "**Plans**: TBD" line near line 362):

       Change `**Plans**: TBD` to:

       ```markdown
       **Plans**: 5 plans complete (2026-05-14)
         1. 12.1-01-PLAN.md — Extend `SlotAnchorMap` with `headerBar: SlotAnchor`; fill 6 switch cases per D-02; update `VideoModeSlotRouterTests`.
         2. 12.1-02-PLAN.md — `MinesweeperGameView+VideoMode.swift` gains `smallZoneLayout` consuming `anchors.headerBar` + `anchors.picker`; `MinesweeperBoardView` untouched.
         3. 12.1-03-PLAN.md — `MergeGameView+VideoMode.swift` gains `smallZoneLayout` (parallel with Mines); `MergeBoardView` untouched.
         4. 12.1-04-PLAN.md — `NonogramGameView+VideoMode.swift` gains `smallZoneLayout` (parallel with Mines + Merge); `NonogramBoardView` 12pt floor untouched.
         5. 12.1-05-PLAN.md — 12-row manual re-audit on Classic + Dracula; append "Phase 12.1 Closure Re-audit" to `12-VIDEO-MANUAL-CHECK.md`; append entries to `Docs/releases/v1.2.md`; flip STATE.md + ROADMAP.md.
       ```

    4. Preserve all 5 Success Criteria lines under §Phase 12.1 (lines 356-361) — they are the locked goal-backward criteria that this plan's `must_haves` block verifies against. Acceptance below enforces preservation via grep counts.

    5. Do NOT touch Phase 13 entry — Phase 13 is "Not started" and remains so until `/gsd-plan-phase 13` runs.
  </action>
  <verify>
    <automated>grep -c "12.1. Small-Zone Routing Gap Closure | 5/5 | Complete | 2026-05-14" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "12.1. Small-Zone Routing Gap Closure | 5/5 | Complete | 2026-05-14" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1` (12.1 row updated)
    - `grep -c "12. Merge + Nonogram Adoption | 6/6 | Closed (gaps closed by Phase 12.1) | 2026-05-13" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1` (P12 row updated)
    - `grep -c "Closed PARTIAL (gaps_found — 12.1 required)" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `0` (old PARTIAL status replaced)
    - `grep -c "Plans\**: TBD" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns ≤ `1` (only Phase 13 may still say TBD; Phase 12.1's TBD is replaced — if grep returns 0, Phase 13 also got filled, which is acceptable but unexpected here)
    - `grep -c "5 plans complete (2026-05-14)" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1` (Phase 12.1 plan list filled in)
    - `grep -c "12.1-01-PLAN.md" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1`
    - `grep -c "12.1-05-PLAN.md" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1`
    - All 5 SC lines preserved under §Phase 12.1: `grep -cE "Picker routing wired|HeaderBar reposition seam exists|Re-audited manual check|Off-path byte-identity preserved|Large-zone path unchanged" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `5`
    - Phase 13 row untouched (still "Not started"): `grep -c "13. Win/Loss Banner + A11y Gating | 0/TBD | Not started" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1`
    - Document header updated: `grep -c "Last updated:\** 2026-05-14" /Users/gabrielnielsen/Desktop/GameKit/.planning/ROADMAP.md` returns `1`
  </acceptance_criteria>
  <done>
    ROADMAP.md v1.2 Progress table reflects closure: Phase 12.1 = 5/5 Complete 2026-05-14; Phase 12 advances to "Closed (gaps closed by Phase 12.1)"; Phase 12.1 narrative §Plans block lists the 5 plans with their summaries; all 5 SC lines preserved; document header bumped to 2026-05-14; Phase 13 row untouched.
  </done>
</task>

</tasks>

<halt_conditions>
## Plan-level Halt / Failure Modes

This plan is `autonomous: false` because Task 1 is a manual sim audit. The following conditions HALT the plan and require gap-closure rather than silent flips:

1. **Any of the 12 rows FAILS on Classic or Dracula** (per Task 1 resume signal `FAIL — ...`).
   - **Action:** Do NOT proceed to Task 2's PASS-marking. Instead, author `.planning/phases/12.1-small-zone-routing-gap-closure/12.1-VERIFICATION.md` documenting which row(s) failed and why. SC1 + SC3 in `12-VIDEO-MANUAL-CHECK.md` stay FAIL. Phase 12.1 cannot close.
   - **Why:** Flipping SC1 + SC3 to PASS without all 12 rows passing re-introduces the exact silent-omission failure mode that caused this gap (per CONTEXT line 55 root cause). The 2026-05-13 P12 sign-off block was a single-leg audit; this closure requires both legs.

2. **A Loud preset surfaces a legibility regression that was NOT present in the Phase 12 Large-zone audit** (per Task 1 resume signal `NEW FINDING — ...`).
   - **Action:** Halt. The Large-zone path passed on Voltage + Dracula in 12-06 — if Small zones break specifically on a Loud preset in a way Large zones didn't catch, that's a new defect surface, not a closure of an existing gap. Queue Phase 12.2 with the new finding documented; leave Phase 12.1 SC1 + SC3 in PARTIAL.
   - **Why:** Per CLAUDE.md §8.12, contrast pass on Classic + one Loud preset is mandatory. A new Loud-preset legibility regression is a Phase 12.2 surface, not a Phase 12.1 sign-off concern.

3. **`MARKETING_VERSION` is NOT `1.1` at HEAD when Task 3 runs.**
   - **Action:** Halt Task 3. Do NOT append a Phase 12.1 entry to `Docs/releases/v1.2.md` — that's the wrong file for a future MARKETING_VERSION. Per CLAUDE.md §0.3, a new release-log file is opened when MARKETING_VERSION is bumped.
   - **Why:** Per ROADMAP §Phase 13 / STATE.md / CONTEXT line 141, the 1.1 → 1.2 bump ships in Phase 13, AFTER Phase 12.1 closes. If `MARKETING_VERSION` is already 1.2 at HEAD, something has skewed the milestone order — surface the drift before continuing.

4. **The 2026-05-13 P12 sign-off block in `12-VIDEO-MANUAL-CHECK.md` is missing or has been edited mid-closure.**
   - **Action:** Halt Task 2. The closure is an ADDITIVE re-audit per D-13 — overwriting the historical record corrupts the audit trail.
   - **Why:** D-13 lock — "Do NOT create a parallel `12.1-MANUAL-CHECK.md` — keeps the audit history in one place." That implies the existing P12 audit content is the durable history; closure is appended below it, not in place of it.

## Why `autonomous: false`

Per CLAUDE.md §8.12, game-screen changes are verified against Classic + at least one Loud/Moody preset before being called done. The 12-row matrix × 2 presets = 24 simulator runs require human eyes — no automated probe can confirm that "the ModePill does not overlap the PiP overlay" on an iPhone 17 Pro Max sim. Task 1's checkpoint is the gate that prevents Task 2 from auto-flipping SC1 + SC3 to PASS without the actual visual confirmation.
</halt_conditions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual sim audit ↔ sign-off doc fill-in | If verifier flips SC1 + SC3 to PASS without actually running each row × preset, the doc becomes false documentation. Task 1 is the checkpoint gate; Task 2 only records the outcome. The halt-condition #1 explicitly forbids silent flips. |
| append-only audit history ↔ closure section | The 2026-05-13 P12 sign-off block is the historical record per D-13. Overwriting it (rather than appending the closure section below) corrupts the audit trail. Acceptance criteria enforce preservation via grep counts. |
| release-log per-version file ↔ MARKETING_VERSION | Appending to `v1.2.md` is only correct while MARKETING_VERSION is 1.1. Task 3 has an explicit pbxproj check before write. |
| STATE.md frontmatter ↔ ROADMAP progress table | Both must agree on Phase 12.1 status. Drift (e.g., STATE says Complete, ROADMAP says Planning) misleads every future session. Task 4 + 5 are sequenced sequentially within Wave 3 to avoid cross-doc skew. |

## STRIDE Threat Register (ASVS L1 — manual verification + docs phase)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-CLOSE-1 | Spoofing | Pre-filled PASS marks on the 12-row matrix without running the audit | mitigate | Task 1 is `checkpoint:human-verify` with a `gate="blocking"` — Task 2 cannot run until the auditor types the resume signal. Halt condition #1 explicitly forbids silent PASS flips on partial results. |
| T-12.1-CLOSE-2 | Tampering | 2026-05-13 P12 sign-off block edited mid-closure (corrupting audit history) | mitigate | Task 2 action explicitly says "APPEND (do NOT edit existing rows — additive only)"; acceptance criteria assert the 2026-05-13 SC2 + SC4 + SC5 PASS rows and "## Gap Description (SC1 + SC3 FAIL)" block remain present via grep counts. Halt condition #4 catches mid-closure edits. |
| T-12.1-CLOSE-3 | Tampering | Existing P9 / P10 / P11 / P12 release-log entries overwritten by Phase 12.1 append | mitigate | Task 3 acceptance asserts `grep -cE "Phase 9|Phase 10|Phase 11|Phase 12"` returns ≥ 12 (existing entries preserved); append-only edit per CLAUDE.md §0.3. The existing "Phase 12 — small-zone picker + chip routing gap (P11 carryforward)" bullet must remain — its presence is the historical link between the gap and the closure. |
| T-12.1-CLOSE-4 | Repudiation | "All 12 rows PASS" claim recorded in the doc without proof | mitigate | Task 1's how-to-verify enumerates 4 pass criteria per row (ModePill placement / HeaderBar placement / gesture set / legibility regression) — the auditor cannot blanket-PASS a row without confirming each. The Sign-off column in the closure matrix records date + verifier per row for traceability. |
| T-12.1-CLOSE-5 | Information disclosure | New A11y label regression on the small-zone reposition surface | accept | A11y label preservation was verified at 12.1-02 / 12.1-03 / 12.1-04 acceptance time (each `smallZoneLayout` consumes the same HeaderBar + ModePill subviews as `existingLayout`, with `compact: false` default → off-path labels inherited). If the manual sweep catches a regression here, it triggers halt-condition #2 (new finding → Phase 12.2). |
| T-12.1-CLOSE-6 | Tampering | MARKETING_VERSION drift between pbxproj and v1.2.md target | mitigate | Task 3 step 1 explicitly grep-checks pbxproj BEFORE the append. Halt-condition #3 halts the task if the version isn't 1.1. |
| T-12.1-CLOSE-7 | Repudiation | STATE.md / ROADMAP.md drift on Phase 12.1 status | mitigate | Tasks 4 + 5 are sequenced within Wave 3 with explicit acceptance criteria cross-referencing the same `2026-05-14 / 5/5 / Complete` triple. Both docs must read the same closure semantics. |
</threat_model>

<verification>
1. **12-row re-audit closure section appended:** `grep -c "Phase 12.1 Closure Re-audit" .planning/phases/12-merge-nonogram-adoption/12-VIDEO-MANUAL-CHECK.md` returns `1`. The 12-row matrix is fully PASS-marked. SC1 + SC3 new sign-off rows dated 2026-05-14.

2. **Release-log entries appended verbatim:** `grep -c "Phase 12.1 — Small-Zone Routing Gap Closure (closes SC1 + SC3)" Docs/releases/v1.2.md` returns `1`; the Fixed + Internal bullets from CONTEXT specifics appear verbatim. Existing P9 / P10 / P11 / P12 entries preserved.

3. **MARKETING_VERSION unchanged:** `grep "MARKETING_VERSION = 1.1" gamekit/gamekit.xcodeproj/project.pbxproj | wc -l` returns ≥ `1` (Phase 13 ships the bump).

4. **STATE.md advanced to Phase 13:** `grep -c "Next action: \`/gsd-plan-phase 13\`" .planning/STATE.md` returns ≥ `1`; frontmatter `last_updated: "2026-05-14T...Z"`; `completed_phases: 13`; `completed_plans: 83`.

5. **ROADMAP.md v1.2 Progress table updated:** Phase 12.1 row shows `5/5 | Complete | 2026-05-14`; Phase 12 row shows `Closed (gaps closed by Phase 12.1)`; Phase 13 row untouched (`0/TBD | Not started`).

6. **ROADMAP §Phase 12.1 SC preservation:** All 5 SC lines (picker routing wired / headerBar reposition seam / re-audited manual check / off-path byte-identity / large-zone untouched) preserved verbatim — this plan's `must_haves` block verifies against them.

7. **Off-path + Large-zone byte-identity (SC4 + SC5) — verified via git diff scoped to phase span:**
   - `git diff HEAD~5 -- gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift` returns ZERO output (D-MS-10 / D-MS-17 + SC4 — the MagnifyGesture stack never moved).
   - `git diff HEAD~5 -- gamekit/gamekit/Games/Merge/MergeBoardView.swift` returns ZERO output (D-MG-10 — swipe gesture preserved).
   - `git diff HEAD~5 -- gamekit/gamekit/Games/Nonogram/NonogramBoardView.swift` returns ZERO output for the `computeLayout` floor seam region (D-NG-10 — 12pt floor lock preserved).
   - `git diff HEAD~5 -- gamekit/gamekit/Core/VideoCompactControlRow.swift` returns ZERO output (SC5 — shared compact-row component unchanged; Phase 12.1 only touched small-zone path).
</verification>

<success_criteria>
- **SC1 (ROADMAP §Phase 12.1):** Picker routing wired in all 3 adopter games. Verifiable via `grep -l "anchors.picker" gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*+VideoMode.swift | wc -l` returning 3 (consumed in Waves 1 + 2; verified in this plan's `must_haves`).
- **SC2 (ROADMAP §Phase 12.1):** HeaderBar reposition seam exists. Verifiable via `grep "let headerBar: SlotAnchor" gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returning 1 + `grep "headerBar:" gamekit/gamekit/Core/VideoModeSlotRouter.swift | wc -l` returning 7 (struct field + 6 switch cases per D-02).
- **SC3 (ROADMAP §Phase 12.1):** 12-row re-audit PASS on Classic + Dracula recorded in `12-VIDEO-MANUAL-CHECK.md` (additive "Phase 12.1 Closure Re-audit" section). SC1 + SC3 sign-off in that doc flips from FAIL (2026-05-13) to PASS (2026-05-14) — the original FAIL rows are preserved as audit history.
- **SC4 (ROADMAP §Phase 12.1):** Off-path byte-identity preserved. `git diff HEAD~5 -- gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*GameView.swift` scoped to off-path body branch (the `if !videoModeStore.isEnabled { return existingLayout }` arm) shows 0 changed lines.
- **SC5 (ROADMAP §Phase 12.1):** Large-zone path untouched. `git diff HEAD~5 -- gamekit/gamekit/Games/{Minesweeper,Merge,Nonogram}/*+VideoMode.swift` scoped to `largeZoneLayout` / `compactRowComposed` functions shows 0 changed lines.

- `Docs/releases/v1.2.md` has Phase 12.1 entries per CLAUDE.md §0.3 + §8.14 using the CONTEXT specifics template verbatim.
- `.planning/STATE.md` advanced to Phase 13 — Current Position narrative + frontmatter both point at `/gsd-plan-phase 13`.
- `.planning/ROADMAP.md` v1.2 Progress table: Phase 12.1 = 5/5 Complete 2026-05-14; Phase 12 = Closed (gaps closed by Phase 12.1).

Phase 12.1 ships when this plan closes. v1.2 advances to 6/7 phases complete (Phase 13 remains for the milestone close).
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-small-zone-routing-gap-closure/12.1-05-SUMMARY.md` per the standard SUMMARY template — focus on:
- Task 1 manual re-audit results: per-row PASS/FAIL outcomes across the 12 rows × 2 presets (Classic + Dracula). Reference auditor + 2026-05-14 date.
- Task 2 closure-section append: confirmation that the existing 2026-05-13 P12 sign-off block was preserved verbatim (audit history intact) and that the new section is purely additive.
- Task 3 release-log append: confirmation that the CONTEXT bullet template was used verbatim (no paraphrasing) + MARKETING_VERSION confirmed 1.1 at HEAD.
- Task 4 + 5 STATE.md + ROADMAP.md flips: confirmation that Phase 12.1 = Complete in both docs + Next action points at Phase 13.
- Phase 12.1 closure: confirm that the SC1 + SC3 gap originally surfaced in `12-VERIFICATION.md` is now closed; v1.2 ship status advances to 6/7 phases (Phase 13 remaining).
- Phase 13 enabling note: with `SlotAnchorMap` now extended with `headerBar`, the forward-compat seam for a `banner: SlotAnchor` field (per `VideoModeSlotRouter.swift`'s Phase 13 note) is now even cleaner — Phase 13 inherits the pattern from 12.1-01.
- Final phase status: SHIPS (with Phase 13 as the next forward-progress phase) OR HALTED (with reference to halt-condition trigger + gap-closure path).
</output>
