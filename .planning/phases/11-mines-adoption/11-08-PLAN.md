---
phase: 11-mines-adoption
plan: 08
type: execute
wave: 5
depends_on: [11-06, 11-07]
files_modified:
  - .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md
  - Docs/releases/v1.2.md
autonomous: false
requirements: [VIDEO-07, VIDEO-08]
requirements_addressed: [VIDEO-07, VIDEO-08]
tags: [verification, legibility-audit, off-restore, release-log, checkpoint, video-mode]

must_haves:
  truths:
    - "Easy (9×9/10) + Medium (16×16/40) playable across all 6 PiP locations — first-tap, reveal, long-press flag, restart, win, loss all complete; manual recipe doc per-location quick-check."
    - "Hard (16×30/99) matches Phase 8 Hard-Mines ADR exactly — smaller-cells implemented, ADR referenced by name in plan body, rejected alternatives NOT re-debated."
    - "Hard validated against ADR screenshots — final render parity for Large-top, Large-bottom, ≥1 Small."
    - "Legibility regression: Classic + Voltage/Dracula × Easy/Medium/Hard × 6 zones (CLAUDE.md §8.12)."
    - "Video Mode Off byte-identical to v1.0/v1.0.6.1 — MagnifyGesture (A11Y-05), Reveal/Flag toggle (MINES-12), animations (MINES-08) unchanged (VIDEO-13 spot-check)."
  artifacts:
    - path: ".planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md"
      provides: "Filled-in 18-row matrix; SC1 + SC3 signed off; final-render parity confirmed against ADR screenshots for Large-top, Large-bottom, and ≥1 Small zone"
      contains: "PASS"
    - path: "Docs/releases/v1.2.md"
      provides: "Phase 11 entries appended per CLAUDE.md §8.14: User-facing change bullet (adoption summary) + Internal changes bullets (chip extraction, BoardView floor, GameView layout branch, HomeView wrap)"
      contains: "Phase 11"
  key_links:
    - from: "Filled 11-VIDEO-MANUAL-CHECK.md SC1 sign-off"
      to: "ROADMAP.md SC1"
      via: "PASS on rows 1-12 + SC1 row in sign-off block"
      pattern: "SC1.*PASS"
    - from: "Filled 11-VIDEO-MANUAL-CHECK.md SC3 sign-off"
      to: "ROADMAP.md SC3 + ADR screenshots"
      via: "PASS on rows 13-18 + SC3 row in sign-off block"
      pattern: "SC3.*PASS"
    - from: "Off-restore spot-check"
      to: "ROADMAP.md SC5 (VIDEO-13)"
      via: "explicit byte-identical render confirmation in this plan's SUMMARY + release-log Risks/notes"
      pattern: "byte-identical|VIDEO-13"
    - from: "Docs/releases/v1.2.md Phase 11 entries"
      to: "11-01-SUMMARY.md, 11-03-SUMMARY.md, 11-04-SUMMARY.md, 11-05-SUMMARY.md"
      via: "release-log bullets cross-reference the SUMMARYs"
      pattern: "11-0[1-7]"
---

<objective>
Close out Phase 11 by:

1. **Running the SC4 legibility audit** (CLAUDE.md §8.12) — Classic + one
   Loud preset (Voltage or Dracula) × Easy / Medium / Hard × all 6 PiP
   zones. Fill `11-VIDEO-MANUAL-CHECK.md` rows 1-18 with PASS / FAIL marks
   based on observed behavior + final-render parity vs ADR screenshots.
2. **Running the SC5 Off-restore spot-check** — toggle Video Mode Off
   inside Settings → re-open Minesweeper → verify the layout renders
   byte-identical to v1.0 / v1.0.6.1:
   - `MinesweeperHeaderBar` visible (mines chip + timer chip).
   - `MinesweeperModePill` visible.
   - Nav-bar toolbar items visible (Back chevron, Restart icon, ToolbarMenu).
   - `MagnifyGesture` (A11Y-05) still pinches the board across 0.8x–2.0x.
   - Reveal/Flag interaction-mode toggle (MINES-12) still flips on tap.
   - Animation pass (MINES-08): reveal cascade, flag spring, win sweep,
     loss shake all fire as in v1.0.6.1.
3. **Signing off SC1 + SC3** in `11-VIDEO-MANUAL-CHECK.md`'s sign-off block.
4. **Appending Phase 11 entries to `Docs/releases/v1.2.md`** per CLAUDE.md
   §8.14 / §0.3 — one User-facing bullet + 4 Internal-changes bullets
   covering chip extraction (Plan 11-01), Video-Mode wrap + layout branch
   (Plans 11-03 + 11-04), cell-size floor (Plan 11-05), and A2 carry-
   forward resolution (Plan 11-06).

This is the phase-close commit per CLAUDE.md §8.10. SC1 + SC3 + SC4 + SC5
all land here; SC2 is closed by Plan 11-05's human-verify checkpoint and
referenced by SC3.

Output: 2 doc files modified (no code changes — the code work landed in
Plans 11-01 through 11-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mines-adoption/11-CONTEXT.md
@.planning/phases/11-mines-adoption/11-PATTERNS.md
@.planning/phases/11-mines-adoption/11-01-SUMMARY.md
@.planning/phases/11-mines-adoption/11-02-SUMMARY.md
@.planning/phases/11-mines-adoption/11-03-SUMMARY.md
@.planning/phases/11-mines-adoption/11-04-SUMMARY.md
@.planning/phases/11-mines-adoption/11-05-SUMMARY.md
@.planning/phases/11-mines-adoption/11-06-SUMMARY.md
@.planning/phases/11-mines-adoption/11-07-SUMMARY.md

# Files this plan modifies
@.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md
@Docs/releases/v1.2.md

# Release-log analog patterns (existing P9 + P10 entries)
# - Docs/releases/v1.2.md §"User-facing changes" P9 + P10 entries
# - Docs/releases/v1.2.md §"Internal changes" P9 + P10 entries
# - Docs/releases/v1.2.md §"Risks / notes" Hard-Mines untouched-MagnifyGesture entry
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual SC4 legibility + SC5 Off-restore sweep on simulator</name>
  <files>.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md</files>
  <action>See &lt;how-to-verify&gt; below. Auditor exercises every row of the 18-row matrix on Classic + Loud preset, then runs the SC5 Off-restore spot-check (HeaderBar / ModePill / toolbar / MagnifyGesture / MINES-12 / MINES-08 all byte-identical with Video Mode Off). Plan 11-08 Task 2 records PASS/FAIL in 11-VIDEO-MANUAL-CHECK.md per observation.</action>
  <verify><automated>grep -cE "(PASS|FAIL)" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md</automated></verify>
  <done>Sweep complete; auditor logs per-row notes; resume signal `SC1+SC3+SC4+SC5 all PASS — phase ships` OR `SC&lt;N&gt; FAIL — &lt;description&gt;` to flag gap-closure.</done>
  <what-built>
    Plans 11-01 through 11-06 land the full Phase 11 code surface:
    - Chip extraction (MinesRemainingChip + TimerChip).
    - VideoModeLocation.isLarge + HomeView wrap + three-way layout branch.
    - Large-zone compact-row composition with D-18 compactness reactions.
    - Video-Mode-aware cell-size floor (locked at Plan 11-05's audit).
    - A2 NavigationStack safeArea adjustment (if Plan 11-06 fired).

    This checkpoint is the SC1 + SC3 + SC4 manual sweep AND the SC5
    Off-restore spot-check. The verifier exercises every cell of the
    18-row matrix in 11-VIDEO-MANUAL-CHECK.md AND independently confirms
    Video Mode Off is byte-identical to v1.0.
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Build + run the app on iPhone 17 Pro Max simulator (`xcodebuild build` then launch).
    2. Settings → Theme → Classic (default preset).
    3. Settings → Video Mode → On.

    **Section A — SC1 + SC4 Easy/Medium sweep (rows 1-12):**

    For each of the 12 Easy/Medium rows in `11-VIDEO-MANUAL-CHECK.md`:

    1. Settings → Video location → select the row's `Zone`.
    2. Home → Minesweeper → switch difficulty to row's `Difficulty` via in-game Settings.
    3. Run each gesture column (First-tap / Reveal / Long-press flag / Restart / Win-or-Loss completes).
    4. Mark PASS in the row's checkboxes.
    5. If any gesture fails or the layout regresses, mark FAIL + add notes.

    Repeat the 12 rows on the Loud preset:
    - Settings → Theme → Dracula (or Voltage — pick one per CLAUDE.md §8.12 "Classic + one Loud preset").
    - For each row, re-verify First-tap + Long-press flag + the rendered legibility (mine glyph, 1-8 SF Symbols, flag glyph, mines-remaining chip text, timer chip text).
    - Annotate in the Notes column if any row regresses on the Loud preset.

    **Section B — SC3 + SC4 Hard sweep (rows 13-18):**

    For each of the 6 Hard rows:

    1. Set the row's Zone in Settings → Video location.
    2. Open Mines → Hard.
    3. Compare the rendered Hard board against the ADR screenshot listed in the row's Notes column:
       - Row 13 (`largeTop`): compare against `mines-hard-classic-pip-large.png` + `mines-hard-dracula-pip-large.png`.
       - Row 14 (`largeBottom`): same screenshots (mirrored vertically — the squeeze case symmetry).
       - Rows 15-18 (Small corners): compare against the 4 `mines-hard-dracula-pip-small-{tl,tr,bl,br}.png` screenshots row-by-row.
    4. Confirm:
       - The board fits inside the available area without horizontal scroll.
       - Adjacency numbers (1-8) are legible at the locked `minCellSizeVideoMode` floor from Plan 11-05.
       - Mine glyph + flag glyph are distinguishable.
       - On `.largeTop` / `.largeBottom`: nav-bar title visible, reserved band visible, compact row visible on the opposite edge, board between them.
       - On Small zones: HeaderBar + Board + ModePill visible (D-02); nav-bar toolbar repositioned per `VideoModeSlotRouter.anchors(for:)` (Back/Restart on the anti-PiP side).
    5. Mark PASS / FAIL per row. Add notes for any final-render parity deviation from the ADR shots.

    Repeat the 6 Hard rows on the Loud preset (Dracula or Voltage).

    **Section C — SC5 Off-restore spot-check:**

    1. Settings → Video Mode → Off.
    2. Re-open Minesweeper (navigate back to Home → tap Mines).
    3. Verify the layout renders v1.0-identical:
       - `MinesweeperHeaderBar` is visible at the top (mines remaining on the left, timer on the right).
       - The board renders at the v1.0 `cellSize` (not the lower Video-Mode floor — confirm via the actual rendered cell width or by switching to Hard and confirming the v1.0 18pt-floor behavior).
       - `MinesweeperModePill` (Reveal/Flag) is visible below the board.
       - Nav-bar toolbar shows: Back chevron (topBarLeading), Restart icon (topBarLeading), ToolbarMenu (topBarTrailing) — per the v1.0 layout, NOT the Small-zone-repositioned layout.
       - Pinch-zoom (`MagnifyGesture`) still works on the board (A11Y-05).
       - Reveal/Flag interaction-mode toggle still flips on tap (MINES-12).
       - On a win, the win-sweep wash + confetti + end-state card all fire as in v1.0.6.1 (MINES-08).
       - On a loss, the loss-shake + cascading mine reveal + end-state card all fire as in v1.0.6.1 (MINES-08).
    4. If any of the above regresses, FAIL SC5 and record the deviation in the Plan 11-08 SUMMARY's Risks/notes section. ROLLBACK target: revert Plan 11-03's three-way branch refactor (the off-path's structural extraction is the highest-risk SC5 surface).

    **Section D — Fill in the matrix + sign-off block:**

    1. Edit `.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md`:
       - Replace `☐` with `☑` (or equivalent — see action below) on each Pass cell.
       - Add row-level Notes for any per-zone observation.
       - Fill in the Sign-off block: SC1 row, SC3 row, top-level Status + Verifier + Date + YAML `signed_off_by` + `signed_off_date`.

    2. Save the file. Plan 11-08 Task 2 then appends to `Docs/releases/v1.2.md`.

    **Resume signal:**
    - Type `SC1+SC3+SC4+SC5 all PASS — phase ships` to confirm.
    - Type `SC<N> FAIL — <description>` to flag a specific SC for gap-closure.
  </how-to-verify>
  <resume-signal>Type `SC1+SC3+SC4+SC5 all PASS — phase ships`, OR `SC<N> FAIL — <description>` to flag a gap.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill 11-VIDEO-MANUAL-CHECK.md with PASS marks + sign-off based on Task 1 results</name>
  <read_first>
    - .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md (current state — 18-row matrix with unchecked boxes)
    - Task 1's manual sweep results (the verifier's observations)
  </read_first>
  <files>.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md</files>
  <behavior>
    - Each row of the matrix has its Pass/Fail cells filled in based on Task 1 observation.
    - The Sign-off block has its SC1 and SC3 rows filled with verifier name, date, and status.
    - The YAML front-matter `signed_off_by` and `signed_off_date` fields are populated.
    - `status:` advances from `pending` → `complete` (or `blocked` if any SC FAILed).
  </behavior>
  <action>
    1. Open `.planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md`.

    2. For each of rows 1-18 in the §Matrix table:
       - In each gesture column (First-tap / Reveal / Long-press flag / Restart / Win/Loss completes), replace `☐` with `☑` per Task 1's observations.
       - In the Pass/Fail column, replace `☐ PASS / ☐ FAIL` with either `☑ PASS / ☐ FAIL` (passing row) or `☐ PASS / ☑ FAIL` (failing row).
       - Add any per-row Notes from Task 1 (especially for Hard rows where final-render parity vs ADR screenshots may need an inline annotation).

    3. In the §Sign-off block:
       - SC1 row: fill `Verifier`, `Date`, and Status with the Task 1 verifier name + today's date + the SC1 outcome.
       - SC3 row: same shape, using the SC3 outcome.
       - Top-level Status: PASS if all 4 SCs (SC1/SC3/SC4 implicit/SC5 implicit) pass; FAIL if any failed; DEFERRED only if the verifier explicitly chose to defer a failing row to gap-closure.
       - Verifier + Date fill the top-level row.

    4. Update the YAML front-matter:
       ```yaml
       status: complete   # or `blocked` if any SC FAIL
       signed_off_by: "<verifier name>"
       signed_off_date: "<YYYY-MM-DD>"
       ```

    5. If any row FAILed and the phase is shipping anyway (e.g., a known-deferred edge case), add a §"Deferred items" section at the bottom of the doc listing which row(s) FAILed and the deferral rationale.
  </action>
  <verify>
    <automated>grep -cE "☑ PASS|☐ PASS / ☑ FAIL" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status: complete\\|status: blocked" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns `1` (YAML status advanced)
    - `grep -c 'signed_off_by: ""' .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns `0` (no longer empty)
    - `grep -c 'signed_off_date: ""' .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns `0` (no longer empty)
    - `grep -cE "☑ PASS|✓ PASS|PASS\\s*$" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns ≥ 18 (at least the 18 matrix rows + 2 SC sign-off rows have a marked status; some may be FAIL — adjust threshold per actual outcome)
    - `grep -c "SC1 — Easy/Medium pass marks all 6 zones" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns `1` (SC1 row preserved)
    - `grep -c "SC3 — Hard final-render parity vs ADR" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns `1` (SC3 row preserved)
    - `grep -c "Plan 11-08 SUMMARY\\|11-08-SUMMARY" .planning/phases/11-mines-adoption/11-VIDEO-MANUAL-CHECK.md` returns ≥ 0 (no constraint — but if Task 1 logged deferrals, the doc should reference the SUMMARY)
  </acceptance_criteria>
  <done>Matrix is filled per Task 1 results; sign-off block has SC1 + SC3 fills; YAML front-matter has verifier + date; status advanced to complete or blocked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append Phase 11 entries to Docs/releases/v1.2.md per CLAUDE.md §8.14</name>
  <read_first>
    - Docs/releases/v1.2.md (full file — confirm structure: §Summary, §User-facing changes, §Internal changes, §Fixes, §Risks/notes, §QA checklist)
    - .planning/phases/11-mines-adoption/11-PATTERNS.md §"Docs/releases/v1.2.md (MODIFIED · release-log append per §8.14)" — verbatim append pattern
    - All 11-0N-SUMMARY.md files (covered by the deps_on chain)
    - gamekit/gamekit.xcodeproj/project.pbxproj — confirm `MARKETING_VERSION` is still `1.1` (Phase 9 + 10 entries note `MARKETING_VERSION remains 1.1; bumped to 1.2 in a later Phase 13`)
  </read_first>
  <files>Docs/releases/v1.2.md</files>
  <behavior>
    - The §User-facing changes section gains a single Phase 11 bullet describing the Minesweeper adoption from the user's perspective.
    - The §Internal changes section gains 4-5 bullets covering chip extraction, Video-Mode wrap + layout branch, compact-row composition, cell-size floor, and (if A2 fired) the safeArea adjustment.
    - The §Risks/notes section gains a "Hard-Mines smaller-cells lock" bullet referencing the ADR + locked `minCellSizeVideoMode` value + the §Rollback condition.
    - `MARKETING_VERSION` stays at `1.1` (no project.pbxproj change in this plan; per existing v1.2.md note, bump happens in Phase 13).
    - The chronological ordering of bullets is preserved — Phase 11 entries are inserted AFTER the existing Phase 10 entries in each section (the file is append-only per CLAUDE.md §0.3).
  </behavior>
  <action>
    1. Open `Docs/releases/v1.2.md`. Locate the Phase 10 entries in:
       - §"User-facing changes" (around line 26 — "Phase 10 (Video Mode layout primitives). No user-facing change.")
       - §"Internal changes" (around line 80 — "Tests (Phase 10) — 2 new test files…")
       - §"Risks / notes" (around line 110 — "Hard-Mines MagnifyGesture UNTOUCHED (CONTEXT D-15, Phase 10)…")

    2. APPEND (do not insert mid-file) a Phase 11 entry to §"User-facing changes" AFTER the Phase 10 bullet:

       ```markdown
       - **Phase 11 (Minesweeper Video Mode adoption).** Minesweeper is the
         first game to reflow when Video Mode is On. On Large PiP zones
         (top/bottom), `MinesweeperHeaderBar` + `MinesweeperModePill` + the
         nav-bar toolbar collapse into a single compact control row
         positioned opposite the reserved video band — Back, stacked
         Mines/Time chip, Reveal/Flag picker, Settings (difficulty menu),
         Restart. On Small PiP corners, the existing layout stays but
         Back / Restart / Menu reposition to the anti-PiP corner per
         `VideoModeSlotRouter.anchors(for:)`. Hard 16×30 renders at a
         smaller cell-size floor (<LOCKED_FLOOR>pt vs the v1.0 18pt) when
         Video Mode is On so the full board fits between the reserved band
         and the compact row; pinch-zoom (A11Y-05) remains the user's
         manual escape hatch. Easy + Medium playable across all 6 PiP
         locations on Classic + Dracula per CLAUDE.md §8.12.
       ```

       Replace `<LOCKED_FLOOR>` with the value from Plan 11-05's audit
       (from `gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift`'s
       `static let minCellSizeVideoMode: CGFloat = <value>`).

    3. APPEND Phase 11 internal-changes bullets to §"Internal changes" AFTER the existing Phase 10 bullet block:

       ```markdown
       - **MinesRemainingChip + TimerChip** (`Games/Minesweeper/`,
         Plan 11-01) — props-only chip subviews extracted from
         `MinesweeperHeaderBar`. Single source of truth for chip
         rendering, shared between HeaderBar (non-video / Small PiP
         zones) and the compact-row stacked slot 2 (Large PiP zones).
         `MinesweeperHeaderBar` shrinks from 133 → ~37 lines; init
         signature unchanged. Phase 3 D-05 timer freeze invariant
         (`.distantPast` anchor) preserved verbatim in `TimerChip`.
       - **MinesweeperGameView three-way layout branch** (Plans 11-03 +
         11-04) — body now reads `@Environment(\.videoModeStore)` +
         `@Environment(\.videoModeCompactness)`. Off path: v1.0
         verbatim. Large path: `VideoCompactControlRow` with D-05
         slot order (Back | stacked Mines⊥Time chip | Reveal/Flag picker
         | difficulty Menu | Restart); HeaderBar + ModePill + toolbar
         hidden. Small path: existing layout + toolbar items
         repositioned per `VideoModeSlotRouter.anchors(for:)`. D-18
         compactness reactions: `.collapsedSettings` folds Settings into
         Restart's primary-action Menu; `.reducedTime` drops the Time
         chip from slot 2's stack.
       - **MinesweeperBoardView Video-Mode-aware floor** (Plan 11-05) —
         new `static let minCellSizeVideoMode: CGFloat = <LOCKED_FLOOR>`
         + `static func minCellSize(videoModeOn:) -> CGFloat` helper.
         Both `cellSize(...)` statics accept a defaulted `floor:`
         parameter. Single gate (`videoModeStore.isEnabled`) per
         `08-HARD-MINES-ADR.md` §How-it-composes; no `location.isLarge`
         or `difficulty == .hard` conditioning. `MagnifyGesture` /
         `.scaleEffect` / `clampZoomScale` / cell-level `LongPressGesture`
         composition byte-identical (D-17 untouched contract verified
         via grep against git HEAD).
       - **HomeView wrap site** (Plan 11-03) — Mines NavigationLink
         destination chains `.videoModeAware(minBoardHeight: 480)`.
         Merge + Nonogram arms unchanged (adopt in Phase 12).
       - **VideoModeLocation.isLarge** (Plan 11-03) — new computed
         property on the enum; exhaustive switch with no default-case so
         a future 7th case fires a compile error at every adopter.
       - **Slot-order supersession** (Plan 11-02, doc-only) —
         `08-COMPACT-ROW-TOKENS.md` + `VIDEO-MODE-LAYOUTS.md`
         Minesweeper slot rows updated to the D-05 revised order:
         `Back | [Mines⊥Time stacked chip] | Reveal/Flag picker |
         Settings | Restart`. Merge + Nonogram slot mappings unchanged.
       ```

       If Plan 11-06 fired the A2 adjustment, ADD an additional bullet:

       ```markdown
       - **VideoModeAware A2 adjustment** (Plan 11-06, conditional) —
         `safeAreaInset(.top)` widened by `<N>pt` on `.largeTop` when
         mounted inside a NavigationStack with the nav-bar title
         visible (D-09). Empirically measured against Mines Hard
         16×30 on iPhone 17 Pro Max; off-path short-circuit and
         Small-zone passthrough unchanged.
       ```

    4. APPEND a Phase 11 risks/notes bullet to §"Risks / notes" AFTER the
       existing Phase 10 untouched-MagnifyGesture entry:

       ```markdown
       - **Hard-Mines smaller-cells lock (Plan 11-05).** Hard 16×30
         renders at `minCellSizeVideoMode = <LOCKED_FLOOR>pt` when
         Video Mode is On (vs v1.0 `minCellSize = 18pt`). Locked
         2026-MM-DD after auditing 10/11/12/13pt on Dracula + Voltage
         per CLAUDE.md §8.12. Rollback condition per
         `.planning/phases/08-video-mode-design/08-HARD-MINES-ADR.md`
         §Rollback: if mis-tap rate regresses on iPhone 17 Pro Max
         post-TestFlight OR §8.12 Dracula legibility regresses, revert
         to v1.0 floor + adopt warning-compromise (Variant 4) as v1.3
         fallback.
       ```

    5. Do NOT bump `MARKETING_VERSION` in `gamekit/gamekit.xcodeproj/project.pbxproj` — the bump to 1.2 happens in Phase 13 per the existing v1.2.md note.

    6. Do NOT touch the §QA checklist section (it's per-version-final, not per-phase).
  </action>
  <verify>
    <automated>grep -c "Phase 11" Docs/releases/v1.2.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 11 (Minesweeper Video Mode adoption)" Docs/releases/v1.2.md` returns `1` (User-facing bullet appended)
    - `grep -c "MinesRemainingChip + TimerChip" Docs/releases/v1.2.md` returns `1` (chip extraction bullet)
    - `grep -c "MinesweeperGameView three-way layout branch" Docs/releases/v1.2.md` returns `1` (layout-branch bullet)
    - `grep -c "MinesweeperBoardView Video-Mode-aware floor" Docs/releases/v1.2.md` returns `1` (floor bullet)
    - `grep -c "HomeView wrap site" Docs/releases/v1.2.md` returns `1` (wrap-site bullet)
    - `grep -c "Slot-order supersession" Docs/releases/v1.2.md` returns `1` (doc-supersession bullet)
    - `grep -c "VideoModeLocation.isLarge" Docs/releases/v1.2.md` returns `1` (isLarge bullet)
    - `grep -c "Hard-Mines smaller-cells lock" Docs/releases/v1.2.md` returns `1` (Risks/notes bullet)
    - `grep -c "08-HARD-MINES-ADR" Docs/releases/v1.2.md` returns ≥ 1 (ADR reference in Risks/notes)
    - `grep -c "Phase 9\\|Phase 10" Docs/releases/v1.2.md` returns ≥ 4 (existing P9 + P10 entries preserved — not deleted)
    - `grep -c "MARKETING_VERSION remains 1.1" Docs/releases/v1.2.md` returns `1` (existing note preserved; no version bump in P11)
    - `grep -c "videoModeStore.isEnabled\\|videoMode.*single gate" Docs/releases/v1.2.md` returns ≥ 0 (the D-12 single-gate detail; descriptive — no strict count)
  </acceptance_criteria>
  <done>Phase 11 entries appended to all 3 release-log sections (User-facing, Internal changes, Risks/notes); existing P9 + P10 entries preserved; MARKETING_VERSION unchanged at 1.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual sweep ↔ matrix doc fill-in | If the verifier marks PASS without actually running each gesture, the matrix becomes false documentation. Task 1 is the human-verify gate; Task 2 only records the outcome. |
| release-log ↔ source-of-truth code | The release log's locked-floor value MUST match `MinesweeperBoardView.minCellSizeVideoMode`. Drift = misleading user-facing summary. |
| Off-restore spot-check ↔ SC5 | SC5 is the only Video-Mode-related cross-cutting invariant (CLAUDE.md cross-cutting + ROADMAP v1.2 invariants). Failing SC5 means rolling back the Plan 11-03 three-way branch refactor. |

## STRIDE Threat Register (ASVS L1, manual verification + docs)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-32 | Spoofing | Pre-filled PASS marks without actual verification | mitigate | Task 1 is `checkpoint:human-verify` with explicit gate — Task 2 cannot run until the human types the resume signal. Acceptance criterion enforces YAML status advance + non-empty `signed_off_by`. |
| T-11-33 | Tampering | Release-log floor value drift | mitigate | Task 3 action explicitly threads `<LOCKED_FLOOR>` placeholder + instructs the executor to source the value from `MinesweeperBoardView.swift` (not freelance a guess). |
| T-11-34 | Tampering | Existing P9 + P10 release-log entries | mitigate | Acceptance asserts P9 + P10 entries are still present (`grep -c "Phase 9\\|Phase 10"` ≥ 4); append-only edit prevents accidental overwrite of shipped-version notes per CLAUDE.md §0.3. |
| T-11-35 | Repudiation | Off-restore byte-identity claim without proof | mitigate | Task 1 Section C explicitly enumerates the v1.0 surfaces that must render unchanged (HeaderBar, ModePill, toolbar, MagnifyGesture, MINES-12, MINES-08). Each is a discrete check; the verifier cannot blanket-PASS without confirming each. |
| T-11-36 | Information disclosure | A11Y label regression in compact row | accept | The Plan 11-04 a11y-label preservation (chip + ModePill + ToolbarMenu + Restart labels all inherited) is verified at code-acceptance time; if the manual sweep catches a regression here, it FAILs SC4 and triggers gap-closure. |
</threat_model>

<verification>
- `11-VIDEO-MANUAL-CHECK.md` has all 18 rows filled with PASS/FAIL marks + sign-off block populated + YAML status = `complete` (or `blocked` if a SC failed).
- `Docs/releases/v1.2.md` has Phase 11 entries in User-facing, Internal changes, and Risks/notes — existing P9 + P10 entries preserved.
- `MARKETING_VERSION` in `gamekit/gamekit.xcodeproj/project.pbxproj` is still `1.1` (per the existing v1.2.md plan — bump happens in Phase 13).
- All 5 SCs (SC1 — Easy/Medium 6-zone playability; SC2 — Hard ADR smaller-cells per Plan 11-05's checkpoint; SC3 — Hard final-render parity vs ADR shots; SC4 — Classic + Loud × E/M/H × 6 zones legibility; SC5 — Off-restore byte-identity) signed off.
</verification>

<success_criteria>
- SC1: rows 1-12 of the matrix marked PASS; SC1 sign-off row in 11-VIDEO-MANUAL-CHECK.md filled with PASS.
- SC2: closed by Plan 11-05's human-verify checkpoint + referenced in this plan's release-log Risks/notes.
- SC3: rows 13-18 of the matrix marked PASS with ADR screenshot parity notes; SC3 sign-off row filled with PASS.
- SC4: each row's notes reflect Classic + Loud preset audit per CLAUDE.md §8.12.
- SC5: Task 1 Section C confirms Off-restore byte-identity; release-log Risks/notes documents the spot-check.
- `Docs/releases/v1.2.md` has Phase 11 entries per CLAUDE.md §8.14.

Phase 11 ships when this plan closes.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mines-adoption/11-08-SUMMARY.md` covering:
- The Task 1 manual-sweep results (per-row, per-preset)
- The Task 1 Section C Off-restore spot-check results (each v1.0 surface verified)
- The locked `minCellSizeVideoMode` value (from Plan 11-05's audit)
- A summary of the release-log entries appended
- Sign-off status for each SC (SC1 / SC2 / SC3 / SC4 / SC5)
- Any deferred items + their rationale
- Final phase status: SHIPS / BLOCKED
</output>
