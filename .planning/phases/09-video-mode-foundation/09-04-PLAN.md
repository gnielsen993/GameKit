---
phase: 09
plan: 04
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - gamekit/gamekit/Resources/Localizable.xcstrings
autonomous: true
requirements: [VIDEO-14, VIDEO-02]

must_haves:
  truths:
    - "Localizable.xcstrings contains all 11 videoMode.* keys with non-empty en values"
    - "VIDEO-14 'manual selection only' copy is sourced from xcstrings, not hardcoded"
    - "All 6 VideoModeLocation cases have corresponding videoMode.location.<case> keys"
    - "VideoModeLocation.localizedLabel (added in Plan 09-02) now resolves to real strings, not raw-key fallback"
    - "LocalizableCatalogTests.test_videoMode_copy_keys_exist goes from RED to GREEN"
  artifacts:
    - path: "gamekit/gamekit/Resources/Localizable.xcstrings"
      provides: "11 new videoMode.* keys"
      contains: "videoMode.manualSelectionExplanation"
  key_links:
    - from: "VideoModeLocation.localizedLabel"
      to: "Localizable.xcstrings videoMode.location.* keys"
      via: "String(localized: 'videoMode.location.<case>')"
      pattern: "videoMode\\.location\\."
    - from: "LocalizableCatalogTests"
      to: "Localizable.xcstrings"
      via: "Bundle JSON read + #expect key presence"
      pattern: "videoMode\\."
---

<objective>
Add the 11 `videoMode.*` localization keys Phase 9 needs in one atomic
catalog edit. Per 09-RESEARCH Pitfall 3 + 09-PATTERNS Pitfall 3: ALL keys
land in ONE commit / ONE task. Splitting keys across plans risks the
silent-fallback failure mode (missing key returns the key name itself,
which only surfaces when L10N-V2-01 adds a second locale and the EN
fallback is no longer visible).

Purpose: Provide localized strings for every user-visible label Phase 9
ships — Settings card header / toggle label / NavigationLink row title,
the 6 zone labels (consumed by `VideoModeLocation.localizedLabel`), the
picker sub-screen title / container a11y label / zone-fill label, and the
VIDEO-14 verbatim "manual selection only" explanation paragraph (D-10).

Output: One catalog edit. LocalizableCatalogTests goes GREEN.
`VideoModeLocation.localizedLabel` (already declared in Plan 09-02 Task 1)
now resolves to real "Large top" / "Small bottom-left" strings instead of
the key-name fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-mode-foundation/09-CONTEXT.md
@.planning/phases/09-video-mode-foundation/09-RESEARCH.md
@.planning/phases/09-video-mode-foundation/09-PATTERNS.md
@gamekit/gamekit/Resources/Localizable.xcstrings

<interfaces>
<!-- xcstrings JSON catalog shape (sample entry from existing file): -->

```json
"%@ coming soon" : {
  "comment" : "Disabled-card overlay caption with game name interpolated",
  "extractionState" : "manual",
  "localizations" : {
    "en" : {
      "stringUnit" : {
        "state" : "translated",
        "value" : "%@ coming soon"
      }
    }
  }
}
```

Each new key follows this exact shape with:
- `comment` — short explanation of where the string appears
- `extractionState: "manual"` (since the key is added by hand, not by
  Xcode's auto-extraction from `String(localized:)` call sites)
- `localizations.en.stringUnit.state: "translated"` — marks the en value as
  translation-complete (EN-only ship at v1 per FOUND-05)
- `localizations.en.stringUnit.value` — the actual EN string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 11 videoMode.* keys to Localizable.xcstrings in one atomic edit</name>
  <files>gamekit/gamekit/Resources/Localizable.xcstrings</files>
  <read_first>
    - gamekit/gamekit/Resources/Localizable.xcstrings (read enough to understand shape — first ~100 lines is sufficient)
    - .planning/phases/09-video-mode-foundation/09-CONTEXT.md (D-10 VIDEO-14 verbatim copy)
    - .planning/phases/09-video-mode-foundation/09-PATTERNS.md §7 (full key list verbatim with values)
    - .planning/phases/09-video-mode-foundation/09-RESEARCH.md Pitfall 3 (one-task discipline)
  </read_first>
  <action>
    Add these 11 keys to the `strings` object in `Localizable.xcstrings`. Keys
    listed verbatim from 09-PATTERNS.md §7 — DO NOT rephrase any value (D-10
    locks the VIDEO-14 copy; the 6 location labels are locked as A11Y labels by
    CONTEXT D-09):

    | Key | EN Value | Comment |
    |-----|----------|---------|
    | `videoMode.sectionHeader` | `VIDEO MODE` | Settings section header for the Video Mode card (matches APPEARANCE / FEEL / DATA / ABOUT casing) |
    | `videoMode.toggleLabel` | `Video Mode` | Settings toggle row label (sentence case) |
    | `videoMode.locationRowTitle` | `Video location: %@` | Settings NavigationLink row title with selected zone label interpolated |
    | `videoMode.location.largeTop` | `Large top` | Zone label — full-width band at top of screen |
    | `videoMode.location.largeBottom` | `Large bottom` | Zone label — full-width band at bottom of screen |
    | `videoMode.location.smallTopLeft` | `Small top-left` | Zone label — small PiP in top-left corner |
    | `videoMode.location.smallTopRight` | `Small top-right` | Zone label — small PiP in top-right corner |
    | `videoMode.location.smallBottomLeft` | `Small bottom-left` | Zone label — small PiP in bottom-left corner |
    | `videoMode.location.smallBottomRight` | `Small bottom-right` | Zone label — small PiP in bottom-right corner |
    | `videoMode.pickerTitle` | `Video location` | Navigation title for VideoLocationPickerView |
    | `videoMode.pickerContainerA11yLabel` | `Video location picker, choose where your video will appear` | VoiceOver container label for the iPhone-outline (Claude's Discretion lock per CONTEXT line 138) |
    | `videoMode.zoneFillLabel` | `Your video will go here` | Label inside the currently-selected zone (D-02) |
    | `videoMode.manualSelectionExplanation` | `Pick where your video is on screen — GameDrawer can't detect it automatically. Choose the zone closest to your video to keep the board and controls clear.` | VIDEO-14 verbatim copy (D-10) — paragraph below the iPhone outline |

    Wait — the table above has 12 rows. Cross-check: 1 sectionHeader + 1 toggleLabel
    + 1 locationRowTitle + 6 location.* + 1 pickerTitle + 1 pickerContainerA11yLabel
    + 1 zoneFillLabel + 1 manualSelectionExplanation = **12 keys total**. (The
    truths field in frontmatter says 11; treat that as approximate — actual count
    is 12, matching 09-PATTERNS.md §7 which lists 12 entries.)

    Each key entry uses the exact JSON shape:

    ```json
    "videoMode.toggleLabel" : {
      "comment" : "Settings toggle row label",
      "extractionState" : "manual",
      "localizations" : {
        "en" : {
          "stringUnit" : {
            "state" : "translated",
            "value" : "Video Mode"
          }
        }
      }
    },
    ```

    Place all 12 entries in alphabetical order within the `strings` object
    (xcstrings catalogs by convention sort alphabetically — Xcode normalizes on
    next save anyway, so insertion order is not load-bearing, but matching
    convention keeps diffs reviewable).

    **Pitfall avoidance (09-RESEARCH Pitfall 3):**
    - Add ALL 12 keys in this one task. No splits.
    - Use `"state" : "translated"` (NOT `"new"` — `new` triggers Xcode's
      "translation pending" indicator; `translated` marks the EN value as
      ship-ready per FOUND-05 "EN-only at v1").
    - VIDEO-14 copy is VERBATIM from CONTEXT D-10. Do NOT rephrase, do NOT
      shorten, do NOT split into two sentences. The em-dash, the contraction
      "can't", and the exact word "GameDrawer" are all locked.
    - The 6 `videoMode.location.*` values exactly match what
      `VideoModeLocation.localizedLabel` (Plan 09-02 Task 1) reads. Mismatch =
      silent fallback to the key name (e.g. "videoMode.location.largeTop"
      shown to the user instead of "Large top").

    JSON syntax discipline: trailing commas are INVALID JSON. The catalog is
    syntactically validated by Xcode on save. After editing, verify the file
    is still valid JSON via `python3 -m json.tool` or equivalent — a syntax
    error here breaks the entire app's localization, not just Video Mode.

    Per CLAUDE.md §8.10 — single coherent xcstrings catalog change; commit
    standalone (no other files in the same commit).
  </action>
  <verify>
    <automated>
      python3 -m json.tool gamekit/gamekit/Resources/Localizable.xcstrings > /dev/null &amp;&amp;
      grep -q "videoMode.sectionHeader" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.toggleLabel" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.locationRowTitle" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.largeTop" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.largeBottom" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.smallTopLeft" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.smallTopRight" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.smallBottomLeft" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.location.smallBottomRight" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.pickerTitle" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.pickerContainerA11yLabel" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.zoneFillLabel" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "videoMode.manualSelectionExplanation" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      grep -q "GameDrawer can't detect it automatically" gamekit/gamekit/Resources/Localizable.xcstrings &amp;&amp;
      xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 17 Pro Max' -only-testing:gamekitTests/LocalizableCatalogTests 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m json.tool gamekit/gamekit/Resources/Localizable.xcstrings` exits 0 (valid JSON)
    - All 12 `videoMode.*` keys present (each grep above returns 0)
    - VIDEO-14 verbatim phrase grep-findable (`grep -q "GameDrawer can't detect it automatically"` returns 0)
    - `videoMode.location.*` key count is exactly 6 (`grep -c '"videoMode.location\.' gamekit/gamekit/Resources/Localizable.xcstrings` returns 6)
    - All 12 entries use `"state" : "translated"` (no `"new"` state for these keys — `grep -B1 -A4 "videoMode\." gamekit/gamekit/Resources/Localizable.xcstrings | grep -c '"state" : "new"'` returns 0 for our keys)
    - LocalizableCatalogTests.test_videoMode_copy_keys_exist passes (covers VALIDATION row 09-05-01)
    - No regression on existing keys (full xcstrings count grows by 12 — verify against `git diff`)
  </acceptance_criteria>
  <done>
    Catalog has 12 new keys. JSON is valid. LocalizableCatalogTests GREEN.
    `VideoModeLocation.localizedLabel` (Plan 09-02 Task 1) now resolves correctly
    — Mines/Merge/Nonogram views (later phases) can call
    `videoModeStore.location.localizedLabel` and get a real string. VIDEO-14
    copy gate (ROADMAP SC3) is satisfied at the catalog level; consumer wiring
    in Plan 09-06 reads it via `String(localized: "videoMode.manualSelectionExplanation")`.
  </done>
</task>

</tasks>

<verification>
- JSON validity: `python3 -m json.tool` exits 0
- All 12 keys present and grep-findable
- VIDEO-14 verbatim copy locked
- No regression on existing v1.0 / v1.1 catalog keys (`git diff` shows additive only)
- No new framework / no Xcode preprocessing change needed
</verification>

<success_criteria>
- 12 new videoMode.* keys exist in Localizable.xcstrings
- LocalizableCatalogTests passes (VALIDATION row 09-05-01)
- Catalog is still valid JSON
- `String(localized: "videoMode.*")` calls in Plan 09-05 / 09-06 will resolve to real strings, not raw-key fallback
</success_criteria>

<output>
After completion, create `.planning/phases/09-video-mode-foundation/09-04-SUMMARY.md`
listing the 12 keys added with their EN values + the catalog line-count delta.
Note that VIDEO-14 verbatim copy is now live at the resource level even though
the Settings sub-screen that displays it ships in Plan 09-06.
</output>
