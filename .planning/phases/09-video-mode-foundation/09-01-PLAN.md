---
phase: 09
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - gamekit/gamekitTests/Core/VideoModeStoreTests.swift
  - gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift
  - gamekit/gamekitTests/App/GameKitAppTests.swift
  - gamekit/gamekitTests/Screens/SettingsViewTests.swift
  - gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift
  - gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift
  - gamekit/gamekitTests/Regression/SC5RegressionTests.swift
autonomous: true
requirements: [VIDEO-01, VIDEO-02, VIDEO-03, VIDEO-04, VIDEO-14]

must_haves:
  truths:
    - "Every per-task automated verify in 09-VALIDATION.md has a Swift Testing @Test that exists in the file system"
    - "All 7 new test files compile (even if every test #expect is a placeholder until Wave 1 ships)"
    - "Test files mirror SettingsStoreFlagsTests isolated-UserDefaults helper pattern verbatim"
    - "RED gates exist before any Wave 1 production code is written (Nyquist compliance)"
  artifacts:
    - path: "gamekit/gamekitTests/Core/VideoModeStoreTests.swift"
      provides: "5 RED tests covering VIDEO-01 / VIDEO-02 / VIDEO-03 store behavior"
      contains: "@Suite, makeIsolatedDefaults(), 5 @Test funcs"
    - path: "gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift"
      provides: "1 RED test for EnvironmentKey injection round-trip"
      contains: "@Test test_environmentKey_returns_injected"
    - path: "gamekit/gamekitTests/App/GameKitAppTests.swift"
      provides: "1 RED test for app-root store construction"
      contains: "@Test test_videoModeStore_injected_at_app_root"
    - path: "gamekit/gamekitTests/Screens/SettingsViewTests.swift"
      provides: "2 RED tests for Toggle binding + conditional row visibility"
    - path: "gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift"
      provides: "2 RED tests for zone tap + a11y labels"
    - path: "gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift"
      provides: "1 RED test for VIDEO-14 copy key existence"
    - path: "gamekit/gamekitTests/Regression/SC5RegressionTests.swift"
      provides: "1 placeholder regression test for SC5 off-state baseline"
  key_links:
    - from: "VideoModeStoreTests.swift"
      to: "SettingsStoreFlagsTests.swift:36-39"
      via: "makeIsolatedDefaults() helper copied verbatim"
      pattern: "func makeIsolatedDefaults\\(\\) -> UserDefaults"
---

<objective>
Author 7 RED-state test files that lock the contract Wave 1+ production code
must satisfy. Each test corresponds to exactly one row in
`09-VALIDATION.md` §Per-Task Verification Map (rows 09-01-01 through
09-07-01). Tests are intentionally RED at the end of this plan — they
reference types (`VideoModeStore`, `VideoModeLocation`,
`EnvironmentValues.videoModeStore`) that do not yet exist; the failing
compile / @testable import is the proof that Wave 1 must produce them.

Purpose: Lock down the exact behavioral contract (default values, key
names, round-trip semantics, enum case set, environment injection)
before any production code is written. Mirrors the v1.0 Plan 05-01 →
05-03 pattern where SettingsStoreFlagsTests landed before Haptics/SFX
production code.

Output: 7 new test files; running the full Swift Testing suite returns
RED for the 13 new @Test funcs (existing v1.0 / v1.1 tests stay GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-mode-foundation/09-CONTEXT.md
@.planning/phases/09-video-mode-foundation/09-RESEARCH.md
@.planning/phases/09-video-mode-foundation/09-VALIDATION.md
@.planning/phases/09-video-mode-foundation/09-PATTERNS.md
@gamekit/gamekitTests/Core/SettingsStoreFlagsTests.swift

<interfaces>
<!-- Existing test pattern executors must mirror verbatim. -->
<!-- From gamekit/gamekitTests/Core/SettingsStoreFlagsTests.swift:23-39: -->

```swift
import Testing
import Foundation
@testable import gamekit

@MainActor
@Suite("SettingsStoreFlags")
struct SettingsStoreFlagsTests {

    // Per-test isolated UserDefaults — mirrors MinesweeperViewModelTests:25-28.
    static func makeIsolatedDefaults() -> UserDefaults {
        let suite = "test-\(UUID().uuidString)"
        return UserDefaults(suiteName: suite)!
    }
    // ...
}
```

Symbols that will be REFERENCED by these tests but do NOT exist yet
(intentional — Wave 1 ships them):
- `VideoModeStore(userDefaults:)` initializer
- `VideoModeStore.isEnabledKey` static constant
- `VideoModeStore.locationKey` static constant
- `VideoModeStore.isEnabled: Bool` property
- `VideoModeStore.location: VideoModeLocation` property
- `enum VideoModeLocation: String, CaseIterable, Sendable` with 6 cases
- `EnvironmentValues.videoModeStore: VideoModeStore`
- `VideoModeLocation.localizedLabel: String` accessor
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: VideoModeStoreTests + VideoModeEnvironmentTests (5 + 1 tests for VIDEO-01 / VIDEO-02 / VIDEO-03)</name>
  <files>
    gamekit/gamekitTests/Core/VideoModeStoreTests.swift,
    gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift
  </files>
  <read_first>
    - .planning/phases/09-video-mode-foundation/09-CONTEXT.md (D-05, D-06, D-07, D-03 locks)
    - .planning/phases/09-video-mode-foundation/09-RESEARCH.md §Topic 3 (pattern invariants 1-7)
    - .planning/phases/09-video-mode-foundation/09-VALIDATION.md rows 09-01-01..09-02-01 (test names locked)
    - gamekit/gamekitTests/Core/SettingsStoreFlagsTests.swift:23-114 (analog — copy @Suite shape + makeIsolatedDefaults verbatim)
  </read_first>
  <behavior>
    VideoModeStoreTests.swift (5 tests, all RED until Wave 1 ships VideoModeStore):
    - Test 1 `test_isEnabled_defaults_to_false` — fresh isolated UserDefaults suite → `VideoModeStore(userDefaults:).isEnabled == false` (covers VIDEO-01 / VALIDATION row 09-01-02 / RESEARCH Topic 3 invariant #3 — `UserDefaults.bool(forKey:)` returns false for unset keys)
    - Test 2 `test_isEnabled_persists` — write `store.isEnabled = true`; re-read `defaults.bool(forKey: VideoModeStore.isEnabledKey) == true`; reload `VideoModeStore(userDefaults: defaults).isEnabled == true` (covers VIDEO-01 / VALIDATION row 09-01-01)
    - Test 3 `test_location_default_is_largeBottom` — fresh suite → `store.location == .largeBottom` (covers VIDEO-02 / VIDEO-03 / VALIDATION row 09-01-04 / CONTEXT D-03)
    - Test 4 `test_location_persists_all_cases` — loop `for loc in VideoModeLocation.allCases`: write `store.location = loc`, reload store from same defaults, `#expect(reloaded.location == loc)` (covers VIDEO-02 / VIDEO-03 / VALIDATION row 09-01-03 — exhaustive enum coverage)
    - Test 5 `test_location_enum_has_6_cases` — `#expect(VideoModeLocation.allCases.count == 6)` AND `#expect(Set(VideoModeLocation.allCases.map(\.rawValue)) == ["largeTop", "largeBottom", "smallTopLeft", "smallTopRight", "smallBottomLeft", "smallBottomRight"])` (covers VIDEO-02 / VALIDATION row 09-01-05 / CONTEXT D-07)
    - Test 6 `test_corruptLocation_fallsBackToLargeBottom` — `defaults.set("garbage", forKey: VideoModeStore.locationKey)`, construct store → `#expect(store.location == .largeBottom)` (covers RESEARCH Topic 3 invariant #4 defensive-default)

    VideoModeEnvironmentTests.swift (1 test, RED until Wave 1 ships EnvironmentKey extension):
    - Test 7 `test_environmentKey_returns_injected` — construct VideoModeStore, write to `EnvironmentValues.videoModeStore`, read back, identity check `===` (class identity proves not-the-default per PATTERNS.md §8 environment-injection skeleton). Covers VIDEO-03 / VALIDATION row 09-02-01.
  </behavior>
  <action>
    Create `gamekit/gamekitTests/Core/VideoModeStoreTests.swift` mirroring
    `SettingsStoreFlagsTests.swift:23-114` verbatim shape: `import Testing`,
    `import Foundation`, `@testable import gamekit`, `@MainActor @Suite("VideoModeStore")`,
    `struct VideoModeStoreTests`, copy `makeIsolatedDefaults()` helper from lines 36-39
    UNCHANGED. Each @Test func body follows the SettingsStoreFlagsTests round-trip
    pattern (write → direct-defaults-read → reload-store-read). All 6 tests in this
    one file (D-validation note merges VideoModeLocationTests into VideoModeStoreTests
    per 09-PATTERNS.md line 23 footnote).

    Create `gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift` with the same
    top-of-file header. Single @Test func mutates `var env = EnvironmentValues()`
    setter on `\.videoModeStore`, asserts identity (`===`).

    Both files reference symbols that do not exist yet (`VideoModeStore`,
    `VideoModeLocation`, `EnvironmentValues.videoModeStore`,
    `VideoModeStore.isEnabledKey`, `VideoModeStore.locationKey`). The compile
    failure IS the RED gate — Wave 1 plan 09-02 produces the types and the tests
    flip to GREEN.

    Test names MUST match 09-VALIDATION.md rows exactly (`test_isEnabled_persists`
    not `setIsEnabled_persists`) — the planner-validation handler greps for
    these exact strings.
  </action>
  <verify>
    <automated>
      test -f gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      test -f gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift &amp;&amp;
      grep -q "@Suite(\"VideoModeStore\")" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -c "@Test" gamekit/gamekitTests/Core/VideoModeStoreTests.swift | grep -q "^6$" &amp;&amp;
      grep -q "test_isEnabled_defaults_to_false" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_isEnabled_persists" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_location_default_is_largeBottom" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_location_persists_all_cases" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_location_enum_has_6_cases" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_corruptLocation_fallsBackToLargeBottom" gamekit/gamekitTests/Core/VideoModeStoreTests.swift &amp;&amp;
      grep -q "test_environmentKey_returns_injected" gamekit/gamekitTests/Core/VideoModeEnvironmentTests.swift &amp;&amp;
      grep -q "makeIsolatedDefaults" gamekit/gamekitTests/Core/VideoModeStoreTests.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@testable import gamekit" gamekit/gamekitTests/Core/VideoModeStoreTests.swift` returns 0
    - `grep -q "@MainActor" gamekit/gamekitTests/Core/VideoModeStoreTests.swift` returns 0
    - `grep -c "VideoModeLocation.allCases" gamekit/gamekitTests/Core/VideoModeStoreTests.swift` returns ≥1 (Test 4 exhaustive loop)
    - `grep -q "VideoModeStore.isEnabledKey" gamekit/gamekitTests/Core/VideoModeStoreTests.swift` returns 0 (proves static-key constant is referenced)
    - `grep -q "VideoModeStore.locationKey" gamekit/gamekitTests/Core/VideoModeStoreTests.swift` returns 0
    - `xcodebuild build -scheme gamekit ...` FAILS at this point (RED state — references types Wave 1 ships)
  </acceptance_criteria>
  <done>
    Two test files exist, each named exactly per 09-VALIDATION.md. 6 + 1 = 7 @Test
    funcs total. Test names match VALIDATION row labels verbatim. RED state
    confirmed — `xcodebuild build` fails on undefined-symbol errors for
    `VideoModeStore`, `VideoModeLocation`, and `EnvironmentValues.videoModeStore`.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: App-root, Settings, picker, xcstrings catalog, and SC5 regression test stubs (5 files / 7 @Test funcs)</name>
  <files>
    gamekit/gamekitTests/App/GameKitAppTests.swift,
    gamekit/gamekitTests/Screens/SettingsViewTests.swift,
    gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift,
    gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift,
    gamekit/gamekitTests/Regression/SC5RegressionTests.swift
  </files>
  <read_first>
    - .planning/phases/09-video-mode-foundation/09-CONTEXT.md (D-01, D-02, D-04, D-09, D-10, D-15)
    - .planning/phases/09-video-mode-foundation/09-VALIDATION.md rows 09-02-02 / 09-03-01 / 09-03-02 / 09-04-01 / 09-04-02 / 09-05-01 / 09-07-01
    - gamekit/gamekitTests/Core/SettingsStoreFlagsTests.swift:23-39 (analog — copy isolated-defaults helper + @Suite shape)
  </read_first>
  <behavior>
    GameKitAppTests.swift (1 test, RED until Wave 2 ships GameKitApp injection):
    - `test_videoModeStore_injected_at_app_root` — construct GameKitApp seam (or
      inspect `_videoModeStore` State init); assert non-nil. Note: SwiftUI App
      tests can only assert construction-time invariants, not view-tree injection.
      Use the approach from 09-PATTERNS.md §8 environment-injection skeleton —
      placeholder body `#expect(EnvironmentValues().videoModeStore != nil)`
      (the default-value path) and leave a `// TODO: real injection assertion`
      comment. Covers VIDEO-03 / VALIDATION row 09-02-02.

    SettingsViewTests.swift (2 tests, RED until Wave 3 ships VIDEO MODE card):
    - `test_videoMode_toggle_binds_to_store` — construct VideoModeStore with
      isolated defaults, write `store.isEnabled = true`, query SettingsView body
      with `.environment(\.videoModeStore, store)`, snapshot-style assertion
      that the Toggle isOn binding round-trips (placeholder body — actual
      SwiftUI view test will use `Bindable(store).isEnabled` setter to verify
      write-through). Covers VIDEO-01 / VALIDATION row 09-03-01.
    - `test_locationRow_visibility_follows_isEnabled` — when `store.isEnabled == false`,
      NavigationLink "Video location: ..." row is absent; when `true`, present.
      Placeholder body for now (`#expect(true)` with TODO marker). Covers VIDEO-01 /
      VALIDATION row 09-03-02.

    VideoLocationPickerViewTests.swift (2 tests, RED until Wave 3 ships picker):
    - `test_zone_tap_updates_location` — loop `for loc in VideoModeLocation.allCases`,
      simulate tap on zone, assert `store.location == loc`. Placeholder body.
      Covers VIDEO-02 / VALIDATION row 09-04-01.
    - `test_zone_a11y_labels` — query the picker view, assert each of 6 zones
      carries `accessibilityLabel` matching `loc.localizedLabel` (D-09 lock —
      "Large top" / "Small bottom-left" / etc.). Placeholder body. Covers VIDEO-02 /
      VALIDATION row 09-04-02.

    LocalizableCatalogTests.swift (1 test, RED until Wave 2 xcstrings plan lands):
    - `test_videoMode_copy_keys_exist` — open `Localizable.xcstrings` resource as
      JSON via Bundle, assert these keys exist with non-empty `en` stringUnit
      values: `videoMode.manualSelectionExplanation`, `videoMode.toggleLabel`,
      `videoMode.sectionHeader`, all 6 `videoMode.location.<case>` keys,
      `videoMode.zoneFillLabel`, `videoMode.pickerTitle`,
      `videoMode.pickerContainerA11yLabel`, `videoMode.locationRowTitle`.
      Covers VIDEO-14 / VALIDATION row 09-05-01.

    SC5RegressionTests.swift (1 test, intentional placeholder until Phases 11/12):
    - `test_off_state_byte_identical` — per CONTEXT D-15, Phase 9 game views do
      not yet read `videoModeStore.isOn`, so the off-state baseline is
      automatically preserved. Test body: `#expect(true)` with this exact comment:
      `// TODO(P11/P12): swap to real snapshot diff once game views consume videoModeStore.isOn`.
      Covers SC5 / VALIDATION row 09-07-01.
  </behavior>
  <action>
    Create each file with the standard test header (`import Testing`,
    `import Foundation`, `@testable import gamekit`, `@MainActor @Suite("...")`).
    All 5 files use the same `makeIsolatedDefaults()` helper as VideoModeStoreTests
    (declare once per file as a `static func` — DO NOT share across files; Swift
    Testing's parallel execution makes shared helpers risky).

    For UI tests (SettingsViewTests, VideoLocationPickerViewTests, GameKitAppTests):
    use placeholder `#expect(true)` bodies with explicit TODO comments referencing
    the VALIDATION row + the production plan that fills it (09-04 for the picker,
    09-05 for Settings). The RED gate is the REFERENCE to missing types
    (`VideoModeStore`, `VideoLocationPickerView`) — actual interaction assertions
    can be filled in as production code lands.

    For LocalizableCatalogTests: real assertion body — read
    `Bundle.main.url(forResource: "Localizable", withExtension: "xcstrings")`,
    parse as JSON via `JSONSerialization`, walk `strings.<key>.localizations.en.stringUnit.value`,
    `#expect` each of the ~11 `videoMode.*` keys present with non-empty value.
    Pattern source: 09-PATTERNS.md §7 lists all key names verbatim.

    For SC5RegressionTests: the test body is the literal placeholder per CONTEXT
    D-15 + 09-PATTERNS.md §8 last paragraph. DO NOT attempt real snapshot
    infrastructure here.

    Test file paths follow existing `gamekitTests/` directory shape — create
    `gamekitTests/App/`, `gamekitTests/Screens/`, `gamekitTests/Resources/`,
    `gamekitTests/Regression/` subdirectories if absent (Xcode 16 synchronized
    root group auto-registers — no pbxproj edit needed per CLAUDE.md §8.8).

    Per CLAUDE.md §8.5 (<500 line cap), no single test file approaches 500 lines —
    each is &lt;100 lines including header. No concern.

    Per CLAUDE.md §8.7 — verify no `* 2.swift` Finder duplicates appear in
    `git status` after creating the files; delete immediately if so.
  </action>
  <verify>
    <automated>
      test -f gamekit/gamekitTests/App/GameKitAppTests.swift &amp;&amp;
      test -f gamekit/gamekitTests/Screens/SettingsViewTests.swift &amp;&amp;
      test -f gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift &amp;&amp;
      test -f gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift &amp;&amp;
      test -f gamekit/gamekitTests/Regression/SC5RegressionTests.swift &amp;&amp;
      grep -q "test_videoModeStore_injected_at_app_root" gamekit/gamekitTests/App/GameKitAppTests.swift &amp;&amp;
      grep -q "test_videoMode_toggle_binds_to_store" gamekit/gamekitTests/Screens/SettingsViewTests.swift &amp;&amp;
      grep -q "test_locationRow_visibility_follows_isEnabled" gamekit/gamekitTests/Screens/SettingsViewTests.swift &amp;&amp;
      grep -q "test_zone_tap_updates_location" gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift &amp;&amp;
      grep -q "test_zone_a11y_labels" gamekit/gamekitTests/Screens/VideoLocationPickerViewTests.swift &amp;&amp;
      grep -q "test_videoMode_copy_keys_exist" gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift &amp;&amp;
      grep -q "test_off_state_byte_identical" gamekit/gamekitTests/Regression/SC5RegressionTests.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "TODO(P11" gamekit/gamekitTests/Regression/SC5RegressionTests.swift` returns the file (placeholder marker is explicit)
    - `grep -c "videoMode.location" gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift` returns ≥6 (each of 6 zone keys referenced)
    - `grep -q "videoMode.manualSelectionExplanation" gamekit/gamekitTests/Resources/LocalizableCatalogTests.swift` returns 0
    - All 5 files have `@testable import gamekit` (`grep -l "@testable import gamekit" gamekit/gamekitTests/{App,Screens,Resources,Regression}/*.swift | wc -l` returns 5)
    - No `* 2.swift` Finder duplicates in `git status` (`git status --porcelain | grep " 2.swift" | wc -l` returns 0)
  </acceptance_criteria>
  <done>
    All 5 files exist, each contains the @Test functions named per
    09-VALIDATION.md. 7 new @Test funcs total in this task (1 + 2 + 2 + 1 + 1).
    Combined with Task 1's 7 funcs = 14 @Test funcs across 7 files for the phase.
    Compile-time RED state — references to `VideoModeStore` /
    `VideoLocationPickerView` / `EnvironmentValues.videoModeStore` /
    `videoMode.*` xcstring keys all fail until Waves 1-3 deliver them.
  </done>
</task>

</tasks>

<verification>
- All 7 new test files compile (after Wave 1 ships VideoModeStore + VideoModeLocation; compile fails intentionally until then — this is the RED gate)
- Test names match 09-VALIDATION.md rows verbatim (each test_* string in VALIDATION column is grep-findable in exactly one test file)
- No Finder duplicates introduced
- No file exceeds 500 lines (CLAUDE.md §8.5)
- xcstrings catalog test references all keys named in 09-PATTERNS.md §7
</verification>

<success_criteria>
- 7 RED test files exist at the locked paths
- 14 @Test funcs covering all 15 rows in 09-VALIDATION.md (SC4 compile-only row 09-06-01 is satisfied by xcodebuild build — no @Test needed; manual row 09-06-02 has no automated assert)
- Wave 1 (09-02) can run its production code against these tests as the GREEN gate
</success_criteria>

<output>
After completion, create `.planning/phases/09-video-mode-foundation/09-01-SUMMARY.md`
listing the 7 files created, the 14 @Test funcs by name, and the validation
rows covered. Note RED state explicitly — `xcodebuild build` failing here is
expected and not a regression.
</output>
