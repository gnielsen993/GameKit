---
phase: 15-arcade-substrate-skeleton
plan: 05
type: execute
wave: 4
depends_on: ["15-04"]
files_modified: []
autonomous: false
requirements: [ARCADE-04, ARCADE-09]

must_haves:
  truths:
    - "Receiving a notification banner on the harness causes no dt-spike / time-jump on resume (D-04)"
    - "Stack and Snake tiles are legible on Classic (Chrome Diner) AND one Loud preset (Voltage/Dracula) (D-08)"
    - "Cold-start time is unchanged from v1.4 baseline — no ArcadeLoopDriver/engine state allocated at launch (SC5)"
  artifacts: []
  key_links:
    - from: "harness scenePhase pause"
      to: "smooth resume after banner"
      via: "manual notification-banner observation"
      pattern: "manual"
---

<objective>
Run the three Phase 15 manual verification gates that no unit test can cover, and record their results before `/gsd:verify-work`: the D-04 notification-banner pause-safety test, the D-08 §8.12 Home-tile theme pass, and the SC5 Instruments cold-start check.

Purpose: These three gates close ROADMAP SC3 (pause-safety on a real banner) and SC5 (unchanged cold-start), and satisfy the §8.12 / D-08 legibility bar for the new tiles. All implementation shipped in Plans 01–04; this plan is pure human-in-the-loop verification.
Output: pass/fail notes captured in the plan SUMMARY (no code files modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arcade-substrate-skeleton/15-CONTEXT.md
@.planning/phases/15-arcade-substrate-skeleton/15-VALIDATION.md
@.planning/phases/15-arcade-substrate-skeleton/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-04 notification-banner pause-safety test (ARCADE-04 / SC3)</name>
  <read_first>
    - 15-VALIDATION.md "D-04 Manual Test Procedure (notification-banner gate)" (the 6 numbered steps)
    - 15-CONTEXT.md D-04/D-05 (.inactive and .background share the same pause handler; banner is .inactive)
    - 15-RESEARCH.md Pitfall 1 (missing .inactive → jump after banner) + Environment Availability (real device preferred; Simulator notification simulation is an acceptable fallback)
  </read_first>
  <what-built>The throwaway Stack/Snake harnesses (Plan 02) drive the real `.arcadeLoop` and pause on `scenePhase .inactive` AND `.background` via the same handler; the driver discards the accumulated gap (`lastDate = nil`) on resume.</what-built>
  <how-to-verify>
    1. Build and install on a real device (preferred) or simulator. Open the app.
    2. Tap the Stack tile; confirm the dot is oscillating and the tick readout is climbing.
    3. Pull down Notification Center to trigger `.inactive`; dismiss it. Confirm the dot resumes from its prior position with no visible jump or skip.
    4. Trigger an actual notification banner (or Simulator > Features > Trigger Notification). After it appears and is dismissed, confirm the oscillation resumes smoothly — the paused gap is discarded, not replayed as a burst.
    5. Repeat on the Snake harness.
    6. Record pass/fail (and device used) in the SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - On banner dismiss, the harness element resumes smoothly with no time-jump / no multi-step burst (SC3).
    - Behavior is identical for a Notification Center pull (`.inactive`) and a home/background transition (`.background`).
    - Result (pass/fail + device) recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <human-check>Observe smooth resume after a notification banner on both harnesses; no dt-spike reaches the tick counter.</human-check>
  </verify>
  <resume-signal>Type "approved" if both harnesses resume cleanly, or describe the jump/skip observed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-08 §8.12 Home-tile theme pass (ARCADE-09)</name>
  <read_first>
    - 15-CONTEXT.md D-07 (accent RGBs) + D-08 (legible on Classic + one Loud preset)
    - CLAUDE.md §8.12 (game-screen theme passes mandatory before done) + §0.1 (Classic = Chrome Diner)
    - 15-03 Task 2 (the drawStack/drawSnake Canvas icons being judged)
  </read_first>
  <what-built>Stack (vivid orange) and Snake (calm green) tiles with custom `GameIconView` Canvas icons (Plan 03) and "Tap to play" captions (Plan 04).</what-built>
  <how-to-verify>
    1. Launch the app; go to Settings and set the preset to Classic (Chrome Diner). Return to Home.
    2. Confirm both new tiles are present, the accent color reads cleanly against the tile surface, and the Stack/Snake Canvas icons are legible (distinct, not muddy).
    3. Switch the preset to a Loud preset — Voltage or Dracula. Return to Home.
    4. Confirm both tiles remain legible (accent + icon contrast holds) under the Loud preset.
    5. Record pass/fail (and which Loud preset was used) in the SUMMARY. If an icon or accent fails legibility, file the fix against Plan 03 (icon) or note the accent constraint.
  </how-to-verify>
  <acceptance_criteria>
    - Both tiles legible (accent + Canvas icon) under Classic (Chrome Diner).
    - Both tiles legible under at least one Loud preset (Voltage or Dracula).
    - Result recorded in the SUMMARY with the Loud preset named.
  </acceptance_criteria>
  <verify>
    <human-check>Both Stack and Snake tiles read clearly on Chrome Diner and on one Loud preset.</human-check>
  </verify>
  <resume-signal>Type "approved" if both tiles pass on both presets, or name the preset/tile that failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC5 Instruments cold-start verification</name>
  <read_first>
    - 15-VALIDATION.md "Manual-Only Verifications" (cold-start row — Instruments App Launch template; confirm no loop/engine alloc before first tile tap)
    - ROADMAP Phase 15 SC5 (cold-start unchanged from v1.4 baseline; lazy init)
    - 15-RESEARCH.md (the harness VM + ArcadeLoopDriver are constructed only when the harness view appears — never at app launch)
  </read_first>
  <what-built>Lazy substrate: `ArcadeLoopDriver`/harness VMs are constructed only when a harness view is pushed (tile tap), not at app launch. No arcade state allocated in `GameKitApp` init.</what-built>
  <how-to-verify>
    1. Build for Release/Profiling on a real device. Open Instruments → App Launch template.
    2. Cold-launch the app (kill first). Confirm launch time is within noise of the v1.4 baseline.
    3. In the allocation/launch trace, confirm NO `ArcadeLoopDriver`, `StackHarnessVM`, or `SnakeHarnessVM` instance is allocated before the first tile tap (they appear only after navigating into a harness).
    4. Record cold-start time and the lazy-init observation (pass/fail) in the SUMMARY.
    Fallback if no device/Instruments available: note it explicitly in the SUMMARY and defer SC5 sign-off to the device-available session (do NOT mark SC5 green without the trace).
  </how-to-verify>
  <acceptance_criteria>
    - Cold-start time unchanged from the v1.4 baseline (within measurement noise).
    - No `ArcadeLoopDriver` / harness VM allocated at launch (lazy init confirmed in the trace).
    - Result recorded in the SUMMARY; if deferred, explicitly flagged as not-yet-verified.
  </acceptance_criteria>
  <verify>
    <human-check>Instruments App Launch trace shows unchanged cold-start and zero arcade-substrate allocations before first tile tap.</human-check>
  </verify>
  <resume-signal>Type "approved" with the measured cold-start, or "deferred — no device" to flag SC5 for a later session.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No code changes and no new trust boundary in this plan — it is verification-only. The arcade input boundary (dt clamp, T-15-01) and the Video Mode exemption (T-15-04) were addressed in Plans 01 and 04.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Denial of Service (verification) | banner/background resume path | mitigate | The D-04 manual gate empirically confirms the clamp + lastDate-reset prevent a post-banner dt burst — closing the one threat (T-15-01/T-15-02) that a unit test cannot fully reproduce. |

No package installs — legitimacy gate not applicable.
</threat_model>

<verification>
- All three checkpoints approved (or SC5 explicitly deferred with reason).
- Full suite re-run green before `/gsd:verify-work`: `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'`.
- §8.14 release-log: NOT triggered — the harness is throwaway and v1.5 ships only when real games land; do not append to Docs/releases.
</verification>

<success_criteria>
ROADMAP SC3 (pause-safe through a real notification banner) and SC5 (unchanged cold-start, lazy init) are confirmed by manual gate, and the new tiles pass the §8.12 / D-08 legibility bar — closing Phase 15's human-verification surface.
</success_criteria>

<output>
Create `.planning/phases/15-arcade-substrate-skeleton/15-05-SUMMARY.md` with the three gate results when done.
</output>
