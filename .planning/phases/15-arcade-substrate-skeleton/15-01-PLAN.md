---
phase: 15-arcade-substrate-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Core/ArcadeGameState.swift
  - gamekit/gamekit/Core/ArcadeLoopDriver.swift
  - gamekit/gamekitTests/Core/ArcadeLoopDriverTests.swift
autonomous: true
requirements: [ARCADE-01, ARCADE-02, ARCADE-03]

must_haves:
  truths:
    - "ArcadeLoopDriver forwards onTick only while isRunning == true; idle/paused/gameOver produce zero ticks"
    - "A 2.0s raw dt clamps to 0.1 and drains to at most 15 fixed-timestep ticks with a clean loop exit"
    - "ArcadeGameState lifecycle enum (idle/running/paused/gameOver) exists, Foundation-only, nonisolated"
  artifacts:
    - path: "gamekit/gamekit/Core/ArcadeGameState.swift"
      provides: "Shared 4-case arcade lifecycle enum"
      contains: "nonisolated enum ArcadeGameState"
    - path: "gamekit/gamekit/Core/ArcadeLoopDriver.swift"
      provides: "TimelineView-based real-time loop ViewModifier + .arcadeLoop modifier"
      contains: "min(rawDt, 0.1)"
    - path: "gamekit/gamekitTests/Core/ArcadeLoopDriverTests.swift"
      provides: "Two locked substrate gate tests"
      contains: "spiral-of-death"
  key_links:
    - from: "Core/ArcadeLoopDriver.swift"
      to: "onTick closure"
      via: "min(rawDt, 0.1) clamp — sole spiral-of-death guard"
      pattern: "min\\(.*0.1\\)"
    - from: "gamekitTests/Core/ArcadeLoopDriverTests.swift"
      to: "ArcadeGameState"
      via: "@testable import gamekit"
      pattern: "@testable import gamekit"
---

<objective>
Build the shared real-time loop substrate — the `ArcadeGameState` lifecycle enum and the `ArcadeLoopDriver` ViewModifier — and gate it with the two locked substrate unit tests BEFORE any game or harness depends on it.

Purpose: This is the phase gate. Both placeholder harnesses (Plan 02) and the eventual Stack/Snake engines (Phases 16/17) consume these two Core files. The `min(rawDt, 0.1)` clamp is the single spiral-of-death guard for the entire arcade subsystem — no VM or engine adds a second clamp.
Output: `Core/ArcadeGameState.swift`, `Core/ArcadeLoopDriver.swift`, `gamekitTests/Core/ArcadeLoopDriverTests.swift`.

ARCADE-03 scope note (PARTIAL): Phase 15 only PARTIALLY satisfies ARCADE-03. The lifecycle enum (`ArcadeGameState`, including the `.gameOver` case), the tap-to-start affordance, and the running/paused proof land in this phase (Plan 01 defines the enum; Plan 02's harness drives the loop, starts on tap, and pauses correctly). The remaining ARCADE-03 surface — actually ENTERING `.gameOver`, the `VideoModeBanner` game-over banner, and the game-over→restart path — is DEFERRED to Phase 16, where the first real game-over trigger exists. Phase 15's harness has no real end-game condition, so it never enters `.gameOver` and shows no banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arcade-substrate-skeleton/15-CONTEXT.md
@.planning/phases/15-arcade-substrate-skeleton/15-RESEARCH.md
@.planning/phases/15-arcade-substrate-skeleton/15-PATTERNS.md
@.planning/phases/15-arcade-substrate-skeleton/15-VALIDATION.md

<interfaces>
<!-- Contracts this plan CREATES — consumed by Plan 02 harnesses and Phases 16/17. -->
Core/ArcadeGameState.swift:
  nonisolated enum ArcadeGameState: Equatable, Hashable, Sendable
    case idle | running | paused | gameOver

Core/ArcadeLoopDriver.swift:
  struct ArcadeLoopDriver: ViewModifier { let isRunning: Bool; let onTick: (_ dt: Double) -> Void }
  extension View { func arcadeLoop(isRunning: Bool, onTick: @escaping (_ dt: Double) -> Void) -> some View }
  Clamp invariant: onTick receives min(rawDt, 0.1); lastDate reset to nil on isRunning -> false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArcadeGameState enum + ArcadeLoopDriver ViewModifier</name>
  <read_first>
    - Core/VideoModeAware.swift (driver analog — exact struct + extension View shape; lines 34–40 imports/header, 74–85 body, 150–175 extension)
    - Games/Merge/MergeGameState.swift (enum analog — lines 14–25: nonisolated enum, Equatable/Hashable/Sendable, Foundation-only)
    - 15-PATTERNS.md "Core/ArcadeLoopDriver.swift" and "Core/ArcadeGameState.swift" sections (exact target code)
    - 15-RESEARCH.md Pattern 1 + Pattern 2 + Pitfall 2 (lastDate reset)
    - 15-CONTEXT.md D-03 (pause lifecycle), Claude's Discretion (accumulator placement is in the VM, NOT this driver)
  </read_first>
  <action>
    Create `Core/ArcadeGameState.swift`: a `nonisolated enum ArcadeGameState: Equatable, Hashable, Sendable` with exactly four cases — `idle`, `running`, `paused`, `gameOver` — Foundation-only (`import Foundation`). Mirror MergeGameState.swift's header-comment discipline; document that the VM owns `private(set) var state: ArcadeGameState` and no persistence is needed.

    Create `Core/ArcadeLoopDriver.swift`: `import SwiftUI` only (no DesignKit — this driver has no UI tokens). Declare `struct ArcadeLoopDriver: ViewModifier` with stored `let isRunning: Bool`, `let onTick: (_ dt: Double) -> Void`, and `@State private var lastDate: Date? = nil`. In `body(content:)`: attach `.background { if isRunning { TimelineView(.animation) { context in Color.clear.onChange(of: context.date) { _, newDate in compute rawDt = lastDate.map { newDate.timeIntervalSince($0) } ?? 0; set lastDate = newDate; call onTick(min(rawDt, 0.1)) } } } }`, then `.onChange(of: isRunning) { _, running in if !running { lastDate = nil } }`. Do NOT use AnyView (no top-level branch). Add `extension View { func arcadeLoop(isRunning:onTick:) -> some View { modifier(ArcadeLoopDriver(...)) } }` with no default parameter values. The `min(rawDt, 0.1)` is the ONLY clamp in the arcade subsystem. Do NOT place a fixed-timestep accumulator here (it lives in the per-game VM per D-Discretion).
  </action>
  <acceptance_criteria>
    - `Core/ArcadeGameState.swift` contains `nonisolated enum ArcadeGameState` and the four case identifiers `idle`, `running`, `paused`, `gameOver`, and imports only Foundation (no SwiftUI/SwiftData).
    - `Core/ArcadeLoopDriver.swift` contains `struct ArcadeLoopDriver: ViewModifier`, the literal `min(rawDt, 0.1)`, the `if !running { lastDate = nil }` reset, and `func arcadeLoop(isRunning: Bool, onTick: @escaping (_ dt: Double) -> Void)`.
    - `grep -n "TimelineView(.animation)" Core/ArcadeLoopDriver.swift` matches (declarative pause, no CADisplayLink).
    - `xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'` succeeds with zero Swift 6 strict-concurrency warnings.
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -iE "warning:.*concurrency|error:" ; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>
  <done>Both Core files compile; ArcadeGameState is Foundation-only; the clamp and lastDate-reset invariants are present; no strict-concurrency warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the two locked substrate gate tests</name>
  <read_first>
    - gamekitTests/Engine/BoardGeneratorTests.swift (test analog — lines 1–17: import Testing/Foundation/@testable, @Suite, nonisolated struct; lines 36–38 #expect with failure message)
    - gamekitTests/Core/ModelContainerSmokeTests.swift (Core test folder location + @MainActor contrast — these tests are nonisolated, NOT @MainActor, because they probe Foundation-only types)
    - 15-RESEARCH.md "Code Examples → Unit test structure for substrate gate" (exact test bodies)
    - 15-VALIDATION.md "Per-Task Verification Map" rows ARCADE-01, ARCADE-02
    - 15-CONTEXT.md D-04 (both tests are mandatory; spiral test injects dt=2.0, asserts ≤15 ticks)
  </read_first>
  <behavior>
    - Test onTickGating: a guard `state == .running` forwards a tick; idle/paused/gameOver forward zero. Sequence idle→running→paused→gameOver yields exactly 1 total tick.
    - Test spiralOfDeathClamp: rawDt 2.0 clamps via min(2.0, 0.1) == 0.1; a fixedDt = 1.0/60.0 accumulator draining 0.1 yields steps ≤ 15 (≈6) and the while-loop terminates.
  </behavior>
  <action>
    Create `gamekitTests/Core/ArcadeLoopDriverTests.swift`: `import Testing`, `import Foundation`, `@testable import gamekit` (NO SwiftUI, NO SwiftData). Declare `@Suite("ArcadeLoopDriver substrate") nonisolated struct ArcadeLoopDriverTests`. Add MARK `// MARK: - SC1a: onTick gating (ROADMAP Phase 15 SC1a)` then `@Test("onTick is gated on .running — other states produce zero ticks") func onTickGating()`: model the VM contract via a local `func simulateTick(state: ArcadeGameState, dt: Double)` containing `guard state == .running else { return }` incrementing a counter; assert zero ticks for `.idle`, one after `.running`, no additional after `.paused` and `.gameOver` (use `#expect` with failure-message strings). Add MARK `// MARK: - SC1b: spiral-of-death clamp (ROADMAP Phase 15 SC1b)` then `@Test("spiral-of-death clamp: dt=2.0 produces at most 15 steps") func spiralOfDeathClamp()`: let rawDt 2.0, maxDt 0.1, fixedDt 1.0/60.0; `#expect(min(rawDt, maxDt) == maxDt)`; drain `var accumulator = clamped` with `while accumulator >= fixedDt { steps += 1; accumulator -= fixedDt }`; `#expect(steps <= 15)`. Every `#expect` carries a descriptive message.
  </action>
  <acceptance_criteria>
    - File exists at `gamekitTests/Core/ArcadeLoopDriverTests.swift` with `nonisolated struct ArcadeLoopDriverTests` and two `@Test` functions named `onTickGating` and `spiralOfDeathClamp`.
    - `xcodebuild test ... -only-testing:gamekitTests/ArcadeLoopDriverTests/onTickGating` exits 0.
    - `xcodebuild test ... -only-testing:gamekitTests/ArcadeLoopDriverTests/spiralOfDeathClamp` exits 0.
    - The spiral test references the literal `0.1` clamp and `1.0 / 60.0` fixed step; no SwiftUI/SwiftData import present.
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:gamekitTests/ArcadeLoopDriverTests 2>&1 | tail -5; test ${PIPESTATUS[0]} -eq 0</automated>
  </verify>
  <done>Both locked tests pass; the substrate is gated. ROADMAP SC1(a) and SC1(b) are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frame timer → substrate | The `onTick` dt (TimelineView frame delta) is the ONLY input crossing into the arcade subsystem. No network, no auth, no persisted untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | ArcadeLoopDriver.onTick | mitigate | Clamp `min(rawDt, 0.1)` bounds an unbounded dt spike (debugger resume, cold-start, banner gap) so the fixed-timestep accumulator can never spiral; proven by `spiralOfDeathClamp`. |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:gamekitTests/ArcadeLoopDriverTests` green (both tests).
- Clean build with zero strict-concurrency warnings.
- `grep -c "nonisolated enum ArcadeGameState" Core/ArcadeGameState.swift` == 1.
</verification>

<success_criteria>
ROADMAP Phase 15 SC1 satisfied: two unit tests gate the substrate (onTick gating + spiral-of-death clamp). Both Core files compile Foundation/SwiftUI-clean and are ready for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/15-arcade-substrate-skeleton/15-01-SUMMARY.md` when done.
</output>
