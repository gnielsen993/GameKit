---
phase: 15-arcade-substrate-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - gamekit/gamekit/Games/Stack/StackHarnessView.swift
  - gamekit/gamekit/Games/Snake/SnakeHarnessView.swift
autonomous: true
requirements: [ARCADE-04, ARCADE-06]

must_haves:
  truths:
    - "Each harness view drives the real .arcadeLoop(isRunning:onTick:) and shows a visibly-moving element"
    - "Each harness pauses on BOTH scenePhase .inactive AND .background, and resumes on .active"
    - "The harness VM owns the fixed-timestep accumulator and gates tick() on state == .running"
  artifacts:
    - path: "gamekit/gamekit/Games/Stack/StackHarnessView.swift"
      provides: "Throwaway Stack live-substrate harness + StackHarnessVM"
      contains: ".arcadeLoop(isRunning:"
    - path: "gamekit/gamekit/Games/Snake/SnakeHarnessView.swift"
      provides: "Throwaway Snake live-substrate harness + SnakeHarnessVM"
      contains: ".arcadeLoop(isRunning:"
  key_links:
    - from: "StackHarnessView / SnakeHarnessView"
      to: "ArcadeLoopDriver"
      via: ".arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt: dt) }"
      pattern: "arcadeLoop\\(isRunning:"
    - from: "harness scenePhase handler"
      to: "vm.pause()"
      via: "case .inactive, .background: vm.pause()"
      pattern: "case .inactive, .background"
---

<objective>
Create the two THROWAWAY live-substrate harness views — `StackHarnessView` and `SnakeHarnessView` — each containing a small `@Observable @MainActor` VM that drives the real `.arcadeLoop`, owns the fixed-timestep accumulator, gates ticks on `.running`, and pauses correctly on `scenePhase`.

Purpose: The harness is the proof surface for the loop's pause-safety (D-01/D-03) and the only way to run the D-04 manual notification-banner test in Phase 15. It is deleted when `StackGameView`/`SnakeGameView` land in Phases 16/17 (D-02) — it never ships to users.
Output: `Games/Stack/StackHarnessView.swift`, `Games/Snake/SnakeHarnessView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arcade-substrate-skeleton/15-CONTEXT.md
@.planning/phases/15-arcade-substrate-skeleton/15-RESEARCH.md
@.planning/phases/15-arcade-substrate-skeleton/15-PATTERNS.md
@gamekit/gamekit/Core/ArcadeLoopDriver.swift
@gamekit/gamekit/Core/ArcadeGameState.swift

<interfaces>
<!-- From Plan 15-01 (already shipped). Use directly — no exploration. -->
extension View { func arcadeLoop(isRunning: Bool, onTick: @escaping (_ dt: Double) -> Void) -> some View }
nonisolated enum ArcadeGameState { case idle | running | paused | gameOver }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StackHarnessView + StackHarnessVM (Games/Stack/)</name>
  <read_first>
    - Games/Merge/MergeViewModel.swift (VM shape analog — lines 18–46: @Observable @MainActor final class, private(set) discipline, counter pattern)
    - Games/Minesweeper/MinesweeperGameView.swift (env declarations analog — lines 41–50 env set; lines 160–172 scenePhase handler — NOTE: Minesweeper intentionally OMITS .inactive; the harness MUST include it)
    - 15-PATTERNS.md "StackHarnessView.swift and SnakeHarnessView.swift" section (CRITICAL DIFFERENCE: .inactive must pause for arcade) + RESEARCH Pitfall 1, 6, 7
    - 15-CONTEXT.md D-01/D-02/D-03 (live throwaway harness, respects pause)
    - CLAUDE.md §1 + §8.5 (token-only colors; file under ~400 lines)
  </read_first>
  <action>
    Create new subfolder `Games/Stack/` and file `StackHarnessView.swift` (both the view and its VM live in this one throwaway file). Add a top-of-file comment: "THROWAWAY — deleted at Phase 16 start, replaced by StackGameView()."

    `StackHarnessVM`: `@Observable @MainActor final class` with `private(set) var state: ArcadeGameState = .idle`, `private(set) var tickCount: Int = 0` (counter-trigger shape, ARCADE-06), `private var accumulator: Double = 0`, `private let fixedDt = 1.0 / 60.0`. `func tick(dt:)` guards `state == .running else return`, then `accumulator += dt; while accumulator >= fixedDt { tickCount += 1; accumulator -= fixedDt }`. Methods: `start()` sets `.running`; `pause()` → `.paused` only if currently `.running`; `resume()` → `.running` only if currently `.paused`; `stop()` → `.idle` and zero accumulator.

    `StackHarnessView`: `@EnvironmentObject themeManager: ThemeManager`, `@Environment(\.colorScheme)`, `@Environment(\.scenePhase)`, `@State private var vm = StackHarnessVM()`, computed `theme`. Body: a ZStack with `theme.colors.background.ignoresSafeArea()`, a moving `Circle().fill(theme.colors.accentPrimary)` whose `.offset(x:)` derives from `vm.tickCount` (visible oscillation), a `Text` readout of `vm.tickCount` in `theme.colors.textSecondary`, and a "Tap to Start" `Button { vm.start() }` shown only when `vm.state == .idle`. Apply `.arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt: dt) }`. Apply `.onChange(of: scenePhase)` switching `case .active: vm.resume()`, `case .inactive, .background: vm.pause()`, `@unknown default: vm.pause()`. Add a back chevron toolbar per DESIGN.md §6 and `.navigationBarBackButtonHidden(true)`. NO hardcoded colors (no `Color.orange`/`Color.green`); NO `.videoModeAware()` (applied at HomeView in Plan 04, not here).

    After writing, build. If xcodebuild reports "cannot find StackHarnessView in scope", the new `Games/Stack/` subfolder did not auto-register under PBXFileSystemSynchronizedRootGroup — per CLAUDE.md §8.8 / RESEARCH A3 add the folder via Xcode's "Add Files" dialog (do NOT hand-edit project.pbxproj). Phase 2 history indicates new subfolders auto-register, so attempt the build first.
  </action>
  <acceptance_criteria>
    - `Games/Stack/StackHarnessView.swift` exists and contains `struct StackHarnessView: View`, `final class StackHarnessVM`, `.arcadeLoop(isRunning:`, and `case .inactive, .background:`.
    - `grep -c "guard state == .running" Games/Stack/StackHarnessView.swift` >= 1 (tick gating present).
    - Pre-commit hardcoded-color hook passes: no `Color.` literal / `Color(red:` in the file (token-only).
    - Clean build with zero strict-concurrency warnings (file compiles even though not yet referenced by HomeView).
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && grep -q "case .inactive, .background" gamekit/Games/Stack/StackHarnessView.swift && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -iE "error:|warning:.*concurrency"; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>Stack harness compiles, drives .arcadeLoop, pauses on .inactive AND .background, and uses only DesignKit tokens.</done>
</task>

<task type="auto">
  <name>Task 2: SnakeHarnessView + SnakeHarnessVM (Games/Snake/)</name>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackHarnessView.swift (just authored — the exact template; Snake is structurally identical)
    - 15-PATTERNS.md "StackHarnessView.swift and SnakeHarnessView.swift" section (same composite analog)
    - 15-CONTEXT.md D-03/D-05 (.inactive and .background share the same pause handler)
  </read_first>
  <action>
    Create new subfolder `Games/Snake/` and file `SnakeHarnessView.swift`, mirroring `StackHarnessView.swift` exactly with `SnakeHarnessVM` and `SnakeHarnessView`. Same THROWAWAY comment, same VM shape (state/tickCount/accumulator/fixedDt, tick guard, start/pause/resume/stop), same env set, same `.arcadeLoop` + scenePhase `case .inactive, .background: vm.pause()` handler, same token-only colors, same back chevron, NO `.videoModeAware()`. The harness visual may differ trivially (e.g. a vertically-oscillating dot) but must remain token-only and pause-respecting. Build; apply the same pbxproj fallback note as Task 1 if `Games/Snake/` does not auto-register.
  </action>
  <acceptance_criteria>
    - `Games/Snake/SnakeHarnessView.swift` exists and contains `struct SnakeHarnessView: View`, `final class SnakeHarnessVM`, `.arcadeLoop(isRunning:`, and `case .inactive, .background:`.
    - `grep -c "guard state == .running" Games/Snake/SnakeHarnessView.swift` >= 1.
    - No `Color.`/`Color(red:` literal (hook passes).
    - Clean build, zero strict-concurrency warnings.
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && grep -q "case .inactive, .background" gamekit/Games/Snake/SnakeHarnessView.swift && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -iE "error:|warning:.*concurrency"; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>Snake harness compiles, structurally matches the Stack harness, pauses on .inactive AND .background, token-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenePhase env → harness VM | OS lifecycle transitions drive pause/resume. No new untrusted input; the dt clamp lives in Plan 01's ArcadeLoopDriver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Denial of Service | harness scenePhase handler | mitigate | `.inactive` AND `.background` both call `vm.pause()`, stopping the loop (zero CPU) on banner/background; the loop's lastDate-reset (Plan 01) discards the gap on resume. |

No package installs in this plan — legitimacy gate not applicable. No network/auth/persistence surface introduced.
</threat_model>

<verification>
- Full suite still green: `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'`.
- Both harness files present; both contain the dual-phase pause handler and the `.arcadeLoop` call site.
- Hardcoded-color pre-commit hook passes for both files.
</verification>

<success_criteria>
Two throwaway harness views exist that exercise the substrate end-to-end and respect the pause lifecycle on both `.inactive` and `.background` — satisfying the implementation half of ROADMAP SC3 (the manual proof is gated in Plan 05). ARCADE-04 (pause) and ARCADE-06 (counter-trigger structure) implemented.
</success_criteria>

<output>
Create `.planning/phases/15-arcade-substrate-skeleton/15-02-SUMMARY.md` when done.
</output>
