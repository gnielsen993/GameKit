---
phase: 15-arcade-substrate-skeleton
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - gamekit/gamekit/Core/GameRoute.swift
  - gamekit/gamekit/Core/GameDescriptor.swift
  - gamekit/gamekit/Screens/HomeView.swift
  - .planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md
autonomous: true
requirements: [ARCADE-09, ARCADE-08]

must_haves:
  truths:
    - "Stack and Snake appear as enabled tiles on Home, captioned 'Tap to play', with no mode chips"
    - "Tapping a tile navigates to its harness screen with NO .videoModeAware() applied"
    - "The Video Mode exemption is recorded in 15-VIDEO-MODE-ADR.md"
  artifacts:
    - path: "gamekit/gamekit/Core/GameRoute.swift"
      provides: "Plain (no-associated-value) .stack / .snake route cases"
      contains: "case stack"
    - path: "gamekit/gamekit/Core/GameDescriptor.swift"
      provides: "AccentRole slot9/slot10 + 2 GameDescriptor.all entries (modes: [], 'Tap to play')"
      contains: "slot9"
    - path: "gamekit/gamekit/Screens/HomeView.swift"
      provides: "destination(for:) cases routing to harness views, no .videoModeAware"
      contains: "StackHarnessView()"
    - path: ".planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md"
      provides: "ARCADE-08 exemption ADR"
      contains: "ARCADE-08"
  key_links:
    - from: "Core/GameDescriptor.all"
      to: "HomeView tile grid (ForEach GameDescriptor.all)"
      via: "two new descriptors → two enabled tiles"
      pattern: "kind: .stack"
    - from: "Screens/HomeView.destination(for:)"
      to: "StackHarnessView / SnakeHarnessView"
      via: ".disableInteractivePop() with NO .videoModeAware() (ADR ARCADE-08)"
      pattern: "StackHarnessView\\(\\)"
---

<objective>
Make Stack and Snake appear as enabled Home tiles that navigate to their harness screens, and commit the Video Mode exemption ADR. This completes ARCADE-09 and physically lands the ARCADE-08 decision (omit `.videoModeAware()` for real-time games).

Purpose: `HomeView` builds tiles from `GameDescriptor.all` (ForEach, lines 129/142) and resolves taps via the exhaustive `destination(for:)` switch (no `default`). Adding `GameRoute.stack/.snake` forces the switch to gain both cases — which reference the Plan 02 harness views — keeping the build green and the tiles live. The `klondike` case (HomeView line 355) is the exact precedent: it already omits `.videoModeAware()`.
Output: edits to `Core/GameRoute.swift`, `Core/GameDescriptor.swift`, `Screens/HomeView.swift`; new `15-VIDEO-MODE-ADR.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arcade-substrate-skeleton/15-CONTEXT.md
@.planning/phases/15-arcade-substrate-skeleton/15-RESEARCH.md
@.planning/phases/15-arcade-substrate-skeleton/15-PATTERNS.md
@gamekit/gamekit/Core/GameRoute.swift
@gamekit/gamekit/Core/GameDescriptor.swift
@gamekit/gamekit/Screens/HomeView.swift

<interfaces>
<!-- From Plans 15-02 and 15-03 (already shipped). -->
struct StackHarnessView: View   // Games/Stack/StackHarnessView.swift
struct SnakeHarnessView: View   // Games/Snake/SnakeHarnessView.swift
enum GameKind { ... case stack; case snake }   // raw "stack" / "snake"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GameRoute cases + HomeView destination routing (no videoModeAware)</name>
  <read_first>
    - Core/GameRoute.swift (self-analog — lines 26–35: existing cases carry associated values for mode deep-linking; new cases are PLAIN)
    - Screens/HomeView.swift (self-analog — destination(for:) lines 336–370, exhaustive over GameRoute, no default; klondike at 355–357 is the exact no-videoModeAware precedent; line 177 GameIconView, ForEach at 129/142)
    - 15-PATTERNS.md "GameRoute enum — plain case pattern" + "HomeView destination(for:) — routing case pattern" + RESEARCH Pattern 8 / Pitfall 4
    - 15-CONTEXT.md D-09 (no associated value), D-10 (omit .videoModeAware for Stack/Snake)
  </read_first>
  <action>
    In `Core/GameRoute.swift`, append after the `.wordGrid` case: `case stack` and `case snake` — PLAIN cases with NO associated value (D-09; endless games have `modes: []`, so no mode parameter ever deep-links here). Do not add `(StackMode?)` or any payload.

    In `Screens/HomeView.swift` `destination(for:)`, append two cases after `.wordGrid`: `case .stack: StackHarnessView().disableInteractivePop()` and `case .snake: SnakeHarnessView().disableInteractivePop()`. Apply ONLY `.disableInteractivePop()` — NO `.videoModeAware()` (ARCADE-08 / D-10; matches the klondike precedent). These are plain cases — no `let` binding. Add an inline comment referencing 15-VIDEO-MODE-ADR.md.
  </action>
  <acceptance_criteria>
    - `Core/GameRoute.swift` contains `case stack` and `case snake` with no parentheses/associated value.
    - `Screens/HomeView.swift` `destination(for:)` contains `case .stack:` returning `StackHarnessView()` and `case .snake:` returning `SnakeHarnessView()`, each with `.disableInteractivePop()` and WITHOUT `.videoModeAware`.
    - The two lines following `case .stack:` contain no `videoModeAware` token (exemption upheld).
    - Clean build, zero strict-concurrency warnings (destination switch now exhaustive).
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && grep -q "StackHarnessView()" gamekit/Screens/HomeView.swift && ! (grep -A2 'case .stack:' gamekit/Screens/HomeView.swift | grep -q videoModeAware) && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -iE "error:|warning:.*concurrency"; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>GameRoute has plain .stack/.snake cases; HomeView navigates both to their harness with no Video Mode adoption; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add AccentRole slot9/slot10 + two GameDescriptor.all entries</name>
  <read_first>
    - Core/GameDescriptor.swift (self-analog — AccentRole enum + index switch lines 34–57; GameDescriptor.all wordGrid entry lines 241–254 as the clearest template)
    - 15-PATTERNS.md "GameDescriptor.AccentRole — slot pattern" + "GameDescriptor.all — entry shape"
    - 15-CONTEXT.md D-06 (captionKey "Tap to play", written once), D-09 (modes: []), RESEARCH Pitfall 5 / Assumptions A1-A2 (verify SF Symbols)
  </read_first>
  <action>
    In `Core/GameDescriptor.swift`, append `case slot9` and `case slot10` to `AccentRole`, and add their `index` entries (`case .slot9: return 8`, `case .slot10: return 9`). Append two `GameDescriptor` entries to `.all`: Stack — `kind: .stack`, `titleKey: "Stack"`, `captionKey: "Tap to play"` (D-06, final caption — NOT "Coming soon"), `symbol:` a stacking SF Symbol, `accent: .slot9`, `route: .stack` (plain), `modes: []` (D-09), `shortMeta: "Endless tower"`; Snake — `kind: .snake`, `titleKey: "Snake"`, `captionKey: "Tap to play"`, `symbol:` a directional SF Symbol, `accent: .slot10`, `route: .snake`, `modes: []`, `shortMeta: "Endless grid"`. Verify both `symbol:` strings exist in the iOS 17 SF Symbols catalog before committing (suggested `square.stack.fill` / `arrow.triangle.turn.up.right.diamond`; if a name is absent, substitute the closest semantic symbol — Claude's discretion per D-09; an invalid name silently renders a "?" but the SF symbol is only used by the upcoming-games list, not the primary tile, which uses GameIconView).
  </action>
  <acceptance_criteria>
    - `Core/GameDescriptor.swift` contains `case slot9`, `case slot10`, `case .slot9: return 8`, `case .slot10: return 9`.
    - `.all` gains entries with `kind: .stack` and `kind: .snake`, both `captionKey: "Tap to play"` and `modes: []` and `route: .stack` / `route: .snake`.
    - No `captionKey: "Coming soon"` token introduced anywhere in the file.
    - Clean build; launching the app shows TWO new enabled tiles (verified in Plan 05 §8.12 gate and the ARCADE-09 manual check).
  </acceptance_criteria>
  <verify>
    <automated>cd gamekit && grep -q "kind: .stack" gamekit/Core/GameDescriptor.swift && grep -q "case slot9" gamekit/Core/GameDescriptor.swift && ! grep -q 'captionKey: "Coming soon"' gamekit/Core/GameDescriptor.swift && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -iE "error:|warning:.*concurrency"; test ${PIPESTATUS[0]} -ne 0</automated>
  </verify>
  <done>Two enabled Stack/Snake descriptors with locked accent slots, "Tap to play", and no mode chips; tiles appear on Home.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Video Mode exemption ADR (ARCADE-08)</name>
  <read_first>
    - 15-RESEARCH.md Pattern 10 (full ADR content skeleton — copy and adapt)
    - 15-CONTEXT.md D-10/D-11 (write ADR in Phase 15; Phase 18 only references/closes it; klondike precedent)
    - Screens/HomeView.swift (the .stack/.snake destination cases just authored — the ADR documents this exact code location)
  </read_first>
  <action>
    Create `.planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md` using the RESEARCH Pattern 10 skeleton: title "ADR: Stack and Snake are exempt from Video Mode (ARCADE-08)", Status "Accepted — 2026-06-26", Satisfies "ARCADE-08 (documentation deliverable)". Sections: Context (Video Mode reflows chrome for a PiP overlay), Decision (Stack/Snake do NOT receive `.videoModeAware()` in `HomeView.destination(for:)`), Rationale (continuous real-time input cannot pause-and-reflow mid-run without desync — fundamentally different from turn-based games), Precedent (klondike already ships without `.videoModeAware()`), Future (a later milestone could add a SUSPEND-on-PiP mode — out of scope for v1.5), Consequences (the destination cases carry only `.disableInteractivePop()`; Phase 18 references and closes this ADR). Note explicitly that this pulls ARCADE-08's documentation deliverable earlier than the ROADMAP/REQUIREMENTS Phase-18 mapping (D-11).
  </action>
  <acceptance_criteria>
    - `15-VIDEO-MODE-ADR.md` exists, contains "ARCADE-08", "Accepted", a Decision section naming `.videoModeAware()`/`destination(for:)`, and the klondike precedent.
    - The ADR's stated code consequence (no `.videoModeAware()` on Stack/Snake) matches Task 1's HomeView edit.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md && grep -q "ARCADE-08" .planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md && grep -qi "klondike" .planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md</automated>
  </verify>
  <done>The Video Mode exemption ADR is committed; ARCADE-08's documentation deliverable is satisfied in Phase 15 (D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary in this plan. Tile routing and descriptor catalog data are app-internal, static, and trusted. The sole arcade input boundary (the dt clamp) lives in Plan 01 (T-15-01).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | HomeView Video Mode exemption | accept | Stack/Snake omit `.videoModeAware()`, so no PiP overlay surface is exposed for these games; the exemption is documented and bounded (no network, no screen-share path introduced). |

No package installs — legitimacy gate not applicable.
</threat_model>

<verification>
- Clean build, zero strict-concurrency warnings (GameRoute + HomeView destination switches exhaustive).
- App launch shows two new enabled tiles; tapping each pushes the harness (confirmed in Plan 05 manual gates).
- `! grep -A2 'case .stack:' Screens/HomeView.swift | grep videoModeAware` (exemption upheld; matches ADR).
- Full suite green: `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'`.
</verification>

<success_criteria>
ROADMAP SC2 satisfied (Stack/Snake enabled tiles navigate to placeholder screens; zero strict-concurrency warnings) and the ARCADE-08 ADR is committed — completing ARCADE-09 and pulling ARCADE-08's doc deliverable into Phase 15 per D-11.
</success_criteria>

<output>
Create `.planning/phases/15-arcade-substrate-skeleton/15-04-SUMMARY.md` when done.
</output>
