---
phase: 18-stats-design-specs-adr
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md
  - .planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md
autonomous: false
requirements: [ARCADE-08]

must_haves:
  truths:
    - "No arcade substrate or engine state is allocated at app launch — all game views instantiate lazily via HomeView navigation; proven structurally plus a reverted debug init-log run (D-09)"
    - "A real-device Instruments App Launch session records the canonical cold-start baseline number — no prior v1.4 baseline exists to compare against (D-10)"
    - "The pending Phase 15 Instruments UAT item is retired (D-10)"
  artifacts:
    - path: ".planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md"
      provides: "Structural lazy-init proof + recorded canonical cold-start baseline (ms, device, OS)"
      contains: "baseline"
  key_links:
    - from: "18-COLD-START-BASELINE.md"
      to: ".planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md"
      via: "retires the pending Instruments item"
      pattern: "retired|Phase 18"
---

<objective>
Confirm cold-start is unchanged (SC4) via the combination approach (D-09): Claude proves the
allocation half structurally (lazy navigationDestination construction; zero arcade/engine
references at app-launch scope), and the user proves the timing half with one real-device
Instruments App Launch session. Because no v1.4 numeric baseline exists anywhere (D-10), this
session RECORDS the absolute number as the canonical cold-start baseline and retires the
plan-sanctioned-deferred Phase 15 Instruments UAT item.

Purpose: "Unchanged from v1.4 baseline" has no numeric anchor. Honesty over theater — record a
real baseline instead of comparing against a phantom number.
Output: `18-COLD-START-BASELINE.md`; retired Phase 15 UAT item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-stats-design-specs-adr/18-CONTEXT.md
@.planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md

<interfaces>
<!-- Structural proof targets (verified line refs): -->
HomeView.swift destination(for:) ~line 359: game views are constructed inside the
  navigationDestination closure — SwiftUI does NOT build a destination until navigation occurs,
  so no arcade view/VM/engine is allocated at launch.
App/ scope (GameKitApp.swift + root scene): should contain ZERO references to StackEngine /
  SnakeEngine / arcade VMs / StackGameView / SnakeGameView.
Prior blocking-checkpoint precedent: 16-07-PLAN.md Task 2 (Instruments recipe) and the
  Phase 17 device-approved session (2026-07-04) — reuse the recipe shape.
15-HUMAN-UAT.md: the SC5 Instruments item is still `pending` (plan-sanctioned-deferred, never run).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Structural cold-start proof (lazy-init inspection + temporary DEBUG init-log)</name>
  <files>.planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md</files>
  <read_first>
    - gamekit/gamekit/Screens/HomeView.swift (destination(for:) ~line 359 — lazy construction)
    - gamekit/gamekit/App/GameKitApp.swift (app-launch scope — confirm no arcade/engine references)
    - gamekit/gamekit/Games/Stack/StackEngine.swift, gamekit/gamekit/Games/Snake/SnakeEngine.swift (confirm they are never referenced at launch)
  </read_first>
  <action>
    Close the allocation half structurally and write findings into `18-COLD-START-BASELINE.md`:
    (1) Inspect HomeView.destination(for:) and confirm all arcade game views (StackGameView,
        SnakeGameView) are constructed inside the navigationDestination closure — i.e. only when a
        user navigates — not eagerly at scene build. Quote the relevant lines.
    (2) Grep the App/ launch scope (GameKitApp.swift + root scene) for any reference to StackEngine,
        SnakeEngine, arcade view models, StackGameView, or SnakeGameView; expected: zero hits. Record
        the grep command + output.
    (3) Optionally add a temporary `#if DEBUG` init-log (e.g. a print/os_log in an arcade engine or VM
        init) and run the app on the simulator to confirm the log does NOT fire at cold launch (only
        after navigating into the game). REVERT the temporary log before finishing — no product code
        ships. Record what you observed.
    Write a "Structural proof" section stating the allocation half is closed: no arcade/engine state
    allocated at launch (all game views lazily instantiated via HomeView navigation).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -z "$(grep -nE 'StackEngine|SnakeEngine|StackGameView|SnakeGameView' gamekit/gamekit/App/*.swift)" && test -f .planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md && grep -qi "structural proof" .planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Grep of `gamekit/gamekit/App/*.swift` for StackEngine/SnakeEngine/StackGameView/SnakeGameView returns ZERO hits (recorded in the doc).
    - The doc's "Structural proof" section quotes the HomeView.destination(for:) lazy-construction lines and states no arcade/engine state is allocated at launch.
    - Any temporary #if DEBUG init-log is reverted — `git status` shows no product-code (.swift) changes after this task.
  </acceptance_criteria>
  <done>The allocation half of SC4 is closed structurally and documented; no product code ships.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs Instruments App Launch and records the canonical baseline</name>
  <what-built>
    The structural lazy-init proof (Task 1) plus a prepared step-by-step Instruments App Launch recipe.
    This session records the FIRST canonical cold-start baseline for GameDrawer (no prior v1.4 number
    exists, D-10) and retires the deferred Phase 15 Instruments UAT item.
  </what-built>
  <how-to-verify>
    On a real device (not simulator), from the recipe:
    1. Build/install the current app (Release config) on the device.
    2. Open Instruments → App Launch template; target GameDrawer.
    3. Cold-launch the app (fully killed beforehand) and record the launch trace 3-5 times.
    4. Read the total launch duration (or "Time to first frame") for each run; note the median in ms.
    5. Record: median ms, device model, and iOS version.
    Reply with the median launch time (ms) + device model + iOS version. If launch feels
    regressed/janky, note that too.
  </how-to-verify>
  <read_first>
    - .planning/phases/16-stack/16-07-PLAN.md (the prior Instruments blocking-checkpoint recipe shape to mirror)
    - .planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md (the pending item to retire)
  </read_first>
  <action>
    Author the step-by-step Instruments recipe (mirroring 16-07's prior checkpoint) and present it as
    a blocking checkpoint (do NOT auto-advance — this needs a real device). When the user returns the
    number, record in `18-COLD-START-BASELINE.md` a "Canonical baseline" section: median launch ms,
    device model, iOS version, date — explicitly framed as the new canonical baseline (not a comparison
    against a phantom v1.4 number, D-10). Then update
    `.planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md` to mark the pending SC5 Instruments
    item as retired, pointing to this Phase 18 baseline doc.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -qi "canonical baseline" .planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md && grep -qiE "ms|millisecond" .planning/phases/18-stats-design-specs-adr/18-COLD-START-BASELINE.md && grep -qi "retired\|Phase 18" .planning/phases/15-arcade-substrate-skeleton/15-HUMAN-UAT.md && echo RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `18-COLD-START-BASELINE.md` "Canonical baseline" section records median launch time in ms, device model, and iOS version.
    - The baseline is framed as the new canonical number (D-10), not a comparison against a nonexistent v1.4 figure.
    - `15-HUMAN-UAT.md` marks the pending Instruments item retired with a pointer to the Phase 18 baseline.
    - The user explicitly returned a real-device number — not auto-advanced.
  </acceptance_criteria>
  <resume-signal>Reply with median launch time (ms) + device model + iOS version.</resume-signal>
  <done>Cold-start timing half closed with a recorded canonical baseline; Phase 15 UAT item retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temporary #if DEBUG init-log → product code | A throwaway diagnostic log must not ship in Release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Tampering | Temporary DEBUG init-log left in product code | mitigate | Task 1 acceptance requires `git status` shows no .swift changes after the log is reverted; #if DEBUG guard means it could never enter Release even if missed. |
| T-18-08 | Repudiation | "Unchanged from v1.4 baseline" claim with no anchor | mitigate | D-10: record the real absolute number as the canonical baseline rather than asserting a comparison against a phantom figure. |
</threat_model>

<verification>
- App/ launch scope free of arcade/engine references (grep).
- Canonical baseline (ms + device + OS) recorded; Phase 15 UAT item retired.
- No product-code changes committed from the temporary DEBUG log.
</verification>

<success_criteria>
- Allocation half of SC4 closed structurally (lazy HomeView navigation, no launch-time arcade allocation).
- Timing half closed by a real-device Instruments session recording the canonical cold-start baseline (D-09/D-10).
- Phase 15 pending Instruments UAT item retired.
</success_criteria>

<output>
Create `.planning/phases/18-stats-design-specs-adr/18-04-SUMMARY.md` when done.
</output>
