---
phase: 18-stats-design-specs-adr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
autonomous: true
requirements: [ARCADE-08]

must_haves:
  truths:
    - "DESIGN.md §12 has a §12.6 Stack entry and a §12.7 Snake entry"
    - "Each entry documents Reduce Motion jump-cut spec, haptic vocabulary, and per-element token map"
    - "The Video Mode exemption ADR call-site comments in HomeView match the amended ADR state"
    - "Stack and Snake engine/config files import neither SwiftUI nor SwiftData"
    - "All Games/Stack and Games/Snake Swift files are <= 400 lines"
  artifacts:
    - path: "DESIGN.md"
      provides: "§12.6 Stack and §12.7 Snake game-specific rule entries (as-built)"
      contains: "### 12.6 Stack"
  key_links:
    - from: "DESIGN.md §12.6/§12.7"
      to: "16-CONTEXT / 17-CONTEXT / 15-VIDEO-MODE-ADR"
      via: "decision-code citations"
      pattern: "16-CONTEXT|17-CONTEXT|15-VIDEO-MODE-ADR"
---

<objective>
Author the two missing DESIGN.md §12 game-specific entries as-built (SC2) and run the
mechanical milestone-close verification gates that ARCADE-08's exemption/consumer-complete
status depends on: the Video Mode exemption ADR call-site check (SC3, D-12), engine purity
grep (SC5, D-13), and the file-size grep (D-14).

Purpose: §12 is the tiebreaker doc for per-game chrome; Stack and Snake ship without entries.
The ADR call-site + purity + file-size gates confirm the amended exemption decision is
faithfully reflected in code and the engines stayed pure before the milestone closes.
Output: DESIGN.md §12.6/§12.7; recorded gate results (in SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-stats-design-specs-adr/18-CONTEXT.md
@.planning/phases/18-stats-design-specs-adr/18-PATTERNS.md
@.planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md

<interfaces>
<!-- Verified facts for the gates (do not re-derive; confirm and record): -->
HomeView.swift destination(for:): Stack carries .videoModeAware(minBoardHeight: 480);
  Snake has NO .videoModeAware and a comment block (~lines 394-395) reading
  "Snake stays exempt (pixel-derived grid cells + continuous steering)". This matches the
  ADR amended 2026-07-02 (Stack exemption lifted; Snake remains exempt). Expected: no edit.
Engine purity target files (flat files — NOTE the roadmap SC5 path Games/*/Engine subdir
  does NOT exist; use these actual paths, D-13):
  Games/Stack/StackEngine.swift, Games/Stack/StackConfig.swift,
  Games/Snake/SnakeEngine.swift, Games/Snake/SnakeConfig.swift
File-size gate (D-14): largest is Games/Stack/StackBoardCanvas.swift at 386 lines (<=400).
DESIGN.md §12 insertion point: §12.5 "Future games checklist" is at line ~656; new
  §12.6/§12.7 land BEFORE it, after §12.4 Sudoku (ends ~654).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write DESIGN.md §12.6 Stack and §12.7 Snake entries</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md §12.3 Nonogram + §12.4 Sudoku (lines ~635-654 — the bullet-list format to match) and §12.5 Future games checklist (insertion anchor)
    - .planning/phases/18-stats-design-specs-adr/18-PATTERNS.md (§ DESIGN.md §12.6/§12.7 — the full as-built bullet content for both games)
    - .planning/phases/16-stack/16-CONTEXT.md (D-05/D-07 accent ramp, D-08 perfect-drop, D-09 game-over, D-10/D-11 best-streak persistence)
    - .planning/phases/17-snake/17-CONTEXT.md (D-02 body ramp, D-04 input, D-07 input haptic, D-08 eat, D-09 high-score, D-10 death)
  </read_first>
  <action>
    Insert two new subsections into DESIGN.md §12, after §12.4 Sudoku and before §12.5 Future games
    checklist, matching the §12.3/§12.4 bullet-list format (6-10 bullets each, citing locked decision
    codes by phase/ADR). Document AS-BUILT per D-11 — where the shipped implementation refined the
    roadmap sketch, record what actually shipped and cite the 16/17 CONTEXT decision code.

    §12.6 Stack must cover:
      - Video Mode: adopted (15-VIDEO-MODE-ADR.md amendment 2026-07-02); StackGameView carries
        .videoModeAware(minBoardHeight: 480). No lives chip, no timer chip; score chip = StackScoreChip.
      - Per-element token map / per-layer accent ramp (16-CONTEXT D-05/D-07): block color cycles the
        active preset's palette via StackPalette — never hardcoded.
      - Perfect-drop celebration (16-CONTEXT D-08): pulse/glow + light haptic + combo-streak counter;
        gated by hapticsEnabled + feedbackAnimation.
      - Reduce Motion jump-cut spec (SC2a — visual only, View-tier @Environment(\.accessibilityReduceMotion)):
        perfect-drop pulse collapses to instant fill (no glow); game-over cuts instantly to banner
        (no slow-mo, no screen shake — brand rule). Engine/VM never read the accessibility flag.
      - Haptic vocabulary (SC2b): normal block land = .impact(weight: .light); perfect drop = distinct
        .light tick; game over = .error; no per-frame haptics ever.
      - Stats shape: StackStatsCard (Phase 18 D-08) — hero High Score; rows Average Score + Runs Played
        + Best Streak (Stack-only, "perfectStreak" BestScore row, 16-CONTEXT D-10/D-11).

    §12.7 Snake must cover:
      - Video Mode: EXEMPT (15-VIDEO-MODE-ADR.md, Accepted 2026-06-26 — pixel-derived grid + continuous
        steering; PiP reflow would desync state); SnakeGameView has NO .videoModeAware modifier. No lives
        chip, no timer chip; score chip = SnakeScoreChip.
      - Per-element token map / body ramp (17-CONTEXT D-02): head = accentPrimary; body fades toward
        surface via opacity steps; food = success (green) or accentPrimary fallback. Board background = background.
      - Direction input (17-CONTEXT D-04): swipe + optional D-pad; one direction lock per tick (no reversal).
      - Reduce Motion jump-cut spec (SC2a): death drain + eat animations cut instantly to banner/state under
        Reduce Motion or animations-off; no screen shake. View-tier gated only.
      - Haptic vocabulary (SC2b): valid direction = .selection; food eaten = .impact(weight: .light);
        new high score mid-run = .success once per run (17-CONTEXT D-09); game over = .error; rejected input
        and per-frame = no haptic.
      - Stats shape: SnakeStatsCard (Phase 18 D-08) — hero High Score; rows Average Score + Runs Played.
        No streak row.

    Keep prose factual and terse. Cite decision codes inline (e.g. "(16-CONTEXT D-08)").
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q "### 12.6 Stack" DESIGN.md && grep -q "### 12.7 Snake" DESIGN.md && awk '/### 12.6 Stack/{f=1} /### 12.5 Future games/{print "ORDER_BAD"} f' DESIGN.md >/dev/null && grep -A40 "### 12.6 Stack" DESIGN.md | grep -qi "reduce motion" && grep -A40 "### 12.6 Stack" DESIGN.md | grep -qi "\.error" && grep -A40 "### 12.7 Snake" DESIGN.md | grep -qi "exempt" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains `### 12.6 Stack` and `### 12.7 Snake`, positioned after §12.4 Sudoku and before §12.5 Future games checklist.
    - The Stack entry documents: Reduce Motion jump-cut (visual only), haptic vocabulary (block land `.impact(.light)`, game over `.error`, no per-frame), per-layer accent token map, and the StackStatsCard shape with Best Streak.
    - The Snake entry documents: Video Mode EXEMPT, Reduce Motion jump-cut, haptic vocabulary, body-ramp token map (head accentPrimary, food success), and the SnakeStatsCard shape without a streak row.
    - Each entry cites at least one decision code (16-CONTEXT / 17-CONTEXT / 15-VIDEO-MODE-ADR).
    - Format matches the §12.3/§12.4 bullet-list style.
  </acceptance_criteria>
  <done>DESIGN.md §12 has faithful as-built entries for both arcade games covering SC2 (a) Reduce Motion, (b) haptic vocabulary, (c) per-element token map.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run milestone-close mechanical gates (ADR call-site, engine purity, file size)</name>
  <files>DESIGN.md</files>
  <read_first>
    - .planning/phases/15-arcade-substrate-skeleton/15-VIDEO-MODE-ADR.md (amended state to check call-site against)
    - gamekit/gamekit/Screens/HomeView.swift (destination(for:) comments ~lines 363-406)
    - gamekit/gamekit/Games/Stack/StackEngine.swift, gamekit/gamekit/Games/Stack/StackConfig.swift, gamekit/gamekit/Games/Snake/SnakeEngine.swift, gamekit/gamekit/Games/Snake/SnakeConfig.swift (purity targets)
  </read_first>
  <action>
    Run three mechanical gates and record the exact command output in the plan SUMMARY (no product-code
    changes expected):
    (1) ADR call-site (SC3, D-12): confirm HomeView.destination(for:) shows Stack WITH
        .videoModeAware(minBoardHeight: 480) and Snake WITHOUT it, plus the exempt comment matching the
        ADR amended 2026-07-02. If — and only if — a comment has drifted from the amended ADR, fix that
        comment (no logic change). Expected outcome: no edit needed.
    (2) Engine purity (SC5, D-13): grep the four ACTUAL flat engine/config files (not the non-existent
        Games/*/Engine subdir the roadmap SC5 names) for `import SwiftUI` / `import SwiftData`. Expected:
        zero hits. Note in the SUMMARY that the roadmap SC5 grep path was adapted to the real flat-file
        layout (this is a verification-path fix, not a code change — Deferred item in 18-CONTEXT).
    (3) File size (D-14): wc -l every Games/Stack/*.swift and Games/Snake/*.swift; confirm the max is
        <= 400 (expected largest: StackBoardCanvas.swift at 386). Record the max file + line count.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -z "$(grep -nE 'import (SwiftUI|SwiftData)' gamekit/gamekit/Games/Stack/StackEngine.swift gamekit/gamekit/Games/Stack/StackConfig.swift gamekit/gamekit/Games/Snake/SnakeEngine.swift gamekit/gamekit/Games/Snake/SnakeConfig.swift)" && MAX=$(wc -l gamekit/gamekit/Games/Stack/*.swift gamekit/gamekit/Games/Snake/*.swift | grep -v ' total' | awk '{print $1}' | sort -rn | head -1) && [ "$MAX" -le 400 ] && grep -q "videoModeAware(minBoardHeight: 480)" gamekit/gamekit/Screens/HomeView.swift && echo "PASS max=$MAX"</automated>
  </verify>
  <acceptance_criteria>
    - Engine purity grep over the four flat engine/config files returns ZERO `import SwiftUI` / `import SwiftData` hits.
    - Max line count across all Games/Stack + Games/Snake Swift files is <= 400 (recorded with the filename).
    - HomeView Stack destination has `.videoModeAware(minBoardHeight: 480)`; Snake destination has no such modifier and the exempt comment matches the amended ADR — recorded as "no edit needed" (or the single comment fix documented).
    - SUMMARY records the SC5 grep-path adaptation (flat files vs. non-existent Games/*/Engine subdir).
  </acceptance_criteria>
  <done>All three mechanical gates pass and are recorded; ADR call-site confirmed faithful to the amended exemption; engines confirmed pure; files within the size cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation + read-only verification; no code paths, no persisted data, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Repudiation | DESIGN.md as-built spec drifting from shipped behavior | mitigate | Entries cite locked decision codes (16/17-CONTEXT, 15-VIDEO-MODE-ADR) and are cross-checked against the purity/call-site gates in Task 2 — spec is anchored to verifiable code state. |
| T-18-06 | Tampering | Engine purity regression (SwiftUI/SwiftData creeping into engines) | mitigate | Grep gate over the four flat engine/config files blocks the milestone close if any UI/persistence import appears. |
</threat_model>

<verification>
- DESIGN.md §12.6/§12.7 present, correctly ordered, covering SC2 (a)(b)(c) for both games.
- Purity grep = zero hits; file-size max <= 400; ADR call-site matches amended ADR.
</verification>

<success_criteria>
- §12.6 Stack and §12.7 Snake authored as-built with decision-code citations (SC2).
- Video Mode exemption ADR call-site verified faithful (SC3).
- Engine purity confirmed on actual flat-file paths (SC5); file-size cap confirmed (D-14).
</success_criteria>

<output>
Create `.planning/phases/18-stats-design-specs-adr/18-03-SUMMARY.md` when done.
</output>
