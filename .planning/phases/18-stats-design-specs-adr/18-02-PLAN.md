---
phase: 18-stats-design-specs-adr
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: []
autonomous: false
requirements: [ARCADE-07]

must_haves:
  truths:
    - "The hero High Score numeral is legible on Classic and on a Loud preset (Voltage or Dracula)"
    - "6-7 digit scores do not clip or truncate at large Dynamic Type"
    - "The 'No runs yet.' empty state is legible on both preset families"
  artifacts:
    - path: ".planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md"
      provides: "Recorded results of the §8.12 theme + Dynamic Type audit (screenshots referenced, pass/fail per preset)"
      contains: "Voltage"
  key_links:
    - from: "screenshots"
      to: "18-STATS-AUDIT.md"
      via: "human legibility judgment"
      pattern: "Dracula|Voltage"
---

<objective>
Verify the redesigned arcade stats cards pass the mandatory §8.12 theme legibility audit
(Classic + one Loud/Moody preset) and the D-04 large-score / Dynamic Type overflow check.
The hero-numeral shape is a new visual pattern, so it must be confirmed legible under
contrasting presets and it must not clip 6-7 digit scores at large accessibility text sizes.

Purpose: A new card shape can regress legibility or overflow under presets not exercised in
the default build (CLAUDE.md §8.12). Automation-first: Claude captures the screenshots; the
human makes the legibility/overflow judgment.
Output: `18-STATS-AUDIT.md` recording the audit result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-stats-design-specs-adr/18-CONTEXT.md
@.planning/phases/18-stats-design-specs-adr/18-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Sim visual-verification recipe (from project memory reference_sim_visual_verification.md):
     throwaway XCUITest + TEST_RUNNER_ env + xcresulttool; set theme via `defaults`,
     Video Mode via UI toggle only. Stats screen needs NO Video Mode. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Capture stats-card screenshots across presets and Dynamic Type</name>
  <files>.planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md</files>
  <read_first>
    - $HOME/.claude/projects/-Users-gabrielnielsen-Desktop-GameKit/memory/reference_sim_visual_verification.md (the throwaway XCUITest + defaults-theme + xcresulttool recipe)
    - gamekit/gamekit/Screens/StatsView.swift (how Stack/Snake sections are hosted, to navigate to them)
    - gamekit/gamekit/Screens/ScoreStatsCard.swift (the shape under test)
  </read_first>
  <action>
    Using the sim visual-verification recipe, seed the store so both Stack and Snake have at least
    one run each AND at least one run with a 6-7 digit high score (to exercise the D-04 overflow case),
    then capture the Stats screen in these configurations:
      1. Classic preset, default Dynamic Type.
      2. A Loud/Moody preset (Voltage or Dracula), default Dynamic Type.
      3. Classic preset, largest non-accessibility Dynamic Type (or an accessibility size) with the
         6-7 digit score present — to check hero-numeral wrap/clip behavior.
      4. An empty-store capture (zero runs) on one preset to confirm the "No runs yet." empty state.
    Set the theme via `defaults` per the recipe (no Video Mode toggle needed — stats screen is not a
    game board). Save the captured images into the phase directory and record their paths plus the
    exact preset / Dynamic Type / seeded-score for each in `18-STATS-AUDIT.md` under a "Captures" section.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -f .planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md && grep -qi "voltage\|dracula" .planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `18-STATS-AUDIT.md` lists at least 4 captures: Classic default, Loud/Moody (Voltage or Dracula), large-Dynamic-Type-with-6-7-digit-score, and empty-store.
    - Each capture entry records preset name, Dynamic Type size, and seeded high-score value.
    - Screenshot files referenced by the doc exist on disk.
  </acceptance_criteria>
  <done>Screenshots for all four audit configurations captured and indexed in 18-STATS-AUDIT.md, ready for human judgment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human legibility + overflow verification (§8.12 + D-04)</name>
  <what-built>
    Redesigned Stack/Snake stats cards (hero High Score numeral in titleLarge + .monospacedDigit(),
    caption label, 1pt border rule, Average Score / Runs Played / Best-Streak rows, "No runs yet."
    empty state) plus the preset + Dynamic Type screenshots captured in Task 1.
  </what-built>
  <how-to-verify>
    Review the screenshots referenced in `18-STATS-AUDIT.md` (and/or run the app):
    1. Classic preset — hero numeral, caption label, border rule, and metric rows all clearly legible;
       text/background contrast reads as premium, not muddy.
    2. Voltage or Dracula preset — same elements legible; the hero numeral does not vanish into the
       accent/background (§8.12).
    3. Large Dynamic Type with a 6-7 digit score — the hero numeral does not clip, truncate, or push
       the border/rows off-card (D-04). Confirm it wraps or scales gracefully.
    4. Empty store — "No runs yet." shows in textTertiary, centered, no blank card.
    Reply "approved" if all four pass, or describe the specific preset/size and defect if any fail.
  </how-to-verify>
  <read_first>
    - .planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md (the captures to judge)
  </read_first>
  <action>
    Present the captures to the user. On approval, record the outcome (PASS + which Loud preset was
    used + confirmation of no 6-7 digit clipping) in `18-STATS-AUDIT.md` under a "Result" heading. If
    the user reports a defect, capture the specific preset/size/defect verbatim so a gap-closure pass
    can fix it in the 18-01 files. Do not auto-approve — legibility is a human judgment.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -qi "result" .planning/phases/18-stats-design-specs-adr/18-STATS-AUDIT.md && echo RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `18-STATS-AUDIT.md` contains a "Result" section stating PASS (with the Loud preset used and a note that 6-7 digit scores do not clip) or a specific recorded defect.
    - The user explicitly replied "approved" (or provided defect details) — not auto-advanced.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the preset/size and defect.</resume-signal>
  <done>Human confirms the new arcade stats shape is legible on Classic + a Loud preset and does not overflow at large Dynamic Type; result recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no code, no persisted-data writes, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | Throwaway XCUITest + seeded sim store | accept | Test artifacts are reverted/not shipped (recipe is throwaway); no product code changes. Seeded store is simulator-only. |
</threat_model>

<verification>
- All four capture configurations exist and are indexed.
- Human approval (or defect record) present in 18-STATS-AUDIT.md.
</verification>

<success_criteria>
- §8.12 audit passes on Classic + one Loud/Moody preset.
- D-04 overflow check passes for 6-7 digit scores at large Dynamic Type.
- Empty state "No runs yet." legible.
- Result recorded; no throwaway test code committed.
</success_criteria>

<output>
Create `.planning/phases/18-stats-design-specs-adr/18-02-SUMMARY.md` when done.
</output>
