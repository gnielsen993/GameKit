---
phase: 02-mines-engines
plan: 06
type: execute
wave: 3
depends_on:
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - gamekit/gamekitTests/gamekitTests.swift
autonomous: true
requirements:
  - MINES-01
tags:
  - swift
  - minesweeper
  - cleanup
  - purity-verification
must_haves:
  truths:
    - "Every engine + model file under Games/Minesweeper/ imports ONLY Foundation (ROADMAP P2 SC5 — load-bearing test of this phase)"
    - "Full test suite runs green: BoardGenerator + RevealEngine + WinDetector all pass via xcodebuild test"
    - "Default Xcode template gamekitTests.swift stub is removed (per PATTERNS.md recommendation; the Engine/*Tests.swift files cover the surface)"
    - "No Finder dupes (* 2.swift) anywhere in the repo (CLAUDE.md §8.7 invariant)"
    - "P3 ViewModel can compile against this engine API without further P2 changes"
  artifacts:
    - path: "(deletion target) gamekit/gamekitTests/gamekitTests.swift"
      provides: "Removal of Xcode template scaffold — engine tests cover the surface"
      contains: "(file should not exist after this plan)"
  key_links:
    - from: "Engine purity"
      to: "ROADMAP P2 SC5"
      via: "grep verification: zero non-Foundation imports under Games/Minesweeper/"
      pattern: "^import (SwiftUI|SwiftData|UIKit|GameplayKit|Observation|Combine)"
    - from: "Full test suite"
      to: "ROADMAP P2 SC1+SC2+SC3+SC4"
      via: "xcodebuild test runs all three @Suite blocks"
      pattern: "Test Suite '.*' passed"
---

<objective>
Wave-3 cleanup + final verification gate. Phase 2 ships when all four prior plans (01–05) have produced files AND the integrated whole satisfies ROADMAP P2 SC5 (engines import only Foundation) AND the full test suite runs green AND the template stub is removed.

Purpose: Plans 03/04/05 each verified their own engine file's purity in isolation. This plan does the *integrated* check: a single recursive grep across the entire `Games/Minesweeper/` subtree, plus a full `xcodebuild test` run that exercises all three suites simultaneously to catch any cross-suite interference. Also handles the small cleanup item PATTERNS.md flagged: delete the Xcode template stub `gamekitTests.swift` (Engine/*Tests cover the surface; keeping the empty stub is dead weight).

Output:
- `gamekit/gamekitTests/gamekitTests.swift` deleted (or repurposed if the executor judges deletion risky — see Task 1).
- A clean `xcodebuild test` run.
- A documented purity verification (full grep output with no matches).

Implements decisions: D-15 (Swift Testing replaces template stub — finalized here), engine purity as cross-cutting verification of D-XX from all prior plans.

Implements requirements: MINES-01 (final integration check across all engine files).

Implements ROADMAP P2 success criteria: SC5 fully (integrated purity verification), and acts as the integration gate that re-confirms SC1+SC2+SC3+SC4 all pass simultaneously.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-mines-engines/02-CONTEXT.md
@.planning/phases/02-mines-engines/02-PATTERNS.md
@CLAUDE.md
@gamekit/gamekitTests/gamekitTests.swift

<interfaces>
<!-- This plan touches no production code. It runs verifications across files
     created by Plans 01-05, and removes one template stub. -->

<!-- PATTERNS.md verbatim guidance on the gamekitTests.swift stub: -->
> "Replacement note: gamekitTests.swift is the default Xcode template scaffold.
>  Per 02-CONTEXT.md 'Existing Code Insights': 'Will be replaced/augmented by
>  the new engine tests under gamekitTests/Engine/.' Planner should explicitly
>  decide: delete the stub or repurpose it into a top-level EngineSmokeTests
>  suite (recommend: delete — Engine/*Tests.swift cover the surface)."

<!-- ROADMAP P2 SC5 verbatim: -->
> "Engines import only Foundation — no SwiftUI, no SwiftData, no ModelContext
>  imports — verified by build target separation."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Engine purity grep + delete template stub</name>
  <files>gamekit/gamekitTests/gamekitTests.swift</files>
  <read_first>
    - .planning/phases/02-mines-engines/02-PATTERNS.md (Replacement note: "recommend: delete — Engine/*Tests.swift cover the surface")
    - gamekit/gamekitTests/gamekitTests.swift (current contents — verify it is still the empty Xcode template before deletion)
    - CLAUDE.md §8.7 (no Finder dupes), §8.10 (commit discipline — this is the small grouped batch for P2 cleanup)
  </read_first>
  <action>
    **Step 1 — Engine purity verification (the load-bearing SC5 check).**

    Run a recursive grep against the entire `Games/Minesweeper/` subtree to confirm every production engine + model file imports ONLY Foundation. Any non-Foundation import is a phase-failing bug:

    ```bash
    cd /Users/gabrielnielsen/Desktop/GameKit
    grep -RE "^import (SwiftUI|SwiftData|UIKit|GameplayKit|Observation|Combine|AppKit|WatchKit|TVUIKit)" \
      gamekit/gamekit/Games/Minesweeper/
    ```

    **Expected output: nothing (exit code 1).** If ANY line is printed, the offending file violates ROADMAP P2 SC5 — abort the plan and surface the violation. The fix is to remove the offending import (the engine file should not need it; if it genuinely does, the engine has been mis-designed and Plan 03/04/05 needs revision).

    Then run a positive grep to confirm `import Foundation` IS present in every production file:

    ```bash
    for f in $(find gamekit/gamekit/Games/Minesweeper -name "*.swift" -type f); do
      if ! grep -q "^import Foundation" "$f"; then
        echo "MISSING import Foundation: $f"
      fi
    done
    ```

    Expected output: empty (every file has `import Foundation`).

    **Step 2 — No-Finder-dupes check (CLAUDE.md §8.7 invariant — runs at every plan boundary).**

    ```bash
    find gamekit -name "* 2.swift" -type f
    ```

    Expected output: empty. If any `* 2.swift` files exist, abort and surface — they break the build (PBXFileSystemSynchronizedRootGroup compiles every `.swift` in the folder; byte-identical dupes cause "invalid redeclaration").

    **Step 3 — Decide: delete vs repurpose `gamekit/gamekitTests/gamekitTests.swift`.**

    Read the file's current contents:
    ```bash
    cat gamekit/gamekitTests/gamekitTests.swift
    ```

    Expected: the unchanged Xcode template (17 lines, single `@Test func example()` with placeholder body — see PATTERNS.md "Established pattern: Swift Testing scaffold" for the exact text).

    If the file is the unchanged template (which is the documented expectation): **DELETE the file.** PATTERNS.md recommends delete: "Engine/*Tests.swift cover the surface."

    ```bash
    rm gamekit/gamekitTests/gamekitTests.swift
    ```

    If the file has been edited to contain real tests since P1 (unexpected — we just verified it's still the template), do NOT delete. Instead, leave it untouched and note the discrepancy in the plan SUMMARY. Do NOT silently overwrite or merge.

    **Step 4 — Final integrated test run (proves all four success criteria simultaneously).**

    Run the entire `gamekitTests` target:

    ```bash
    cd gamekit && xcodebuild test \
      -scheme gamekit \
      -destination 'platform=iOS Simulator,name=iPhone 16' \
      -quiet 2>&1 | tail -40
    ```

    Expected near the end:
    - `Test Suite 'BoardGenerator' passed` — proves SC1 + SC2
    - `Test Suite 'RevealEngine' passed` — proves SC3
    - `Test Suite 'WinDetector' passed` — proves SC4
    - `** TEST SUCCEEDED **` — overall green
    - No `'gamekitTests'` suite failure (since we deleted that template stub, it should not appear)

    If the command exits non-zero or any `@Suite` reports failure, abort the plan and surface — the integrated run found a regression that the per-plan isolated runs missed (e.g. a name collision, a `@testable import` issue, or a build-graph problem).

    **Step 5 — File listing snapshot for the SUMMARY.**

    Run a tree listing of everything Phase 2 produced for the SUMMARY's "files created" section:

    ```bash
    find gamekit/gamekit/Games/Minesweeper gamekit/gamekitTests/Engine gamekit/gamekitTests/Helpers \
      -name "*.swift" -type f | sort
    ```

    Expected output (8 files):
    ```
    gamekit/gamekit/Games/Minesweeper/Engine/BoardGenerator.swift
    gamekit/gamekit/Games/Minesweeper/Engine/RevealEngine.swift
    gamekit/gamekit/Games/Minesweeper/Engine/WinDetector.swift
    gamekit/gamekit/Games/Minesweeper/MinesweeperBoard.swift
    gamekit/gamekit/Games/Minesweeper/MinesweeperCell.swift
    gamekit/gamekit/Games/Minesweeper/MinesweeperDifficulty.swift
    gamekit/gamekit/Games/Minesweeper/MinesweeperGameState.swift
    gamekit/gamekit/Games/Minesweeper/MinesweeperIndex.swift
    gamekit/gamekitTests/Engine/BoardGeneratorTests.swift
    gamekit/gamekitTests/Engine/RevealEngineTests.swift
    gamekit/gamekitTests/Engine/WinDetectorTests.swift
    gamekit/gamekitTests/Helpers/SeededGenerator.swift
    ```

    (12 files total: 5 models + 3 engines + 3 tests + 1 test helper = 12. The 5+3=8 production files + 3 test files + 1 helper.)

    **No code changes in this plan beyond the deletion of the template stub.** All verification is read-only grep + xcodebuild; the only mutation is `rm gamekit/gamekitTests/gamekitTests.swift`.
  </action>
  <verify>
    <automated>
      ! grep -RE "^import (SwiftUI|SwiftData|UIKit|GameplayKit|Observation|Combine|AppKit|WatchKit|TVUIKit)" gamekit/gamekit/Games/Minesweeper/ &&
      [ -z "$(find gamekit -name '* 2.swift' -type f)" ] &&
      ! test -f gamekit/gamekitTests/gamekitTests.swift &&
      test -f gamekit/gamekit/Games/Minesweeper/Engine/BoardGenerator.swift &&
      test -f gamekit/gamekit/Games/Minesweeper/Engine/RevealEngine.swift &&
      test -f gamekit/gamekit/Games/Minesweeper/Engine/WinDetector.swift &&
      cd gamekit && xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -3 | grep -q "TEST SUCCEEDED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -RE "^import (SwiftUI|SwiftData|UIKit|GameplayKit|Observation|Combine|AppKit|WatchKit|TVUIKit)" gamekit/gamekit/Games/Minesweeper/` returns exit 1 with no matches (ROADMAP P2 SC5 proven)
    - Every `.swift` file under `gamekit/gamekit/Games/Minesweeper/` contains `import Foundation` (positive grep confirms)
    - `find gamekit -name "* 2.swift" -type f` returns empty (CLAUDE.md §8.7 invariant)
    - File `gamekit/gamekitTests/gamekitTests.swift` does NOT exist after this plan (or, if executor opted not to delete due to non-template content, the deviation is documented in the SUMMARY)
    - All 12 expected files (5 models + 3 engines + 3 tests + 1 helper) exist at their documented paths
    - Full `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'` run returns `** TEST SUCCEEDED **`
    - No engine file references SwiftUI/SwiftData/UIKit/GameplayKit/Observation/Combine
  </acceptance_criteria>
  <done>
    Engine purity proven by integrated grep. Full test suite green. Template stub removed. Phase 2 is shippable; P3 can begin building MinesweeperViewModel against this engine API.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Verification + deletion only. Reads file system, runs xcodebuild test, removes one template file. No untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | T (Tampering) | engine purity invariant | mitigate | Integrated recursive grep across `Games/Minesweeper/` is the load-bearing SC5 check. Per-plan checks in 03/04/05 verified individual files; this plan runs the cross-cutting verification that catches "engine X imports SwiftUI but engine Y doesn't" inconsistency. Acceptance criteria fail-stops on any non-Foundation import. |
| T-02-14 | T (Tampering) | template stub deletion | mitigate | Step 3 reads the file before deletion; if content has drifted from the documented Xcode template, the executor refuses to delete and surfaces in SUMMARY. Prevents accidentally deleting real test code. |

Per planning_context: ASVS L1 N/A; this plan is a project-rule integrity gate, not security threat.
</threat_model>

<verification>
End-to-end Plan 06 verification:
- ROADMAP P2 SC5 fully proven: integrated grep confirms zero non-Foundation imports across all 8 production files
- Full test suite (`xcodebuild test`) passes — re-confirming SC1 + SC2 + SC3 + SC4 simultaneously
- Template stub `gamekitTests.swift` removed (or non-deletion documented)
- No Finder dupes anywhere
- All 12 P2 files present at expected paths
- Project hygiene invariants from CLAUDE.md §8.7 + §8.8 + §8.10 honored
</verification>

<success_criteria>
- [ ] Recursive `grep -RE "^import (SwiftUI|SwiftData|...)"` on `Games/Minesweeper/` returns no matches (SC5)
- [ ] Every engine + model file has `import Foundation` (positive check)
- [ ] `gamekit/gamekitTests/gamekitTests.swift` no longer exists
- [ ] No `* 2.swift` files anywhere in repo
- [ ] `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16'` reports `** TEST SUCCEEDED **`
- [ ] All 8 production files (5 models + 3 engines) and all 4 test-target files (3 test suites + 1 helper) exist
</success_criteria>

<output>
After completion, create `.planning/phases/02-mines-engines/02-06-SUMMARY.md` summarizing:
- Verification results (purity grep output, test suite output excerpt, file count)
- Was the template stub deleted? (Yes / No + rationale if No.)
- Total Phase 2 lines of code produced (sum of `wc -l` over all 12 files)
- Phase 2 SC coverage table (every SC1..SC5 with the test that proves it)
- Whether `pbxproj` needed any hand-patching across all 4 prior plans (expected: no, per CLAUDE.md §8.8 — but capture the answer for future test-target work)
- Confirmation that P3 can begin: the engine API surface is `BoardGenerator.generate`, `RevealEngine.reveal`, `WinDetector.isWon` / `WinDetector.isLost` — Plan 03 of P3 will consume these from the MinesweeperViewModel
- Any deviations from CONTEXT.md / PATTERNS.md / PITFALLS.md across the entire phase
</output>
