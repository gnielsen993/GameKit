---
phase: 07-release
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - assets/icon/light.svg
  - assets/icon/dark.svg
  - assets/icon/tinted.svg
  - assets/icon/source/light.png
  - assets/icon/source/dark.png
  - assets/icon/source/tinted.png
  - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png
  - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png
  - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png
  - .planning/phases/07-release/07-02-PLAN.md
autonomous: false
requirements_addressed: []
tags:
  - icon
  - assets
  - sc1
  - wave-2
  - p7
must_haves:
  truths:
    - "AI-generated arcade-machine app icon ships in v1.0; user has reviewed and approved the chosen render before export"
    - "Three variants ship (light + dark + tinted) — same arcade-machine subject retoned per appearance, NOT three different scenes"
    - "1024² PNGs land in `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/{icon-light,icon-dark,icon-tinted}.png` replacing the P1 placeholders; AppIcon.appiconset/Contents.json (P1) is untouched"
    - "Master source committed to repo at `assets/icon/` — vector SVG masters OR raster PNG masters per Discretion #4 with the decision documented in this plan tail"
    - "AI service used + vector-vs-raster decision recorded inline in this plan (Discretion #4 + #5) so v1.0.x re-exports are reproducible"
    - "No Finder-dupe `* 2.png` or `* 2.svg` files leak into the appiconset or `assets/icon/` (CLAUDE.md §8.7)"
    - "Xcode 16 synchronized root group auto-registers the new PNGs (no `project.pbxproj` hand-patch per CLAUDE.md §8.8)"
    - "Atomic commit `feat(07-02): real arcade-machine app icon (light/dark/tinted)` per CLAUDE.md §8.10 — does NOT bundle with schema deploy, metadata, or any other P7 work"
  artifacts:
    - path: "assets/icon/"
      provides: "Master icon assets at repo root (sibling to gamekit/) — SVG vectors OR PNG rasters per Discretion #4"
      contains: "light"
    - path: "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png"
      provides: "1024² light-mode arcade-machine render replacing P1 placeholder"
      min_bytes: 10000
    - path: "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png"
      provides: "1024² dark-mode neon-glow arcade-machine render replacing P1 placeholder"
      min_bytes: 10000
    - path: "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png"
      provides: "1024² monochrome silhouette arcade-machine render replacing P1 placeholder"
      min_bytes: 10000
  key_links:
    - from: "assets/icon/"
      to: "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/"
      via: "PNG export from master at 1024x1024"
      pattern: "icon-(light|dark|tinted)\\.png"
    - from: "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json"
      to: "icon-{light,dark,tinted}.png"
      via: "P1 Contents.json wiring (3-slot: universal-light + luminosity-dark + tinted) — UNCHANGED"
      pattern: "icon-light\\.png|icon-dark\\.png|icon-tinted\\.png"
---

<objective>
Replace the P1 placeholder app icons in `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/` with the real arcade-machine icon set (light + dark + tinted) per CONTEXT D-01, D-02, D-03. SC1 of Phase 7 requires "Real app icon (replacing the placeholder from FOUND-06) ships in `Assets.xcassets`" — this plan delivers that lock.

**What ships:**
- AI-generated arcade-machine icon, three variants (light = warm cabinet / dark = neon-glow cabinet / tinted = monochrome silhouette) — SAME subject retoned per appearance, NOT three different scenes.
- Master assets committed at `assets/icon/` — SVG vectors at `assets/icon/{light,dark,tinted}.svg` OR raster masters at `assets/icon/source/{light,dark,tinted}.png` (Discretion #4 — decision recorded in this plan after AI render is reviewed).
- 1024² PNG renders dropped into `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/{icon-light,icon-dark,icon-tinted}.png`. The P1 Contents.json wiring is already correct (3 slots: universal-light + luminosity-dark + tinted) — only the PNG files swap.
- Decision tail at the end of this plan after Task 1 completes: AI service used (Discretion #5) + vector vs raster master (Discretion #4).

**Why this is autonomous: false:**
Task 1 generates an AI render → Task 2 is a [BLOCKING] CHECKPOINT where the user approves the chosen render. Without explicit user approval the icon does not ship. The remaining tasks (export, drop into appiconset, verify, commit) are autonomous.

**Out of scope for this plan (do NOT bundle):**
- CloudKit Production schema deploy (07-03 — runs in parallel as a separate Wave-2 plan).
- App Store metadata / privacy / screenshots (07-04).
- Release checklist authoring (07-05).
- TestFlight upload + SC1–SC5 sweep (07-06).
- Per-game alt-icon variants (DIST-V2-01 — REQUIREMENTS v2 deferred).
- Editing `Contents.json` — it's already correct from P1 (3 slots wired).
- Editing `project.pbxproj` — Xcode 16 synchronized root group auto-registers PNGs added to existing folders (CLAUDE.md §8.8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-release/07-CONTEXT.md
@gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI render → Repo | AI-generated raster crosses into the repo. Subject must match user direction ("old school arcade machine"); user is the gating reviewer. |
| `assets/icon/` masters → `AppIcon.appiconset` PNGs | Export step is automatable; risk is wrong dimensions (must be exactly 1024x1024) or wrong colorspace (must be sRGB; alpha allowed but not required). |
| Finder-dupe collision | Drag-drop into `assets/icon/` or appiconset can spawn `* 2.png`/`* 2.svg` siblings — these break the synchronized root group with "invalid redeclaration" (CLAUDE.md §8.7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-icon-subject-drift | Compliance | AI render subject | mitigate | Task 2 BLOCKING checkpoint: user approves the chosen render before export. Subject must be "old school arcade machine" per D-01 verbatim — NOT abstract logo, letter mark, or random scene. |
| T-07-02-finder-dupe | Tampering / Build break | `assets/icon/` + `AppIcon.appiconset/` | mitigate | Verify acceptance criterion `! ls "*\ 2.png"` and `! ls "*\ 2.svg"` in both directories before commit (CLAUDE.md §8.7). |
| T-07-02-wrong-dimensions | Integrity | 1024² PNG export | mitigate | `sips -g pixelWidth -g pixelHeight` per file confirms 1024x1024 in acceptance criteria. App Store will reject sub-1024 icons. |
| T-07-02-pbxproj-leak | Integrity | git history | mitigate | CLAUDE.md §8.8 — synchronized root group auto-registers PNGs in `Assets.xcassets`. Acceptance criterion confirms `git status` shows `Assets.xcassets/AppIcon.appiconset/*.png` modifications and `assets/icon/*` additions but NO `gamekit.xcodeproj/project.pbxproj` change. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Generate AI arcade-machine render (3 variants) and stage candidates for review</name>
  <files>
    - assets/icon/source/light-candidate.png (or .svg)
    - assets/icon/source/dark-candidate.png (or .svg)
    - assets/icon/source/tinted-candidate.png (or .svg)
  </files>
  <read_first>
    - .planning/phases/07-release/07-CONTEXT.md (D-01 lines 46 verbatim subject lock; D-02 lines 47 retone rule; D-03 line 48 master path; Discretion #4 line 81 vector-vs-raster; Discretion #5 line 82 AI service freedom)
    - .planning/PROJECT.md (positioning posture: "calm, premium, fully theme-customizable" — informs aesthetic direction)
    - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json (P1 3-slot wiring — confirms target filenames `icon-light.png` / `icon-dark.png` / `icon-tinted.png`)
    - CLAUDE.md (§8.7 Finder-dupe hygiene; §8.8 do-not-hand-patch-pbxproj)
  </read_first>
  <action>
    Generate the AI arcade-machine render using the planner-picked AI service (Discretion #5 — Midjourney / DALL-E / Sora / Ideogram / etc., based on availability + output quality). Three variants. Same subject, retoned per appearance.

    **Subject (verbatim from D-01):** "old school arcade machine."

    **Variant retone rules (verbatim from D-02 + CONTEXT specifics line 178):**
    - **Light:** warm cabinet (warm woodgrain / cream highlights / soft golden glow on the screen)
    - **Dark:** neon-glow cabinet (cool neon outlines / dark cabinet body / glowing CRT screen)
    - **Tinted:** monochrome silhouette (single-tone, alpha-clean, screen rendered as a high-contrast cutout — the iOS system applies the user's accent tint)

    **Hard rules:**
    - Same arcade-machine subject across all three. NOT three different scenes / angles / cabinets.
    - 1024 x 1024 pixels.
    - sRGB colorspace.
    - Alpha allowed but not required (tinted variant typically has alpha for the silhouette).
    - No text on the cabinet (no fake "Mines" or "GameKit" lettering — App Store rejects icons with reads-as-text overlays per Apple HIG).

    **Suggested prompt skeleton (planner adapts to chosen AI service):**
    > "An iconic stylized vintage arcade cabinet, three-quarter front view, square 1:1 composition, no text or lettering, premium minimalist aesthetic, app icon style with rounded shoulders, [warm woodgrain & golden CRT glow | dark cabinet with cyan/magenta neon outlines & glowing CRT | flat monochrome silhouette on transparent background], iOS 17 app icon, 1024x1024."

    **Output staging:**
    Save the three candidate renders to `assets/icon/source/`:
    - `assets/icon/source/light-candidate.png` (or `.svg` if the chosen tool emits vector)
    - `assets/icon/source/dark-candidate.png`
    - `assets/icon/source/tinted-candidate.png`

    Use `-candidate.png` suffix (NOT the final `light.png` / `dark.png` / `tinted.png`) so Task 2 has a clean rename gate. Final file naming happens after user approval in Task 4.

    **Document the AI service choice in `assets/icon/AI_PROVENANCE.md`** (new file at `assets/icon/AI_PROVENANCE.md`):
    ```
    # Icon Provenance (P7 Plan 07-02)
    AI service used: <name + version, e.g. "Midjourney v6.1" or "DALL-E 3 via ChatGPT 2026-04-27">
    Generation date: 2026-04-27
    Subject (verbatim from CONTEXT D-01): "old school arcade machine"
    Variants generated: light (warm cabinet) / dark (neon-glow) / tinted (monochrome silhouette)
    Candidate renders staged at: assets/icon/source/{light,dark,tinted}-candidate.png
    Iterations: <N rounds; brief note on what was rejected before this candidate>
    Approval: pending Task 2 [BLOCKING] CHECKPOINT
    ```

    Do NOT commit yet. Task 5 stages the final commit after user approval and export.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; test -f assets/icon/source/light-candidate.png -o -f assets/icon/source/light-candidate.svg &amp;&amp; test -f assets/icon/source/dark-candidate.png -o -f assets/icon/source/dark-candidate.svg &amp;&amp; test -f assets/icon/source/tinted-candidate.png -o -f assets/icon/source/tinted-candidate.svg &amp;&amp; test -f assets/icon/AI_PROVENANCE.md &amp;&amp; grep -q "old school arcade machine" assets/icon/AI_PROVENANCE.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 3 candidate files exist under `assets/icon/source/` (`-candidate.png` or `-candidate.svg`): see automated verify
    - For PNG candidates: dimensions are exactly 1024×1024: `for f in assets/icon/source/*-candidate.png; do sips -g pixelWidth -g pixelHeight "$f" | grep -E "pixel(Width|Height): 1024"; done` (each file produces 2 matching lines)
    - `AI_PROVENANCE.md` exists with verbatim subject string from D-01: `grep -F "old school arcade machine" assets/icon/AI_PROVENANCE.md`
    - `AI_PROVENANCE.md` names the AI service + version: `grep -E "AI service used: .+" assets/icon/AI_PROVENANCE.md`
    - No Finder-dupe files leaked into staging (CLAUDE.md §8.7): `! find assets/icon -name "* 2.png" -o -name "* 2.svg" | grep .`
    - Final filenames `assets/icon/{light,dark,tinted}.{svg,png}` and `gamekit/.../icon-{light,dark,tinted}.png` are NOT yet present (those land after user approval in Task 4): `! test -f assets/icon/light.svg && ! test -f assets/icon/source/light.png`
  </acceptance_criteria>
  <done>Three AI-generated candidate renders staged at `assets/icon/source/*-candidate.{png|svg}`; provenance recorded; ready for user approval at Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] CHECKPOINT — User reviews and approves arcade-machine renders (Discretion #5 + #4)</name>
  <what-built>
    Three candidate AI-generated arcade-machine renders staged at `assets/icon/source/{light,dark,tinted}-candidate.{png|svg}` (per Task 1).
    Provenance recorded at `assets/icon/AI_PROVENANCE.md`.
  </what-built>
  <how-to-verify>
    Open each candidate file:
    - `assets/icon/source/light-candidate.{png|svg}` — should depict a warm-toned arcade cabinet (woodgrain/cream highlights, soft golden CRT glow).
    - `assets/icon/source/dark-candidate.{png|svg}` — should depict the SAME arcade cabinet with a dark body and cool neon outlines / glowing CRT screen.
    - `assets/icon/source/tinted-candidate.{png|svg}` — should depict the SAME arcade cabinet as a flat monochrome silhouette (alpha-clean for tinted slot).

    **Verbatim subject lock from CONTEXT D-01:** "old school arcade machine."

    **Verbatim retone rule from CONTEXT D-02 / specifics line 178:** "Same arcade-machine subject, retoned per appearance. Light = warm cabinet. Dark = neon-glow cabinet. Tinted = monochrome silhouette."

    **Approval criteria:**
    1. Subject is recognizably an arcade machine (vintage cabinet aesthetic). Not abstract logo, letter mark, or unrelated scene.
    2. All three variants depict the SAME cabinet retoned — NOT three different cabinets / angles / scenes.
    3. No text/lettering on the cabinet that reads as words (Apple HIG rejects icons with text overlays).
    4. Tinted variant works as a single-tone silhouette (the iOS system applies the user's accent color).
    5. Composition reads at small sizes (mentally simulate the icon at 60×60 on a Home Screen — does the cabinet still register?).

    **Vector-vs-raster decision (Discretion #4):**
    Inspect the AI output quality. Two paths:
    - **Vectorize:** If the raster output is clean enough that vectorizing in Affinity Designer / Inkscape would preserve fidelity (or if the AI emitted SVG natively), Task 4 will commit final SVG masters at `assets/icon/{light,dark,tinted}.svg`.
    - **Ship raster as master:** If the AI raster is high-quality and vectorization adds no fidelity (typical for photorealistic / heavily-textured renders), Task 4 will commit the raster as the master at `assets/icon/source/{light,dark,tinted}.png` and SKIP the SVG step.

    Tell the executor which path to take in your resume signal so Task 4 picks the right export workflow.

    **Public-app-name candidate is OUT OF SCOPE for this checkpoint** — that surfaces in 07-04 Task 4 (App Store metadata plan).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved — vectorize: ship SVG masters at assets/icon/{light,dark,tinted}.svg`
    - `approved — raster master: ship PNG masters at assets/icon/source/{light,dark,tinted}.png (NO SVG step)`
    - `re-render: <feedback>` (Task 1 re-runs with adjusted prompt)
    - `re-render with different AI service: <which service> — <feedback>` (planner picks alternate per Discretion #5)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Promote approved candidates to final masters at `assets/icon/`</name>
  <files>
    - assets/icon/light.svg (if vectorize path) OR assets/icon/source/light.png (if raster path)
    - assets/icon/dark.svg (if vectorize path) OR assets/icon/source/dark.png (if raster path)
    - assets/icon/tinted.svg (if vectorize path) OR assets/icon/source/tinted.png (if raster path)
    - assets/icon/AI_PROVENANCE.md (updated with approval timestamp + chosen path)
  </files>
  <read_first>
    - .planning/phases/07-release/07-CONTEXT.md (D-03 line 48 + Discretion #4 line 81 — master path rules)
    - assets/icon/AI_PROVENANCE.md (Task 1's record — append the chosen path)
    - User's Task 2 resume signal (vectorize OR raster master)
  </read_first>
  <action>
    Promote the approved candidates from `assets/icon/source/*-candidate.{png|svg}` to final masters per the Task 2 resume signal.

    **Path A — Vectorize (if user approved `vectorize`):**
    1. Open each `*-candidate.{png|svg}` in Affinity Designer / Inkscape (planner picks based on tool availability).
    2. If candidates are PNG: trace to vector with appropriate precision; clean up control points; flatten to single-page SVG with embedded raster fallback only if necessary.
    3. If candidates are already SVG: copy through; do not re-trace.
    4. Export as `assets/icon/light.svg`, `assets/icon/dark.svg`, `assets/icon/tinted.svg` (lowercase, no spaces).
    5. Verify each SVG opens in Preview / browser and renders correctly at 1024x1024.

    **Path B — Raster master (if user approved `raster master`):**
    1. Copy each `*-candidate.png` to its final name:
       - `cp assets/icon/source/light-candidate.png assets/icon/source/light.png`
       - `cp assets/icon/source/dark-candidate.png assets/icon/source/dark.png`
       - `cp assets/icon/source/tinted-candidate.png assets/icon/source/tinted.png`
    2. If candidates exist only as `.svg`, rasterize at 1024×1024 first using the planner's chosen tool (e.g. `rsvg-convert -w 1024 -h 1024 in.svg -o out.png`).
    3. Confirm dimensions remain exactly 1024×1024 sRGB.

    **Both paths — clean up `*-candidate` staging files:**
    Remove the candidate files after promotion (they're an artifact of the review gate, not the master):
    `rm -f assets/icon/source/*-candidate.{png,svg}`

    **Update `AI_PROVENANCE.md`:**
    Append a final block:
    ```
    ## Approval (2026-04-27)
    User approved candidates via Task 2 [BLOCKING] CHECKPOINT.
    Master path chosen (Discretion #4): <vector | raster>
    Final masters:
    - <list of final master file paths>
    Candidate staging files removed after promotion.
    ```

    Do NOT commit yet — Task 4 exports to the appiconset, then Task 5 commits the whole batch atomically.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; { ( test -f assets/icon/light.svg &amp;&amp; test -f assets/icon/dark.svg &amp;&amp; test -f assets/icon/tinted.svg ) || ( test -f assets/icon/source/light.png &amp;&amp; test -f assets/icon/source/dark.png &amp;&amp; test -f assets/icon/source/tinted.png ); } &amp;&amp; ! ls assets/icon/source/*-candidate.* 2>/dev/null | grep . &amp;&amp; grep -q "Master path chosen" assets/icon/AI_PROVENANCE.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Either Path A masters exist (`assets/icon/{light,dark,tinted}.svg`) OR Path B masters exist (`assets/icon/source/{light,dark,tinted}.png`): see automated verify
    - For Path B PNG masters: dimensions are exactly 1024×1024: `for f in assets/icon/source/{light,dark,tinted}.png; do test -f "$f" || continue; sips -g pixelWidth -g pixelHeight "$f" | grep -E "pixel(Width|Height): 1024"; done` (each existing file produces 2 matching lines)
    - Candidate staging files removed: `! ls assets/icon/source/*-candidate.* 2>/dev/null | grep .`
    - No Finder-dupe files in `assets/icon/` (CLAUDE.md §8.7): `! find assets/icon -name "* 2.png" -o -name "* 2.svg" | grep .`
    - `AI_PROVENANCE.md` records the chosen path: `grep -E "Master path chosen \(Discretion #4\): (vector|raster)" assets/icon/AI_PROVENANCE.md`
  </acceptance_criteria>
  <done>Approved masters live under `assets/icon/`; vector-vs-raster decision recorded; ready for export to appiconset.</done>
</task>

<task type="auto">
  <name>Task 4: Export 1024² PNGs into AppIcon.appiconset and verify Xcode preview</name>
  <files>
    - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png
    - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png
    - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png
  </files>
  <read_first>
    - assets/icon/ (whichever masters exist from Task 3 — SVG or raster)
    - gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json (P1 wiring — confirms target filenames; do NOT edit)
    - CLAUDE.md (§8.7 Finder-dupe hygiene; §8.8 synchronized root group auto-registers PNGs)
    - .planning/phases/07-release/07-CONTEXT.md (Existing Code Insights line 153 — Contents.json already correct, P7 swaps PNGs only)
  </read_first>
  <action>
    Export the three master assets to 1024×1024 sRGB PNGs at the exact paths the P1 Contents.json wires:
    - `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png`
    - `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png`
    - `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png`

    **If Path A (SVG masters):**
    Use `rsvg-convert` (or equivalent SVG-to-PNG tool — Inkscape CLI / Affinity Designer Export Persona):
    ```
    rsvg-convert -w 1024 -h 1024 assets/icon/light.svg  -o gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png
    rsvg-convert -w 1024 -h 1024 assets/icon/dark.svg   -o gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png
    rsvg-convert -w 1024 -h 1024 assets/icon/tinted.svg -o gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png
    ```

    **If Path B (raster masters at `assets/icon/source/*.png`):**
    Copy directly (the masters are already 1024×1024):
    ```
    cp assets/icon/source/light.png  gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png
    cp assets/icon/source/dark.png   gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png
    cp assets/icon/source/tinted.png gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png
    ```

    **Do NOT touch `Contents.json`.** The P1 wiring (3 slots: universal-light + luminosity-dark + tinted) is already correct — see `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json`. Editing it risks breaking the appearance routing.

    **Do NOT touch `gamekit.xcodeproj/project.pbxproj`** (CLAUDE.md §8.8 — Xcode 16 synchronized root group auto-registers PNGs added to existing folders).

    **Verify Xcode preview** (manual sanity check — open `gamekit.xcodeproj` in Xcode briefly, click the AppIcon asset, confirm light/dark/tinted slots render the new arcade-machine art instead of the P1 placeholder). Capture a screenshot at `.planning/phases/07-release/screenshots/xcode-appicon-preview.png` so the diff is visually documented.

    **Finder-dupe scrub:** Run `find gamekit/gamekit/Assets.xcassets/AppIcon.appiconset -name "* 2.png"` to confirm no `icon-light 2.png` etc. landed (CLAUDE.md §8.7). If any appear, `rm` them before proceeding.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; for f in icon-light icon-dark icon-tinted; do test -f "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/${f}.png" || { echo "missing $f"; exit 1; }; sips -g pixelWidth -g pixelHeight "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/${f}.png" | grep -qE "pixelWidth: 1024" || { echo "$f wrong width"; exit 1; }; sips -g pixelWidth -g pixelHeight "gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/${f}.png" | grep -qE "pixelHeight: 1024" || { echo "$f wrong height"; exit 1; }; done &amp;&amp; ! find gamekit/gamekit/Assets.xcassets/AppIcon.appiconset -name "* 2.png" | grep . &amp;&amp; test -f .planning/phases/07-release/screenshots/xcode-appicon-preview.png &amp;&amp; ! git diff --name-only -- gamekit.xcodeproj/project.pbxproj | grep . &amp;&amp; ! git diff --name-only -- gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json | grep . &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 3 PNGs exist at the locked paths (verified above)
    - Each PNG is exactly 1024×1024 (verified above via `sips`)
    - No Finder-dupe `* 2.png` files in appiconset (CLAUDE.md §8.7): `! find gamekit/gamekit/Assets.xcassets/AppIcon.appiconset -name "* 2.png" | grep .`
    - `Contents.json` UNCHANGED (P1 wiring intact): `! git diff --name-only -- gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/Contents.json | grep .`
    - `project.pbxproj` UNCHANGED (no hand-patch per CLAUDE.md §8.8): `! git diff --name-only -- gamekit.xcodeproj/project.pbxproj | grep .`
    - Xcode preview screenshot captured at `.planning/phases/07-release/screenshots/xcode-appicon-preview.png`: see automated verify
    - Visual sanity (manual eye check): Xcode AppIcon asset shows arcade-machine renders in all 3 slots, NOT the FOUND-06 placeholder. (Recorded by the screenshot at the path above.)
  </acceptance_criteria>
  <done>Three 1024² arcade-machine PNGs in place, P1 Contents.json undisturbed, no pbxproj drift, Xcode preview confirms the swap.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit (CLAUDE.md §8.10) — `feat(07-02): real arcade-machine app icon (light/dark/tinted)`</name>
  <files>(no new file content — git commit step)</files>
  <read_first>
    - CLAUDE.md (§8.10 atomic commits — one feature per commit; never bundle large feature with unrelated fixes)
    - .planning/phases/07-release/07-CONTEXT.md (D-01..D-03; this commit closes the icon feature)
  </read_first>
  <action>
    Stage and commit the icon feature as a single atomic commit. Reject any accidental bundle.

    **Step 1 — Sanity check the working tree:**
    ```bash
    cd /Users/gabrielnielsen/Desktop/GameKit
    git status --short
    ```

    Expected files (and ONLY these — refuse to commit if other paths show):
    - `?? assets/icon/AI_PROVENANCE.md` (new)
    - `?? assets/icon/light.svg` OR `?? assets/icon/source/light.png` (depending on Path A vs B)
    - `?? assets/icon/dark.svg`  OR `?? assets/icon/source/dark.png`
    - `?? assets/icon/tinted.svg` OR `?? assets/icon/source/tinted.png`
    - ` M gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png`
    - ` M gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png`
    - ` M gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png`
    - `?? .planning/phases/07-release/screenshots/xcode-appicon-preview.png` (new)

    If `gamekit.xcodeproj/project.pbxproj` shows modified → STOP, surface to user (CLAUDE.md §8.8 violation).
    If `Contents.json` shows modified → STOP, surface to user (P1 wiring should be intact).
    If `* 2.png` or `* 2.svg` appear → STOP, surface to user (CLAUDE.md §8.7 violation).

    **Step 2 — Stage explicitly:**
    ```bash
    git add assets/icon/ \
            gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-light.png \
            gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-dark.png \
            gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-tinted.png \
            .planning/phases/07-release/screenshots/xcode-appicon-preview.png
    ```

    **Step 3 — Commit with HEREDOC (per CLAUDE.md commit examples):**

    ```
    feat(07-02): real arcade-machine app icon (light/dark/tinted)

    Replaces the P1 FOUND-06 placeholder with the AI-generated
    arcade-machine icon set per CONTEXT D-01..D-03. Same vintage
    cabinet retoned per appearance — light = warm cabinet, dark =
    neon-glow cabinet, tinted = monochrome silhouette.

    - assets/icon/  : <SVG vector | PNG raster> masters per Discretion #4
    - assets/icon/AI_PROVENANCE.md : AI service + iteration record
    - AppIcon.appiconset/icon-{light,dark,tinted}.png : 1024² PNG renders
      replacing P1 placeholders; P1 Contents.json (3-slot wiring) UNCHANGED
    - .planning/phases/07-release/screenshots/xcode-appicon-preview.png :
      visual diff confirmation

    No project.pbxproj change (CLAUDE.md §8.8 synchronized root group
    auto-registers PNGs in existing folders). No Finder-dupes (§8.7).

    Closes SC1-icon obligation; CloudKit Production schema deploy
    (07-03) and remaining SC1 verification land separately.

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    **Step 4 — Verify:**
    ```bash
    git log --oneline -1
    git status
    git show --stat HEAD --name-only --pretty=format:
    ```

    Expected: HEAD message starts with `feat(07-02): real arcade-machine app icon`. Working tree clean. Files in commit are confined to `assets/icon/`, `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-*.png`, and `.planning/phases/07-release/screenshots/xcode-appicon-preview.png`.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; git log --oneline -1 | grep -E "feat\(07-02\): real arcade-machine app icon" &amp;&amp; [ "$(git status --short | wc -l)" -eq 0 ] &amp;&amp; ! git show --stat HEAD --name-only --pretty=format: | grep -E "^gamekit\.xcodeproj/project\.pbxproj$" &amp;&amp; ! git show --stat HEAD --name-only --pretty=format: | grep -E "Contents\.json$" &amp;&amp; git show --stat HEAD --name-only --pretty=format: | grep -qE "AppIcon\.appiconset/icon-light\.png" &amp;&amp; git show --stat HEAD --name-only --pretty=format: | grep -qE "AppIcon\.appiconset/icon-dark\.png" &amp;&amp; git show --stat HEAD --name-only --pretty=format: | grep -qE "AppIcon\.appiconset/icon-tinted\.png" &amp;&amp; git log -1 --format=%B HEAD | grep -F "Co-Authored-By: Claude Opus 4.7" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `feat(07-02): real arcade-machine app icon`: see automated verify
    - Working tree clean post-commit: `git status --short` returns zero lines
    - Commit includes all 3 appiconset PNGs: see automated verify
    - Commit does NOT include `project.pbxproj` (CLAUDE.md §8.8): see automated verify
    - Commit does NOT include `Contents.json` (P1 wiring intact): see automated verify
    - Co-Authored-By trailer present: see automated verify
    - No Finder-dupe files in commit: `! git show --stat HEAD --name-only --pretty=format: | grep -E " 2\.(png|svg)$"`
  </acceptance_criteria>
  <done>Single atomic feat commit lands the real app icon; P1 Contents.json + pbxproj untouched; ready for parallel CloudKit deploy (07-03) and downstream metadata (07-04).</done>
</task>

</tasks>

<verification>
- 3 candidate AI renders generated and reviewed at the [BLOCKING] CHECKPOINT (Task 2)
- 3 master assets committed at `assets/icon/` (vector or raster per Discretion #4 — recorded in this plan + AI_PROVENANCE.md)
- 3 1024² PNGs committed at `gamekit/gamekit/Assets.xcassets/AppIcon.appiconset/icon-{light,dark,tinted}.png`
- P1 `Contents.json` UNCHANGED (3-slot wiring intact)
- `project.pbxproj` UNCHANGED (CLAUDE.md §8.8 — no hand-patch)
- No Finder-dupe files (CLAUDE.md §8.7 — no `* 2.png` / `* 2.svg`)
- `AI_PROVENANCE.md` records AI service + vector-vs-raster decision (Discretion #4 + #5)
- Xcode preview screenshot captured at `.planning/phases/07-release/screenshots/xcode-appicon-preview.png`
- Single atomic commit `feat(07-02): real arcade-machine app icon (light/dark/tinted)` per CLAUDE.md §8.10
</verification>

<success_criteria>
1. SC1-icon obligation closed: real arcade-machine icon ships in `Assets.xcassets`, replacing the P1 FOUND-06 placeholder.
2. All three variants depict the SAME arcade-machine subject retoned per appearance (D-02 lock); user explicitly approved at Task 2 [BLOCKING] CHECKPOINT.
3. Master path decision (vector vs raster, Discretion #4) recorded in `assets/icon/AI_PROVENANCE.md` so v1.0.x re-exports are reproducible.
4. AI service used (Discretion #5) recorded in `assets/icon/AI_PROVENANCE.md`.
5. Atomic commit per CLAUDE.md §8.10; commit identifies it as the icon feature tied to Plan 07-02 — does NOT bundle with schema deploy / metadata / verification work.
6. Xcode 16 synchronized root group auto-registered the new PNGs (no `project.pbxproj` change in commit).
7. Downstream plans (07-04 metadata uses these icons in screenshots; 07-06 TestFlight ships these icons in the binary) inherit the real icon.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release/07-02-SUMMARY.md` documenting:
- AI service chosen (Discretion #5) and iteration count
- Vector-vs-raster master decision (Discretion #4) with reasoning
- User approval timestamp from Task 2 [BLOCKING] CHECKPOINT
- File paths added: master `assets/icon/*` + 3 appiconset PNGs + AI_PROVENANCE.md + xcode preview screenshot
- Atomic commit hash
- Lessons learned: AI-icon-generation workflow worth promoting to project RETROSPECTIVE.md (e.g. "candidate → BLOCKING approval → master promotion → appiconset export → atomic commit" pattern is now established for any future icon refresh)
</output>
