---
phase: 07-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md
autonomous: true
requirements_addressed: []
tags:
  - doc-drift
  - cleanup
  - wave-1
  - p7-prep
must_haves:
  truths:
    - "ROADMAP.md plan-completion counts reflect ACTUAL state (P3 4/4 Complete, P4 6/6 Complete, P5 7/7 Complete, P6 9/9 Complete) per v1.0-MILESTONE-AUDIT.md"
    - "REQUIREMENTS.md traceability table shows every shipped requirement as Complete (or appropriate phase tag), no stale 'Pending' on already-verified work"
    - "06-VERIFICATION.md frontmatter status flips pending → complete; sign-off rows reflect 06-UAT.md result (6/6 SC pass, 2026-04-27)"
    - "STATE.md current_position advances to Phase 7; progress.completed_phases = 6"
    - "Single docs-only atomic commit per CLAUDE.md §8.10 — does NOT bundle with icon, schema, metadata, or any other P7 work"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Refreshed plan-completion counts for P3/P4/P5/P6"
      contains: "Phase 6 SIWA"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table reflects actual verified status"
      contains: "PERSIST-04"
    - path: ".planning/phases/06-cloudkit-siwa/06-VERIFICATION.md"
      provides: "Status flipped to complete; sign-off filled"
      contains: "status: complete"
    - path: ".planning/STATE.md"
      provides: "current_position advanced to Phase 7"
      contains: "Phase: 7"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/v1.0-MILESTONE-AUDIT.md"
      via: "audit findings drive ROADMAP refresh"
      pattern: "Phase \\d:.*Complete"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "current_position reads from latest in-progress phase"
      pattern: "Phase: 7"
---

<objective>
FIRST plan of Phase 7. Doc-drift cleanup per CONTEXT D-18 — refresh planning artifacts to match the actual project state captured in `v1.0-MILESTONE-AUDIT.md` (audited 2026-04-27). This plan ships ZERO code changes; everything modified lives under `.planning/`.

**Why this is plan 01 and not bundled:**
Per CLAUDE.md §8.10 (atomic commits — one feature or one grouped batch per commit), doc cleanup must NOT be bundled with the icon swap, the CloudKit schema deploy, the metadata authoring, or any other P7 plan. This plan ships as a single `docs(07-01): refresh planning artifacts after milestone audit` commit.

**Why first (not last):**
P7's subsequent plans (icon production, schema deploy, metadata, checklist, TestFlight sweep) reference current ROADMAP / STATE state to derive task verifications. If ROADMAP still shows P5 In progress and STATE still shows position "06-09 Task 1 complete; Task 2 BLOCKING," every downstream plan inherits stale context. Cleanup goes first.

**What ships:**
- ROADMAP.md plan-completion counts refreshed for P3/P4/P5/P6 per audit findings
- REQUIREMENTS.md traceability table flipped from Pending → Complete (or appropriate tag) for every shipped requirement
- 06-VERIFICATION.md frontmatter `status: pending` → `status: complete`; sign-off rows filled per 06-UAT.md (6/6 SC pass)
- STATE.md `current_position` advances from "06-09 Task 1 complete; Task 2 BLOCKING" to "Phase 7 in progress"; `progress.completed_phases` = 6
- Single atomic commit

**Out of scope for this plan:**
- New REQ-IDs (P7 has none — verifies cross-cutting invariants from earlier phases)
- ANY code change in `gamekit/`
- ROADMAP Phase 7 plan list (will be auto-populated by the planner workflow when this plan set lands)
- Creating `.planning/Docs/` (Discretion #10 — not creating an empty directory)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-cloudkit-siwa/06-VERIFICATION.md
@.planning/phases/07-release/07-CONTEXT.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts ↔ Reality | Doc-drift means ROADMAP/REQUIREMENTS/STATE no longer reflect what shipped. Downstream P7 plans read these as ground truth; stale truth = bad downstream decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-doc-drift | Repudiation / Tampering | Planning artifacts | mitigate | Source-of-truth = `v1.0-MILESTONE-AUDIT.md` (audited 2026-04-27, frozen at that timestamp). All edits in this plan trace back to specific audit findings. |
| T-07-01-bundled-commit | Integrity | git history | mitigate | CLAUDE.md §8.10 enforces atomic commits. Verification grep checks that the commit modifies only `.planning/*.md` paths — no `gamekit/` files. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh ROADMAP.md plan-completion counts and Phase 6 row</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — note Phase row table at lines 163-171 and Phase 5/6 plan checkboxes)
    - .planning/v1.0-MILESTONE-AUDIT.md (lines 18-24 tech_debt:roadmap items; lines 44-53 phase verification table)
    - .planning/phases/07-release/07-CONTEXT.md (D-18 verbatim spec lines 71-73)
    - CLAUDE.md (§8.10 atomic commits)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Make the following EXACT changes (these are the only changes — do not refactor unrelated text):

    **Change 1 — Phase Status Table at lines 163-171.** Replace the existing rows for Phase 3 / Phase 4 / Phase 5 / Phase 6 with the verified-actual values:

    ```
    | Phase | Plans Complete | Status | Completed |
    |-------|----------------|--------|-----------|
    | 1. Foundation | 8/8 | Complete | 2026-04-25 |
    | 2. Mines Engines | 6/6 | Complete | 2026-04-25 |
    | 3. Mines UI | 4/4 | Complete | 2026-04-25 |
    | 4. Stats & Persistence | 6/6 | Complete | 2026-04-26 |
    | 5. Polish | 7/7 | Complete | 2026-04-26 |
    | 6. CloudKit + Sign in with Apple | 9/9 | Complete | 2026-04-27 |
    | 7. Release | 0/6 | In progress | - |
    ```

    Source: v1.0-MILESTONE-AUDIT.md lines 44-53. The "0/6" for Phase 7 is this plan-set's own count (six plans 07-01..07-06 once all are written).

    **Change 2 — Phase 3 plans block (around line 79-83).** Each line item (`03-01-PLAN.md` through `03-04-PLAN.md`) has a `- [ ]` or `- [x]` checkbox. Per audit, all four are shipped. Verify they all read `- [x]`. If any reads `- [ ]`, flip to `- [x]`. (Audit shows 03-01, 03-02, 03-03 already `[x]`; 03-04 may still be `[ ]` — flip to `[x]`.)

    **Change 3 — Phase 4 plans block (around line 96-103).** Verify all six (`04-01-PLAN.md` through `04-06-PLAN.md`) read `- [x]`. Audit confirms all six shipped.

    **Change 4 — Phase 5 plans block (around line 114-121).** Verify all seven (`05-01-PLAN.md` through `05-07-PLAN.md`) read `- [x]`. Audit confirms.

    **Change 5 — Phase 6 plans block (around line 134-143).** Verify all nine (`06-01-PLAN.md` through `06-09-PLAN.md`) read `- [x]`. Note: `06-03-PLAN.md` is currently shown with `[~]` (in-progress symbol per audit) and `06-09-PLAN.md` is `[ ]`. Flip both to `[x]` per v1.0-MILESTONE-AUDIT.md "06-cloudkit-siwa: UAT pass / VERIFICATION.md template stale" (UAT = full sign-off; doc drift only). Remove any inline status comment like "[Tasks 1+2 done; Task 3 checkpoint:human-verify still pending]" on 06-03 since it no longer reflects state.

    **Change 6 — Phase 7 entry (around line 146-156).** Update only the `**Plans:** TBD` line to `**Plans:** 6 plans`. DO NOT add the plan list yet — the planner workflow auto-populates it when the planner finishes writing this plan set. Goal/Depends on/Requirements/SC1-SC5 stay BYTE-IDENTICAL.

    Save the file. Do NOT touch any other section (Overview, Cross-Cutting Invariants, Out-of-Scope Reminder, Research Flags).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -E "^\| 3\. Mines UI \| 4/4 \| Complete" .planning/ROADMAP.md && grep -E "^\| 4\. Stats &amp; Persistence \| 6/6 \| Complete" .planning/ROADMAP.md && grep -E "^\| 5\. Polish \| 7/7 \| Complete" .planning/ROADMAP.md && grep -E "^\| 6\. CloudKit \+ Sign in with Apple \| 9/9 \| Complete" .planning/ROADMAP.md && grep -E "^\| 7\. Release \| 0/6 \| In progress" .planning/ROADMAP.md && grep -E "^\*\*Plans:\*\* 6 plans" .planning/ROADMAP.md && ! grep -E "^- \[~\] 06-03-PLAN" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP Phase Status Table shows Phase 3 = 4/4 Complete, Phase 4 = 6/6 Complete, Phase 5 = 7/7 Complete, Phase 6 = 9/9 Complete, Phase 7 = 0/6 In progress
    - All Phase 3/4/5/6 plan rows show `- [x]` checkboxes (zero `- [ ]` or `- [~]` remain in those four blocks): `! grep -E "^- \[(\s|~)\] 0[3-6]-" .planning/ROADMAP.md`
    - Phase 7 entry has `**Plans:** 6 plans` (NOT `TBD`): `grep -F "**Plans:** 6 plans" .planning/ROADMAP.md`
    - Phase 7 SC1-SC5 text BYTE-IDENTICAL to current (no accidental edits): `grep -F "Real app icon (replacing the placeholder from FOUND-06) ships in" .planning/ROADMAP.md && grep -F "Privacy nutrition label is answered \"Data Not Collected\"" .planning/ROADMAP.md`
    - Phase 7 plan list NOT yet populated (planner workflow handles that): `! grep -E "^- \[ \] 07-0[1-6]-PLAN" .planning/ROADMAP.md`
    - No accidental edits to Cross-Cutting Invariants section: `grep -F "Bundle ID stability:" .planning/ROADMAP.md`
  </acceptance_criteria>
  <done>ROADMAP.md reflects actual state per v1.0-MILESTONE-AUDIT.md; ready for downstream P7 plans to read as ground truth.</done>
</task>

<task type="auto">
  <name>Task 2: Sync REQUIREMENTS.md traceability table to actual verified state</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — checkbox list lines 12-92 and traceability table lines 169-205)
    - .planning/v1.0-MILESTONE-AUDIT.md (Requirements Coverage section, lines 56-115 — the source of truth for which REQ-IDs are satisfied)
    - .planning/phases/07-release/07-CONTEXT.md (D-18 — REQUIREMENTS sync is part of doc-drift cleanup)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Two sections need updating:

    **Section A — Top-of-file checkbox list (lines 12-92).** Per v1.0-MILESTONE-AUDIT.md, all 35 v1 requirements are `satisfied`. Verify every requirement reads `- [x]`. Audit lines 56-115 enumerate the complete list. Specific items the audit flagged as still showing `Pending` in the table (line 21) include: FOUND-01..07, SHELL-01, MINES-02/05/06/07/11, PERSIST-02/03, THEME-01/02/03, A11Y-03/04. Check each and flip to `- [x]` if not already.

    The only requirement that may legitimately stay nuanced is MINES-10 (audit line 99): "satisfied — SFX gating verified; audible playback deferred (G-1, silent fallback acceptable per D-12)." Mark `- [x]` on the checkbox; keep any descriptive text intact. Audit treats this as satisfied per spec (silent fallback IS the contract per P5 D-12).

    **Section B — Traceability table (lines 169-205).** The Status column for each requirement currently mostly reads `Pending` even though the work has shipped. Update each row's Status column to one of:
    - `Complete (NN-MM)` where NN is the phase number and MM is the plan number (e.g. `Complete (06-08)` for PERSIST-04)
    - `Complete (NN)` if the audit credits the entire phase rather than a specific plan (e.g. `Complete (01)` for FOUND-01..07 + SHELL-01)
    - `Complete (05, G-1 deferred)` for MINES-10 — preserves the audit's nuance

    Use audit lines 56-115 as the source of truth. If the audit doesn't pin a specific plan, use the phase tag.

    Save the file. Do NOT alter:
    - The v2 Requirements section (lines 94-141)
    - The Out of Scope table (lines 143-163)
    - The Coverage / Notes-on-placement footer (lines 207-219)
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && [ "$(grep -cE '^- \[ \] \*\*' .planning/REQUIREMENTS.md)" -eq 0 ] &amp;&amp; [ "$(grep -cE '\| Pending \|' .planning/REQUIREMENTS.md)" -eq 0 ] &amp;&amp; grep -F "PERSIST-04 | Phase 6 | Complete (06-08)" .planning/REQUIREMENTS.md &amp;&amp; grep -F "FOUND-01 | Phase 1 | Complete" .planning/REQUIREMENTS.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero unchecked v1 requirements remain in checkbox list: `! grep -E "^- \[ \] \*\*" .planning/REQUIREMENTS.md` (the v2 list uses different formatting and is excluded)
    - Zero `Pending` rows in traceability table: `! grep -E "\| Pending \|" .planning/REQUIREMENTS.md`
    - All 35 v1 requirements appear in traceability table with non-empty Status: `[ "$(grep -cE "Complete \(0[1-6]" .planning/REQUIREMENTS.md)" -ge 35 ]`
    - Specific spot-checks (sample five rows from audit lines 56-115):
      - `grep -E "FOUND-01 \| Phase 1 \| Complete" .planning/REQUIREMENTS.md`
      - `grep -E "MINES-10 \| Phase 5 \| Complete \(05" .planning/REQUIREMENTS.md`
      - `grep -E "PERSIST-04 \| Phase 6 \| Complete \(06-08\)" .planning/REQUIREMENTS.md`
      - `grep -E "PERSIST-05 \| Phase 6 \| Complete \(06-08\)" .planning/REQUIREMENTS.md`
      - `grep -E "PERSIST-06 \| Phase 6 \| Complete" .planning/REQUIREMENTS.md`
    - v2 Requirements section preserved: `grep -F "## v2 Requirements" .planning/REQUIREMENTS.md`
    - Out of Scope table preserved: `grep -F "Banner / interstitial / video ads" .planning/REQUIREMENTS.md`
    - Coverage line still reads 35/35: `grep -E "Mapped to phases: 35" .planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>REQUIREMENTS.md traceability reflects actual verified status across all 35 v1 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Flip 06-VERIFICATION.md status to complete and fill sign-off rows</name>
  <files>.planning/phases/06-cloudkit-siwa/06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md (current frontmatter status: pending; all SC checkboxes unchecked despite UAT showing pass)
    - .planning/v1.0-MILESTONE-AUDIT.md (line 16-17: "06-VERIFICATION.md still in template state... 06-UAT.md shows 6/6 SC pass and 06-VALIDATION.md is signed off 2026-04-27")
    - .planning/phases/06-cloudkit-siwa/06-UAT.md (the actual UAT result — read enough to confirm 6/6 SC pass; do NOT read past first 100 lines)
    - .planning/phases/07-release/07-CONTEXT.md (D-18 — verification status flip is part of doc-drift cleanup)
  </read_first>
  <action>
    Edit `.planning/phases/06-cloudkit-siwa/06-VERIFICATION.md`. This is a doc-only refresh — flip the frontmatter and fill the sign-off table to match the already-completed UAT outcome.

    **Frontmatter changes:**
    - `status: pending  # ...` → `status: complete  # signed off via UAT.md 2026-04-27`
    - `signed_off_by: ""` → `signed_off_by: "User (UAT — see 06-UAT.md)"`
    - `signed_off_date: ""` → `signed_off_date: "2026-04-27"`
    - `fallback_used:` line: leave as-is unless 06-UAT.md explicitly calls out 2-sim fallback (read 06-UAT.md to confirm; if not mentioned, leave the field empty since the original test was via 06-UAT not via the template).

    **Inline note at top of file (immediately after the `# Phase 6 — Manual SC1-SC5 Verification Checklist` heading):**

    Add this paragraph as a single block:

    ```
    > **Status note (added 2026-04-27 during P7 doc-drift cleanup per 07-01-PLAN):**
    > This template was authored by Plan 06-09 Task 1. The actual SC1-SC5 sweep was
    > performed against `06-UAT.md` (see that document for full evidence and 6/6 SC
    > pass details) and signed off in `06-VALIDATION.md` on 2026-04-27. Rather than
    > re-run the sweep against this template's evidence fields, the sign-off rows
    > below have been marked PASS with a pointer to 06-UAT.md as the canonical
    > evidence source. The unchecked checkboxes throughout the SC1-SC5 sections
    > remain unchecked because the canonical evidence lives in 06-UAT.md, not here.
    ```

    **Sign-off table (the table near the end of the file, with rows for SC1-SC5).** For each of the 5 rows, fill the columns as:
    - Verifier: `User (UAT)`
    - Date: `2026-04-27`
    - Status: replace `☐ PASS / ☐ FAIL / ☐ DEFERRED` with `✓ PASS (per 06-UAT.md)`

    **Phase-close criteria checkboxes (just below the sign-off table):**
    - "All 5 SCs PASS or DEFERRED-WITH-REASON-DOCUMENTED" → flip to `[x]`
    - "No Critical gaps open" → flip to `[x]`
    - "Major gaps converted..." → flip to `[x]` if no gaps; else leave `[ ]` and note gap-closure-plan ID. (Audit found no critical gaps; mark `[x]`.)

    **Phase-Close Updates section (very end of file):**
    - "Update .planning/ROADMAP.md..." → flip to `[x]` (this plan's Task 1 handles it)
    - "Update .planning/STATE.md..." → flip to `[x]` (this plan's Task 4 handles it)
    - "Atomic commit..." → leave `[ ]` (this plan's eventual commit will tick it; the executor's commit step is the trigger)
    - "Optional: REQUIREMENTS.md..." → flip to `[x]` (this plan's Task 2 handles it)

    Do NOT modify the per-SC test instructions (SC1-SC5 step-by-step) — those remain as authored by Plan 06-09. Only the frontmatter, the inline status note, the sign-off table, and the close-criteria checklists change.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; grep -E "^status: complete" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md &amp;&amp; grep -E '^signed_off_date: "2026-04-27"' .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md &amp;&amp; grep -F "✓ PASS (per 06-UAT.md)" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md &amp;&amp; grep -F "P7 doc-drift cleanup per 07-01-PLAN" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md &amp;&amp; [ "$(grep -c '✓ PASS (per 06-UAT.md)' .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md)" -ge 5 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status` flipped to `complete`: `grep -E "^status: complete" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md`
    - `signed_off_by` populated: `grep -E '^signed_off_by: "User \(UAT' .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md`
    - `signed_off_date: "2026-04-27"`: see automated verify
    - Inline status note references 07-01-PLAN: see automated verify
    - All 5 sign-off rows show `✓ PASS (per 06-UAT.md)`: at least 5 occurrences (automated)
    - Per-SC test instructions BYTE-IDENTICAL preserved (sample): `grep -F "Restart to enable iCloud sync" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md && grep -F "kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly" .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md`
    - Phase-close ROADMAP/STATE/REQUIREMENTS checkboxes flipped to `[x]`: `[ "$(grep -E '^- \[x\] Update `\.planning/(ROADMAP|STATE)\.md`|^- \[x\] Optional: update `\.planning/REQUIREMENTS\.md`' .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md | wc -l)" -ge 3 ]`
  </acceptance_criteria>
  <done>06-VERIFICATION.md status accurately reflects the completed UAT; doc-drift item from audit closed.</done>
</task>

<task type="auto">
  <name>Task 4: Advance STATE.md current_position to Phase 7</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — note position frozen at "06-09 Task 1 complete; Task 2 BLOCKING")
    - .planning/v1.0-MILESTONE-AUDIT.md (lines 22-24: STATE.md tech_debt note)
    - .planning/phases/07-release/07-CONTEXT.md (D-18 — STATE.md advance is part of doc-drift cleanup)
    - CLAUDE.md (no direct rule, but §8.10 atomic commits constrains this to be docs-only)
  </read_first>
  <action>
    Edit `.planning/STATE.md`. Surgical changes only — do not refactor.

    **Frontmatter (lines 1-15):**
    - `status: completed` — leave as-is (the v1.0 milestone IS still in progress; the audit shows P7 not started; "completed" here refers to the milestone-tracker's wider semantics, leave intact unless context shows otherwise from other gsd commands)
    - `stopped_at: Phase 7 context gathered` — already correct per current state, leave intact
    - `last_updated:` → bump to today's ISO 8601 timestamp (use `2026-04-27T<HH:MM:SS>.000Z` matching the format already in file; the executor sets the actual time at commit)
    - `last_activity: 2026-04-27` — leave (today)
    - `progress.completed_phases:` → `6` (was `6`, leave; audit confirms 6/7 phases complete)
    - `progress.percent:` → leave at `100` if already 100, OR set to `86` (= 6/7 phases) if more accurate. Per the existing state file (line 14: `percent: 100`), leave unchanged unless the gsd-state semantics demand otherwise. Default: leave.

    **Body — Current Position section (lines 26-31):**

    Replace the existing 5-line block (Phase / Plan / Status / Last activity / Progress) with:

    ```
    Phase: 7
    Plan: 01 — doc-drift cleanup (D-18) IN PROGRESS; remaining P7 plans 02-06 queued (icon, schema deploy, metadata+privacy+screenshots, checklist+verification template, TestFlight+SC1-SC5 sweep). Phase 6 closed 2026-04-27 per v1.0-MILESTONE-AUDIT.md (6/9 plans + UAT 6/6 SC pass; 06-VERIFICATION.md doc-drift resolved by this plan's Task 3).
    Status: P7 wave 1 in progress
    Last activity: 2026-04-27

    Progress: [██████████] 100%
    ```

    (The progress bar is the existing v1.0-milestone bar; if percent stayed at 100, keep the visual. If it was set to 86, change the bar accordingly: `[████████▌ ] 86%`.)

    **Body — Decisions list (lines 96-239):**

    Append a single new bullet at the END of the existing decisions list (just before the `### Pending Todos` heading):

    `- 07-01: P7 doc-drift cleanup — refreshed ROADMAP.md plan-completion counts (P3 4/4, P4 6/6, P5 7/7, P6 9/9), synced REQUIREMENTS.md traceability (35/35 Complete), flipped 06-VERIFICATION.md status pending → complete with sign-off rows pointing to 06-UAT.md as canonical evidence per v1.0-MILESTONE-AUDIT.md, advanced STATE.md current_position to Phase 7. Single docs-only commit per CLAUDE.md §8.10. ZERO code changes.`

    **Body — Pending Todos section (lines 241-244):**

    Replace the entire block (the bullet referencing "06-03 Task 3 (checkpoint:human-verify) — BLOCKING for Plan 06-09 SC3") with:

    ```
    - **P7 work queued**: Plans 07-02 (icon production), 07-03 (CloudKit Production schema deploy), 07-04 (App Store metadata + privacy policy + screenshots), 07-05 (release checklist + 07-VERIFICATION.md template), 07-06 (TestFlight upload + SC1-SC5 manual sweep + submit). See `.planning/phases/07-release/` for plan files.
    ```

    **Body — Blockers/Concerns (lines 246-248):**

    Replace the entire block (the two bullets about Plan 06-03 paused and SKIP_OBSERVER_TESTS resolved) with:

    ```
    - *(no active blockers — Phase 7 ready to execute. Earlier P6 06-03 Task 3 checkpoint and 06-09 BLOCKING checkpoint were resolved via the manual UAT recorded in 06-UAT.md and the doc-drift sweep in this plan.)*
    ```

    **Session Continuity (lines 258-264):**

    Update `Stopped at:` to `P7 doc-drift cleanup landing` and bump the `Planned Phase:` line:

    ```
    Last session: --stopped-at
    Stopped at: P7 doc-drift cleanup landing
    Resume file: --resume-file

    **Planned Phase:** 7 (release) — 6 plans — 2026-04-27T<HH:MM:SS>.000Z
    ```

    Do NOT touch the Performance Metrics tables (lines 36-93) — those are auto-generated by gsd telemetry; manual edits there will be overwritten on the next plan completion.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; grep -E "^Phase: 7$" .planning/STATE.md &amp;&amp; grep -E "^Plan: 01 — doc-drift cleanup" .planning/STATE.md &amp;&amp; grep -F "07-01: P7 doc-drift cleanup" .planning/STATE.md &amp;&amp; grep -F "**Planned Phase:** 7 (release) — 6 plans" .planning/STATE.md &amp;&amp; ! grep -F "06-09 Task 1 complete" .planning/STATE.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `Phase: 7` in current position: `grep -E "^Phase: 7$" .planning/STATE.md`
    - Plan line names doc-drift cleanup (D-18): `grep -E "^Plan: 01 — doc-drift" .planning/STATE.md`
    - 07-01 decision bullet appended: `grep -F "07-01: P7 doc-drift cleanup" .planning/STATE.md`
    - Pending Todos block now lists P7 plans 02-06 (not the old 06-03 Task 3 todo): `grep -F "Plans 07-02 (icon production)" .planning/STATE.md && ! grep -F "06-03 Task 3 (checkpoint:human-verify)" .planning/STATE.md`
    - Blockers/Concerns reset to "no active blockers": `grep -F "no active blockers" .planning/STATE.md`
    - Planned Phase advanced to 7: `grep -F "**Planned Phase:** 7 (release) — 6 plans" .planning/STATE.md`
    - Old in-progress note removed: `! grep -F "06-09 Task 1 complete" .planning/STATE.md`
    - Performance Metrics block byte-preserved (sample): `grep -F "| Phase 06-cloudkit-siwa P08 | 13 | 2 tasks | 1 files |" .planning/STATE.md`
  </acceptance_criteria>
  <done>STATE.md advanced to Phase 7, doc-drift visible at the top-of-file glance is gone.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit (CLAUDE.md §8.10) — docs-only, no code changes</name>
  <files>(no new file content — git commit step)</files>
  <read_first>
    - CLAUDE.md (§8.10 atomic commits — one feature or one grouped batch per commit; never bundle large feature with unrelated fixes)
    - .planning/phases/07-release/07-CONTEXT.md (D-18 — single docs-only commit at start of phase)
  </read_first>
  <action>
    Stage and commit ONLY the four files this plan modified. Reject any accidental bundle.

    Step 1 — Sanity check that no `gamekit/` files leaked into the working tree:

    ```bash
    cd /Users/gabrielnielsen/Desktop/GameKit
    git status --short
    ```

    Expected: ONLY four lines, all under `.planning/`:
    - ` M .planning/ROADMAP.md`
    - ` M .planning/REQUIREMENTS.md`
    - ` M .planning/STATE.md`
    - ` M .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md`

    If any `gamekit/` path or any other unrelated path appears, STOP — surface to the user; do NOT bundle.

    Step 2 — Stage explicitly (per CLAUDE.md commit-discipline: never `git add -A` / `git add .`):

    ```bash
    git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/phases/06-cloudkit-siwa/06-VERIFICATION.md
    ```

    Step 3 — Commit with the exact message below (HEREDOC per CLAUDE.md commit examples):

    ```
    docs(07-01): refresh planning artifacts after milestone audit

    Aligns ROADMAP.md / REQUIREMENTS.md / STATE.md / 06-VERIFICATION.md
    with the v1.0-MILESTONE-AUDIT.md (audited 2026-04-27).

    - ROADMAP.md: P3 4/4 / P4 6/6 / P5 7/7 / P6 9/9 Complete; P7 = 6 plans In progress
    - REQUIREMENTS.md: 35/35 v1 requirements traced as Complete (or appropriate phase tag)
    - 06-VERIFICATION.md: status pending → complete; sign-off rows point to 06-UAT.md as canonical evidence
    - STATE.md: current_position advances to Phase 7; pending todos reset; blockers cleared

    Per CONTEXT D-18 (first plan of P7 = doc-drift cleanup, single docs-only commit
    before icon/schema/metadata work). Zero code changes.

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    Step 4 — Verify success:

    ```bash
    git log --oneline -1
    git status
    ```

    Expected: HEAD commit message starts with `docs(07-01): refresh planning artifacts`. Working tree clean.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit &amp;&amp; git log --oneline -1 | grep -E "docs\(07-01\): refresh planning artifacts" &amp;&amp; [ "$(git status --short | wc -l)" -eq 0 ] &amp;&amp; [ "$(git show --stat HEAD --name-only --pretty=format: | grep -v '^$' | grep -vE '^\.planning/' | wc -l)" -eq 0 ] &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit message starts with `docs(07-01): refresh planning artifacts`: see automated verify
    - Working tree clean post-commit: `git status --short` returns zero lines
    - Commit touched ONLY `.planning/` paths (no `gamekit/` leak): `git show --stat HEAD --name-only --pretty=format: | grep -v '^.planning/' | grep -v '^$'` returns zero lines
    - Commit touched exactly 4 files: `[ "$(git show --stat HEAD --name-only --pretty=format: | grep -cE '^\.planning/')" -eq 4 ]`
    - Co-Authored-By trailer present: `git log -1 --format=%B HEAD | grep -F "Co-Authored-By: Claude Opus 4.7"`
  </acceptance_criteria>
  <done>Single atomic docs-only commit landed per CLAUDE.md §8.10. P7 plan 01 closed; downstream plans (07-02..07-06) can now read ROADMAP/STATE as ground truth.</done>
</task>

</tasks>

<verification>
- ROADMAP.md plan-completion counts reflect actual state (P3 4/4, P4 6/6, P5 7/7, P6 9/9 Complete; P7 0/6 In progress)
- REQUIREMENTS.md traceability table shows zero `Pending` rows; all 35 v1 requirements traced as Complete (or appropriate phase tag)
- 06-VERIFICATION.md frontmatter status: complete; sign-off rows show ✓ PASS (per 06-UAT.md) on all 5 SCs
- STATE.md current_position = Phase 7; pending todos reflect P7 plans 02-06; blockers cleared
- Single atomic commit `docs(07-01): refresh planning artifacts after milestone audit` landed
- Zero `gamekit/` files modified
</verification>

<success_criteria>
1. All four planning files (ROADMAP.md / REQUIREMENTS.md / STATE.md / 06-VERIFICATION.md) reflect post-audit reality.
2. Doc-drift items D-1, D-2, D-3 from v1.0-MILESTONE-AUDIT.md tech_debt list are closed.
3. Single atomic commit per CLAUDE.md §8.10; commit message identifies it as doc-drift cleanup tied to D-18.
4. Downstream P7 plans (02-06) can now read ROADMAP/REQUIREMENTS/STATE as ground truth without inheriting stale context.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release/07-01-SUMMARY.md` documenting:
- 4 files refreshed (ROADMAP, REQUIREMENTS, STATE, 06-VERIFICATION)
- Atomic commit hash
- Audit items closed (D-1 doc-drift CAF deferred is NOT closed by this plan — it's tech debt punted to v1.0.1; only D-2/D-3 from the audit's `roadmap` and `state` blocks are closed here)
- Lessons learned: any drift-prevention pattern worth promoting to project RETROSPECTIVE.md (e.g. "audit → cleanup-as-first-plan-of-next-phase" pattern is now established for future milestones)
</output>
