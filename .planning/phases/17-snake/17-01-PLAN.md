---
phase: 17-snake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Games/Snake/SnakeEngine.swift
  - gamekit/gamekit/Games/Snake/SnakeConfig.swift
  - gamekit/gamekitTests/Games/Snake/SnakeEngineTests.swift
autonomous: true
requirements: [SNAKE-01, SNAKE-02, SNAKE-04]
must_haves:
  truths:
    - "The snake advances one cell per tickInterval, growing on food and dying on self-collision"
    - "Wrap mode (default) re-enters the opposite edge; wall mode ends the run on edge contact"
    - "The same pinned seed produces identical food-spawn sequences, grow events, and collision outcomes across two runs"
    - "dt=1/60 and dt=1/120 over 5 simulated seconds produce the same cell-move count and collision state"
    - "tickInterval ramps down with score and floors at minTickInterval (>= 100ms)"
  artifacts:
    - path: "gamekit/gamekit/Games/Snake/SnakeEngine.swift"
      provides: "Pure Foundation-only grid engine: step(dt:nextDirection:)->SnakeFrame"
      contains: "struct SnakeEngine"
    - path: "gamekit/gamekit/Games/Snake/SnakeConfig.swift"
      provides: "Tuning constants: default + testFixed"
      contains: "struct SnakeConfig"
    - path: "gamekit/gamekitTests/Games/Snake/SnakeEngineTests.swift"
      provides: "Determinism + ProMotion + wrap/wall/self-collision tests"
      contains: "SnakeEngineTests"
  key_links:
    - from: "SnakeEngine.step"
      to: "SnakeFrame.cellMoveAlpha"
      via: "min(cellAccumulator / tickInterval, 1.0)"
      pattern: "cellMoveAlpha"
    - from: "SnakeEngine.spawnFood"
      to: "injected rng"
      via: "Int.random(in:using:&rng)"
      pattern: "using: &rng"
---

<objective>
Build `SnakeEngine` — the pure, Foundation-only, seeded, deterministic grid engine — plus its `SnakeConfig` tuning struct and the determinism/ProMotion/collision test suite that gates the phase.

Purpose: This is the phase foundation. Every downstream plan (VM, Canvas, GameView) depends on the `SnakeCell` / `SnakeDirection` / `SnakeEvent` / `SnakeFrame` types and the engine contract defined here. The determinism tests are the Phase 17 SC2 gate.
Output: `SnakeEngine.swift`, `SnakeConfig.swift`, `SnakeEngineTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-RESEARCH.md
@.planning/phases/17-snake/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SnakeConfig tuning struct</name>
  <files>gamekit/gamekit/Games/Snake/SnakeConfig.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackConfig.swift (exact analog — file shape, header comment, default + testFixed statics)
    - .planning/phases/17-snake/17-RESEARCH.md §Pattern 2 (speed-ramp constants) + §Tuning Constants table
    - .planning/phases/17-snake/17-PATTERNS.md §SnakeConfig.swift section
  </read_first>
  <behavior>
    - `SnakeConfig.default` fields resolve to the RESEARCH baseline values (cols 20, rows 32, wrap default, startTickInterval 0.200, minTickInterval 0.100, intervalDecrement 0.002, fixedDt 1.0/60.0).
    - `SnakeConfig.testFixed` uses a small stable grid (10x10) decoupled from device-calibration values so unit tests are deterministic regardless of default tuning changes.
    - `wallMode` is a mutable `var` (tests flip it; VM flips it) with default `false` (wrap).
  </behavior>
  <action>
    Create `SnakeConfig` as a `nonisolated struct SnakeConfig: Sendable`, Foundation-only, mirroring `StackConfig`'s file shape. Fields: `cols: Int`, `rows: Int`, `var wallMode: Bool`, `startTickInterval: Double`, `minTickInterval: Double`, `intervalDecrement: Double`, `startLength: Int`, `fixedDt: Double`. Provide two static constants — `default` (cols 20, rows 32, wallMode false, startTickInterval 0.200, minTickInterval 0.100, intervalDecrement 0.002, startLength 3, fixedDt 1.0/60.0) and `testFixed` (cols 10, rows 10, wallMode false, same time constants, startLength 3, fixedDt 1.0/60.0). Add a header comment noting: no dt clamp here (that lives in ArcadeLoopDriver only, ARCADE-02), Foundation only, grid/ramp values are MEDIUM-confidence device-calibration baselines. Do NOT import SwiftUI/UIKit/DesignKit.
  </action>
  <verify>
    <automated>grep -c "import SwiftUI\|import UIKit\|import DesignKit" gamekit/gamekit/Games/Snake/SnakeConfig.swift | grep -qx 0 && echo PURE_OK</automated>
  </verify>
  <done>SnakeConfig.swift exists with `default` and `testFixed` statics; no SwiftUI/UIKit/DesignKit imports; minTickInterval == 0.100.</done>
  <acceptance_criteria>
    - Source contains `nonisolated struct SnakeConfig: Sendable`.
    - Source contains both `static let \`default\`` and `static let testFixed`.
    - `grep "import" SnakeConfig.swift` shows only `import Foundation`.
    - `minTickInterval` literal is `0.100` (SNAKE-04 plateau floor, CONTEXT-locked ">=100ms tick").
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SnakeEngine value types + step logic</name>
  <files>gamekit/gamekit/Games/Snake/SnakeEngine.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackEngine.swift (exact analog — nonisolated struct, guard-on-gameOver, private frame() helper, seconds-based ramp)
    - .planning/phases/17-snake/17-RESEARCH.md §Pattern 1 (full engine reference), §Pattern 2 (ramp), §Common Pitfalls 3/4
    - .planning/phases/17-snake/17-PATTERNS.md §SnakeEngine.swift section + §Shared Patterns 7 (engine purity)
    - gamekitTests/Helpers/SeededGenerator.swift (test PRNG the engine's rng seam accepts)
  </read_first>
  <behavior>
    - `step(dt:nextDirection:)` returns `.none` immediately when `gameOver` is already true (post-death no-op).
    - Below tickInterval accumulation, `step` only advances `cellAccumulator` and returns `.none` with an updated `cellMoveAlpha` in [0,1].
    - On a cell-move tick: applies `nextDirection` only if it is not the opposite of `currentDirection` (double-guard); moves the head one cell; wrap mode wraps toroidally, wall mode sets `gameOver` on out-of-bounds.
    - Self-collision is tested against the body EXCLUDING the tail cell (the tail vacates on a non-eating move).
    - On landing on food: head inserts, tail stays (grow), `score += 1`, new food spawns via injected rng, event `.ate`.
    - `tickInterval` after each cell move = `max(cfg.minTickInterval, cfg.startTickInterval - Double(score) * cfg.intervalDecrement)`.
  </behavior>
  <action>
    Create `SnakeEngine.swift`, Foundation-only, mirroring `StackEngine`'s purity discipline. Declare `nonisolated struct SnakeCell: Hashable` (col/row Int), `nonisolated enum SnakeDirection: Equatable, Sendable` with a computed `opposite`, `nonisolated enum SnakeEvent: Equatable` (`none` / `ate(food: SnakeCell)` / `died`), and `nonisolated struct SnakeFrame: Equatable` carrying `body`, `prevBody`, `food`, `currentDirection`, `score`, `cellMoveAlpha`, `gameOver`, `event`. Declare `struct SnakeEngine` with `private(set)` state (`body`, `prevBody`, `food`, `currentDirection` default `.right`, `score`, `gameOver`), private `cellAccumulator`, private `tickInterval`, and `private var rng: any RandomNumberGenerator`. `init(cfg:rng:)` takes `some RandomNumberGenerator = SystemRandomNumberGenerator()`, seeds the 3-cell starting body at vertical center moving right per `cfg.startLength`, sets `prevBody = body`, `tickInterval = cfg.startTickInterval`, then calls `spawnFood()`. Implement `step`, private `spawnFood()` (filter occupied cells, `Int.random(in: 0..<candidates.count, using: &rng)`), and private `frame(event:)` exactly per RESEARCH §Pattern 1. Snapshot `prevBody = body` at the start of each cell move (Gaffer anchor). Do NOT import SwiftUI/UIKit/DesignKit; use `Int` for coordinates and `Double` for time — no `CGPoint`/`CGFloat`/`Date.now`. Do NOT add a second dt clamp.
  </action>
  <verify>
    <automated>grep -Ec "import SwiftUI|import UIKit|CGFloat|CGPoint|Date\.now|modelContext" gamekit/gamekit/Games/Snake/SnakeEngine.swift | grep -qx 0 && echo PURITY_OK</automated>
  </verify>
  <done>SnakeEngine.swift compiles; purity grep returns empty; step() reproduces the RESEARCH §Pattern 1 contract; types SnakeCell/SnakeDirection/SnakeEvent/SnakeFrame are exported for downstream plans.</done>
  <acceptance_criteria>
    - `grep -E "import SwiftUI|import UIKit|CGFloat|CGPoint|Date\.now|modelContext" SnakeEngine.swift` returns empty.
    - Source contains `struct SnakeEngine`, `struct SnakeFrame`, `enum SnakeDirection`, `enum SnakeEvent`, `struct SnakeCell`.
    - Source contains `using: &rng` in spawnFood (seeded injection).
    - Source contains `max(cfg.minTickInterval,` (ramp floor).
    - Self-collision check operates on `body.dropLast()` (tail-exclusion).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SnakeEngineTests — determinism, ProMotion, wrap/wall/self-collision</name>
  <files>gamekit/gamekitTests/Games/Snake/SnakeEngineTests.swift</files>
  <read_first>
    - gamekitTests/Games/Stack/StackEngineTests.swift (exact analog — @Suite nonisolated struct, proMotionEquivalence shape, post-gameOver no-op assertion)
    - gamekitTests/Helpers/SeededGenerator.swift (SplitMix64 seed injection)
    - .planning/phases/17-snake/17-RESEARCH.md §Code Examples (SC2 test bodies: seedDeterminism, proMotionEquivalence, wallCollision, toroidalWrap, selfCollision)
    - .planning/phases/17-snake/17-VALIDATION.md §Per-Task Verification Map (SC2 row)
  </read_first>
  <behavior>
    - `seedDeterminism`: two engines with `SeededGenerator(seed: 42)` fed an identical direction sequence over ~10s produce equal `food`, `body`, `gameOver` at every step and equal final `score`.
    - `proMotionEquivalence`: a straight run (nextDirection nil) for 5 simulated seconds at dt=1/60 and dt=1/120 (same seed 99) yields equal `score` and `gameOver` — the SC2 gate.
    - `wallCollision`: with `wallMode = true` on a 5x5 grid, stepping `.right` repeatedly reaches `gameOver`.
    - `toroidalWrap`: with `wallMode = false` on a 10x10 grid, the head transitions from `col == cols-1` to `col == 0` without dying.
    - `selfCollision`: driving the snake into a U-turn back onto itself sets `gameOver`.
  </behavior>
  <action>
    Create `SnakeEngineTests.swift` as a `@Suite("SnakeEngine determinism") nonisolated struct SnakeEngineTests` with `@testable import gamekit`, `import Testing`, `import Foundation`. Implement the five `@Test` functions per RESEARCH §Code Examples using `SnakeConfig.testFixed` (flip `wallMode`/`cols`/`rows` on local copies as the test bodies require). Inject `SeededGenerator(seed:)` into `SnakeEngine(cfg:rng:)`. Assert with `#expect`. The file auto-registers under the `gamekitTests` target via PBXFileSystemSynchronizedRootGroup (CLAUDE.md §8.8) — do NOT edit project.pbxproj. If `Int.random(in:using:)` proves flaky across the two seeded runs (Assumption A3), pin the food-spawn assertion on `body`/`gameOver`/`score` equality rather than exact `food` cell equality and note it in the summary.
  </action>
  <verify>
    <automated>xcodebuild test -project gamekit/gamekit.xcodeproj -scheme gamekit -only-testing:gamekitTests/SnakeEngineTests -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>All five SnakeEngineTests pass; SC2 (seedDeterminism + proMotionEquivalence) is green.</done>
  <acceptance_criteria>
    - `xcodebuild test ... -only-testing:gamekitTests/SnakeEngineTests` exits 0 with all tests passing.
    - Suite contains `seedDeterminism`, `proMotionEquivalence`, `wallCollision`, `toroidalWrap`, `selfCollision`.
    - proMotionEquivalence asserts equal score AND gameOver across dt=1/60 vs dt=1/120.
    - No `project.pbxproj` change in `git status`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture/VM → engine | Direction input crosses into pure engine as a 4-value enum |

Offline, local-only, single-player. No network, no auth, no external input parsing beyond a direction enum.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | SnakeEngine direction input | mitigate | Direction is a closed 4-value enum; 180° reversal double-guarded inside step() (Task 2) |
| T-17-02 | Denial of Service (self) | frame-rate divergence | mitigate | Seconds-based tickInterval + ProMotion equivalence test (Task 3); no second dt clamp |
| T-17-SC | Tampering | package installs | accept | No packages installed this phase (first-party Swift only); audit N/A per RESEARCH §Package Legitimacy |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:gamekitTests/SnakeEngineTests` green (SC2).
- Purity grep on SnakeEngine.swift + SnakeConfig.swift empty.
- `wc -l gamekit/gamekit/Games/Snake/SnakeEngine.swift` < 400 (CLAUDE.md §8.5).
- No `?? *2.swift` dupes in `git status` (CLAUDE.md §8.7).
</verification>

<success_criteria>
SnakeEngine + SnakeConfig exist as pure Foundation-only value types; the SnakeEngineTests suite passes including the SC2 determinism + ProMotion equivalence gate; the SnakeCell/SnakeDirection/SnakeEvent/SnakeFrame contract is available for Plans 03/04.
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-01-SUMMARY.md` when done.
</output>
