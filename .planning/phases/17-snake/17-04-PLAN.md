---
phase: 17-snake
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift
autonomous: true
requirements: [SNAKE-06, SNAKE-07]
must_haves:
  truths:
    - "The snake renders as a single continuous rounded path with a head distinguished by two eye dots"
    - "The body shades head-to-tail using ArcadePalette.layer(forIndex:theme:) (head most saturated) (D-02)"
    - "Food renders as a circle in a contrasting token; the board is a flat border-tinted well with no grid lines (D-03)"
    - "Between cell moves the body smoothly lerps prevBody->body by cellMoveAlpha; under Reduce Motion alpha is forced to 0 (jump-cut) (D-04)"
    - "Wrap-boundary segments do not draw a streak across the board"
    - "Eating food produces a brief head pulse animation (D-08), gated by feedbackAnimation and skipped under Reduce Motion"
  artifacts:
    - path: "gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift"
      provides: "Props-only Canvas: board well, continuous body path, head eyes, food, Gaffer lerp, RM jump-cut"
      contains: "struct SnakeBoardCanvas"
  key_links:
    - from: "SnakeBoardCanvas"
      to: "ArcadePalette.layer(forIndex:theme:)"
      via: "per-segment stroke color"
      pattern: "ArcadePalette.layer"
    - from: "SnakeBoardCanvas.segPos"
      to: "cellMoveAlpha"
      via: "reduceMotion ? 0.0 : cellMoveAlpha"
      pattern: "reduceMotion"
---

<objective>
Build `SnakeBoardCanvas` — the props-only `Canvas` view that draws the board well (D-03), the continuous rounded snake path with head eye-dots (D-01), the head-to-tail body gradient via `ArcadePalette` (D-02), the food circle, smooth Gaffer interpolation between cell moves (D-04), and the Reduce-Motion jump-cut path (SNAKE-07/SC5).

Purpose: This is the visual identity of Snake. It reads engine snapshot state passed in as props and renders with DesignKit tokens only (SNAKE-06).
Output: `SnakeBoardCanvas.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-RESEARCH.md
@.planning/phases/17-snake/17-PATTERNS.md
@DESIGN.md

<interfaces>
From Plan 17-01 (SnakeEngine.swift): struct SnakeCell { col: Int; row: Int }, struct SnakeFrame, enum SnakeDirection.
From Plan 17-02 (Core/ArcadePalette.swift): enum ArcadePalette { struct Layer { let base: Color; let next: Color; let blend: Double }; static func layer(forIndex i: Int, theme: Theme) -> Layer } — forIndex 0 = head/most-saturated end.
DesignKit tokens available: theme.colors.{border, background, surface, success, accentPrimary, textSecondary}, theme.charts.chart1...6, theme.radii.card.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SnakeBoardCanvas — board well, body path, head eyes, food, Gaffer lerp</name>
  <files>gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackBoardCanvas.swift (exact analog — props-only Canvas, Gaffer lerp with reduceMotion snap, ArcadePalette/StackPalette per-index usage, token-only draw via blend modes)
    - gamekit/gamekit/Games/Merge/ board well treatment (border-tinted rounded container reference for D-03)
    - .planning/phases/17-snake/17-RESEARCH.md Pattern 4 (Canvas body-as-path, eye dots, food circle), Common Pitfalls 1 (wrap-boundary lerp)
    - .planning/phases/17-snake/17-PATTERNS.md SnakeBoardCanvas.swift section + Shared Pattern 6 (token discipline)
    - DESIGN.md §3.0 (depth rules — flat well, no sheen on non-controls) + §3 (component dictionary)
  </read_first>
  <action>
    Create struct SnakeBoardCanvas: View, props-only (no @State, no @Environment). Props: let body: [SnakeCell], let prevBody: [SnakeCell], let cellMoveAlpha: Double, let food: SnakeCell, let currentDirection: SnakeDirection, let theme: Theme, let reduceMotion: Bool, let fxEnabled: Bool, let cols: Int, let rows: Int. In the Canvas closure compute cellSize = size.width / CGFloat(cols) and alpha = reduceMotion ? 0.0 : cellMoveAlpha (SNAKE-07). Provide a segPos(_ i:) helper that lerps prevBody[i] -> body[i] at alpha, returning the cell-center CGPoint. Draw, in order: (1) the board well — a RoundedRectangle path at theme.radii.card stroked with theme.colors.border (flat, no sheen, D-03, no grid lines); (2) the body as adjacent stroked sub-paths (tail->head), lineWidth ~= cellSize * 0.78, lineCap/lineJoin .round, each segment colored ArcadePalette.layer(forIndex: i, theme: theme).base with layer.next alpha-blended at layer.blend for the D-02 gradient (index 0 = head, most saturated); (3) the head eye dots — two small circles filled with theme.colors.background, offset toward currentDirection; (4) the food as a filled Circle using theme.colors.success (default per RESEARCH Open Question 1; shape already differs from the body path); (5) the D-08 head pulse — add prop let headPulse: Double (0 = rest, 1 = pulse peak; the view layer in Plan 17-05 animates it 1->0 over ~150ms on eatCount change when fxEnabled) and scale the head segment's stroke width by (1 + 0.25 * headPulse) so the food visibly shrinks into a briefly swelling head; the prop stays 0 when !fxEnabled or reduceMotion, so the pulse is fully gated by feedbackAnimation and skipped under Reduce Motion. Use ONLY DesignKit tokens and ArcadePalette — no Color(red:)/Color(hex:)/system color names. Keep the file under 400 lines; if FX helpers grow, split into SnakeBoardFX.swift (mirroring StackBoardFX.swift).
  </action>
  <verify>
    <automated>grep -q "struct SnakeBoardCanvas" gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift && grep -q "ArcadePalette.layer" gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift && grep -Ec "Color\(red:|Color\(hex:|\.green\b|\.blue\b" gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift | grep -qx 0 && echo TOKENS_OK</automated>
  </verify>
  <done>SnakeBoardCanvas draws board well + continuous body path + head eyes + food circle; body uses ArcadePalette ramp; token-discipline grep empty.</done>
  <acceptance_criteria>
    - Source contains struct SnakeBoardCanvas: View with props-only fields (no @State/@Environment).
    - Source contains ArcadePalette.layer(forIndex:.
    - Source contains reduceMotion ? 0.0 : cellMoveAlpha (or equivalent alpha gate).
    - grep for Color(red:|Color(hex:|.green|.blue returns empty.
    - Head eye dots use theme.colors.background; food fill uses a semantic token (success or accentPrimary).
    - Source contains a headPulse: Double prop that scales the head segment stroke width (D-08 head pulse).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wrap-boundary lerp handling + build verify</name>
  <files>gamekit/gamekit/Games/Snake/SnakeBoardCanvas.swift</files>
  <read_first>
    - .planning/phases/17-snake/17-RESEARCH.md Common Pitfalls 1 (segment lerp across wrap boundary) + Open Questions 3 (skip-stroke vs coordinate-adjust)
  </read_first>
  <behavior>
    - When two adjacent body indices span a wrap (abs(curr.col - prev.col) > cols/2 OR abs(curr.row - prev.row) > rows/2), the connecting stroke between them is NOT drawn as a straight line across the board; each endpoint terminates cleanly with its rounded cap at the edge (skip-stroke approach per Open Question 3).
    - The wrap-boundary handling is inert in wall mode (no wrap occurs) and under Reduce Motion (alpha 0 = cells snap to their own positions, no cross-board interpolation).
  </behavior>
  <action>
    In segPos / the body-draw loop, detect wrap jumps by comparing adjacent body cells: if abs(curr.col - prev.col) > cols/2 or abs(curr.row - prev.row) > rows/2 for a segment pair, skip that connecting stroke and let each cell's rounded cap read as "exits one edge, re-enters the other" (the correct toroidal metaphor). Do not attempt velocity-based diagonal lerp. Then build the full app target to confirm the Canvas compiles against the Plan 17-01 SnakeFrame types and the Plan 17-02 ArcadePalette. This Canvas is not yet mounted in a view (Plan 05 wires it) — build-only verification here; visual/RM/§8.12 audits run in Plan 05's checkpoint.
  </action>
  <verify>
    <automated>xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -12</automated>
  </verify>
  <done>Wrap-boundary streak is prevented via skip-stroke; SnakeBoardCanvas compiles into the app target.</done>
  <acceptance_criteria>
    - Source contains a wrap-jump guard comparing adjacent cells against cols/2 or rows/2.
    - xcodebuild build ... -scheme gamekit exits 0.
    - wc -l SnakeBoardCanvas.swift < 400 (and SnakeBoardFX.swift < 400 if split).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. SnakeBoardCanvas is a pure render surface consuming in-process engine snapshot state; no input handling, persistence, or network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Information Disclosure | rendered content | accept | No sensitive data rendered (score + game field only) |
| T-17-08 | Denial of Service (self) | per-frame draw cost at 60Hz | mitigate | Immediate-mode Canvas (no per-cell view tree); TimelineView paused when idle/banner (wired in Plan 05) |
| T-17-SC | Tampering | package installs | accept | No packages installed (first-party Swift only) |
</threat_model>

<verification>
- Full target builds (`xcodebuild build -scheme gamekit`).
- Token-discipline grep on SnakeBoardCanvas.swift empty (SNAKE-06 gate).
- File-size cap: `wc -l` < 400.
- Visual §8.12 legibility + Reduce Motion jump-cut are verified in Plan 05's human-verify checkpoint.
</verification>

<success_criteria>
SnakeBoardCanvas renders the D-01 continuous rounded body with head eye-dots, the D-02 head-to-tail ArcadePalette gradient, the D-03 flat border-tinted board well with no grid lines, and the food circle, all token-only (SNAKE-06); smooth Gaffer interpolation between cell moves with a Reduce-Motion jump-cut (alpha 0) path (SNAKE-07); wrap-boundary segments never streak across the board; the app builds green.
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-04-SUMMARY.md` when done.
</output>
