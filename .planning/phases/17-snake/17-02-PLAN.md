---
phase: 17-snake
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Core/ArcadePalette.swift
  - gamekit/gamekit/Games/Stack/StackPalette.swift
autonomous: true
requirements: [SNAKE-06]
must_haves:
  truths:
    - "ArcadePalette.layer(forIndex:theme:) produces an accent-derived per-index color ramp using only DesignKit chart tokens (D-02)"
    - "Stack's existing tower rendering still compiles and behaves identically after StackPalette becomes a forwarding shim"
  artifacts:
    - path: "gamekit/gamekit/Core/ArcadePalette.swift"
      provides: "Shared arcade body/block color ramp: layer(forIndex:theme:)->Layer"
      contains: "enum ArcadePalette"
    - path: "gamekit/gamekit/Games/Stack/StackPalette.swift"
      provides: "Thin forwarding shim to ArcadePalette (zero Stack call-site changes)"
      contains: "ArcadePalette"
  key_links:
    - from: "ArcadePalette.layer"
      to: "theme.charts.chart1...chart6"
      via: "ramp array of chart tokens"
      pattern: "theme.charts.chart"
---

<objective>
Promote `StackPalette.layer(forIndex:theme:)` to `Core/ArcadePalette` — a shared accent-derived color ramp — because Snake is the second game to use identical body-ramp code (CLAUDE.md §4 two-game promotion threshold, CONTEXT D-02).

Purpose: Snake's continuous body gradient (head darkest, fading to tail) reuses Stack's tower ramp verbatim. Extracting to Core/ removes duplication and gives Plan 04's Canvas a token-only palette source.
Output: `Core/ArcadePalette.swift` (new), `Games/Stack/StackPalette.swift` (converted to a forwarding shim).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-RESEARCH.md
@.planning/phases/17-snake/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ArcadePalette to Core/ and forward StackPalette</name>
  <files>gamekit/gamekit/Core/ArcadePalette.swift, gamekit/gamekit/Games/Stack/StackPalette.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackPalette.swift (the exact source to promote — full 64-line file)
    - .planning/phases/17-snake/17-RESEARCH.md §Pattern 8 + §Open Questions 2 (forward-wrapper decision)
    - .planning/phases/17-snake/17-PATTERNS.md §Core/ArcadePalette.swift section (verbatim source + shim shape)
    - CLAUDE.md §4 (2-game promotion rule) + §1 (token discipline)
  </read_first>
  <action>
    Create `Core/ArcadePalette.swift` by moving `StackPalette`'s content verbatim: `enum ArcadePalette` with a `struct Layer { let base: Color; let next: Color; let blend: Double }` and `static func layer(forIndex i: Int, theme: Theme) -> Layer` reading the six-token ramp `theme.charts.chart1...chart6`. Rename `blocksPerStop` to `segmentsPerStop` (per D-02: "segments" covers both Stack tower layers and Snake body segments), and expose `static var blocksPerStop: Int { segmentsPerStop }` so existing Stack call sites compile with zero changes. Add a doc comment: "Accent-derived per-index color ramp for arcade games; consumed by Stack (tower layer) and Snake (body segment)." Then convert `Games/Stack/StackPalette.swift` into a forwarding shim — replace its body with `typealias StackPalette = ArcadePalette` (keep `import SwiftUI` + `import DesignKit`) and a header comment noting the promotion happened in Phase 17. Do NOT change any Stack call site. Use only DesignKit tokens — no `Color(red:)`/`Color(hex:)`.
  </action>
  <verify>
    <automated>grep -q "enum ArcadePalette" gamekit/gamekit/Core/ArcadePalette.swift && grep -q "ArcadePalette" gamekit/gamekit/Games/Stack/StackPalette.swift && grep -Ec "Color\(red:|Color\(hex:" gamekit/gamekit/Core/ArcadePalette.swift | grep -qx 0 && echo PALETTE_OK</automated>
  </verify>
  <done>ArcadePalette.swift exists with layer(forIndex:theme:); StackPalette.swift forwards to it; no raw color literals; full project builds.</done>
  <acceptance_criteria>
    - `Core/ArcadePalette.swift` contains `enum ArcadePalette` and `func layer(forIndex i: Int, theme: Theme) -> Layer`.
    - `ArcadePalette` exposes `segmentsPerStop` and a `blocksPerStop` alias.
    - `Games/Stack/StackPalette.swift` references `ArcadePalette` (shim) and contains no `layer(forIndex:` implementation body.
    - `grep -E "Color\(red:|Color\(hex:" Core/ArcadePalette.swift` returns empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build-verify Stack regression + Snake body-ramp index mapping</name>
  <files>gamekit/gamekit/Core/ArcadePalette.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackBoardCanvas.swift (StackPalette.layer call site — line ~182, confirms zero-change compile)
    - .planning/phases/17-snake/17-RESEARCH.md §Assumptions Log A4 (index-0 = most-saturated head assumption)
  </read_first>
  <action>
    Build the full app target to confirm Stack's `StackPalette.layer(forIndex:theme:)` call sites resolve through the shim unchanged (SC-adjacent regression guard for the promotion). Confirm the ramp indexing contract for Snake: `forIndex: 0` yields the head end of the ramp (`chart1`, most saturated per D-02 "head darkest") and higher indices fade toward the tail — this is the mapping Plan 04 will use as `layer(forIndex: i)` where `i = 0` is the head segment. Do NOT add Snake-specific code here; only document the index contract in the ArcadePalette doc comment so Plan 04's executor uses the correct direction (per A4, Plan 04's §8.12 audit may reverse the mapping if a preset's chart1 is not the most saturated).
  </action>
  <verify>
    <automated>xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -15</automated>
  </verify>
  <done>Full app target builds green with StackPalette forwarding to ArcadePalette; index contract documented for Plan 04.</done>
  <acceptance_criteria>
    - `xcodebuild build ... -scheme gamekit` exits 0 (Stack compiles unchanged through the shim).
    - ArcadePalette doc comment states `forIndex: 0` = head/most-saturated end of the ramp.
    - No `project.pbxproj` change in `git status`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan moves a pure token-derivation function between files; it introduces no input handling, persistence, or network surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | Stack regression via promotion | mitigate | Forwarding shim + full-target build verify (Task 2); zero Stack call-site edits |
| T-17-SC | Tampering | package installs | accept | No packages installed (first-party Swift only) |
</threat_model>

<verification>
- Full app target builds (`xcodebuild build -scheme gamekit`).
- Token-discipline grep on ArcadePalette.swift empty.
- No `?? *2.swift` dupes in `git status`.
</verification>

<success_criteria>
`Core/ArcadePalette.swift` provides the shared `layer(forIndex:theme:)` ramp using only DesignKit chart tokens; `StackPalette` forwards to it with zero Stack call-site changes; the app builds green; Plan 04 can consume `ArcadePalette.layer(forIndex:theme:)` for the Snake body gradient.
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-02-SUMMARY.md` when done.
</output>
