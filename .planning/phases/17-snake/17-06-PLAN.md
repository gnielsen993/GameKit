---
phase: 17-snake
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Screens/SnakeStatsCard.swift
  - gamekit/gamekit/Screens/StatsView.swift
autonomous: true
requirements: [SNAKE-05]
must_haves:
  truths:
    - "The Stats screen shows a Snake high score (from BestScore where difficultyRaw == endless) and runs played"
    - "With no Snake games played the card shows an explicit empty state, not a blank space"
  artifacts:
    - path: "gamekit/gamekit/Screens/SnakeStatsCard.swift"
      provides: "Props-only Snake stats card: High Score + Runs Played + empty state"
      contains: "struct SnakeStatsCard"
    - path: "gamekit/gamekit/Screens/StatsView.swift"
      provides: "Snake section renders SnakeStatsCard using existing @Query pairs"
      contains: "SnakeStatsCard("
  key_links:
    - from: "SnakeStatsCard"
      to: "BestScore endless row"
      via: "bestScores.first { $0.difficultyRaw == endless }"
      pattern: "endless"
    - from: "StatsView .snake section"
      to: "SnakeStatsCard"
      via: "snakeRecords + snakeBestScores @Query pairs (already declared)"
      pattern: "snakeBestScores"
---

<objective>
Replace the Phase 15 Snake stats placeholder with `SnakeStatsCard` — a props-only card showing the persisted high score and runs played (SNAKE-05 display side), mirroring `StackStatsCard`.

Purpose: The score written by `SnakeViewModel` (Plan 03, key `"endless"`) becomes visible on the Stats screen. This surface is independent of the game runtime — it reads the existing `snakeRecords` / `snakeBestScores` `@Query` pairs already declared in StatsView (Phase 15).
Output: `SnakeStatsCard.swift` (new), `StatsView.swift` (placeholder replaced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-PATTERNS.md

<interfaces>
From gamekit/gamekit/Screens/StackStatsCard.swift (exact analog): struct StackStatsCard { let theme: Theme; let records: [GameRecord]; let bestScores: [BestScore] } with highScoreText, runsPlayed, emptyState, metricsGrid, metricRow.
From gamekit/gamekit/Screens/StatsView.swift: @Query pairs snakeRecords + snakeBestScores are already declared (~lines 137-145); the Snake placeholder to replace is at ~lines 228-237. The Stack card is mounted at ~lines 221-226 as the pattern to copy.
Serialization key: BestScore.difficultyRaw == "endless" (matches Plan 03 write path; renaming = data break, D-12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SnakeStatsCard props-only card</name>
  <files>gamekit/gamekit/Screens/SnakeStatsCard.swift</files>
  <read_first>
    - gamekit/gamekit/Screens/StackStatsCard.swift (exact analog — full file: highScoreText, runsPlayed, emptyState, metricsGrid, metricRow, token usage, accessibility)
    - .planning/phases/17-snake/17-PATTERNS.md Screens/SnakeStatsCard.swift section
    - CLAUDE.md §8.2 (reusable views are data-driven, not data-fetching) + §8.3 (explicit empty state)
  </read_first>
  <behavior>
    - highScoreText returns the score of the BestScore row where difficultyRaw == "endless", or "—" when absent.
    - runsPlayed == records.count.
    - When records.isEmpty the body renders the empty state text "No Snake games played yet." (not a blank view).
    - Two metrics only (Phase 17 scope; full shape deferred to Phase 18): High Score, Runs Played.
  </behavior>
  <action>
    Create struct SnakeStatsCard: View, props-only: let theme: Theme, let records: [GameRecord], let bestScores: [BestScore]. Mirror StackStatsCard exactly: a computed highScoreText that reads bestScores.first(where: { $0.difficultyRaw == "endless" })?.score (fallback "—"), runsPlayed = records.count, an emptyState ViewBuilder ("No Snake games played yet."), and a metricsGrid with two metricRow entries ("High Score", highScoreText) and ("Runs Played", "\(runsPlayed)") separated by a 1pt theme.colors.border rule, each row using theme.typography.monoNumber + .monospacedDigit() and combined accessibility labels. All colors via DesignKit tokens. The "endless" literal must exactly match Plan 03's write path.
  </action>
  <verify>
    <automated>grep -q "struct SnakeStatsCard" gamekit/gamekit/Screens/SnakeStatsCard.swift && grep -q '"endless"' gamekit/gamekit/Screens/SnakeStatsCard.swift && grep -Ec "Color\(red:|Color\(hex:|\.green\b|\.blue\b" gamekit/gamekit/Screens/SnakeStatsCard.swift | grep -qx 0 && echo CARD_OK</automated>
  </verify>
  <done>SnakeStatsCard is props-only, reads the "endless" BestScore row, shows an empty state, token-only.</done>
  <acceptance_criteria>
    - Source contains struct SnakeStatsCard: View with (theme, records, bestScores) props and no modelContext / @Query.
    - Source reads difficultyRaw == "endless".
    - Empty state string "No Snake games played yet." present.
    - Token-discipline grep empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire SnakeStatsCard into StatsView (replace placeholder)</name>
  <files>gamekit/gamekit/Screens/StatsView.swift</files>
  <read_first>
    - gamekit/gamekit/Screens/StatsView.swift (the Phase 15 Snake placeholder ~lines 228-237 to replace; the Stack mount ~lines 221-226 as the target shape; snakeRecords + snakeBestScores @Query pairs ~lines 137-145)
    - .planning/phases/17-snake/17-PATTERNS.md StatsView integration excerpt
  </read_first>
  <behavior>
    - The Snake section (guarded by the existing shows(.snake) condition and section header) renders DKCard { SnakeStatsCard(theme: theme, records: snakeRecords, bestScores: snakeBestScores) } instead of the placeholder Text.
    - No new @Query is added — the existing snakeRecords / snakeBestScores pairs are reused.
  </behavior>
  <action>
    Replace the Phase 15 Snake placeholder block in StatsView.swift (the DKCard wrapping the "No Snake games yet." Text) with a DKCard wrapping SnakeStatsCard(theme: theme, records: snakeRecords, bestScores: snakeBestScores), mirroring the Stack card mount exactly. Keep the existing shows(.snake) guard and the SNAKE section header. Do NOT declare new @Query properties. Build the full target.
  </action>
  <verify>
    <automated>grep -q "SnakeStatsCard(" gamekit/gamekit/Screens/StatsView.swift && grep -Ec "No Snake games yet" gamekit/gamekit/Screens/StatsView.swift | grep -qx 0 && xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8</automated>
  </verify>
  <done>StatsView renders SnakeStatsCard from existing queries; old placeholder gone; app builds.</done>
  <acceptance_criteria>
    - StatsView.swift contains SnakeStatsCard(theme: theme, records: snakeRecords, bestScores: snakeBestScores).
    - The old "No Snake games yet." placeholder Text is removed.
    - No new @Query added (git diff shows only the placeholder swap).
    - Full target builds green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. SnakeStatsCard is a props-only read surface over locally-persisted, non-PII score data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | unknown BestScore rows | accept | Existing predicate filters gameKindRaw == "snake"; unknown rows are ignored, not panicked |
| T-17-13 | Information Disclosure | displayed score | accept | Score/run-count only; no PII |
| T-17-SC | Tampering | package installs | accept | No packages installed (first-party Swift only) |
</threat_model>

<verification>
- Full target builds green.
- Token-discipline grep on SnakeStatsCard.swift empty.
- `wc -l SnakeStatsCard.swift` < 400.
- Empty-state renders (verified visually in Plan 05's checkpoint when Stats is opened with no runs).
</verification>

<success_criteria>
The Stats screen shows a Snake High Score (from the "endless" BestScore row) and Runs Played via a props-only SnakeStatsCard, with an explicit empty state; the Phase 15 placeholder is removed; the card reuses the existing @Query pairs and the app builds green (SNAKE-05 display side).
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-06-SUMMARY.md` when done.
</output>
