---
phase: 17-snake
plan: 05
type: execute
wave: 3
depends_on: ["17-03", "17-04"]
files_modified:
  - gamekit/gamekit/Games/Snake/SnakeGameView.swift
  - gamekit/gamekit/Games/Snake/SnakeGameView+Chrome.swift
  - gamekit/gamekit/Games/Snake/SnakeScoreChip.swift
  - gamekit/gamekit/Screens/HomeView.swift
autonomous: false
requirements: [SNAKE-01, SNAKE-03]
user_setup: []
must_haves:
  truths:
    - "Tapping the Snake home tile opens SnakeGameView (not the harness); the harness file is deleted"
    - "Swiping the board or tapping the D-pad changes direction; the D-pad is always visible below the board (D-05, D-06)"
    - "Swiping left from the board's left edge does NOT pop the NavigationStack (.defersSystemGestures(on: .all) + .disableInteractivePop())"
    - "Eating food rolls the score chip; a mid-run high score pulses once; collision drains the snake then shows VideoModeBanner after 500ms (D-09, D-10)"
    - "The in-game toolbar menu toggles Wall mode: On/Off with an abandon alert when a run is in progress (D-11, D-12)"
    - "git diff on ArcadeLoopDriver.swift + ArcadeGameState.swift is empty (SC3)"
  artifacts:
    - path: "gamekit/gamekit/Games/Snake/SnakeGameView.swift"
      provides: "Game chrome: ZStack, arcadeLoop, scenePhase, gestures, banner, grayscale drain, toolbar"
      contains: "struct SnakeGameView"
    - path: "gamekit/gamekit/Games/Snake/SnakeGameView+Chrome.swift"
      provides: "idle content, game-over banner content, back chevron, D-pad, wall-mode toolbar, abandon alert"
      contains: "SnakeDPad"
    - path: "gamekit/gamekit/Games/Snake/SnakeScoreChip.swift"
      provides: "Props-only score chip with numericText content transition"
      contains: "struct SnakeScoreChip"
    - path: "gamekit/gamekit/Screens/HomeView.swift"
      provides: "Snake route mounts SnakeGameView (no .videoModeAware)"
      contains: "SnakeGameView()"
  key_links:
    - from: "SnakeGameView board"
      to: "vm.tryEnqueueDirection"
      via: "DragGesture.onEnded + SnakeDPad onDirection"
      pattern: "tryEnqueueDirection"
    - from: "SnakeGameView board"
      to: "system nav gesture"
      via: ".defersSystemGestures(on: .all)"
      pattern: "defersSystemGestures"
    - from: "HomeView .snake route"
      to: "SnakeGameView"
      via: ".disableInteractivePop() (no .videoModeAware)"
      pattern: "SnakeGameView()"
---

<objective>
Assemble `SnakeGameView` — the playable game screen — wiring the arcade loop, scenePhase pause, swipe gesture with `.defersSystemGestures(on: .all)` (SC1), the always-visible D-pad (SNAKE-03/D-06), the feedback triples (D-07/D-08/D-09/D-10), the wall-mode toolbar menu + abandon alert (D-11), the game-over banner with 500ms pre-roll and grayscale drain, and the score chip. Swap the Home route from the harness to the real game, delete the harness, and verify SC3 (zero Core diff). Final task is the game-screen human-verify checkpoint (§8.12 audit, SC1 device swipe, Reduce Motion jump-cut, §12.5 checklist).

Purpose: This is where Snake becomes playable end-to-end (SNAKE-01) on the Phase 15 substrate with zero substrate modifications (SC3).
Output: `SnakeGameView.swift`, `SnakeGameView+Chrome.swift`, `SnakeScoreChip.swift`, `HomeView.swift` (edit), harness deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-RESEARCH.md
@.planning/phases/17-snake/17-PATTERNS.md
@DESIGN.md

<interfaces>
From Plan 17-03 (SnakeViewModel.swift): @MainActor final class SnakeViewModel { var state: ArcadeGameState; var frame: SnakeFrame; var prevBody: [SnakeCell]; var eatCount/enqueueCount/highScoreCount: Int; var wallMode: Bool; var showingAbandonAlert: Bool; func tick(dt:); func tryEnqueueDirection(_:); func start()/pause()/resume()/restart(); func attachGameStats(_:); func requestWallModeToggle()/confirmWallModeChange()/cancelWallModeChange() }
From Plan 17-04 (SnakeBoardCanvas.swift): struct SnakeBoardCanvas(body:prevBody:cellMoveAlpha:food:currentDirection:theme:reduceMotion:fxEnabled:cols:rows:headPulse:) — headPulse: Double is the D-08 head pulse progress (0 = rest, 1 = peak)
From Core (consumed as-is): .arcadeLoop(isRunning:onTick:), VideoModeBanner + VideoModeBannerContent, .videoModeBannerTransition, .feedbackAnimation, .chipShadow(), .buttonStyle(.pressable), .disableInteractivePop().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SnakeScoreChip + SnakeGameView + chrome (D-pad, toolbar, banner, gestures)</name>
  <files>gamekit/gamekit/Games/Snake/SnakeScoreChip.swift, gamekit/gamekit/Games/Snake/SnakeGameView.swift, gamekit/gamekit/Games/Snake/SnakeGameView+Chrome.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackGameView.swift (arcadeLoop, scenePhase both .inactive+.background, 500ms game-over pre-roll, counter-trigger sensoryFeedback with hapticsEnabled FIRST guard, GameStats one-shot injection, grayscale drain, VideoModeBanner mount)
    - gamekit/gamekit/Games/Stack/StackGameView+Chrome.swift (idleContent, gameOverContent, backChevron)
    - gamekit/gamekit/Games/Stack/StackScoreChip.swift (score chip shape to mirror)
    - gamekit/gamekit/Games/Words/FiveLetter/FiveLetterGameView.swift (ellipsis.circle topBarTrailing menu — lines ~186-199)
    - gamekit/gamekit/Games/Merge/MergeGameView.swift (.alert(isPresented:) abandon-alert binding — lines ~112-124)
    - .planning/phases/17-snake/17-RESEARCH.md Pattern 5 (D-pad + swipe), Common Pitfalls 2 (both .inactive AND .background)
    - .planning/phases/17-snake/17-PATTERNS.md SnakeGameView.swift + SnakeScoreChip.swift sections + Shared Patterns 1-9
    - DESIGN.md §3 (chips, D-pad buttons), §5 (layout skeleton — D-pad in mode-pill slot below board), §6 (back chevron/toolbar), §8 (haptic vocabulary), §10.3 (500ms pre-roll), §10.6 (compound triple)
  </read_first>
  <behavior>
    - Board area applies the swipe DragGesture(minimumDistance: 10) mapping dominant axis to a direction and calling vm.tryEnqueueDirection, plus .defersSystemGestures(on: .all).
    - The D-pad (SnakeDPad) is always visible below the board in the mode-pill slot (never overlaying the board); each button >=44pt hit target, surface fill, 1pt border, theme.radii.button, .chipShadow(), .buttonStyle(.pressable); the opposite-direction button renders enabled and is a silent no-op via the queue rule.
    - Haptics via counter-trigger with hapticsEnabled as the FIRST guard: eatCount -> .impact(weight:.light,intensity:0.7) (D-08); enqueueCount -> .selection (D-07); highScoreCount -> .impact(weight:.medium,intensity:1.0) (D-09). No duplicate .error (VideoModeBanner fires it).
    - Score chip rolls via .contentTransition(.numericText(countsDown:false)) gated by .feedbackAnimation.
    - D-08 head pulse: onChange of vm.eatCount, when fxEnabled and !reduceMotion, set headPulse=1 then animate to 0 over ~150ms and pass it into SnakeBoardCanvas; otherwise headPulse stays constant 0.
    - On game-over: board Group .grayscale(1.0) with 0.5s easeOut gated on fxEnabled (D-10 drain); VideoModeBanner(outcome:.loss, location:.largeBottom) after a 500ms Task.sleep pre-roll when fxEnabled, instant when RM/animations off.
    - scenePhase: both .inactive AND .background call vm.pause(); .active calls vm.resume().
    - GameStats injected once via .task; toolbar has back chevron (topBarLeading) + ellipsis.circle menu (topBarTrailing) with "Wall mode: On/Off"; abandon alert binds $vm.showingAbandonAlert.
  </behavior>
  <action>
    Create SnakeScoreChip as a props-only View (theme, score) mirroring StackScoreChip, adding .contentTransition(.numericText(countsDown: false)) on the score Text gated with .feedbackAnimation(.default, value: score) (D-08 roll). Create SnakeGameView: View mirroring StackGameView's environment reads and @State vm = SnakeViewModel(), but WITHOUT @Environment(\.videoModeStore) and WITHOUT any .videoModeAware() — Snake is Video Mode exempt per 15-VIDEO-MODE-ADR.md. Compose the ZStack: board (SnakeBoardCanvas inside a TimelineView(.animation(paused: !fxEnabled || vm.state == .idle || showBanner))) wrapped with the swipe DragGesture + .defersSystemGestures(on: .all); the SnakeScoreChip overlay; the SnakeDPad below the board; the idle content; and the VideoModeBanner when showBanner. Wire .arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt: dt) }, the scenePhase onChange (both .inactive and .background -> pause), the 500ms game-over pre-roll onChange, the grayscale drain, the three counter-trigger .sensoryFeedback modifiers (hapticsEnabled FIRST), the D-08 head pulse (@State headPulse: Double = 0; onChange of vm.eatCount when fxEnabled and !reduceMotion: headPulse = 1 then withAnimation(.easeOut(duration: 0.15)) { headPulse = 0 }; passed into SnakeBoardCanvas), and the one-shot .task GameStats injection. Put idleContent ("Snake" + "Swipe or tap D-pad to start" + DKButton Start -> vm.start()), gameOverContent (VideoModeBannerContent outcome .loss, title "Game over", primary "Restart" -> vm.restart(); showBanner=false, a11y "Game over. Score N. Restart"), backChevron, the SnakeDPad struct, the wall-mode toolbar menu, and the abandon .alert into SnakeGameView+Chrome.swift to keep both files under 400 lines. All colors via DesignKit tokens only.
  </action>
  <verify>
    <automated>grep -q "defersSystemGestures(on: .all)" gamekit/gamekit/Games/Snake/SnakeGameView.swift && grep -q "struct SnakeDPad" gamekit/gamekit/Games/Snake/SnakeGameView+Chrome.swift && grep -Ec "videoModeAware" gamekit/gamekit/Games/Snake/SnakeGameView.swift | grep -qx 0 && grep -Ec "Color\(red:|Color\(hex:|\.green\b|\.blue\b" gamekit/gamekit/Games/Snake/SnakeGameView.swift gamekit/gamekit/Games/Snake/SnakeGameView+Chrome.swift gamekit/gamekit/Games/Snake/SnakeScoreChip.swift | grep -qx 0 && xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8</automated>
  </verify>
  <done>SnakeGameView builds with swipe + defersSystemGestures + D-pad + toolbar wall-mode + banner + grayscale drain + counter-trigger haptics; no videoModeAware; token-discipline grep empty across all three files.</done>
  <acceptance_criteria>
    - SnakeGameView.swift contains .defersSystemGestures(on: .all) and .arcadeLoop(isRunning:.
    - Both .inactive and .background appear in the scenePhase handler.
    - Three .sensoryFeedback modifiers use hapticsEnabled ? vm.<counter> : 0.
    - grep videoModeAware in SnakeGameView.swift returns empty (ADR exemption).
    - Token-discipline grep empty across the three Snake view files.
    - Each of the three files is < 400 lines; full target builds green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HomeView routing swap, delete harness, SC3 zero-diff gate, release log</name>
  <files>gamekit/gamekit/Screens/HomeView.swift, gamekit/gamekit/Games/Snake/SnakeHarnessView.swift</files>
  <read_first>
    - gamekit/gamekit/Screens/HomeView.swift (destination(for:) .snake route — lines ~400-402: currently SnakeHarnessView().disableInteractivePop(); Stack route at ~396-399 for the .videoModeAware comparison)
    - .planning/phases/17-snake/17-PATTERNS.md Screens/HomeView.swift EDIT section + File: Delete section
    - gamekit/gamekit/Core/ArcadeLoopDriver.swift + gamekit/gamekit/Core/ArcadeGameState.swift (must remain byte-identical — SC3)
    - Docs/releases/TEMPLATE.md + the current MARKETING_VERSION in gamekit/gamekit.xcodeproj/project.pbxproj (CLAUDE.md §0.3/§8.14)
  </read_first>
  <behavior>
    - The .snake case in HomeView.destination(for:) returns SnakeGameView().disableInteractivePop() with NO .videoModeAware() (contrast Stack which has it) — include a comment citing 15-VIDEO-MODE-ADR.md.
    - SnakeHarnessView.swift is deleted (Phase 15 throwaway); it auto-deregisters via PBXFileSystemSynchronizedRootGroup — no project.pbxproj edit.
    - git diff over Core/ArcadeLoopDriver.swift and Core/ArcadeGameState.swift for the phase range is empty (SC3).
    - The release log for the current MARKETING_VERSION gains a Snake bullet.
  </behavior>
  <action>
    Edit HomeView.swift: replace the .snake destination body SnakeHarnessView() with SnakeGameView(), keeping .disableInteractivePop() and adding a one-line comment "NO .videoModeAware() — Snake exempt per 15-VIDEO-MODE-ADR.md". Delete gamekit/gamekit/Games/Snake/SnakeHarnessView.swift (do NOT hand-edit project.pbxproj — §8.8). Run the SC3 gate: git diff over Core/ArcadeLoopDriver.swift and Core/ArcadeGameState.swift for the Phase 17 commit range must return empty; if it is non-empty, revert the substrate edits (they are prohibited). Read the current MARKETING_VERSION from gamekit/gamekit.xcodeproj/project.pbxproj; if Docs/releases/v{version}.md does not exist create it from Docs/releases/TEMPLATE.md, then append a User-facing bullet noting Snake is now playable (swipe + D-pad, wrap/wall modes, endless high score) per CLAUDE.md §8.14 — land it in this plan's commit.
  </action>
  <verify>
    <automated>grep -q "SnakeGameView()" gamekit/gamekit/Screens/HomeView.swift && test ! -e gamekit/gamekit/Games/Snake/SnakeHarnessView.swift && git diff --quiet HEAD -- gamekit/gamekit/Core/ArcadeLoopDriver.swift gamekit/gamekit/Core/ArcadeGameState.swift && echo SC3_CLEAN && xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8</automated>
  </verify>
  <done>Home routes to SnakeGameView; harness deleted; SC3 git diff empty; release log updated; app builds.</done>
  <acceptance_criteria>
    - HomeView.swift .snake case contains SnakeGameView() and no .videoModeAware.
    - gamekit/gamekit/Games/Snake/SnakeHarnessView.swift does not exist.
    - git diff on Core/ArcadeLoopDriver.swift + Core/ArcadeGameState.swift is empty (SC3).
    - No project.pbxproj change in git status; no `?? *2.swift` dupes.
    - Docs/releases/v{current}.md contains a Snake bullet.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Game-screen acceptance — §8.12 theme audit, SC1 device swipe, Reduce Motion, §12.5 checklist</name>
  <action>Perform the human verification steps in <how-to-verify> on a real device and across the required presets, then either approve or report the failing item. No code is written in this task; it is the acceptance gate for the assembled Snake game screen (SC1, §8.12, SNAKE-07/SC5, §12.5).</action>
  <what-built>
    Snake is fully playable: swipe + D-pad steering, capacity-2 direction queue, wrap/wall toolbar toggle, grow-on-food with score-chip roll, mid-run high-score pulse, self/wall collision -> grayscale drain -> VideoModeBanner, and score persistence. This checkpoint is the human sign-off gate that automated tests cannot cover: on-device system-gesture arbitration (SC1), visual legibility across presets (§8.12), and Reduce-Motion motion perception (SNAKE-07/SC5).
  </what-built>
  <how-to-verify>
    1. SC1 (real device required): Run on an iPhone. Start a Snake run. Swipe right starting from the LEFT screen edge over the board — the snake turns and the NavigationStack does NOT pop back to Home. Repeat swiping from the right edge. Also confirm the D-pad below the board turns the snake and is always visible during play.
    2. SC4: Swipe two directions rapidly before the next cell move (e.g. up then right) — both turns register in order. Swipe directly opposite the current heading (e.g. left while moving right) — it is rejected with no haptic and no death.
    3. §8.12 (SNAKE-06): Play under the Classic (Chrome Diner) preset AND one Loud/Moody preset (Voltage or Dracula). Confirm the body head-to-tail gradient, the head eye-dots, the food circle, and the board well are all clearly legible on both. If the food token (success) collides with a body chart token on a Loud preset, switch the food fill to accentPrimary (RESEARCH Open Question 1) and re-verify.
    4. SNAKE-07 / SC5 (Reduce Motion): Simulator/device Settings -> Accessibility -> Reduce Motion ON. Play a run: the snake teleports cell-to-cell each tick with NO between-cell interpolation, and the death transition cuts instantly to the banner with no drain — while grow/collision/scoring behave identically to the animated path.
    5. Feel: play several runs and confirm the speed ramp feels calm early and plateaus (no runaway speed); confirm no screen shake ever occurs (brand rule D-10).
    6. §12.5 new-game checklist: confirm the DESIGN.md §12.5 checklist items pass for Snake.
  </how-to-verify>
  <resume-signal>Type "approved" once SC1 (no nav pop), §8.12 legibility (Classic + one Loud/Moody), and Reduce Motion jump-cut all pass; otherwise describe the failing item so it can be fixed before phase verification.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch/gesture → VM | Swipe + D-pad turn input enters the direction queue |
| system nav gesture ↔ board swipe | Left-edge swipe arbitration (SC1) |

Offline, local-only, single-player. No network, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Denial of Service (self) | dt burst after scenePhase resume | mitigate | Both .inactive and .background call vm.pause() (Pitfall 2); ArcadeLoopDriver clamp caps resume dt |
| T-17-10 | Tampering | substrate modification to fit Snake | mitigate | SC3 git-diff gate on ArcadeLoopDriver.swift + ArcadeGameState.swift (Task 2) blocks any Core change |
| T-17-11 | Elevation of Privilege | left-edge swipe hijacking nav | mitigate | .defersSystemGestures(on: .all) + .disableInteractivePop(); SC1 device verify (Task 3) |
| T-17-SC | Tampering | package installs | accept | No packages installed (first-party Swift only) |
</threat_model>

<verification>
- Full target builds green (`xcodebuild build -scheme gamekit`).
- SC3 automated gate: `git diff` on the two Core substrate files empty.
- Token-discipline grep empty across all Snake view files (SNAKE-06).
- Human-verify checkpoint (Task 3): SC1 device swipe, §8.12 audit, Reduce Motion jump-cut, §12.5 checklist.
- Full `gamekitTests` suite green (SnakeEngineTests + GameStatsTests).
- No `?? *2.swift` dupes; no project.pbxproj change.
</verification>

<success_criteria>
Snake is fully playable end-to-end (SNAKE-01): swipe + always-visible D-pad steering (SNAKE-03), wrap/wall toolbar toggle, grow-on-food, self/wall collision -> drain -> banner, score persistence. Left-edge swipe does not pop navigation (SC1). Zero Core substrate diff (SC3). §8.12 legibility on Classic + one Loud/Moody preset and Reduce-Motion jump-cut both confirmed by human verification.
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-05-SUMMARY.md` when done.
</output>
