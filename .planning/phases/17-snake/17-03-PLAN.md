---
phase: 17-snake
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - gamekit/gamekit/Games/Snake/SnakeViewModel.swift
  - gamekit/gamekitTests/Core/GameStatsTests.swift
autonomous: true
requirements: [SNAKE-02, SNAKE-03, SNAKE-05]
must_haves:
  truths:
    - "A capacity-2 direction queue preserves rapid turns queued before the next cell move fires"
    - "A 180-degree reversal (against the queue's last entry or the engine's current direction) is rejected and fires no haptic; accepted turns fire .selection (D-07)"
    - "The VM fixed-step accumulator drives engine cell moves at 1/60s and pops one queued direction per cell move"
    - "On game-over the score persists exactly once via GameStats.record(gameKind:.snake, mode:\"endless\", outcome:.loss)"
    - "Toggling wall mode with score>0 raises an abandon alert; with score==0 it applies immediately; the choice persists under snake.wallMode (D-12)"
  artifacts:
    - path: "gamekit/gamekit/Games/Snake/SnakeViewModel.swift"
      provides: "@Observable @MainActor VM: accumulator, direction queue, counters, persistence, wall-mode abandon-alert"
      contains: "final class SnakeViewModel"
    - path: "gamekit/gamekitTests/Core/GameStatsTests.swift"
      provides: "recordSnakeRunHigherOnly persistence test"
      contains: "recordSnakeRunHigherOnly"
  key_links:
    - from: "SnakeViewModel.tick"
      to: "engine.step(dt:nextDirection:)"
      via: "while accumulator >= fixedDt, pop directionQueue per cell move"
      pattern: "engine.step"
    - from: "SnakeViewModel game-over"
      to: "GameStats.record"
      via: "mode:endless outcome:loss score:engine.score"
      pattern: "endless"
    - from: "SnakeViewModel.applyWallModeToggle"
      to: "UserDefaults snake.wallMode"
      via: "set(forKey:)"
      pattern: "snake.wallMode"
---

<objective>
Build `SnakeViewModel` — the `@Observable @MainActor` bridge between `ArcadeLoopDriver` and `SnakeEngine`: the fixed-step accumulator, the capacity-2 direction queue with 180-degree rejection (SNAKE-03/SC4), event counters for haptics, one-shot score persistence (SNAKE-05), and the wall-mode abandon-alert toggle (SNAKE-02, D-11/D-12). Add the Snake persistence unit test.

Purpose: This is the state owner. The Canvas (Plan 04) and GameView (Plan 05) read its published render state and call its input methods.
Output: `SnakeViewModel.swift`, plus `recordSnakeRunHigherOnly` in the existing `GameStatsTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-snake/17-CONTEXT.md
@.planning/phases/17-snake/17-RESEARCH.md
@.planning/phases/17-snake/17-PATTERNS.md

<interfaces>
From Plan 17-01 (gamekit/gamekit/Games/Snake/SnakeEngine.swift):
- struct SnakeEngine { init(cfg:rng:); mutating func step(dt: Double, nextDirection: SnakeDirection?) -> SnakeFrame; var currentDirection: SnakeDirection { get }; var score: Int { get } }
- enum SnakeDirection { case up, down, left, right; var opposite: SnakeDirection }
- struct SnakeFrame { body; prevBody; food; currentDirection; score; cellMoveAlpha; gameOver; event }
- enum SnakeEvent { case none; case ate(food:); case died }

From gamekit/gamekit/Core/GameStats.swift:
- func record(gameKind: GameKind, mode: String, outcome: GameOutcome, durationSeconds: Int, score: Int) throws
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SnakeViewModel core — accumulator, direction queue, lifecycle, counters</name>
  <files>gamekit/gamekit/Games/Snake/SnakeViewModel.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Stack/StackViewModel.swift (exact analog — @Observable @MainActor class, tick(dt:) accumulator, counter-trigger fields, restart, start/pause/resume/stop, attachGameStats one-shot)
    - gamekit/gamekit/Core/ArcadeGameState.swift (state enum idle/running/paused/gameOver — consumed, NOT modified)
    - .planning/phases/17-snake/17-RESEARCH.md Pattern 3 (direction queue), Common Pitfalls 5/6 (effective-current guard; enqueueCount inside guard)
    - .planning/phases/17-snake/17-PATTERNS.md SnakeViewModel.swift section
  </read_first>
  <behavior>
    - tryEnqueueDirection(_:) computes effectiveCurrent = directionQueue.last ?? engine.currentDirection; rejects (returns false, no counter bump) when dir == effectiveCurrent.opposite OR directionQueue.count >= 2.
    - On accept: appends to queue AND increments enqueueCount (the increment is INSIDE the guard — Pitfall 6: rejected input fires no haptic).
    - tick(dt:) guards on state == .running, accumulates dt, and in the while accumulator >= fixedDt loop pops one direction from the queue per cell move and calls engine.step(dt: fixedDt, nextDirection:).
    - On .ate event: eatCount += 1; if engine.score crosses the persisted best exactly once this run: highScoreCount += 1.
    - restart() re-creates the engine with SystemRandomNumberGenerator(), clears queue and counters, resets accumulator, snaps prevBody, sets state = .idle.
    - start/pause/resume/stop mirror StackViewModel (pause/resume guard on current state).
  </behavior>
  <action>
    Create SnakeViewModel as an @Observable @MainActor final class SnakeViewModel, Foundation import only (plus app module types). State surface: private(set) var state: ArcadeGameState = .idle, private(set) var frame: SnakeFrame, private(set) var prevBody: [SnakeCell], private(set) var eatCount = 0, private(set) var enqueueCount = 0, private(set) var highScoreCount = 0, private(set) var gameStats: GameStats?, plus private engine, accumulator, fixedDt (engine.cfg.fixedDt), directionQueue: [SnakeDirection] (maxQueueDepth 2), didAttachStats, and a bestScoreAtStart: Int snapshot for the once-per-run high-score crossing. Implement tryEnqueueDirection(_:) -> Bool (@discardableResult) per Pattern 3 with the increment inside the guard. Implement tick(dt:) mirroring StackViewModel's accumulator loop but popping directionQueue per cell move. Implement attachGameStats(_:) as a one-shot guard, restart(), and start/pause/resume/stop. Do NOT clamp dt (ArcadeLoopDriver already clamps min(rawDt, 0.1)). Do NOT publish the whole frame at 60Hz beyond what StackViewModel already does (chrome vs render-state separation — RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>grep -q "final class SnakeViewModel" gamekit/gamekit/Games/Snake/SnakeViewModel.swift && grep -q "maxQueueDepth" gamekit/gamekit/Games/Snake/SnakeViewModel.swift && xcodebuild build -project gamekit/gamekit.xcodeproj -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -8</automated>
  </verify>
  <done>SnakeViewModel core compiles; direction queue rejects 180-degree + caps at 2; enqueueCount increments only on accept; tick() drives engine at fixedDt.</done>
  <acceptance_criteria>
    - Source contains @Observable @MainActor final class SnakeViewModel.
    - tryEnqueueDirection guards use directionQueue.last ?? engine.currentDirection (Pitfall 5).
    - enqueueCount += 1 appears AFTER both reject guards (Pitfall 6).
    - tick contains while accumulator >= fixedDt and engine.step.
    - Full target builds green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence hookup + wall-mode abandon-alert + persistence test</name>
  <files>gamekit/gamekit/Games/Snake/SnakeViewModel.swift, gamekit/gamekitTests/Core/GameStatsTests.swift</files>
  <read_first>
    - gamekit/gamekit/Games/Merge/MergeViewModel.swift (requestModeChange / confirmModeChange / cancelModeChange abandon-alert pattern — lines ~175-207)
    - gamekit/gamekit/Core/GameStats.swift (record() overload + evaluateBestScore higher-only semantics — lines ~113-289)
    - gamekitTests/Core/GameStatsTests.swift (makeStats() helper + @MainActor @Suite pattern to extend)
    - .planning/phases/17-snake/17-RESEARCH.md Pattern 6 (wall-mode toggle), Pattern 7 (persistence), Code Examples (recordSnakeRunHigherOnly)
  </read_first>
  <behavior>
    - On the game-over transition inside tick(): set state = .gameOver, call try? gameStats?.record(gameKind: .snake, mode: "endless", outcome: .loss, durationSeconds: 0, score: engine.score) exactly once, then return (halt the while loop). Never persist per tick or per food.
    - requestWallModeToggle(): if engine.score > 0 set showingAbandonAlert = true; else applyWallModeToggle().
    - confirmWallModeChange() clears the alert and applies; cancelWallModeChange() clears the alert only.
    - applyWallModeToggle() flips wallMode, writes it to UserDefaults.standard under key "snake.wallMode", and calls restart() (which re-creates the engine with the new wallMode via cfg).
    - wallMode initializes from UserDefaults.standard.bool(forKey: "snake.wallMode") (default false = wrap).
    - recordSnakeRunHigherOnly test: recording score 15 then 8 leaves the BestScore("snake","endless") row at 15 (higher-only), with exactly one GameRecord per run.
  </behavior>
  <action>
    Extend SnakeViewModel with var showingAbandonAlert = false, private(set) var wallMode: Bool (init from UserDefaults key "snake.wallMode"), and the toggle methods requestWallModeToggle() / confirmWallModeChange() / cancelWallModeChange() / private applyWallModeToggle() mirroring MergeViewModel.requestModeChange. Ensure the engine is constructed from a SnakeConfig whose wallMode reflects the VM's wallMode (build a per-instance cfg = .default with wallMode overwritten). Wire the one-shot GameStats.record(...) call on the game-over transition inside tick() with mode: "endless", outcome: .loss, durationSeconds: 0, score: engine.score. Then add recordSnakeRunHigherOnly to the existing GameStatsTests.swift (do NOT create a new test file) using the makeStats() helper: record 15 then 8, assert one GameRecord for gameKindRaw == "snake" and BestScore(...,"endless").score == 15. The "endless" and "snake.wallMode" strings are PERMANENT serialization keys — renaming is a data break (D-12).
  </action>
  <verify>
    <automated>grep -q '"endless"' gamekit/gamekit/Games/Snake/SnakeViewModel.swift && grep -q '"snake.wallMode"' gamekit/gamekit/Games/Snake/SnakeViewModel.swift && xcodebuild test -project gamekit/gamekit.xcodeproj -scheme gamekit -only-testing:gamekitTests/GameStatsTests -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>Score persists once on game-over with mode "endless"; wall-mode abandon-alert works; snake.wallMode persists; recordSnakeRunHigherOnly passes green.</done>
  <acceptance_criteria>
    - Source contains mode: "endless", outcome: .loss, and gameKind: .snake in the record call.
    - Source writes UserDefaults key "snake.wallMode" and reads it at init.
    - requestWallModeToggle gates on engine.score > 0 for the abandon alert.
    - recordSnakeRunHigherOnly is present in GameStatsTests.swift and the test run exits 0.
    - No new test file created; no project.pbxproj change in git status.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture → VM direction queue | User turn input enters as a 4-value enum, buffered (max 2) |
| VM → SwiftData / UserDefaults | Score row and wall-mode Bool persisted locally |

Offline, local-only. No network, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | UserDefaults snake.wallMode | accept | Bool with known-good default (false = wrap); no gameplay-security implication |
| T-17-05 | Denial of Service (self) | stale SwiftData store on schema drift | accept | Phase adds NO new @Model; CLAUDE.md §8.9 runbook covers stale simulator store |
| T-17-06 | Tampering | duplicated score writes | mitigate | record() called exactly once on the game-over transition, then return (Task 2 behavior) |
| T-17-SC | Tampering | package installs | accept | No packages installed (first-party Swift only) |
</threat_model>

<verification>
- `xcodebuild test ... -only-testing:gamekitTests/GameStatsTests` green (SNAKE-05).
- Full target builds (`xcodebuild build -scheme gamekit`).
- `wc -l gamekit/gamekit/Games/Snake/SnakeViewModel.swift` < 400.
- No `?? *2.swift` dupes in `git status`; no `project.pbxproj` change.
</verification>

<success_criteria>
SnakeViewModel drives the engine via a 1/60s accumulator; the capacity-2 direction queue rejects 180-degree reversals silently (SNAKE-03/SC4); score persists exactly once on game-over via GameStats.record with mode "endless" (SNAKE-05); wall-mode toggles via the abandon-alert pattern and persists under snake.wallMode (SNAKE-02); recordSnakeRunHigherOnly passes.
</success_criteria>

<output>
Create `.planning/phases/17-snake/17-03-SUMMARY.md` when done.
</output>
