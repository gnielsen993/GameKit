---
phase: 04-stats-persistence
plan: 06
type: execute
wave: 4
depends_on:
  - 04-05
files_modified:
  - .planning/phases/04-stats-persistence/04-VERIFICATION.md
autonomous: false
requirements:
  - PERSIST-02
  - PERSIST-03
  - SHELL-03
tags:
  - manual-verification
  - theme-matrix
  - real-device
  - acceptance
user_setup: []
must_haves:
  truths:
    - "Stats survive force-quit on iOS Simulator AND physical device — verified by win-then-immediately-swipe-up-from-app-switcher and relaunch sequence (PERSIST-02 / SC1 / SC5)"
    - "Stats survive crash — verified by deliberate crash-after-win injection (temporary debug toggle, removed before SUMMARY) and relaunch (SC5)"
    - "Stats survive device-reboot on physical device — won game on hardware → reboot → relaunch shows record present (SC5)"
    - "StatsView renders correctly across all 6 audit presets (forest / bubblegum / barbie / cream / dracula / voltage) in BOTH empty state AND populated-with-50-game-seed state — screenshot artifacts attached (THEME-01 / CLAUDE.md §8.12)"
    - "SettingsView DATA section + Reset alert renders correctly across all 6 presets — danger-tinted Reset glyph distinct from row label on barbie/voltage litmus presets (UI-SPEC §Theme Matrix Medium-risk callout)"
    - "Export → Files → Reset → Import round-trip works end-to-end on PHYSICAL iPhone (real-device security-scoped URL handling — RESEARCH Pitfall 5 — only fully exercised on hardware)"
    - "Schema-mismatch import alert displays D-21 verbatim copy when a hand-edited `schemaVersion: 99` JSON is imported"
    - "VoiceOver reads each StatsView difficulty row as a single combined phrase (UI-SPEC §A11y labels — partial A11Y polish; full pass at P5)"
  artifacts:
    - path: ".planning/phases/04-stats-persistence/04-VERIFICATION.md"
      provides: "Manual verification report — 7 sections covering PERSIST-02/03 + SC1-5 + theme matrix + a11y partial pass; screenshots inline as evidence"
      contains: "PERSIST-02"
  key_links:
    - from: "Plan 06"
      to: "ROADMAP.md Phase 4"
      via: "marks SC1-5 verified — phase ready to close via /gsd-transition"
      pattern: "verified"
---

<objective>
Final phase gate — exercise every PERSIST/SHELL-03 contract that automated tests cannot reach, capture evidence in `04-VERIFICATION.md`, and certify P4 ready for `/gsd-transition` to Phase 5.

Per `04-VALIDATION.md` §Manual-Only Verifications, eight behaviors must be verified manually because:
1. **Force-quit survival** — real iOS process termination ≠ simulator quit (PERSIST-02 / SC1 / SC5)
2. **Crash survival** — real crash ≠ controlled exit (SC5)
3. **Device-reboot survival** — OS-level state flush only happens on real reboot (SC5)
4. **6-preset legibility (StatsView populated)** — visual judgment across audit set (THEME-01 / CLAUDE.md §8.12)
5. **6-preset legibility (SettingsView DATA section)** — same
6. **Reset alert reads cleanly under all 6 presets** — system alert chrome but trigger row visual must be unambiguous
7. **`fileExporter` real-device round-trip** — `.fileImporter` security-scoped URL handling differs sim vs device (RESEARCH Pitfall 5 — only fully exercises on hardware)
8. **Schema-mismatch import alert** — manual JSON edit + import path

This plan is **autonomous: false** — it ends with a human-verify checkpoint that pauses for the user. The automated suite already ran in Plan 05 Task 5; this plan is purely human-eyes-and-device.

Output:
- `04-VERIFICATION.md` — 7 sections with screenshots / observations / pass/fail per success criterion.
- (Optional) Notes for `04-NOTES.md` if any deferred items surface.

Implements decisions: D-22, D-23 (Reset alert visual verification), D-21 (schema-mismatch alert visual + copy).

Implements requirements: PERSIST-02 (force-quit/crash/reboot survival manually proven), PERSIST-03 (Export/Import end-to-end on real device), SHELL-03 (StatsView legible across audit presets).

ROADMAP P4 success criteria touched: SC1 (synchronous save proven on real device — automated proof in Plan 02 covers logic; this plan covers IO durability), SC2 (StatsView legible + empty state across audit), SC4 (real-device round-trip), SC5 (full multi-failure-mode survival pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-stats-persistence/04-CONTEXT.md
@.planning/phases/04-stats-persistence/04-VALIDATION.md
@.planning/phases/04-stats-persistence/04-UI-SPEC.md
@.planning/phases/04-01-PLAN.md
@.planning/phases/04-02-PLAN.md
@.planning/phases/04-03-PLAN.md
@.planning/phases/04-04-PLAN.md
@.planning/phases/04-05-PLAN.md
@CLAUDE.md
@.planning/phases/03-mines-ui/03-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual verification — force-quit / crash / reboot + 6-preset theme matrix + real-device fileExporter round-trip + schema-mismatch alert + VoiceOver partial</name>
  <what-built>
    Plans 01-05 cumulative output:
    - SwiftData schema: GameRecord + BestTime @Model classes; CloudKit-compat (no @Attribute(.unique), all properties optional/defaulted, schemaVersion: Int = 1).
    - Single shared ModelContainer constructed in GameKitApp.init() reading SettingsStore.cloudSyncEnabled (default false in P4) for the cloudKitDatabase ternary.
    - GameStats service writing via synchronous `try modelContext.save()` per call (PERSIST-02 / SC1 path).
    - StatsExporter codec: `.sortedKeys + .iso8601 + .prettyPrinted` JSON envelope, decode-then-validate-then-transaction order on import (RESEARCH Pitfalls 6 + 7).
    - VM 5th tail-of-init seam `gameStats: GameStats?`; `recordTerminalState(outcome:)` called from .lost and .won branches AFTER `freezeTimer()`.
    - GameView constructs GameStats lazily via `.task` modifier and one-shot `attachGameStats` setter.
    - StatsView shows MINESWEEPER section with DKCard + Grid of 3 difficulty rows (Easy / Medium / Hard) + "No games played yet." empty state; `monoNumber + .monospacedDigit()` paired pattern for stat numerals.
    - SettingsView adds DATA section with Export / Import / Reset rows; Reset glyph tinted `theme.colors.danger`; `.fileExporter` + `.fileImporter` + 2 `.alert` modifiers; security-scoped URL bookends per RESEARCH Pitfall 5.
    - ~15 new auto-extracted xcstrings keys covering UI-SPEC §Copywriting Contract.
    - Out of scope (deferred to P5/P6): SHELL-02 polish (theme picker / haptics toggle / SFX toggle); SHELL-04 intro flow; PERSIST-04/05/06 (CloudKit sync / Sign in with Apple); MINES-08/09/10 (animation / haptics / SFX).
  </what-built>
  <how-to-verify>
    Create `.planning/phases/04-stats-persistence/04-VERIFICATION.md` and populate the 7 sections below as you proceed. Pause at each section for the user to confirm pass/fail; if any section fails, halt and document the failure mode — do NOT proceed past failed gates.

    The simulator UDID `51B89A5F-01EC-4DFA-AD8A-6CAEF0683E1E` is the canonical iPhone 16 simulator from `04-VALIDATION.md`; use it unless the user specifies otherwise. Sections requiring real hardware are flagged.

    ---

    **Section 1 — PERSIST-02 / SC1 / SC5 force-quit survival (Simulator)**

    1. Boot the simulator via `xcrun simctl boot 51B89A5F-01EC-4DFA-AD8A-6CAEF0683E1E` (or use Xcode → Run).
    2. Build + install + launch the GameKit app on the simulator.
    3. Tap "Minesweeper" card on Home → navigate into game.
    4. Set difficulty to Hard via the trailing toolbar Menu.
    5. Play a Hard game to completion (win or loss — either way, a `GameRecord` should land).
    6. Confirm end-state overlay appears (Plan 03 contract).
    7. **Immediately** force-quit the app — `xcrun simctl terminate 51B89A5F-01EC-4DFA-AD8A-6CAEF0683E1E com.lauterstar.gamekit` (or Cmd-Shift-H twice + swipe up the app card from the App Switcher).
    8. Relaunch the app.
    9. Tap the Stats tab.
    10. **Pass criterion:** the Hard row shows `1 game · 0/1 win · 0%/100% · matching best time`. The record persisted across the force-quit kill.
    11. Document in 04-VERIFICATION.md Section 1: pass/fail + the exact StatsView row state.

    If FAIL: the synchronous `try modelContext.save()` in `GameStats.record` did not flush before kill. Debug by re-running with extra logging in the VM `recordTerminalState` call site; check for an actor-hop that defers the save.

    ---

    **Section 2 — PERSIST-02 / SC5 crash survival (Simulator)**

    Setup:
    1. Add a TEMPORARY crash-after-win toggle to `MinesweeperViewModel.swift` for this section ONLY:
       ```swift
       // TEMPORARY P4-06 verification — REMOVE before SUMMARY
       #if DEBUG
       static var crashAfterWin = false
       #endif
       ```
       Inside `recordTerminalState(outcome:)` after the `try? gameStats?.record(...)` line:
       ```swift
       #if DEBUG
       if outcome == .win, MinesweeperViewModel.crashAfterWin {
           fatalError("P4-06 verification — crash AFTER record save")
       }
       #endif
       ```
       Set `MinesweeperViewModel.crashAfterWin = true` somewhere in the launch path (e.g. `GameKitApp.init()` debug-only block).

    2. Rebuild + relaunch.
    3. Play a winning Easy game (any difficulty works; Easy is fastest).
    4. The app should crash IMMEDIATELY after the win record saves. Confirm crash via Xcode debugger or simctl log.
    5. Relaunch the app.
    6. Tap Stats tab. **Pass criterion:** the win record is present (the synchronous save happened BEFORE the fatalError).
    7. Remove the temporary toggle code completely. Rebuild to confirm clean state.
    8. Document in Section 2: pass/fail + the exact toggle code added/removed (mirrors P3 verification's "remove toggle before phase end" pattern).

    ---

    **Section 3 — PERSIST-02 / SC5 device-reboot survival (Physical Device — REQUIRED)**

    NOTE: Device-reboot survival is the only acceptance criterion that demands physical hardware. Simulator state is not a valid proxy because the simulator does not exercise the real OS-level fsync barrier under reboot.

    1. Connect a physical iPhone via USB.
    2. Build + run the GameKit scheme to the device (Xcode "Run" with device selected).
    3. Play a winning Hard game.
    4. Confirm the win is recorded by tapping Stats tab and seeing the Hard row populated.
    5. Power off the device (hold Side + Volume Down on iPhone X+, slide to power off).
    6. Power back on; unlock; relaunch GameKit (the app icon launches stand-alone — no Xcode attached).
    7. Tap Stats tab.
    8. **Pass criterion:** the Hard row still shows the original game count + best time.
    9. Document in Section 3: pass/fail + device model + iOS version.

    If FAIL: the WAL fsync barrier did not commit before reboot. This is the strictest test of `try modelContext.save()` durability. Debug by inspecting the SwiftData store path post-reboot for incomplete WAL files.

    ---

    **Section 4 — THEME-01 / CLAUDE.md §8.12 — 6-preset theme matrix audit (StatsView)**

    The 6 audit presets per UI-SPEC §Theme Matrix: forest (Classic) · bubblegum (Sweet) · barbie (Bright) · cream (Soft) · dracula (Moody) · voltage (Loud). Both empty state AND populated state must read cleanly on each.

    For EACH of the 6 presets:
    1. In Settings → Appearance → tap the preset (or use the launch-flag override if available).
    2. Tap Stats tab.
    3. **Empty state subsection:** if no records, observe "No games played yet." in `theme.colors.textTertiary`. Capture screenshot. Verify text is legible against `theme.colors.surface` of the DKCard.
    4. **Populated state subsection:** seed 50 games (run the test target's `seedFiftyGames` helper from the StatsExporterTests via a hand-coded debug button, OR play 5 quick Easy games to populate at least one row). Capture screenshot.
    5. Verify per the UI-SPEC §Color audit table:
       - **forest (Classic):** "MINESWEEPER" header, DKCard surface contrast, monospaced stat numerals all read cleanly.
       - **bubblegum (Sweet):** Same; verify `textTertiary` empty-state copy not washed out against blush surface.
       - **barbie (Bright) — MEDIUM RISK:** Saturated hot-pink surface; verify all four columns of stat numerals are distinguishable from background.
       - **cream (Soft):** Low-contrast warm preset; verify column-header divider rule (`theme.colors.border`) is visible.
       - **dracula (Moody, dark):** High-contrast neon; should pass naturally.
       - **voltage (Loud) — LITMUS PRESET:** Acid-yellow on violet-black. Verify `textPrimary` readable, `monoNumber` numerals readable.
    6. Document in Section 4: 6 row table — `preset | empty state PASS/FAIL | populated state PASS/FAIL | screenshot path | notes`.

    If any preset FAILS: do NOT lower the threshold. The fix is a token re-assignment in DesignKit, NOT a per-preset override in StatsView.swift. Surface as a Plan 5 prerequisite if found.

    ---

    **Section 5 — THEME-01 — 6-preset theme matrix audit (SettingsView DATA section)**

    Same 6 presets. For EACH:
    1. Switch preset.
    2. Tap Settings tab.
    3. Capture screenshot of full Settings screen showing APPEARANCE + DATA + ABOUT sections.
    4. Verify:
       - **Reset row leading `trash` glyph** is `theme.colors.danger` and visually distinct from row label.
       - **Export / Import row glyphs** are `theme.colors.textPrimary` and read against the surface.
       - The 1pt `theme.colors.border` dividers BETWEEN rows are visible (especially on cream which is low-contrast).
       - **barbie litmus** (MEDIUM RISK per UI-SPEC §Theme Matrix): if the danger red blends with the bright surface, fall back to also tinting the Reset row LABEL `danger` for redundant signal — open a Plan 05 follow-up if needed (do not edit Plan 05 here; this is a Plan 5 polish concern OR a Plan 04 patch ticket).
       - **voltage litmus**: verify all three row labels readable against the dark surface; verify `danger` glyph legible against violet background.
    5. Document in Section 5: 6-row table.

    ---

    **Section 6 — Reset alert + Schema-mismatch import alert visual + copy verification**

    **Reset alert visual (D-22 / D-23):**
    1. On any preset (forest is fine — system alert chrome doesn't theme), tap Settings → Reset stats.
    2. Capture screenshot of the alert.
    3. Verify:
       - Title: "Reset all stats?"
       - Body: "This deletes all your Minesweeper games and best times. This can't be undone."
       - Two buttons: Cancel (default role, leading) + Reset all stats (destructive role, trailing).
       - System tints the destructive button in red automatically.
    4. Tap Cancel — verify alert dismisses, no state change.
    5. Re-trigger; tap Reset all stats — verify Stats tab now shows empty state.

    **Schema-mismatch import alert (D-21):**
    1. Export current stats: Settings → Export stats → save to Files (call it `gamekit-stats-{date}.json`).
    2. Open the exported file in a plain text editor (Files app → Text Editor / iA Writer / share-to-Pages, OR macOS-side via simctl extraction).
    3. Edit the JSON: change `"schemaVersion": 1` to `"schemaVersion": 99` at the top level. Save.
    4. In the GameKit app: Settings → Import stats → pick the edited file.
    5. Capture screenshot of the resulting alert.
    6. Verify:
       - Title: "Couldn't import stats"
       - Body: "This file was exported from a newer GameKit. Update the app and try again."
       - Single OK button.
    7. Tap OK — verify Stats tab still shows the original (un-replaced) data — RESEARCH Pitfall 6 mitigation working.

    **Generic decode-failed alert** (optional but recommended):
    1. Export stats; manually corrupt the JSON (delete a closing brace).
    2. Import the corrupted file.
    3. Verify the alert body reads "The file couldn't be read. Check that it's a GameKit stats export and try again."
    4. Verify original data intact.

    Document in Section 6: pass/fail per alert + screenshot paths.

    ---

    **Section 7 — PERSIST-03 / SC4 real-device fileExporter round-trip (Physical Device — RECOMMENDED)**

    `.fileExporter` and `.fileImporter` security-scoped URL handling differs between simulator and physical device. Per RESEARCH Pitfall 5, the security-scoped resource bookends (`startAccessingSecurityScopedResource()` / `stopAccessingSecurityScopedResource()`) ARE optional in simulator (passes without them) but REQUIRED on device (fails silently without them).

    On physical iPhone (or simulator if device unavailable, but document the limitation):
    1. Play 5+ games to populate stats.
    2. Settings → Export stats.
    3. iOS document picker appears; save to Files → "On My iPhone" or iCloud Drive.
    4. Verify the file appears at the chosen location with the filename `gamekit-stats-YYYY-MM-DD.json`.
    5. Open the file in Files app → preview JSON content → verify the envelope shape (`schemaVersion: 1`, `gameRecords: [...]`, `bestTimes: [...]`).
    6. In GameKit: Settings → Reset stats → confirm. StatsView shows empty state.
    7. Settings → Import stats → pick the saved file.
    8. **Pass criterion:** StatsView refreshes with original game counts + best times. No alert (success is silent per UI-SPEC §Interaction Contracts).
    9. Document in Section 7: pass/fail + device + iOS version + filename verified + count of restored rows.

    If FAIL on device but PASS in simulator: the security-scoped URL bookends are not firing. Re-verify Plan 05 Task 4's `defer { url.stopAccessingSecurityScopedResource() }` is present and that `let didStart = url.startAccessingSecurityScopedResource()` is captured (not just called and discarded).

    ---

    **Section 8 — A11Y-02 partial — VoiceOver StatsView pass**

    Full A11Y polish is P5 (per REQUIREMENTS.md mapping); P4 verifies the partial baked-in labels work.

    1. Settings → Accessibility → VoiceOver → ON (on simulator: Cmd+F5).
    2. Play 3-5 quick Easy games to populate the row.
    3. Tap Stats tab.
    4. Three-finger swipe right to navigate rows. Each MinesDifficultyStatsRow should read as ONE combined phrase, e.g.:
       - "Easy: 12 games, 8 wins, 67 percent, best time 1 minute 42 seconds"
       - "Medium: 0 games, 0 wins, no percent yet, no best time yet"
       - "Hard: 0 games, 0 wins, no percent yet, no best time yet"
    5. Verify VoiceOver does NOT read the 4 columns separately ("12... 8... 67%... 1:42") — that would mean `.accessibilityElement(children: .combine)` is missing.
    6. Document in Section 8: pass/fail + sample of read-aloud text.

    ---

    **Final wrap (after Sections 1-8):**

    1. Clean any temporary code (Section 2's crash toggle).
    2. `cd gamekit && xcodebuild test ... -quiet` once more — full automated suite must still be green after the manual verification cycle.
    3. Write the summary section in `04-VERIFICATION.md`:
       - Total sections: 8.
       - Pass count.
       - Any deferred items surfaced (Plan 5 prerequisites — e.g. barbie/voltage Reset glyph contrast).
       - Sign-off: developer initials + date.
    4. Pause for the user to confirm the verification report is acceptable.
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark P4 verified and proceed to /gsd-transition. Type detailed pass/fail observations per section if any failed — the planner will surface follow-up tickets and you can re-run failed sections after fixes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual verification → ROADMAP closure | The decision to mark P4 verified rests on this checkpoint. Failures here cannot be auto-detected; the user is the only authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | T (Tampering) | Section 2 crash toggle leakage into production | mitigate | Section 2 explicitly documents the toggle as TEMPORARY with `#if DEBUG` guard AND requires removal before SUMMARY. The pre-commit hook scope does not catch this directly, but the `xcodebuild test` after toggle removal verifies clean state. The SUMMARY records the exact code added/removed for audit. |
| T-04-29 | I (Information Disclosure) | Verification screenshots may contain user data | accept | Screenshots are local-only artifacts in `04-VERIFICATION.md`. The user controls what gets recorded. No PII in stats anyway (game counts + times only). |
| T-04-30 | D (Denial of Service) | Verification gate blocks transition | accept | Manual verification IS the gate by design (per `04-VALIDATION.md` §Manual-Only Verifications). A failed gate halts P4 closure — this is correct behavior, not a denial threat. |
| T-04-31 | T (Tampering) | Theme matrix subjective | accept | "Legibility" is judged by human eyes; the audit set of 6 presets and the explicit Medium-risk callouts (barbie, voltage) reduce subjectivity. The SUMMARY records exact preset + screenshot evidence so a future regression has a baseline. |

ASVS L1 N/A — manual gate is governance, not a security control.
</threat_model>

<verification>
After Task 1 completes (user types "approved"):
- `04-VERIFICATION.md` exists with 8 populated sections.
- Section 1 (force-quit): documented PASS with StatsView row state.
- Section 2 (crash): documented PASS; the temporary crash toggle is REMOVED (verified via `grep -q "P4-06 verification" gamekit/gamekit/Games/Minesweeper/MinesweeperViewModel.swift` returns NO match).
- Section 3 (device-reboot): documented PASS (or N/A if no physical device available — but this is a SC5 hard gate; offer a partial-pass note in that case).
- Section 4 (StatsView 6-preset): all 6 rows PASS (or surfaced as Plan 5 prerequisite).
- Section 5 (SettingsView 6-preset): same.
- Section 6 (Reset alert + schema-mismatch alert + generic alert): all PASS with screenshot paths + verbatim copy match per D-21/D-22/D-23.
- Section 7 (real-device round-trip): PASS on physical device (or simulator with documented limitation).
- Section 8 (VoiceOver StatsView): PASS — combined-phrase reading verified.
- Final SUMMARY section: total pass count, deferred items, sign-off.
- `xcodebuild test` final run: still green.
</verification>

<success_criteria>
- [ ] `04-VERIFICATION.md` created with 8 sections + final SUMMARY.
- [ ] Section 1 force-quit survival PASS (or DOCUMENTED FAIL with debug action).
- [ ] Section 2 crash survival PASS; temporary toggle code REMOVED before SUMMARY.
- [ ] Section 3 device-reboot survival PASS on physical device (REQUIRED for SC5).
- [ ] Section 4 6-preset StatsView matrix: 6/6 PASS (or surfaced as Plan 5 prerequisite).
- [ ] Section 5 6-preset SettingsView DATA matrix: 6/6 PASS (or P5 prerequisite).
- [ ] Section 6 Reset alert + schema-mismatch alert + generic decode alert: all 3 PASS with verbatim copy match.
- [ ] Section 7 real-device fileExporter round-trip PASS (REQUIRED for PERSIST-03 / SC4 real-world validation).
- [ ] Section 8 VoiceOver combined-phrase reading PASS.
- [ ] Full automated suite still green after Section 2's toggle add/remove cycle.
- [ ] User explicitly types "approved" or per-section pass/fail before this plan marks done.
</success_criteria>

<output>
After completion, the developer should also create `.planning/phases/04-stats-persistence/04-06-SUMMARY.md` summarizing:
- Verification report status: 8/8 PASS (or partial with documented gaps).
- Devices used: simulator UDID + physical device model + iOS version.
- Any deferred items pushed to Plan 5 prerequisites (e.g. barbie/voltage Reset glyph contrast if it surfaced).
- Wave 4 closed; Phase 4 ready for `/gsd-transition` to Phase 5.
- Final invariant check: VM still Foundation-only; all `Color(...)` literals absent from `Games/Minesweeper/`, `Screens/`, and `Core/`; CloudKit container ID `iCloud.com.lauterstar.gamekit` triple-locked (PROJECT.md, smoke test, GameKitApp.init).

After the user approves, run:
```bash
gsd-sdk query commit "docs(04): mark Phase 4 verified — VERIFICATION.md complete" \
  .planning/phases/04-stats-persistence/04-VERIFICATION.md \
  .planning/phases/04-stats-persistence/04-06-SUMMARY.md
```

Then prompt the user to run `/gsd-transition` to close Phase 4 and stage Phase 5 (Polish — research-flagged).
</output>
</content>
