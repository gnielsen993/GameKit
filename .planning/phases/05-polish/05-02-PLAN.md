---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gamekit/gamekit/Resources/Audio/tap.caf
  - gamekit/gamekit/Resources/Audio/win.caf
  - gamekit/gamekit/Resources/Audio/loss.caf
  - gamekit/gamekit/Resources/Audio/LICENSE.md
  - gamekit/gamekit/Resources/Haptics/win.ahap
  - gamekit/gamekit/Resources/Haptics/loss.ahap
autonomous: false
requirements: [MINES-09, MINES-10]
must_haves:
  truths:
    - "Bundle.main.url(forResource: \"tap\", withExtension: \"caf\") returns non-nil at runtime"
    - "Bundle.main.url(forResource: \"win\", withExtension: \"caf\") returns non-nil at runtime"
    - "Bundle.main.url(forResource: \"loss\", withExtension: \"caf\") returns non-nil at runtime"
    - "Bundle.main.url(forResource: \"win\", withExtension: \"ahap\") returns non-nil at runtime"
    - "Bundle.main.url(forResource: \"loss\", withExtension: \"ahap\") returns non-nil at runtime"
    - "win.ahap and loss.ahap are valid CHHapticPattern JSON (CHHapticPattern(contentsOf:) does not throw)"
  artifacts:
    - path: "gamekit/gamekit/Resources/Audio/tap.caf"
      provides: "Cell-reveal SFX cue (~30KB target)"
    - path: "gamekit/gamekit/Resources/Audio/win.caf"
      provides: "Win chime SFX (~50KB target)"
    - path: "gamekit/gamekit/Resources/Audio/loss.caf"
      provides: "Loss thud SFX (~40KB target)"
    - path: "gamekit/gamekit/Resources/Audio/LICENSE.md"
      provides: "Provenance for all 3 CAF files (CC0 source or hand-rolled)"
    - path: "gamekit/gamekit/Resources/Haptics/win.ahap"
      provides: "3-transient ascending haptic pattern per D-07"
      contains: "HapticTransient"
    - path: "gamekit/gamekit/Resources/Haptics/loss.ahap"
      provides: "Continuous-decay + 2 sharp transients haptic pattern per D-07"
      contains: "HapticContinuous"
  key_links:
    - from: "Resources/Audio/*.caf"
      to: "Bundle.main (via Xcode 16 PBXFileSystemSynchronizedRootGroup)"
      via: "auto-registration (CLAUDE.md §8.8)"
      pattern: "Bundle\\.main\\.url\\(forResource:.*withExtension: \"caf\"\\)"
    - from: "Resources/Haptics/*.ahap"
      to: "Bundle.main (auto-registered)"
      via: "auto-registration"
      pattern: "Bundle\\.main\\.url\\(forResource:.*withExtension: \"ahap\"\\)"
---

<objective>
Ship the 5 audio + haptic resource files (3 CAF, 2 AHAP) plus LICENSE.md provenance documentation. These are non-Swift assets that Wave 2's Haptics service and SFXPlayer service depend on for `Bundle.main.url(...)` lookups.

Purpose: Without these files on disk, Wave 2's HapticsTests / SFXPlayerTests fail their bundle-lookup assertions; Plan 06's `.onChange(of: vm.phase) → Haptics.playAHAP / SFXPlayer.play` calls become silent no-ops at runtime even with all the gating right. The AHAP JSON shapes are locked verbatim by D-07 + CONTEXT Specifics — this plan authors them literally.

Output: 6 NEW files in `gamekit/gamekit/Resources/Audio/` and `gamekit/gamekit/Resources/Haptics/` (folders auto-register per CLAUDE.md §8.8 — no pbxproj edits).

Why a checkpoint plan: CAF authoring requires either a CC0 source or hand-recorded WAV input — these are creative inputs (ear-tuned per CONTEXT Specifics). Claude can author the AHAP JSON literally from the spec, but the CAF files require the user to provide source audio (or approve a CC0 source URL Claude proposes). Hence: hybrid plan with 2 autonomous tasks (AHAP JSON + LICENSE scaffold) + 1 human-action checkpoint for CAF placement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-RESEARCH.md
@.planning/phases/05-polish/05-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author win.ahap and loss.ahap (CoreHaptics JSON)</name>
  <read_first>
    - .planning/phases/05-polish/05-CONTEXT.md (D-07 + Specifics — exact AHAP shapes)
    - .planning/phases/05-polish/05-RESEARCH.md (§Pattern 6 / Code Examples 6 — AHAP JSON examples)
    - Apple AHAP reference: https://developer.apple.com/documentation/corehaptics/representing-haptic-patterns-in-ahap-files
  </read_first>
  <files>
    - gamekit/gamekit/Resources/Haptics/win.ahap (NEW — folder auto-registers)
    - gamekit/gamekit/Resources/Haptics/loss.ahap (NEW)
  </files>
  <action>
    Create the `gamekit/gamekit/Resources/Haptics/` folder if it does not exist (Xcode 16 PBXFileSystemSynchronizedRootGroup auto-registers — no pbxproj edits per CLAUDE.md §8.8).

    **Author `win.ahap`** literally per CONTEXT D-07 + Specifics line 178:
    - 3 transient events at t=0/200/400 ms (i.e. `Time: 0.0`, `0.2`, `0.4`).
    - Each transient: `EventType: HapticTransient`, `EventDuration` omitted (transients are instantaneous), with `EventParameters: [{ParameterID: HapticIntensity, ParameterValue: 0.7}, {ParameterID: HapticSharpness, ParameterValue: 0.5}]`.
    - Top-level `Version: 1.0`, `Metadata: {Project: "GameKit", Created: "2026-04-26", Description: "Win arpeggio (P5 D-07)"}`.

    Concrete JSON template (write exactly this, normalized to the AHAP schema — RESEARCH §Code Examples 6 is the canonical reference):

    ```json
    {
      "Version": 1.0,
      "Metadata": {
        "Project": "GameKit",
        "Created": "2026-04-26",
        "Description": "Win arpeggio (P5 D-07): 3 ascending intensity-0.7 transients over 0.4s"
      },
      "Pattern": [
        { "Event": { "Time": 0.0, "EventType": "HapticTransient", "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 0.7},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.5}
        ]}},
        { "Event": { "Time": 0.2, "EventType": "HapticTransient", "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 0.7},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.5}
        ]}},
        { "Event": { "Time": 0.4, "EventType": "HapticTransient", "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 0.7},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.5}
        ]}}
      ]
    }
    ```

    **Author `loss.ahap`** literally per CONTEXT D-07 + Specifics line 179:
    - 1 continuous event from t=0 to t=0.5 with intensity decay `1.0 → 0.2` (use `ParameterCurve` with `ParameterID: HapticIntensityControl` and curve points at t=0.0 value 0.0 base offset 1.0, t=0.5 value 0.0 base offset 0.2 — OR simpler shape: continuous event with `EventDuration: 0.5`, `HapticIntensity: 1.0`, `HapticSharpness: 0.5`, paired with a `ParameterCurve` ramp).
    - 2 sharp transients at t=0.1 (`HapticIntensity: 0.9, HapticSharpness: 0.9`) and t=0.25 (`HapticIntensity: 0.7, HapticSharpness: 0.8`).
    - Top-level Metadata as above with description "Loss rumble (P5 D-07): 0.5s continuous decay + 2 sharp transients".

    Concrete JSON template:

    ```json
    {
      "Version": 1.0,
      "Metadata": {
        "Project": "GameKit",
        "Created": "2026-04-26",
        "Description": "Loss rumble (P5 D-07): 0.5s continuous decay + 2 sharp transients @ 100/250ms"
      },
      "Pattern": [
        { "Event": { "Time": 0.0, "EventType": "HapticContinuous", "EventDuration": 0.5, "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 1.0},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.5}
        ]}},
        { "ParameterCurve": { "ParameterID": "HapticIntensityControl", "Time": 0.0, "ParameterCurveControlPoints": [
            {"Time": 0.0, "ParameterValue": 0.0},
            {"Time": 0.5, "ParameterValue": -0.8}
        ]}},
        { "Event": { "Time": 0.1, "EventType": "HapticTransient", "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 0.9},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.9}
        ]}},
        { "Event": { "Time": 0.25, "EventType": "HapticTransient", "EventParameters": [
            {"ParameterID": "HapticIntensity", "ParameterValue": 0.7},
            {"ParameterID": "HapticSharpness", "ParameterValue": 0.8}
        ]}}
      ]
    }
    ```

    Validate JSON shape: each file must parse as valid JSON (e.g. `python3 -m json.tool < win.ahap` succeeds with no errors). The HapticsTests in Plan 03 will validate runtime parseability via `CHHapticPattern(contentsOf:)`.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Resources/Haptics &amp;&amp; python3 -m json.tool &lt; win.ahap &gt; /dev/null &amp;&amp; python3 -m json.tool &lt; loss.ahap &gt; /dev/null &amp;&amp; ls -la win.ahap loss.ahap</automated>
  </verify>
  <acceptance_criteria>
    - `gamekit/gamekit/Resources/Haptics/win.ahap` exists
    - `gamekit/gamekit/Resources/Haptics/loss.ahap` exists
    - Both files parse as valid JSON (`python3 -m json.tool` exit 0)
    - `grep -c "HapticTransient" gamekit/gamekit/Resources/Haptics/win.ahap` returns 3
    - `grep "HapticContinuous" gamekit/gamekit/Resources/Haptics/loss.ahap` matches
    - `grep -c "HapticTransient" gamekit/gamekit/Resources/Haptics/loss.ahap` returns 2
    - `grep "ParameterCurve" gamekit/gamekit/Resources/Haptics/loss.ahap` matches (decay envelope present)
    - `grep "\"Version\": 1.0" gamekit/gamekit/Resources/Haptics/win.ahap` matches
    - File sizes between 200B and 4KB each (pure JSON, no binary payload)
  </acceptance_criteria>
  <done>Both AHAP JSON files exist in Resources/Haptics/, parse as valid JSON, and match the D-07 spec (3 transients for win; continuous + 2 transients for loss with decay curve).</done>
</task>

<task type="auto">
  <name>Task 2: Author Resources/Audio/LICENSE.md provenance scaffold</name>
  <read_first>
    - .planning/phases/05-polish/05-CONTEXT.md (D-08 + Specifics — CAF format spec)
    - .planning/phases/05-polish/05-RESEARCH.md (UI-SPEC §Registry Safety reference for provenance)
    - CLAUDE.md (§1 — no third-party backend; this implies hand-rolled OR CC0 source)
  </read_first>
  <files>
    - gamekit/gamekit/Resources/Audio/LICENSE.md (NEW — folder auto-registers when CAF arrives in Task 3)
  </files>
  <action>
    Create the `gamekit/gamekit/Resources/Audio/` folder if it does not exist.

    Write `LICENSE.md` with this structure (Markdown, no emojis):

    ```markdown
    # Audio Asset Provenance — GameKit Phase 5

    All `.caf` files in this folder are 16-bit 44.1 kHz mono per CONTEXT D-08 + Specifics.
    Encoded via `afconvert -f caff -d LEI16@44100 -c 1 input.wav output.caf`.

    | File | Source | License | Notes |
    |------|--------|---------|-------|
    | tap.caf | (TBD — fill in at Task 3 checkpoint) | (TBD) | Cell-reveal cue, ~30KB target, CONTEXT D-08 |
    | win.caf | (TBD) | (TBD) | Win chime, ~50KB target, CONTEXT D-08 |
    | loss.caf | (TBD) | (TBD) | Loss thud, ~40KB target, CONTEXT D-08 |

    ## Recording / Sourcing Constraints

    - **License:** must be CC0 / Public Domain OR hand-recorded by the project author. No CC-BY (attribution requirement complicates v1 ABOUT screen).
    - **Format:** CAF (Core Audio Format), 16-bit, 44.1 kHz, mono.
    - **Length:** tap < 200 ms; win 0.4–0.8 s; loss 0.4–0.8 s.
    - **Loudness:** peak normalized to -3 dBFS to leave headroom under user music (per AVAudioSession.ambient mix posture, D-09).
    - **Vibe (per PROJECT.md "calm + premium"):** subtle, not jarring. tap = soft click; win = single soft chime; loss = low muted thud (no harsh sfx).

    ## Suggested CC0 sources

    - https://freesound.org/ — filter by CC0 license
    - https://opengameart.org/ — filter by Public Domain
    - Hand-record via Voice Memos → export WAV → `afconvert` to CAF.

    ## Update history

    - 2026-04-26 (Plan 05-02): scaffold created; tap/win/loss to be filled at Task 3 checkpoint.
    ```

    No code; pure Markdown. Documents the format constraints and license posture so the user knows what to do at the Task 3 checkpoint.
  </action>
  <verify>
    <automated>test -f /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Resources/Audio/LICENSE.md &amp;&amp; grep -c "tap.caf\|win.caf\|loss.caf" /Users/gabrielnielsen/Desktop/GameKit/gamekit/gamekit/Resources/Audio/LICENSE.md</automated>
  </verify>
  <acceptance_criteria>
    - `gamekit/gamekit/Resources/Audio/LICENSE.md` exists
    - Mentions all three filenames: `tap.caf`, `win.caf`, `loss.caf`
    - Mentions the format spec (16-bit, 44.1kHz, mono, CAF) per D-08
    - Mentions the license requirement (CC0 / public domain / hand-recorded)
    - File size > 500 bytes (substantive content, not a stub)
  </acceptance_criteria>
  <done>Resources/Audio/LICENSE.md exists with provenance scaffold ready for the Task 3 checkpoint to fill in.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Place tap.caf / win.caf / loss.caf in Resources/Audio/</name>
  <what-built>
    - 2 AHAP files (win.ahap, loss.ahap) in Resources/Haptics/ (Task 1 — autonomous, complete)
    - LICENSE.md scaffold in Resources/Audio/ (Task 2 — autonomous, complete)
  </what-built>
  <how-to-verify>
    Per CONTEXT D-08 + Specifics, the user must provide three `.caf` files because:
    - CAF authoring is **ear-tuned** (per ROADMAP §Research Flags line 161: "`.ahap` haptic file authoring is ear-tuned, not docs-driven — plan iteration time, not specification time" — same posture for SFX).
    - Source audio is creative input that Claude cannot generate (no Bash CLI for audio synthesis available; no LLM ability to author waveforms).

    **User action required (one of two paths):**

    **Path A — Provide CC0 source files:**
    1. Visit https://freesound.org/ (filter Creative Commons 0 license).
    2. Download three short WAV/MP3 clips matching the vibe in `Resources/Audio/LICENSE.md`:
       - tap: short soft click (~150ms)
       - win: single calm chime (~600ms)
       - loss: low muted thud (~500ms)
    3. Convert each to CAF: `afconvert -f caff -d LEI16@44100 -c 1 input.wav tap.caf` (etc).
    4. Place `tap.caf`, `win.caf`, `loss.caf` in `gamekit/gamekit/Resources/Audio/`.
    5. Update `Resources/Audio/LICENSE.md` Source/License columns with the freesound URL + CC0 attribution.

    **Path B — Hand-record:**
    1. Use Voice Memos (Mac/iOS) to record three clips per the spec.
    2. Export each as WAV.
    3. Convert via `afconvert` as above.
    4. Place files; update LICENSE.md noting "hand-recorded by [author]".

    **After files are placed, verify in shell:**
    ```bash
    ls -la gamekit/gamekit/Resources/Audio/*.caf
    file gamekit/gamekit/Resources/Audio/tap.caf      # expect "Core Audio Format..."
    afinfo gamekit/gamekit/Resources/Audio/tap.caf    # expect 16-bit, 44100 Hz, 1 ch
    ```

    Each file should be 10-100 KB (sanity range — < 10 KB suggests truncation; > 200 KB suggests stereo or higher bit-depth — fix and re-encode).

    **Why this cannot be automated:** Claude has no audio synthesis tool available in this environment. The pure-JSON AHAP files (Task 1) ARE author-able; CAF binary audio is not. This is the rare CONTEXT D-08-equivalent of the "1% checkpoint:human-action" carve-out.
  </how-to-verify>
  <resume-signal>Type "files placed" once `tap.caf`, `win.caf`, `loss.caf` are in `Resources/Audio/` and `afinfo` confirms 16-bit / 44.1kHz / mono per file. The LICENSE.md Source + License columns must be updated with provenance.</resume-signal>
  <acceptance_criteria>
    - `gamekit/gamekit/Resources/Audio/tap.caf` exists, file size between 5KB and 200KB
    - `gamekit/gamekit/Resources/Audio/win.caf` exists, file size between 5KB and 200KB
    - `gamekit/gamekit/Resources/Audio/loss.caf` exists, file size between 5KB and 200KB
    - `file gamekit/gamekit/Resources/Audio/tap.caf` reports "Core Audio Format" or `afinfo` reports format = `'lpcm'` 16-bit 44.1kHz 1ch
    - `Resources/Audio/LICENSE.md` Source + License columns updated (no remaining "(TBD)" placeholders for the three CAF rows)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk↔bundle | Resources are bundled at compile-time; no runtime fetching, no network, no third-party SDK |
| user-supplied audio↔bundle | CAF files placed manually by user; license provenance documented in LICENSE.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Audio file metadata leaking author identifier (e.g. iTunes tags, recorder name) | mitigate | `afconvert -f caff -d LEI16@44100 -c 1` produces a clean CAF without ID3-equivalent tags. Document in LICENSE.md. |
| T-05-05 | Repudiation | License attribution mismatch (e.g. claiming CC0 for a CC-BY source) | mitigate | LICENSE.md lists Source + License per file; user-action checkpoint requires those columns be filled before resume. Provides audit trail at App Store submission (P7). |
| T-05-06 | Tampering | AHAP JSON malformed → CHHapticPattern init throws at runtime | mitigate | JSON validity verified via `python3 -m json.tool` post-creation; runtime parse validated by Plan 03 HapticsTests. Failure path is non-fatal (logged + silent no-op per CONTEXT D-11). |
</threat_model>

<verification>
- All 5 NEW asset files exist in their respective `Resources/Audio/` and `Resources/Haptics/` folders
- AHAP JSON validates via `python3 -m json.tool` (Task 1 automated)
- CAF files conform to D-08 format spec (Task 3 checkpoint — `afinfo` confirms 16-bit / 44.1kHz / mono)
- `xcodebuild build -scheme gamekit` still succeeds (auto-registration via Xcode 16 PBXFileSystemSynchronizedRootGroup confirmed)
- `Resources/Audio/LICENSE.md` provenance complete (Source + License filled for all 3 CAF entries)
- The new folders are visible to Xcode without any `project.pbxproj` edits (CLAUDE.md §8.8 validation)
</verification>

<success_criteria>
1. `Bundle.main.url(forResource: "tap" / "win" / "loss", withExtension: "caf")` returns non-nil at runtime (gates Plan 03 SFXPlayerTests).
2. `Bundle.main.url(forResource: "win" / "loss", withExtension: "ahap")` returns non-nil at runtime (gates Plan 03 HapticsTests).
3. `CHHapticPattern(contentsOf: <ahap-url>)` does not throw on either file (validated in Plan 03 HapticsTests).
4. `Resources/Audio/LICENSE.md` provenance is complete and ready for the P7 App Store privacy nutrition review.
5. The build succeeds without any `project.pbxproj` modifications (confirms CLAUDE.md §8.8 auto-registration for new sibling folders under top-level `Resources/`).
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-02-SUMMARY.md` covering:
- Asset list with byte sizes and license attribution
- AHAP JSON validation results
- afinfo output for the three CAF files (proves 16-bit / 44.1kHz / mono)
- Confirmation that the build succeeded with NO `project.pbxproj` diff (CLAUDE.md §8.8 evidence)
- Any deviation from D-07 / D-08 spec (none expected — these are locked literals)
</output>
