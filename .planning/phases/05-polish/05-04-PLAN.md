---
phase: 05-polish
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - gamekit/gamekit/Screens/SettingsView.swift
  - gamekit/gamekit/Screens/FullThemePickerView.swift
  - gamekit/gamekit/Resources/Localizable.xcstrings
autonomous: true
requirements: [SHELL-02, THEME-03, A11Y-01]
must_haves:
  truths:
    - "Settings APPEARANCE section renders 5 Classic preset swatches inline + 'More themes & custom colors' NavigationLink (D-14)"
    - "Settings AUDIO section renders 2 system Toggle rows bound to settingsStore.hapticsEnabled and .sfxEnabled (D-15)"
    - "Settings DATA section preserved BYTE-IDENTICAL from P4 (D-16)"
    - "Settings ABOUT section renders Version (mono) / Privacy (inline disclosure) / Acknowledgments (NavigationLink) (D-17)"
    - "Section order is APPEARANCE → AUDIO → DATA → ABOUT (D-13)"
    - "FullThemePickerView renders DKThemePicker(catalog: .all) inside a themed ScrollView"
    - "AUDIO toggles expose their label string to VoiceOver (A11Y-02): VoiceOver reads 'Haptics, switch button, on/off' and 'Sound effects, switch button, on/off' per UI-SPEC line 174-175"
  artifacts:
    - path: "gamekit/gamekit/Screens/SettingsView.swift"
      provides: "Rebuilt SettingsView with all 4 sections + new SettingsToggleRow + AcknowledgmentsView (file-private)"
      contains: "audioSection"
    - path: "gamekit/gamekit/Screens/FullThemePickerView.swift"
      provides: "NavigationLink destination wrapping DKThemePicker(catalog: .all)"
      contains: "DKThemePicker"
    - path: "gamekit/gamekit/Resources/Localizable.xcstrings"
      provides: "P5 Settings strings auto-extracted"
  key_links:
    - from: "Screens/SettingsView.swift"
      to: "Core/SettingsStore.swift via @Environment(\\.settingsStore)"
      via: "Toggle isOn binding to settingsStore.hapticsEnabled / .sfxEnabled"
      pattern: "Bindable\\(settingsStore\\)\\.(hapticsEnabled|sfxEnabled)"
    - from: "Screens/SettingsView.swift APPEARANCE section"
      to: "Screens/FullThemePickerView.swift"
      via: "NavigationLink destination"
      pattern: "NavigationLink.*FullThemePickerView"
    - from: "Screens/SettingsView.swift SettingsToggleRow"
      to: "VoiceOver runtime label"
      via: "Toggle(label, isOn:) — visible label hidden via .labelsHidden() but VoiceOver still reads the string passed to the label parameter"
      pattern: "Toggle\\(label, isOn: \\$isOn\\)"
---

<objective>
Rebuild the Settings spine: replace P1 stub APPEARANCE + ABOUT sections, add a new AUDIO section between APPEARANCE and DATA, ship the FullThemePickerView NavigationLink destination, and preserve the P4 DATA section verbatim. Auto-extract all new strings into Localizable.xcstrings.

Purpose: Closes SHELL-02 (Settings spine) and the entry point for THEME-03 (FullThemePickerView is the user surface where ThemeManager.overrides becomes editable via DKThemePicker's built-in custom-color UI). Wires the Wave 1 SettingsStore flags (hapticsEnabled / sfxEnabled) to actual user-facing controls.

Output: 1 EDIT to SettingsView.swift (rebuild 3 sections, add 1 file-private struct + 1 file-private destination view), 1 NEW FullThemePickerView.swift, 1 EDIT to Localizable.xcstrings (auto-extracted via SWIFT_EMIT_LOC_STRINGS=YES).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-RESEARCH.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@CLAUDE.md
@gamekit/gamekit/Screens/SettingsView.swift
@gamekit/gamekit/Screens/SettingsComponents.swift
@gamekit/gamekit/Screens/StatsView.swift

<interfaces>
P1 stub APPEARANCE (lines 113-123 of current SettingsView.swift) — REPLACE.
P4 DATA section divider pattern: `Rectangle().fill(theme.colors.border).frame(height: 1)` between rows.
P4 SettingsActionRow private struct (lines 217-239) — copy shape for new SettingsToggleRow but with system Toggle.
settingsNavRow helper from SettingsComponents.swift — used unchanged for the chevron-trailing rows.
DKThemePicker init signature: `DKThemePicker(themeManager:theme:scheme:catalog:maxGridHeight:grouped:)`.
PresetCatalog filters: `.core` (5 Classic) and `.all` (34 across 6 categories).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FullThemePickerView.swift (NavigationLink destination)</name>
  <read_first>
    - gamekit/gamekit/Screens/StatsView.swift (analog — NavigationStack-free destination + theme env-read pattern)
    - .planning/phases/05-polish/05-CONTEXT.md (D-14)
    - .planning/phases/05-polish/05-PATTERNS.md ("Screens/FullThemePickerView.swift (NEW)" section)
    - .planning/phases/05-polish/05-UI-SPEC.md (Layout & Sizing — FullThemePickerView root)
    - ../DesignKit/Sources/DesignKit/Components/DKThemePicker.swift (init signature)
    - ../DesignKit/Sources/DesignKit/Theme/PresetCatalog.swift (.all filter)
  </read_first>
  <files>gamekit/gamekit/Screens/FullThemePickerView.swift (NEW)</files>
  <action>
    Create `gamekit/gamekit/Screens/FullThemePickerView.swift` (~70 lines).

    File body:

    ```swift
    //
    //  FullThemePickerView.swift
    //  gamekit
    //
    //  P5 (D-14, SHELL-02, THEME-03): NavigationLink destination from Settings APPEARANCE.
    //  Renders the full DKThemePicker(catalog: .all) including DesignKit's built-in
    //  custom-color editor — the user surface for ThemeManager.overrides per D-25.
    //  Pushed onto Settings' NavigationStack — does NOT own its own stack.
    //

    import SwiftUI
    import DesignKit

    struct FullThemePickerView: View {
        @EnvironmentObject private var themeManager: ThemeManager
        @Environment(\.colorScheme) private var colorScheme

        private var theme: Theme { themeManager.theme(using: colorScheme) }

        var body: some View {
            ScrollView {
                VStack(alignment: .leading, spacing: theme.spacing.l) {
                    DKThemePicker(
                        themeManager: themeManager,
                        theme: theme,
                        scheme: colorScheme,
                        catalog: PresetCatalog.all,
                        maxGridHeight: nil,
                        grouped: true
                    )
                }
                .padding(theme.spacing.l)
            }
            .background(theme.colors.background.ignoresSafeArea())
            .navigationTitle(String(localized: "More themes"))
            .navigationBarTitleDisplayMode(.inline)
        }
    }
    ```

    Key constraints (locked by PATTERNS + UI-SPEC):
    - **No NavigationStack here** — parent SettingsView owns one (PATTERNS line 497).
    - **Theme env-read pattern** identical to StatsView (PATTERNS Pattern C).
    - **`grouped: true`** for the full catalog (UI-SPEC §Layout — full picker shows category headings; the inline 5-swatch in APPEARANCE uses `grouped: false`).
    - **`maxGridHeight: nil`** so the embedded picker uses its intrinsic height inside the outer ScrollView.
    - **`.navigationBarTitleDisplayMode(.inline)`** per UI-SPEC §Layout — less chrome than `.large`.
    - **String(localized: "More themes")** for the navigation title (auto-extracted).
    - Zero `Color(...)` literals, zero hardcoded paddings — all reads `theme.{spacing,colors}` (FOUND-07 hook compliance).

    DO NOT add a back button, custom toolbar items, or any other chrome. The system NavigationStack handles the back button automatically.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15' 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `gamekit/gamekit/Screens/FullThemePickerView.swift` exists
    - `grep "struct FullThemePickerView: View" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "DKThemePicker" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "PresetCatalog.all" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "grouped: true" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "maxGridHeight: nil" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "navigationBarTitleDisplayMode(.inline)" gamekit/gamekit/Screens/FullThemePickerView.swift` matches
    - `grep "NavigationStack" gamekit/gamekit/Screens/FullThemePickerView.swift` returns NO matches (parent owns the stack)
    - `grep -E "Color\\(|cornerRadius:|padding\\([0-9]" gamekit/gamekit/Screens/FullThemePickerView.swift` returns NO matches (FOUND-07 hook compliance)
    - `wc -l gamekit/gamekit/Screens/FullThemePickerView.swift | awk '{print ($1 < 100)}'` returns 1
    - `xcodebuild build -scheme gamekit` succeeds
  </acceptance_criteria>
  <done>FullThemePickerView compiles, renders DKThemePicker(.all) with grouped category sections, no NavigationStack ownership, FOUND-07 hook clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SettingsView APPEARANCE + AUDIO + ABOUT sections (DATA preserved verbatim) + xcstrings sync</name>
  <read_first>
    - gamekit/gamekit/Screens/SettingsView.swift (entire file — additive + section-replacement edits)
    - gamekit/gamekit/Screens/SettingsComponents.swift (settingsSectionHeader + settingsNavRow helpers)
    - .planning/phases/05-polish/05-CONTEXT.md (D-13 through D-17 — section order, content per section)
    - .planning/phases/05-polish/05-PATTERNS.md ("Screens/SettingsView.swift (EDIT)" section — full edit map)
    - .planning/phases/05-polish/05-UI-SPEC.md (Component Inventory + Copywriting Contract for Settings AUDIO + ABOUT; lines 174-175 lock the VoiceOver phrase "Haptics/Sound effects, switch button, on/off")
    - .planning/phases/04-stats-persistence/04-05-SUMMARY.md (xcstringstool sync command lock)
  </read_first>
  <files>
    - gamekit/gamekit/Screens/SettingsView.swift (EDIT — 3 section rebuilds + 2 new file-private structs)
    - gamekit/gamekit/Resources/Localizable.xcstrings (EDIT — auto-extracted P5 strings via xcstringstool sync)
  </files>
  <action>
    Open `gamekit/gamekit/Screens/SettingsView.swift`. Apply these edits in order. P4 DATA section (lines 126-162 — Export / Import / Reset rows + Reset confirmation alert) MUST remain BYTE-IDENTICAL per CONTEXT D-16.

    **STEP 1 — Section order in `body` `VStack` (CONTEXT D-13):**

    Reorder so the section composition reads `appearanceSection → audioSection → dataSection → aboutSection`. The vertical gap between sections uses `theme.spacing.xl` (UI-SPEC §Spacing — already in P4 layout, preserve).

    **STEP 2 — APPEARANCE section (REPLACE P1 stub at lines 113-123):**

    Replace the existing `appearanceSection` `@ViewBuilder` body with a `DKCard` containing a 5-swatch `DKThemePicker(catalog: .core, grouped: false)`, a 1pt `theme.colors.border` divider, and a `NavigationLink(destination: FullThemePickerView())` wrapped around `settingsNavRow(theme:title:)` with title `String(localized: "More themes & custom colors")`. Use `.buttonStyle(.plain)` on the NavigationLink. Per CONTEXT D-14 + CLAUDE.md §2 theme-picker UX convention.

    **STEP 3 — AUDIO section (NEW — between APPEARANCE and DATA):**

    Add a new `@ViewBuilder private var audioSection: some View` containing `settingsSectionHeader(theme: theme, String(localized: "AUDIO"))` followed by a `DKCard(theme: theme)` whose body is a `VStack(spacing: 0)` of: SettingsToggleRow(glyph: `iphone.radiowaves.left.and.right`, label: `String(localized: "Haptics")`, isOn: `Bindable(settingsStore).hapticsEnabled`); 1pt `theme.colors.border` divider; SettingsToggleRow(glyph: `speaker.wave.2.fill`, label: `String(localized: "Sound effects")`, isOn: `Bindable(settingsStore).sfxEnabled`).

    Per CONTEXT D-15 + UI-SPEC §Copywriting AUDIO surfaces. SF Symbols locked: `iphone.radiowaves.left.and.right` (Haptics) + `speaker.wave.2.fill` (Sound effects). `Bindable(settingsStore)` produces Bindings for the @Observable store.

    **STEP 4 — DATA section (NO CHANGES — preserve P4 verbatim per CONTEXT D-16):**

    Hard constraint: do NOT touch `dataSection` lines 126-162. Verify post-edit that `git diff gamekit/gamekit/Screens/SettingsView.swift` shows ZERO changes inside the `dataSection` `@ViewBuilder` body.

    **STEP 5 — ABOUT section (REPLACE P1 stub at lines 164-174):**

    Replace `aboutSection` `@ViewBuilder` with three rows in a `DKCard { VStack(spacing: 0) { ... } }`:

    1. **Version row**: `HStack { Text(String(localized: "Version")).font(theme.typography.body).foregroundStyle(theme.colors.textPrimary); Spacer(); Text(versionDisplay).font(theme.typography.monoNumber).monospacedDigit().foregroundStyle(theme.colors.textSecondary) }.frame(minHeight: 44)`.
    2. 1pt divider.
    3. **Privacy row** (inline disclosure per UI-SPEC option A): a `Button { withAnimation(.easeInOut(duration: theme.motion.fast)) { isPrivacyExpanded.toggle() } } label: { HStack { Text("Privacy"); Spacer(); Image(systemName: isPrivacyExpanded ? "chevron.up" : "chevron.down").foregroundStyle(theme.colors.textTertiary) }.frame(minHeight: 44).contentShape(Rectangle()) }.buttonStyle(.plain)`. When `isPrivacyExpanded == true`, render `Text(String(localized: "All data stored locally. CloudKit sync optional. No analytics. No tracking.")).font(theme.typography.body).foregroundStyle(theme.colors.textSecondary).frame(maxWidth: .infinity, alignment: .leading).padding(.bottom, theme.spacing.s)`.
    4. 1pt divider.
    5. **Acknowledgments row**: `NavigationLink(destination: AcknowledgmentsView()) { settingsNavRow(theme: theme, title: String(localized: "Acknowledgments")) }.buttonStyle(.plain)`.

    Add `private var versionDisplay: String` computed property reading `Bundle.main.infoDictionary["CFBundleShortVersionString"]` and `["CFBundleVersion"]`, returning `"\(version) (\(build))"` with fallbacks `"1.0"` / `"1"`.

    Add `@State private var isPrivacyExpanded: Bool = false` near the other @State declarations at the top of `SettingsView`.

    Note on `padding(.bottom, theme.spacing.s)`: this IS token-driven (`theme.spacing.s`), not a hardcoded integer. The pre-commit hook rejects `padding(<int>)` patterns — `padding(.bottom, theme.spacing.s)` passes (the integer-literal pattern is what's banned, not all `.padding()` calls).

    **STEP 6 — Add file-private `SettingsToggleRow` struct** (mirror `SettingsActionRow` shape from lines 217-239):

    ```swift
    private struct SettingsToggleRow: View {
        let theme: Theme
        let glyph: String
        let label: String
        @Binding var isOn: Bool

        var body: some View {
            HStack(spacing: theme.spacing.s) {
                Image(systemName: glyph)
                    .foregroundStyle(theme.colors.textTertiary)
                Text(label)
                    .font(theme.typography.body)
                    .foregroundStyle(theme.colors.textPrimary)
                Spacer()
                Toggle(label, isOn: $isOn)
                    .labelsHidden()
                    .tint(theme.colors.accentPrimary)
            }
            .frame(minHeight: 44)
            .contentShape(Rectangle())
        }
    }
    ```

    **A11Y CRITICAL (A11Y-02 + UI-SPEC line 174-175 lock):** `Toggle(label, isOn: $isOn)` — NOT `Toggle("", isOn: $isOn)`. The `label` parameter is what `.labelsHidden()` HIDES VISUALLY but is what VoiceOver READS. Passing `""` produces "switch button, on" with no name; passing `label` produces "Haptics, switch button, on" / "Sound effects, switch button, on" matching UI-SPEC §A11y rows 174-175 verbatim and the Plan 07 SC5 VoiceOver expectation table. The leading visible `Text(label)` already shows the label sighted-user-side, so `.labelsHidden()` keeps the system Toggle from rendering its own duplicate label — no visual change vs. passing `""`, only the a11y string differs. This satisfies A11Y-02 (VoiceOver labels on buttons) without manual `.accessibilityLabel(...)` overrides.

    Per UI-SPEC §Component Inventory + §Color line 103 (Toggle `.tint(theme.colors.accentPrimary)` recommended; locks accent reservation site #5).

    **STEP 7 — Add file-private destination `AcknowledgmentsView`** at the end of the file:

    Reads theme via `@EnvironmentObject themeManager` + `@Environment(\.colorScheme)`. Body is a `ScrollView { VStack(alignment: .leading, spacing: theme.spacing.m) { ... } }` containing three `Text` lines:
    - `Text(String(localized: "DesignKit · own work")).font(theme.typography.body).foregroundStyle(theme.colors.textPrimary)`
    - `Text(String(localized: "SF Symbols · Apple Inc.")).font(theme.typography.body).foregroundStyle(theme.colors.textPrimary)`
    - `Text(String(localized: "GameKit is built with care, by hand, with no telemetry, no ads, and no third-party dependencies beyond the above.")).font(theme.typography.caption).foregroundStyle(theme.colors.textSecondary)`

    Outer modifiers: `.padding(theme.spacing.l)`, `.background(theme.colors.background.ignoresSafeArea())`, `.navigationTitle(String(localized: "Acknowledgments"))`, `.navigationBarTitleDisplayMode(.inline)`. Per UI-SPEC §Copywriting ABOUT (3 credit lines verbatim).

    **STEP 8 — xcstrings sync.**

    Build the project with `SWIFT_EMIT_LOC_STRINGS=YES` (already enabled per FOUND-05). All new `String(localized:)` keys auto-extract to `.stringsdata` files in the build output. Run the existing P4 `xcstringstool sync` workflow per `04-05-SUMMARY.md` lock — populates `Localizable.xcstrings` with new entries and removes orphaned automatic entries. Open the catalog in Xcode and verify zero stale entries flagged. New EN keys to expect: `AUDIO`, `Haptics`, `Sound effects`, `More themes & custom colors`, `More themes`, `Version`, `Privacy`, `Acknowledgments`, the privacy body sentence, the three Acknowledgments lines.

    **STEP 9 — File-size sanity check.**

    Current SettingsView.swift is ~240 lines; new content adds ~120 lines (audioSection + SettingsToggleRow struct + rebuilt aboutSection + AcknowledgmentsView struct + isPrivacyExpanded state + versionDisplay) → ~360 lines total. Under CLAUDE.md §8.1 ~400 line soft cap and well under §8.5 500-line hard cap. If actual count exceeds 400, extract `AcknowledgmentsView` to a sibling file `Screens/AcknowledgmentsView.swift` (per UI-SPEC §Component Inventory line 232 fallback).

    **STEP 10 — FOUND-07 hook smoke check.**

    Run `bash .githooks/pre-commit` (or equivalent — the hook scans `Games/` and `Screens/` for `Color(`, hardcoded `cornerRadius:`, hardcoded `padding(<int>)`, and `* 2.swift` files). The hook MUST exit 0 against the new SettingsView.swift content.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15' 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `gamekit/gamekit/Screens/SettingsView.swift` contains `private var audioSection: some View`
    - `grep "iphone.radiowaves.left.and.right" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "speaker.wave.2.fill" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "Bindable(settingsStore).hapticsEnabled" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "Bindable(settingsStore).sfxEnabled" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "private struct SettingsToggleRow" gamekit/gamekit/Screens/SettingsView.swift` matches
    - **A11Y-02 lock:** `grep -F 'Toggle(label, isOn: $isOn)' gamekit/gamekit/Screens/SettingsView.swift` matches (Toggle receives the label string for VoiceOver per UI-SPEC line 174-175)
    - **A11Y-02 negative check:** `grep -F 'Toggle("", isOn:' gamekit/gamekit/Screens/SettingsView.swift` returns ZERO matches (empty-string Toggle label is the bug fixed by this revision; would produce "switch button, on" with no name in VoiceOver and fail Plan 07 SC5)
    - `grep -F '.labelsHidden()' gamekit/gamekit/Screens/SettingsView.swift` matches (visual label still suppressed; only the a11y string is exposed via the label parameter)
    - `grep "private struct AcknowledgmentsView" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "FullThemePickerView()" gamekit/gamekit/Screens/SettingsView.swift` matches (NavigationLink destination wired)
    - `grep "PresetCatalog.core" gamekit/gamekit/Screens/SettingsView.swift` matches (5-swatch inline picker)
    - `grep "grouped: false" gamekit/gamekit/Screens/SettingsView.swift` matches (5-swatch inline picker is single row)
    - `grep "private var versionDisplay" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "isPrivacyExpanded" gamekit/gamekit/Screens/SettingsView.swift` matches
    - `grep "monospacedDigit()" gamekit/gamekit/Screens/SettingsView.swift` matches (Version row)
    - P4 DATA section preserved verbatim: `git diff gamekit/gamekit/Screens/SettingsView.swift -- ` shows no changes inside the `dataSection` body (verify by extracting the dataSection block from HEAD~ and the new file, byte-comparing)
    - `grep -E "Color\\(|cornerRadius:" gamekit/gamekit/Screens/SettingsView.swift` returns no matches (FOUND-07 hook clean)
    - `grep -E "padding\\(\\s*[0-9]+\\s*\\)" gamekit/gamekit/Screens/SettingsView.swift` returns no matches (no integer-literal padding)
    - `wc -l gamekit/gamekit/Screens/SettingsView.swift | awk '{print ($1 < 500)}'` returns 1 (under hard cap)
    - `xcodebuild build -scheme gamekit` succeeds
    - `gamekit/gamekit/Resources/Localizable.xcstrings` contains the new EN keys: `AUDIO`, `Haptics`, `Sound effects`, `More themes & custom colors`, `More themes`, `Version`, `Privacy`, `Acknowledgments` (verify via `grep '"AUDIO"\|"Haptics"\|"Sound effects"' gamekit/gamekit/Resources/Localizable.xcstrings`)
    - Section order in `body` is APPEARANCE → AUDIO → DATA → ABOUT (verify by extracting the order of section method calls inside the body VStack)
  </acceptance_criteria>
  <done>SettingsView rebuild ships APPEARANCE/AUDIO/DATA/ABOUT in the right order; AUDIO toggles round-trip to SettingsStore AND expose their label string to VoiceOver per A11Y-02; DATA section byte-identical to P4; xcstrings populated with new EN keys; FOUND-07 hook clean; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input↔SettingsStore | Toggle changes flow through Bindable to @Observable @MainActor store; iOS handles touch input safely |
| Bundle.infoDictionary↔Version display | Read-only system bundle metadata; no PII |
| FullThemePickerView↔ThemeManager.overrides | DesignKit's custom-color UI writes to the existing override pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | Privacy row inline copy claims behavior the app might violate | mitigate | Copy is "All data stored locally. CloudKit sync optional. No analytics. No tracking." — every claim is verifiable: SwiftData local-only by default per P4 SC1 (CloudKit gated by D-08 cloudSyncEnabled=false default), no analytics SDKs imported (verify by `grep -rE "import (Firebase|Mixpanel|Amplitude)"`), no telemetry endpoints (verify by `grep -rE "URLSession|fetch\("`). |
| T-05-12 | Tampering | Version display showing wrong build number after release | mitigate | `Bundle.main.infoDictionary["CFBundleVersion"]` is the live build number — auto-updates on each Xcode build per FOUND-04. Defensive `?? "1"` fallback prevents crash if Info.plist is malformed. |
| T-05-13 | Spoofing | Acknowledgments missing required attribution (e.g. SF Symbols Apple) | mitigate | Three required attribution lines locked in UI-SPEC §Copywriting; tested by `grep "SF Symbols · Apple Inc." gamekit/gamekit/Screens/SettingsView.swift`. Drift caught at App Store review. |
| T-05-17 | Information Disclosure | Toggle a11y label drift — VoiceOver reads "switch button, on" without naming the control (regression risk if `Toggle("", isOn:)` re-introduced) | mitigate | Acceptance criteria locks `grep -F 'Toggle(label, isOn: $isOn)'` matches AND `grep -F 'Toggle("", isOn:'` returns zero. Plan 07 SC5 VoiceOver verification table independently confirms "Haptics, switch button, on" / "Sound effects, switch button, on" reads. Gap caught by checker pre-execution; locked here so it cannot regress. |
</threat_model>

<verification>
- `xcodebuild build -scheme gamekit` succeeds
- All FOUND-07 grep checks clean (no `Color(`, no integer-literal padding/cornerRadius, no `* 2.swift` dupes)
- `xcodebuild test -scheme gamekit` runs the regression suite (Plan 05-01 SettingsStoreFlagsTests + P3+P4 tests) — all pass
- xcstrings catalog has the new EN keys; no stale entries flagged
- A11Y-02 a11y check: `grep -F 'Toggle(label, isOn: $isOn)' gamekit/gamekit/Screens/SettingsView.swift` matches AND `grep -F 'Toggle("", isOn:' gamekit/gamekit/Screens/SettingsView.swift` returns zero (revision-locked)
- Manual smoke (during Plan 07 verification, not here): launch app → Settings tab → see APPEARANCE/AUDIO/DATA/ABOUT in order; tap "More themes & custom colors" → FullThemePickerView pushes; toggle Haptics + Sound effects → re-launch app → toggle states persist; **VoiceOver swipe over Haptics row reads "Haptics, switch button, on/off" (NOT "switch button, on/off" alone)**
- P4 DATA section diff is empty (preserve invariant proven)
</verification>

<success_criteria>
1. SettingsView ships APPEARANCE (5-swatch + nav link) → AUDIO (2 toggles) → DATA (P4 verbatim) → ABOUT (Version mono / Privacy inline-disclosure / Acknowledgments link) in that exact order.
2. Both AUDIO toggles round-trip to `settingsStore.hapticsEnabled` / `settingsStore.sfxEnabled` (proven by Plan 05-01 SettingsStoreFlagsTests staying green).
3. Both AUDIO toggles expose their label string to VoiceOver via `Toggle(label, isOn:)` + `.labelsHidden()` — VoiceOver reads "Haptics, switch button, on/off" and "Sound effects, switch button, on/off" per UI-SPEC line 174-175 (A11Y-02 lock).
4. FullThemePickerView renders DKThemePicker(catalog: .all, grouped: true) inside SettingsView's NavigationStack — verified by build + manual nav.
5. AcknowledgmentsView renders 3 credit lines with correct typography tokens.
6. Localizable.xcstrings populated with new EN keys; no stale entries; build with SWIFT_EMIT_LOC_STRINGS=YES warns zero.
7. Pre-commit hook (FOUND-07) passes against the new content.
8. P4 DATA section byte-identical (zero diff inside the dataSection body).
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-04-SUMMARY.md` covering:
- Files added/edited with line counts (especially: new SettingsView.swift line count vs CLAUDE.md §8.1 400-line soft cap)
- Build + regression test results
- Confirmation that DATA section diff is empty (P4 verbatim preserved)
- Confirmation that `Toggle(label, isOn: $isOn)` is in place and `Toggle("", isOn:)` returns zero matches (A11Y-02 revision lock)
- xcstrings new key list (8-10 keys expected)
- Section order proof (extract from `body` source)
- Lock the navigation graph for Plan 05/06: "RootTabView → SettingsView → FullThemePickerView (NavigationLink) AND RootTabView → SettingsView → AcknowledgmentsView (NavigationLink)"
</output>
</output>
