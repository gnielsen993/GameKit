---
phase: 05-polish
plan: 07
type: execute
wave: 4
depends_on: [05-04, 05-05, 05-06]
files_modified:
  - .planning/phases/05-polish/05-VERIFICATION.md
autonomous: false
requirements: [THEME-01, THEME-03, A11Y-01, A11Y-02, A11Y-03, A11Y-04, MINES-08, MINES-09, MINES-10, SHELL-02, SHELL-04]
must_haves:
  truths:
    - "SC1 (animation pass) verified: cascade + flag spring + win sweep + loss shake all timed via theme.motion tokens; near-zero under Reduce Motion"
    - "SC2 (haptics + SFX) verified: 4 haptic events fire under hapticsEnabled=true; AHAP files play; SFX off by default; toggles work"
    - "SC3 (Settings spine + intro) verified: 4 sections in order; intro shows on first launch; not on second"
    - "SC4 (theme matrix + custom palette) verified: Mines UI legible on Forest/Bubblegum/Barbie/Cream/Dracula/Voltage in play AND loss state; ThemeManager.overrides reaches Mines grid"
    - "SC5 (a11y) verified: VoiceOver reads cells + buttons + intro steps; Dynamic Type AX5 scales non-grid text; Reduce Motion produces static end-state"
  artifacts:
    - path: ".planning/phases/05-polish/05-VERIFICATION.md"
      provides: "Manual verification report covering SC1-SC5 with screenshot evidence + gap log"
      contains: "## Theme Matrix Audit"
  key_links:
    - from: "05-VERIFICATION.md"
      to: "Plans 05-01 through 05-06 SUMMARYs"
      via: "Cross-references confirming each SC tied to artifact decisions"
      pattern: "05-0[1-6]-SUMMARY"
---

<objective>
Manual verification checkpoint for Phase 5. Run the full SC1-SC5 audit set: animation pass behavior, haptic + SFX firing under each toggle state, Settings spine + intro flow, 6-preset theme matrix legibility (12 surfaces × 6 presets × 2 states ≈ 72 screenshots — RESEARCH §Summary line 129), THEME-03 custom-palette pipeline smoke, full a11y sweep (VoiceOver + Dynamic Type AX5 + Reduce Motion). Generate `05-VERIFICATION.md` with evidence + gap log.

Purpose: Closes the phase. Without this checkpoint, the polish promise (PROJECT.md "premium feel") is unproven. Any gap surfaced here either gets fixed inline (small) or feeds into a `--gaps` plan-phase (big). This is the final gate before Phase 6 starts.

Output: `.planning/phases/05-polish/05-VERIFICATION.md` with screenshots, observed-vs-expected per SC, and a gap log. ROADMAP P5 marked complete only after gap log is empty (or gaps deferred with documented rationale).

Why a checkpoint plan: every SC requires interactive verification that Claude cannot perform autonomously — touch hardware (50-tap iPhone SE long-press gate is reused in P5 audit posture), eye-test 72 theme combinations, hear haptics + SFX, hear/see VoiceOver output, observe Reduce Motion behavior. Build steps + grep audits run autonomously; visual + sensory gates are user-driven.

Notes for the user: Nyquist VALIDATION.md is INTENTIONALLY ABSENT for this phase (per planning context). Verification proceeds without a Dimension-8 strategy doc. If the gap log identifies coverage holes that warrant a validation strategy retroactively, capture as a P7 / post-MVP follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-RESEARCH.md
@.planning/phases/05-polish/05-UI-SPEC.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-02-SUMMARY.md
@.planning/phases/05-polish/05-03-SUMMARY.md
@.planning/phases/05-polish/05-04-SUMMARY.md
@.planning/phases/05-polish/05-05-SUMMARY.md
@.planning/phases/05-polish/05-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated audit suite — final FOUND-07 grep + full test run + adversarial token check</name>
  <read_first>
    - all 6 prior plan SUMMARYs (05-01 through 05-06)
    - .planning/phases/05-polish/05-RESEARCH.md (§Manual-Only Verifications + §Summary)
  </read_first>
  <files>
    (no files written in this task — pure verification commands; results captured in 05-VERIFICATION.md by Task 2)
  </files>
  <action>
    Run the following automated gates and capture exact output (stdout + exit code) for inclusion in `05-VERIFICATION.md` Task 2:

    **Gate 1 — Adversarial FOUND-07 grep (no Color literals or hardcoded paddings/radii in any P5 file):**
    ```bash
    cd /Users/gabrielnielsen/Desktop/GameKit
    grep -RnE "Color\\(\\." gamekit/gamekit/Games/Minesweeper/ \
      gamekit/gamekit/Screens/IntroFlowView.swift \
      gamekit/gamekit/Screens/FullThemePickerView.swift \
      gamekit/gamekit/Screens/SettingsView.swift \
      gamekit/gamekit/Screens/RootTabView.swift \
      gamekit/gamekit/Core/Haptics.swift \
      gamekit/gamekit/Core/SFXPlayer.swift
    # Expected: zero matches
    grep -RnE "padding\\(\\s*[0-9]+\\s*\\)|cornerRadius:\\s*[0-9]+" gamekit/gamekit/Games/Minesweeper/ gamekit/gamekit/Screens/ gamekit/gamekit/Core/
    # Expected: zero matches
    ```

    **Gate 2 — VM Foundation-only invariant:**
    ```bash
    grep -E "import (SwiftUI|Combine|SwiftData)" gamekit/gamekit/Games/Minesweeper/MinesweeperViewModel.swift
    grep -E "import (SwiftUI|Combine|SwiftData)" gamekit/gamekit/Games/Minesweeper/MinesweeperPhase.swift
    # Expected: zero matches in either file
    ```

    **Gate 3 — Single AVAudioSession setCategory site:**
    ```bash
    grep -rn "setCategory" gamekit/gamekit/
    # Expected: exactly 1 match, in Core/SFXPlayer.swift
    ```

    **Gate 4 — Full test suite green:**
    ```bash
    cd /Users/gabrielnielsen/Desktop/GameKit/gamekit
    xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15' 2>&1 | tail -40
    # Expected: ** TEST SUCCEEDED ** with all suites green:
    # - P2 BoardGeneratorTests, RevealEngineTests, WinDetectorTests
    # - P3 MinesweeperViewModelTests
    # - P4 ModelContainerSmokeTests, GameStatsTests, StatsExporterTests
    # - P5-01 SettingsStoreFlagsTests
    # - P5-03 HapticsTests, SFXPlayerTests
    # - P5-06 MinesweeperPhaseTransitionTests
    ```

    **Gate 5 — File-size caps (CLAUDE.md §8.1 + §8.5):**
    ```bash
    wc -l gamekit/gamekit/Games/Minesweeper/MinesweeperViewModel.swift \
          gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift \
          gamekit/gamekit/Games/Minesweeper/MinesweeperCellView.swift \
          gamekit/gamekit/Games/Minesweeper/MinesweeperGameView.swift \
          gamekit/gamekit/Games/Minesweeper/MinesweeperPhase.swift \
          gamekit/gamekit/Screens/IntroFlowView.swift \
          gamekit/gamekit/Screens/FullThemePickerView.swift \
          gamekit/gamekit/Screens/SettingsView.swift \
          gamekit/gamekit/Screens/RootTabView.swift \
          gamekit/gamekit/Core/Haptics.swift \
          gamekit/gamekit/Core/SFXPlayer.swift \
          gamekit/gamekit/Core/SettingsStore.swift \
          gamekit/gamekit/App/GameKitApp.swift
    # Expected: every file < 500 lines (hard cap §8.5); SettingsView + IntroFlowView ideally < 400 (soft cap §8.1)
    ```

    **Gate 6 — No Finder-dupe `* 2.swift` files:**
    ```bash
    find gamekit/gamekit/ -name "* 2.swift"
    # Expected: zero results
    ```

    **Gate 7 — xcstrings clean:**
    ```bash
    # Open Resources/Localizable.xcstrings in Xcode; verify zero stale (orphaned) entries flagged.
    # Capture screenshot of Xcode catalog editor showing the EN column populated for all P5 keys.
    ```

    **Gate 8 — Bundle assets present at runtime:**
    Build for simulator, install, and run a minimal in-app probe (or insert a temp DEBUG-only assertion in `GameKitApp.init()` that crashes if any of `tap.caf` / `win.caf` / `loss.caf` / `win.ahap` / `loss.ahap` are missing from `Bundle.main`). For verification, simply launch the app — if Plan 03's HapticsTests + SFXPlayerTests passed (Gate 4), bundle inclusion is already proven.

    Capture all output for Task 2's verification report.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit/gamekit && xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15' 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1: zero `Color(.` matches in any P5 file (FOUND-07 hook clean post-implementation)
    - Gate 1: zero integer-literal `padding(N)` or `cornerRadius: N` in `Games/` or `Screens/` or `Core/`
    - Gate 2: VM remains Foundation-only (no SwiftUI/Combine/SwiftData imports in MinesweeperViewModel.swift OR MinesweeperPhase.swift)
    - Gate 3: exactly 1 `setCategory` call across the project (in Core/SFXPlayer.swift)
    - Gate 4: `xcodebuild test` reports `** TEST SUCCEEDED **` with at least these suites green: BoardGeneratorTests, RevealEngineTests, WinDetectorTests, MinesweeperViewModelTests, ModelContainerSmokeTests, GameStatsTests, StatsExporterTests, SettingsStoreFlagsTests, HapticsTests, SFXPlayerTests, MinesweeperPhaseTransitionTests
    - Gate 5: every Swift file under CLAUDE.md §8.5 500-line hard cap; flagged surfaces SettingsView + IntroFlowView under 400-line soft cap
    - Gate 6: zero `* 2.swift` files
    - Gate 7: xcstrings catalog has all P5 EN keys populated; zero stale entries (manual verify in Xcode)
    - Gate 8: bundle asset presence proven by Gate 4 HapticsTests + SFXPlayerTests passing
    - All output captured (stdout + exit codes) for inclusion in Task 2's report
  </acceptance_criteria>
  <done>All 8 automated gates pass; output captured for the manual verification document.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual SC1-SC5 audit — animation, haptics, SFX, themes, accessibility</name>
  <what-built>
    - Plan 05-01: MinesweeperPhase enum + SettingsStore extension + tests
    - Plan 05-02: 3 CAF + 2 AHAP resource files + LICENSE.md
    - Plan 05-03: Core/Haptics.swift + Core/SFXPlayer.swift + GameKitApp wiring + tests
    - Plan 05-04: SettingsView rebuild (APPEARANCE / AUDIO / DATA / ABOUT) + FullThemePickerView
    - Plan 05-05: IntroFlowView + RootTabView .fullScreenCover wiring
    - Plan 05-06: Mines animation pass (VM phase + BoardView cascade + CellView .sensoryFeedback + GameView .phaseAnimator + .keyframeAnimator + .onChange Haptics/SFX)
    - Task 1 (this plan): all 8 automated gates green
  </what-built>
  <how-to-verify>
    Build + install on iPhone 15 simulator AND on a physical iPhone (haptics require hardware — simulator AHAP playback is a no-op per Apple docs). Walk through each SC in order. Take a screenshot at every numbered step. Compile findings into `.planning/phases/05-polish/05-VERIFICATION.md` per the template at the bottom of this section.

    **SC1 — Animation pass verification (MINES-08 + A11Y-03)**

    Default Reduce Motion = OFF. Open Settings → Accessibility → Motion → Reduce Motion → confirm OFF.

    1.1. Launch app → Home → tap Minesweeper card → choose Easy.
    1.2. Tap any cell. **EXPECT:** cascade reveal — cells appear with staggered fade per CONTEXT D-01 (engine-order delay, total ≤250ms). Screenshot mid-cascade if possible.
    1.3. Long-press a hidden cell. **EXPECT:** flag glyph appears with `.symbolEffect(.bounce)` spring per CONTEXT D-04. Screenshot at peak bounce.
    1.4. Long-press the same cell to remove flag. **EXPECT:** bounce again on removal.
    1.5. Play through to a Win. **EXPECT:** win-wash success-tint sweeps the board (alpha 0 → 0.25 → 0 over `theme.motion.slow`). End-state DKCard fades in concurrently. Screenshot at peak wash.
    1.6. Restart, play through to a Loss (intentionally tap a known mine). **EXPECT:** board shakes 3 bumps horizontally per CONTEXT D-03 (+8 → −8 → +4 → 0 over 400ms). All mines reveal; incorrectly-flagged cells X'd. End-state DKCard fades in. Screenshot mid-shake if possible.

    Now toggle Reduce Motion = ON in iOS Settings. Repeat 1.2 through 1.6:
    1.7. Cascade: **EXPECT** all cells reveal simultaneously, no fade.
    1.8. Flag spring: **EXPECT** glyph swap is instant.
    1.9. Win wash: **EXPECT** instant tint at peak (no fade in/out).
    1.10. Loss shake: **EXPECT** no shake (mineHit overlay just appears).

    Record observed-vs-expected per substep.

    **SC2 — Haptics + SFX verification (MINES-09 + MINES-10)**

    Reset Reduce Motion to OFF. Settings → Audio: confirm Haptics ON (default), Sound effects OFF (default).

    2.1. Tap a cell on a hardware device. **EXPECT:** subtle `.selection` haptic per CONTEXT D-07 (gating proven by it firing).
    2.2. Long-press to flag. **EXPECT:** light `.impact` haptic + flag bounce.
    2.3. Win a game. **EXPECT:** AHAP "win" arpeggio (3 ascending transients over 0.6s) per D-07.
    2.4. Lose a game. **EXPECT:** AHAP "loss" rumble (0.5s continuous-decay + 2 sharp transients) per D-07.
    2.5. Open Settings → Audio → toggle Haptics OFF. Restart and play. **EXPECT:** zero haptics fire (gating-at-source proof).
    2.6. Toggle Haptics ON, Sound effects ON. Play through. **EXPECT:** tap.caf on each reveal, win.caf on win, loss.caf on loss. AVAudioSession.ambient — start playing user music BEFORE launching the game, verify music continues playing through the SFX (no ducking). This proves D-09.
    2.7. Toggle Sound effects OFF, restart. **EXPECT:** zero SFX fires.

    **SC3 — Settings spine + intro verification (SHELL-02 + SHELL-04)**

    Fresh-install scenario:

    3.1. Delete the app from device → reinstall fresh build → launch. **EXPECT:** IntroFlowView appears via `.fullScreenCover`.
    3.2. Verify Step 1 "Make it yours": title at theme.typography.titleLarge, body copy, 5 Classic swatches read-only (taps = no-op per D-19), Continue button bottom-trailing, Skip top-trailing.
    3.3. Tap Continue → Step 2 "Track your progress": hand-coded sample stats card (Easy 12/8/67%/1:42 ; Medium 5/2/40%/4:15 ; Hard —).
    3.4. Tap Continue → Step 3 "Sync across devices": SIWA button + Skip below in DKCard. Tap SIWA → **EXPECT** the system sheet opens then dismisses immediately (or simply nothing happens visibly per D-21 no-op posture). Verify a log entry in Console.app under category=auth.
    3.5. Tap Skip top-trailing on any step. **EXPECT:** cover dismisses; tab bar visible.
    3.6. Cold-relaunch the app. **EXPECT:** intro does NOT show (hasSeenIntro persisted).
    3.7. Delete app, reinstall, this time tap through Continue/Continue/Done. **EXPECT:** Done dismisses identically to Skip; relaunch — no intro.

    Settings spine:

    3.8. Settings tab → verify section order APPEARANCE → AUDIO → DATA → ABOUT.
    3.9. APPEARANCE: 5 Classic swatches inline; tap "More themes & custom colors" → FullThemePickerView pushes; verify all 34 presets + Custom panel; back-arrow returns.
    3.10. AUDIO: Haptics + Sound effects toggles round-trip (toggle, kill app, relaunch, verify state preserved).
    3.11. DATA: P4 Export / Import / Reset rows render unchanged (regression check — open Reset alert, cancel; do NOT tap Reset).
    3.12. ABOUT: Version row shows correct release + build (e.g. "1.0 (1)"); Privacy row tap toggles inline-disclosure body copy; Acknowledgments NavigationLink → AcknowledgmentsView with 3 credit lines.

    **SC4 — Theme matrix + custom palette verification (THEME-01 + THEME-03 + A11Y-04)**

    For each of the 6 audit-set presets — `forest` (Classic) · `bubblegum` (Sweet) · `barbie` (Bright) · `cream` (Soft) · `dracula` (Moody) · `voltage` (Loud) — perform play AND loss state captures (UI-SPEC §Color audit):

    4.1. Set Settings → APPEARANCE preset to the test preset.
    4.2. Open Mines → start Easy. Screenshot the play state (cells partially revealed, mine counter, timer, flag glyph visible if possible).
    4.3. Lose intentionally. Screenshot the loss state (all mines revealed, X on wrong flag, end-state DKCard with `theme.colors.danger` title).
    4.4. Repeat 4.2 / 4.3 for Win state too (one preset is enough for win-wash demo since the wash color is `theme.colors.success` across all presets — Voltage and Barbie are the litmus presets per UI-SPEC §Color).
    4.5. Verify per-preset legibility: numbered cells distinct from background; flag color distinct from mine; X overlay distinct from the cell tile; end-state DKCard text contrast adequate.

    Total: 6 presets × 3 states (play / loss / one win) = ~18 screenshots minimum. Per RESEARCH §Summary line 129: "12 surfaces × 6 presets = 72 screenshots, half play state and half loss state per ROADMAP SC4 verbatim." Capture the spec-required count if hardware permits.

    THEME-03 custom palette pipeline (D-25/D-26):

    4.6. Settings → APPEARANCE → "More themes & custom colors" → tap "Custom" tab inside DKThemePicker.
    4.7. Override `accentPrimary` to a vivid non-default color (e.g. magenta).
    4.8. Back to Mines → Restart. Screenshot. **EXPECT:** Restart button background tint, mine flag color, header timer color all honor the magenta override per UI-SPEC §THEME-03 verification list. Numbered cells (gameNumber palette) DO NOT inherit `accentPrimary` (this is correct per UI-SPEC line 124).
    4.9. Reset overrides via the picker; verify default colors return.

    **SC5 — Accessibility verification (A11Y-01 + A11Y-02 + A11Y-03)**

    5.1. iOS Settings → Accessibility → Display & Text Size → Larger Text → drag to AX5. Open the app. Verify each surface listed in CONTEXT D-27 scales without layout breakage:
    - HomeView card labels
    - Settings rows
    - StatsView per-difficulty rows
    - Settings ABOUT (Version / Privacy / Acknowledgments)
    - IntroFlowView all 3 steps (delete app, reinstall to re-trigger)
    - end-state DKCard title/body
    - HeaderBar mine counter + timer
    Verify the Mines grid stays fixed-size (D-28 carve-out — cell digits do NOT scale with Dynamic Type).

    5.2. iOS Settings → Accessibility → VoiceOver ON. Open Mines → Easy.
    - Triple-tap to focus cells; **EXPECT** "Unrevealed, row N column M" / "Revealed, X mines adjacent, row N column M" / "Flagged, row N column M" per P3 a11y label format (preserved from P3).
    - Buttons: Restart, ToolbarMenu (difficulty picker), end-state Restart and Change difficulty all read sensibly.
    - Settings: each row reads label + value (Toggle reads "Switch button, on/off"; NavigationLink reads as a button).
    - IntroFlowView: each step reads "Step N of 3. {title}. {body}" per D-24 + UI-SPEC §Copywriting Step container line. Skip/Continue/Done buttons read verbose accessibility labels ("Skip intro" / "Continue to next step" / "Finish intro").

    5.3. Reduce Motion ON during a Win replay → **EXPECT** static end-state overlay with NO shake or sweep (this overlaps SC1 1.7-1.10 above; capture once).

    **Output: write `.planning/phases/05-polish/05-VERIFICATION.md`** using this template:

    ```markdown
    # Phase 5: Polish — Verification Report

    **Date:** {YYYY-MM-DD}
    **Build:** {git short SHA}
    **Devices:** iPhone 15 Simulator + {physical device name and iOS version}

    ## Automated Gates (Task 1)

    | Gate | Result | Notes |
    |------|--------|-------|
    | 1. FOUND-07 grep (no Color literals / hardcoded paddings) | PASS / FAIL | ... |
    | 2. VM Foundation-only invariant | PASS / FAIL | ... |
    | 3. Single setCategory call site | PASS / FAIL | ... |
    | 4. Full test suite green | PASS / FAIL | {test count} |
    | 5. File-size caps | PASS / FAIL | ... |
    | 6. No Finder-dupe files | PASS / FAIL | ... |
    | 7. xcstrings clean | PASS / FAIL | ... |
    | 8. Bundle assets present | PASS / FAIL | ... |

    ## SC1 — Animation pass (MINES-08 + A11Y-03)
    | Step | Expected | Observed | Screenshot |
    |------|----------|----------|------------|
    | 1.2 cascade | ... | ... | sc1-cascade.png |
    | 1.3 flag spring | ... | ... | sc1-flag.png |
    | 1.5 win wash | ... | ... | sc1-win.png |
    | 1.6 loss shake | ... | ... | sc1-loss.png |
    | 1.7-1.10 Reduce Motion | all instant | ... | sc1-rm-{cascade,flag,win,loss}.png |

    ## SC2 — Haptics + SFX (MINES-09 + MINES-10)
    | Step | Expected | Observed | Notes |
    |------|----------|----------|-------|
    | 2.1 tap haptic | ... | ... | hardware device required |
    | ... | | | |

    ## SC3 — Settings spine + intro (SHELL-02 + SHELL-04)
    | Step | Expected | Observed | Screenshot |
    |------|----------|----------|------------|
    | 3.1 first-launch intro | ... | ... | sc3-intro-step1.png |
    | ... | | | |

    ## SC4 — Theme matrix + custom palette (THEME-01 + THEME-03 + A11Y-04)
    ### Theme Matrix Audit (6 presets × 3 states ≈ 18 screenshots)
    | Preset | Play | Loss | Win | Notes |
    |--------|------|------|-----|-------|
    | forest | ✓ sc4-forest-play.png | ✓ sc4-forest-loss.png | ✓ sc4-forest-win.png | |
    | bubblegum | ... | ... | ... | |
    | barbie | ... | ... | ... | inactive page indicator dot legibility (UI-SPEC line 117 risk) |
    | cream | ... | ... | ... | |
    | dracula | ... | ... | ... | |
    | voltage | ... | ... | ... | success-green vs accent-yellow proximity (UI-SPEC line 120) |

    ### THEME-03 custom palette
    Before override: sc4-custom-before.png
    After accentPrimary → magenta: sc4-custom-after.png
    Restart button + flag + header timer honor override: PASS / FAIL

    ## SC5 — Accessibility (A11Y-01 + A11Y-02 + A11Y-03)
    ### Dynamic Type AX5
    | Surface | Scaled cleanly? | Screenshot |
    |---------|-----------------|------------|
    | HomeView cards | PASS / FAIL | sc5-ax5-home.png |
    | Settings rows | ... | ... |
    | StatsView rows | ... | ... |
    | IntroFlowView all 3 steps | ... | ... |
    | end-state DKCard | ... | ... |

    Mines grid stays fixed-size: PASS / FAIL

    ### VoiceOver
    | Element | Reads as | PASS/FAIL |
    |---------|----------|-----------|
    | Cells | "Unrevealed, row X column Y" | ... |
    | Restart button | "Restart" | ... |
    | Settings AUDIO toggles | "Haptics, switch button, on" | ... |
    | IntroFlowView Step 1 on focus | "Step 1 of 3. Make it yours. ..." | ... |

    ### Reduce Motion
    Win replay with RM ON: static overlay, no shake/sweep — PASS / FAIL

    ## Gap Log

    | ID | SC | Severity | Description | Resolution |
    |----|----|----------|-------------|------------|
    | G-1 | ... | major / minor | ... | fix-inline / `--gaps` plan / defer with rationale |

    ## Sign-off

    All SC1-SC5 PASS with the gaps above noted. Phase 5 ready to mark complete in ROADMAP.md.

    Per CONTEXT (Nyquist VALIDATION.md intentionally absent): no Dimension-8 strategy doc was authored for P5. If gap log surfaces patterns suggesting one is needed, capture as P7 follow-up.
    ```
  </how-to-verify>
  <resume-signal>
    Type "verification complete" once `.planning/phases/05-polish/05-VERIFICATION.md` exists, all SC1-SC5 sections are filled with observed-vs-expected, screenshots are attached or file-paths recorded, and the Gap Log either is empty OR every gap is marked with a clear resolution path (fix-inline / `--gaps` plan / defer with documented rationale).

    If Gap Log contains MAJOR severity items: type "gaps found — running --gaps". The orchestrator will then invoke `/gsd-plan-phase --gaps` against this VERIFICATION.md to produce gap-closure plans.

    If all gaps are MINOR / deferred: type "ship phase 5". The orchestrator updates ROADMAP.md to mark Phase 5 complete.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/05-polish/05-VERIFICATION.md` exists
    - All 8 automated gates from Task 1 captured in the Automated Gates table
    - SC1-SC5 sections each have observed-vs-expected per step
    - Theme Matrix Audit covers all 6 audit-set presets with at least play + loss screenshots each
    - THEME-03 custom palette before/after screenshots present
    - VoiceOver section confirms cell + button + intro labels read sensibly
    - Dynamic Type AX5 section confirms all listed non-grid surfaces scale; grid stays fixed
    - Reduce Motion confirmation present
    - Gap Log explicit (even if empty — empty table with "no gaps observed" note is acceptable)
    - Sign-off line present with verifier's identifier (or "self-verified by user")
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification report↔reality | Manual audit is the sole gate for SC1-SC5; no automated runtime validation of theme legibility or animation feel. The audit IS the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Repudiation | Verifier signs off on a SC without actually performing the steps (e.g. checks all PASS without running the app) | accept | Solo developer + Claude workflow per CLAUDE.md introduction. The user IS the verifier. Screenshot evidence + observed-vs-expected fields make false sign-off detectable on read-back. P7 release-checklist provides a second pass. |
| T-05-23 | Spoofing | A theme preset that "looks legible" in simulator is actually unreadable on hardware (e.g. P3 OLED dim) | mitigate | UI-SPEC §Color identifies Voltage + Barbie as litmus presets — explicitly require hardware testing for these two. Captured in audit table notes. |
| T-05-24 | Information Disclosure | VoiceOver reads PII or stats data verbatim during the audit on a shared device | accept | Stats are local-only and personal to the device; no PII in the model. The Privacy row inline copy explicitly states "All data stored locally." |
</threat_model>

<verification>
- All 8 automated gates from Task 1 PASS
- 05-VERIFICATION.md document exists with SC1-SC5 sections filled
- Theme matrix covers 6 presets × at least play+loss states
- THEME-03 custom palette before/after evidence captured
- VoiceOver + Dynamic Type + Reduce Motion sections present
- Gap Log explicit (empty OR every gap has a resolution path)
- Final sign-off triggers either `/gsd-plan-phase --gaps` (if MAJOR gaps) or ROADMAP update (if clean)
</verification>

<success_criteria>
1. SC1 (animation pass) verified — cascade, flag spring, win sweep, loss shake all visible under Reduce Motion=OFF and dampened to instant under Reduce Motion=ON.
2. SC2 (haptics + SFX) verified on hardware — 4 haptic events fire under hapticsEnabled=true; AHAP files play; SFX OFF by default; toggles silence both layers; AVAudioSession.ambient does not duck user music.
3. SC3 (Settings spine + intro) verified — APPEARANCE / AUDIO / DATA / ABOUT in correct order; first-launch intro shows; second launch does not; both Skip and Done dismiss + persist.
4. SC4 (theme matrix + custom palette) verified — Mines UI legible on all 6 audit-set presets in play AND loss state; ThemeManager.overrides reaches Mines grid (Restart button + flag + header timer honor accentPrimary override).
5. SC5 (a11y) verified — Dynamic Type AX5 scales all listed non-grid text; grid stays fixed; VoiceOver reads cell + button + intro labels per spec; Reduce Motion produces static end-state.
6. Gap Log either empty or every gap has a documented resolution path (fix-inline / `--gaps` plan / defer with rationale).
7. ROADMAP.md updated to mark Phase 5 complete (or sub-orchestrator triggers `/gsd-plan-phase --gaps`).
</success_criteria>

<output>
After completion, the `.planning/phases/05-polish/05-VERIFICATION.md` IS the SUMMARY for Plan 07. Additionally, append a brief `.planning/phases/05-polish/05-07-SUMMARY.md` covering:
- Verification verdict: PASS / GAPS-FOUND / DEFERRED-WITH-RATIONALE
- Total screenshot count
- Devices used
- Time-to-verify (rough)
- Final phase status note for STATE.md update by the orchestrator
</output>
