---
phase: 16-stack
plan: 05
type: execute
wave: 2
depends_on: ["16-03", "16-04"]
files_modified:
  - gamekit/gamekit/Games/Stack/StackGameView.swift
  - gamekit/gamekit/Screens/HomeView.swift
  - gamekit/gamekit/Games/Stack/StackHarnessView.swift
autonomous: true
requirements: [STACK-01, STACK-03, STACK-05, STACK-06]
user_setup: []

must_haves:
  truths:
    - "Tapping the board drops the block; the run is fully playable idle -> running -> game-over -> restart"
    - "Score chip AND a visible combo/streak counter render during the run (D-04)"
    - "Perfect-drop fires a distinct medium-impact haptic via perfectCount; normal drop fires light-impact via dropCount; hapticsEnabled is the FIRST guard collapsing the trigger to 0 (05-03 D-10)"
    - "Game-over presents VideoModeBanner(.loss) with final score + restart; the banner fires its own .error haptic (no duplicate)"
    - "Game-over plays a ~0.5s slow-mo + color-drain pre-roll (animations on, RM off); under Reduce Motion it is an instant cut to the banner (D-09, no screen shake ever)"
    - "scenePhase .inactive AND .background both pause the loop; .active resumes; loop is off at game-over (zero CPU)"
    - "Home Stack card launches StackGameView (no .videoModeAware); the throwaway StackHarnessView is deleted"
  artifacts:
    - path: "gamekit/gamekit/Games/Stack/StackGameView.swift"
      provides: "Chrome + lifecycle + arcadeLoop + tap + banner + feedback wiring"
      contains: "StackViewModel"
    - path: "gamekit/gamekit/Screens/HomeView.swift"
      provides: ".stack destination swapped to StackGameView"
      contains: "StackGameView()"
  key_links:
    - from: "StackGameView .arcadeLoop"
      to: "StackViewModel.tick(dt:)"
      via: ".arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt:) }"
      pattern: "arcadeLoop\\(isRunning"
    - from: "StackGameView .sensoryFeedback"
      to: "vm.perfectCount / vm.dropCount"
      via: "hapticsEnabled-first-guard trigger"
      pattern: "hapticsEnabled \\? vm\\."
    - from: "HomeView .stack case"
      to: "StackGameView"
      via: "destination swap (StackHarnessView removed)"
      pattern: "StackGameView\\(\\)"
---

<objective>
Compose `StackGameView` — the chrome + lifecycle shell that hosts `StackBoardCanvas`, drives the arcade loop, wires tap input, presents the idle screen and the `VideoModeBanner` game-over surface, and routes perfect/normal-drop haptics + the game-over slow-mo with full settings/Reduce Motion gating. Then swap the Home destination to `StackGameView` and delete the throwaway `StackHarnessView`.

Purpose: Make Stack fully playable end-to-end on real hardware (SC1), with the §8/§10 feedback design (D-08/D-09) and the Reduce Motion path (STACK-06) wired through the existing toggles.
Output: `Games/Stack/StackGameView.swift`, edited `Screens/HomeView.swift`, deleted `Games/Stack/StackHarnessView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md
@.planning/phases/16-stack/16-03-SUMMARY.md
@.planning/phases/16-stack/16-04-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Analogs: StackHarnessView (loop+scenePhase+toolbar), MergeGameView (GameStats attach, banner, settings env). -->
From StackViewModel (16-03): state, score/streak snapshot, perfectCount, dropCount, pendingDrop (settable),
  tick(dt:), start()/pause()/resume()/stop()/restart(), attachGameStats(_:)
From StackBoardCanvas (16-04): props-only Canvas view taking (snapshot, theme, reduceMotion, alpha)
Core/VideoModeBanner.swift: VideoModeBanner(outcome: .loss, ...) — fires its own .error haptic on appear
  (lines 126-136); takes plain Bool gates (hapticsEnabled/reduceMotion/animationsEnabled, lines 49-51);
  entrance via .videoModeBannerTransition(reduceMotion:animationsEnabled:) (lines 177-189)
Environment: themeManager, colorScheme, scenePhase, modelContext, settingsStore (hapticsEnabled/animationsEnabled),
  accessibilityReduceMotion, dismiss
HomeView .stack case (verified): line 382 `StackHarnessView()`; ADR ARCADE-08 comment at line 378 (keep);
  NO .videoModeAware on this case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StackGameView — chrome, loop, tap, banner, feedback gating</name>
  <files>gamekit/gamekit/Games/Stack/StackGameView.swift</files>
  <read_first>
    - 16-PATTERNS.md "StackGameView.swift" section (lines 101-146) — env, loop, toolbar, banner, haptics, idle
    - 16-RESEARCH.md Pattern 5 feedback table + slow-mo notes (lines 365-376)
    - gamekit/gamekit/Games/Stack/StackHarnessView.swift lines 75-151 (loop + scenePhase + back chevron + idle button)
    - gamekit/gamekit/Games/Merge/MergeGameView.swift (GameStats .task attach with didInjectStats guard ~line 45/128-133; banner; settings env)
    - gamekit/gamekit/Core/VideoModeBanner.swift lines 49-51, 126-136, 177-189
    - DESIGN.md §3 (info/score chip), §8 (haptic vocabulary + counter-trigger + hapticsEnabled FIRST guard), §10.3 (game-over 500ms pre-roll), §12.5 (new-game checklist)
  </read_first>
  <action>
    Create `StackGameView` with the environment + theme setup from StackHarnessView.swift:77-83 + MergeGameView (themeManager, colorScheme, scenePhase, modelContext, settingsStore, accessibilityReduceMotion, dismiss) and `@State private var vm = StackViewModel()`. Base ZStack on `theme.colors.background.ignoresSafeArea()`. Compose: (a) `StackBoardCanvas(...)` reading the VM snapshot, given `reduceMotion` + the accumulator alpha; (b) the chrome overlay — a score chip and a VISIBLE combo/streak counter during the run (D-04), using `theme.typography.*.monospacedDigit()` (DESIGN §3/§4); (c) the idle / tap-to-start screen when `state == .idle` (StackHarnessView.swift:108-114) — every data view ships an explicit idle/empty state (§8.3). Tap input: `.onTapGesture { vm.pendingDrop = true }`. Loop + lifecycle: copy verbatim from StackHarnessView.swift:135-149 — `.arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt: dt) }` and the scenePhase switch where BOTH `.inactive` and `.background` call `vm.pause()` and `.active` calls `vm.resume()`. Back chevron toolbar verbatim (StackHarnessView.swift:120-133); keep `navigationBarBackButtonHidden(true)`. GameStats attach via `.task { ... GameStats(modelContext:) ...; vm.attachGameStats(...) }` with a `didInjectStats` one-shot guard (MergeGameView). Game-over surface: when `state == .gameOver`, present `VideoModeBanner(outcome: .loss, ...)` with final score + restart wired to `vm.restart()`, passing plain Bools (`settingsStore.hapticsEnabled`, `reduceMotion`, `settingsStore.animationsEnabled`); gate the entrance with `.videoModeBannerTransition(reduceMotion:animationsEnabled:)`. The banner fires its own `.error` — do NOT add another game-over haptic. Counter-trigger haptics (hapticsEnabled is the FIRST guard, 05-03 D-10): `.sensoryFeedback(.impact(weight: .medium), trigger: settingsStore.hapticsEnabled ? vm.perfectCount : 0)` (perfect, distinct class per D-08) and `.sensoryFeedback(.impact(weight: .light), trigger: settingsStore.hapticsEnabled ? vm.dropCount : 0)` (normal drop). Game-over slow-mo (D-09): when animations on AND `!reduceMotion`, run the ~500ms view-only slow-mo + tower color-drain pre-roll (DESIGN §10.3) before the banner presents — the loop is already stopped so there is no live sim to slow; do NOT `Task.sleep` purely for delay when animations off / RM on — instant cut to banner. NEVER screen shake (any preset/setting). Keep NO `.videoModeAware()`. Keep file < 250 lines (split a sibling extension if it grows).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'arcadeLoop(isRunning: vm.state == .running)' gamekit/gamekit/Games/Stack/StackGameView.swift && grep -q 'hapticsEnabled ? vm.perfectCount' gamekit/gamekit/Games/Stack/StackGameView.swift && grep -q 'VideoModeBanner' gamekit/gamekit/Games/Stack/StackGameView.swift && test -z "$(grep -n 'videoModeAware' gamekit/gamekit/Games/Stack/StackGameView.swift)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.arcadeLoop(isRunning: vm.state == .running) { dt in vm.tick(dt:) }` present; scenePhase `.inactive` AND `.background` both call `vm.pause()`.
    - Two `.sensoryFeedback` with `settingsStore.hapticsEnabled ? vm.perfectCount/ dropCount : 0` (hapticsEnabled FIRST guard); perfect uses `.medium`, drop uses `.light`.
    - No duplicate game-over haptic (`grep -c "\.error" StackGameView.swift` == 0 — banner owns it).
    - `VideoModeBanner(outcome: .loss` present with restart wired to `vm.restart()`; entrance uses `.videoModeBannerTransition`.
    - Game-over pre-roll guarded by `animationsEnabled && !reduceMotion`; instant-cut path when off/RM. No screen-shake API used.
    - A visible combo/streak counter renders during the run (D-04).
    - No `.videoModeAware` (grep empty). `wc -l` < 250. `xcodebuild build` succeeds.
  </acceptance_criteria>
  <done>StackGameView is fully playable: tap-to-drop, idle/running/game-over/restart, gated haptics, banner, slow-mo + RM instant-cut, scenePhase pause. Builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap Home destination to StackGameView + delete harness</name>
  <files>gamekit/gamekit/Screens/HomeView.swift, gamekit/gamekit/Games/Stack/StackHarnessView.swift</files>
  <read_first>
    - 16-PATTERNS.md "Screens/HomeView.swift" section (lines 214-217) + "StackHarnessView.swift (DELETE)" (line 28)
    - gamekit/gamekit/Screens/HomeView.swift lines 378-386 (ADR comment + .stack case + .snake case)
  </read_first>
  <action>
    In `HomeView.destination(for:)` swap the `.stack` case body from `StackHarnessView()` to `StackGameView()` (one-line swap). Keep the ADR ARCADE-08 comment (HomeView.swift:378) and keep NO `.videoModeAware()` on the `.stack` case (real-time games are Video-Mode exempt). Keep `.disableInteractivePop()` if the case already carries it. Do NOT touch the `.snake` case (Phase 17). Then DELETE `gamekit/gamekit/Games/Stack/StackHarnessView.swift` (throwaway per Phase 15 D-02; new files auto-register via PBXFileSystemSynchronizedRootGroup so no pbxproj edit is needed for the deletion either — CLAUDE.md §8.8). Confirm no remaining references to `StackHarnessView` / `StackHarnessVM` anywhere. Verify no `* 2.swift` Finder-dupe appeared (§8.7).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'StackGameView()' gamekit/gamekit/Screens/HomeView.swift && test ! -f gamekit/gamekit/Games/Stack/StackHarnessView.swift && test -z "$(grep -rn 'StackHarness' gamekit/gamekit)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - HomeView `.stack` case launches `StackGameView()`; ADR comment retained; NO `.videoModeAware` on the case.
    - `StackHarnessView.swift` deleted; zero remaining `StackHarness` references in the source tree (grep empty).
    - `.snake` case untouched.
    - No `* 2.swift` dupe files (`git status` clean of `?? *2.swift`).
    - `xcodebuild build` succeeds; launching the Stack card opens the playable game.
  </acceptance_criteria>
  <done>Home launches the real Stack game; throwaway harness removed with no dangling references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → game | Single tap gesture; no untrusted parsing |
| scenePhase transitions → loop | OS lifecycle events drive pause/resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Denial of Service (self) | loop running while backgrounded | mitigate | Both .inactive and .background call vm.pause(); loop off at game-over (verified Plan 16-07 Instruments) |
| T-16-13 | Tampering / accessibility regression | feedback not gated | mitigate | hapticsEnabled FIRST-guard on both .sensoryFeedback triggers; animations + reduceMotion independently gate the slow-mo; no screen shake |
| T-16-14 | Denial of Service (self) | leftover harness causing duplicate-symbol / dead route | mitigate | StackHarnessView deleted; zero-reference grep gate; no Finder-dupe check |
</threat_model>

<verification>
- App builds; Stack card launches StackGameView; harness deleted with no references.
- Loop + scenePhase pause wired; haptics gated hapticsEnabled-first; banner reused; slow-mo/RM paths present.
- StackGameView < 250 lines.
</verification>

<success_criteria>
Stack is fully playable end-to-end from Home (SC1): tap-to-drop, visible combo, gated perfect/normal haptics, cinematic game-over with Reduce Motion instant-cut, scenePhase pause, restart. Harness removed.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-05-SUMMARY.md` when done.
</output>
