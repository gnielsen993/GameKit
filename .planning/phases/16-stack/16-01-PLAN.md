---
phase: 16-stack
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gamekit/gamekit/Games/Stack/StackConfig.swift
  - gamekit/gamekit/Games/Stack/StackEngine.swift
  - gamekit/gamekitTests/Games/Stack/StackEngineTests.swift
autonomous: true
requirements: [STACK-01, STACK-02, STACK-03]
user_setup: []

must_haves:
  truths:
    - "StackEngine drops, trims overhang, narrows the block, and ends the run when overlap reaches the minWidth floor"
    - "A near-perfect drop (within tolerance) increments the streak; N consecutive perfects expand width; one imperfect resets the streak to 0 with no recovery"
    - "Speed ramps linearly with score then plateaus at plateauScore (~80)"
    - "dt=1/60 and dt=1/120 over 5 simulated seconds produce identical score, gameOver, and tower widths (closed-form oscillation)"
    - "StackEngine imports Foundation only — no SwiftUI/UIKit/SwiftData/CoreGraphics/Date.now"
  artifacts:
    - path: "gamekit/gamekit/Games/Stack/StackEngine.swift"
      provides: "Pure value-type drop/trim/streak/ramp engine with closed-form oscillation"
      contains: "struct StackEngine"
    - path: "gamekit/gamekit/Games/Stack/StackConfig.swift"
      provides: "Tuning constants (.default + .testFixed)"
      contains: "struct StackConfig"
    - path: "gamekit/gamekitTests/Games/Stack/StackEngineTests.swift"
      provides: "Determinism + edge-case suite"
      contains: "func proMotionEquivalence"
  key_links:
    - from: "StackEngineTests.proMotionEquivalence"
      to: "StackEngine.step(dt:input:)"
      via: "two step streams (1/60, 1/120) compared for equal widths"
      pattern: "a.placed.map.*== b.placed.map"
---

<objective>
Ship the pure, deterministic `StackEngine` plus its tuning `StackConfig` and the locked Swift Testing suite that gates SC2 (ProMotion equivalence) and the STACK-01/02/03 mechanics. This is the Wave 0 test-first foundation per `16-VALIDATION.md` — the engine and its tests land in the SAME commit per CLAUDE.md §5.

Purpose: Prove the hardest correctness requirement (frame-rate-independent physics via closed-form oscillation) in headless Foundation-only Swift before any view, VM, or rendering exists. Every downstream plan builds against this locked contract.
Output: `StackEngine.swift`, `StackConfig.swift`, `StackEngineTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md
@.planning/phases/16-stack/16-VALIDATION.md

<interfaces>
<!-- The full StackEngine + value-struct contract is spelled out in 16-RESEARCH.md Pattern 1 (lines 167-289). Adopt verbatim. -->
Value structs (all Equatable; Sendable like MergeEngine's MergeEvent/SlideResult):
  PlacedBlock { var centerX: Double; var width: Double }
  StackInput { var drop: Bool = false }
  StackEvent: enum { case none, perfect(index: Int), trim(overhangWidth: Double), miss }
  StackFrame { currentCenterX, currentWidth: Double; score, streak, bestStreak: Int; gameOver: Bool; event: StackEvent }
StackEngine surface:
  init(cfg: StackConfig = .default)
  var score: Int { placed.count }
  private(set) var placed: [PlacedBlock]
  private(set) var streak, bestStreak: Int
  private(set) var gameOver: Bool
  mutating func step(dt: Double, input: StackInput) -> StackFrame
  func rampSpeed(forScore:) -> Double   // expose at least internal so the plateau test can call it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StackConfig tuning constants (.default + .testFixed)</name>
  <files>gamekit/gamekit/Games/Stack/StackConfig.swift</files>
  <read_first>
    - 16-RESEARCH.md Tuning Constants table (lines 503-520) — exact values
    - 16-PATTERNS.md "StackConfig.swift" section (lines 52-58)
    - gamekit/gamekit/Core/ArcadeLoopDriver.swift (confirm dt already clamped at line 42 — engine adds NO second clamp)
  </read_first>
  <action>
    Create `struct StackConfig` (Foundation-only) with stored `let` fields and two static presets `static let default` and `static let testFixed`. Fields and baseline values from the research table: `fixedDt = 1.0/60.0`, `playfieldWidth = 1.0`, `playfieldCenter = 0.5`, `startingWidth = 0.62`, `startSpeed = 0.35`, `maxSpeed = 0.90`, `plateauScore = 80`, `perfectTolerance = 0.025`, `streakThreshold = 5`, `expandAmount = 0.04`, `minWidth = 0.015`, `cycleLength = 6`. Per D-03 these are TUNING CONSTANTS — add a doc comment marking them play-test baselines (MEDIUM confidence) to be calibrated against the speed ramp. `testFixed` may use the same values or simplified deterministic ones; it exists so `StackEngineTests` runs against a stable config. Keep file < 60 lines. Do NOT add a `dt` clamp here (D-08 substrate note: clamp lives in ArcadeLoopDriver only).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q "static let `default`\|static let \`default\`\|static let testFixed" gamekit/gamekit/Games/Stack/StackConfig.swift && grep -q "streakThreshold" gamekit/gamekit/Games/Stack/StackConfig.swift</automated>
  </verify>
  <acceptance_criteria>
    - `StackConfig` declares all 12 fields named above.
    - Both `static let default` and `static let testFixed` exist.
    - `grep -v '^[[:space:]]*//' gamekit/gamekit/Games/Stack/StackConfig.swift | grep -c "import" ` shows only `import Foundation` (no SwiftUI/SwiftData).
    - `wc -l` < 60.
  </acceptance_criteria>
  <done>StackConfig compiles, exposes .default and .testFixed, all tuning constants present and commented as calibratable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StackEngine pure value type with closed-form oscillation</name>
  <files>gamekit/gamekit/Games/Stack/StackEngine.swift</files>
  <read_first>
    - 16-RESEARCH.md Pattern 1 (lines 167-289) — the full step() body and tri() oscillation
    - 16-PATTERNS.md "StackEngine.swift" section (lines 34-49)
    - gamekit/gamekit/Games/Merge/Engine/MergeEngine.swift (lines 1-46) — purity header + Equatable Sendable value-struct convention
    - gamekit/gamekit/Games/Minesweeper/Engine/RevealEngine.swift (purity contract analog)
  </read_first>
  <behavior>
    - step with drop=false advances blockElapsed only; returns event .none, gameOver false.
    - step with drop=true and offset <= perfectTolerance: streak += 1, appends a block at the block-below centerX; width expands by expandAmount only once streak >= streakThreshold (capped at startingWidth); returns .perfect(index:).
    - step with drop=true and offset > tolerance but overlap > minWidth: streak resets to 0, appends a block centered on the overlap region with width = overlapWidth; returns .trim(overhangWidth:).
    - step with drop=true and overlap <= minWidth: gameOver = true, bestStreak = max(bestStreak, streak), returns .miss; subsequent steps are no-ops returning .none.
    - rampSpeed(forScore:) is linear startSpeed→maxSpeed across 0…plateauScore, constant thereafter: rampSpeed(forScore: 80) == rampSpeed(forScore: 200).
    - currentCenterX is a pure function of (blockElapsed * oscSpeed) via triangle wave — never integrated velocity.
  </behavior>
  <action>
    Create `import Foundation` only. Declare the four Equatable value structs from the interfaces block (`PlacedBlock`, `StackInput`, `StackEvent`, `StackFrame`) and `struct StackEngine` exactly per 16-RESEARCH.md Pattern 1. Implement the closed-form `tri(_:)` triangle wave and the computed `currentCenterX` (travel = playfieldWidth − currentWidth; center sweeps via `tri(blockElapsed * oscSpeed)`), `rampSpeed(forScore:)` (expose as `func`, at least internal, so the plateau test can call it), `spawnNext(width:)` (resets blockElapsed, sets oscSpeed = rampSpeed(forScore: score), alternates startSide), and `mutating func step(dt:input:) -> StackFrame`. `score` is a computed `placed.count`. Base block seeded centered at playfieldCenter, full startingWidth. CRITICAL (SC2 keystone): oscillation MUST be closed-form on accumulated `blockElapsed`; do NOT use velocity+bounce integration. No RNG (Claude's Discretion: oscillation is deterministic — do not introduce SeedableRNG). No second dt clamp. Keep file < 300 lines (split into StackConfig already done). In comments, do NOT write the literal forbidden-token strings (`Color(red:`, `import SwiftUI`, `modelContext`, `Date.now`) — paraphrase ("no view-layer or persistence imports") so the purity grep gate stays clean.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -z "$(grep -rn 'import SwiftUI\|import UIKit\|import SwiftData\|import CoreGraphics\|modelContext\|Date.now' gamekit/gamekit/Games/Stack/StackEngine.swift)" && echo PURE</automated>
  </verify>
  <acceptance_criteria>
    - Engine-purity grep returns empty (command above prints PURE).
    - `struct StackEngine`, `struct PlacedBlock`, `enum StackEvent`, `struct StackFrame` all present; structs are `Equatable`.
    - `rampSpeed` is callable from the test target (not `private`).
    - No `speedPerFrame` / `stepsPerTick` / per-frame oscillation tokens (`grep -c "perFrame\|PerFrame" StackEngine.swift` == 0 after stripping comments).
    - `wc -l gamekit/gamekit/Games/Stack/StackEngine.swift` < 300.
  </acceptance_criteria>
  <done>StackEngine compiles Foundation-only; step() implements drop/trim/perfect/miss + streak recovery per D-01/02/03; oscillation is closed-form. Commit TOGETHER with Task 3 (engine + tests, CLAUDE.md §5).</done>
</task>

<task type="auto">
  <name>Task 3: StackEngineTests — determinism + edge cases (locked Wave 0 gate)</name>
  <files>gamekit/gamekitTests/Games/Stack/StackEngineTests.swift</files>
  <read_first>
    - 16-RESEARCH.md Code Examples (lines 440-481) — proMotionEquivalence full body
    - 16-VALIDATION.md Per-Task Verification Map (lines 42-49) + Wave 0 Requirements (lines 58-62)
    - 16-PATTERNS.md "StackEngineTests.swift" section (lines 235-246)
    - gamekit/gamekitTests/Engine/MergeEngineTests.swift (lines 1-23) — `nonisolated @Suite` Swift Testing shape
  </read_first>
  <action>
    Create `import Testing` + `import Foundation` + `@testable import gamekit`. Declare `@Suite("StackEngine determinism") nonisolated struct StackEngineTests` (nonisolated — engine is Foundation-only, no actor isolation). Implement four `@Test` funcs against `StackConfig.testFixed`: (1) `proMotionEquivalence` — run identical drop sim-times at fixedDt 1/60 and 1/120 over 5s, `#expect` equal `score`, `gameOver`, and `placed.map(\.width)` (body from research lines 452-473); (2) `completeMissGameOver` — drive a drop far off-center so overlap <= minWidth, `#expect(frame.event == .miss && engine.gameOver)`; (3) `streakRecoveryAndReset` — drive `streakThreshold` consecutive perfects (drop at center), assert width expands only after threshold and never above startingWidth, then one imperfect drop resets `streak == 0` and gives no width recovery (D-01); (4) `rampSpeedPlateau` — `#expect(engine.rampSpeed(forScore: 80) == engine.rampSpeed(forScore: 200))` (STACK-02). To land perfect/imperfect drops deterministically, compute the drop sim-time from the closed-form `tri()` (the center is known at any blockElapsed) rather than guessing.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && xcodebuild test -scheme gamekit -only-testing:gamekitTests/StackEngineTests -destination 'platform=iOS Simulator,name=iPhone 15' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four @Test funcs present: `proMotionEquivalence`, `completeMissGameOver`, `streakRecoveryAndReset`, `rampSpeedPlateau`.
    - `xcodebuild test -only-testing:gamekitTests/StackEngineTests` passes (TEST SUCCEEDED).
    - `proMotionEquivalence` asserts on all three of score, gameOver, and per-block widths.
    - Suite is `nonisolated struct` (matches MergeEngineTests).
  </acceptance_criteria>
  <done>StackEngineTests green; SC2 ProMotion equivalence proven; STACK-01/02/03 edge cases locked. Engine + tests committed together per CLAUDE.md §5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → engine input | Single boolean `drop`; no parsing, no untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial of Service (self) | StackEngine.step loop | accept | Engine is allocation-light pure value type; spiral-of-death already clamped in ArcadeLoopDriver (Phase 15); engine adds no clamp by design |
| T-16-02 | Tampering | tuning constants | accept | Constants are compiled-in, not user-editable; no persistence at engine layer |
</threat_model>

<verification>
- `xcodebuild test -only-testing:gamekitTests/StackEngineTests` passes.
- Engine-purity grep empty.
- All three files < their caps (Config < 60, Engine < 300, Tests reasonable).
</verification>

<success_criteria>
StackEngine + StackConfig exist Foundation-only; StackEngineTests proves SC2 determinism + STACK-01/02/03 mechanics; engine and tests landed in one commit.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-01-SUMMARY.md` when done.
</output>
