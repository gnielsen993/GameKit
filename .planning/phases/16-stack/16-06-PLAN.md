---
phase: 16-stack
plan: 06
type: execute
wave: 1
depends_on: ["16-02"]
files_modified:
  - gamekit/gamekit/Screens/StackStatsCard.swift
  - gamekit/gamekit/Screens/StatsView.swift
autonomous: true
requirements: [STACK-04]
user_setup: []

must_haves:
  truths:
    - "Stats screen shows a minimal Stack section: high score, runs played, best perfect streak (D-10)"
    - "StackStatsCard is a props-only view in its own file (StatsView is already 496 lines)"
    - "High score reads the BestScore row mode 'endless'; best streak reads mode 'perfectStreak'; runs played = stack GameRecord count"
    - "An explicit empty state shows when no Stack runs exist"
    - "The card never runs @Query — StatsView owns the existing stackRecords / stackBestScores queries"
  artifacts:
    - path: "gamekit/gamekit/Screens/StackStatsCard.swift"
      provides: "props-only Stack stats card with empty state"
      contains: "struct StackStatsCard"
    - path: "gamekit/gamekit/Screens/StatsView.swift"
      provides: "Stack section wired to StackStatsCard (placeholder replaced)"
      contains: "StackStatsCard"
  key_links:
    - from: "StatsView .stack section"
      to: "StackStatsCard"
      via: "passes theme + stackRecords + stackBestScores"
      pattern: "StackStatsCard\\(theme:"
    - from: "StackStatsCard"
      to: "BestScore rows"
      via: "filter difficultyRaw == endless / perfectStreak"
      pattern: "difficultyRaw == .endless.|perfectStreak"
---

<objective>
Replace the Phase 15 Stack placeholder on the Stats screen with a real, minimal, props-only `StackStatsCard` showing high score, runs played, and best perfect streak (D-10). The full ARCADE-07 score-based stats shape is Phase 18 — keep this minimal.

Purpose: Surface the persisted Stack metrics (the read side of D-11) using the existing `@Query` pairs, in its own file so StatsView (already 496 lines) does not breach the §8.5 cap.
Output: `Screens/StackStatsCard.swift`, edited `Screens/StatsView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md
@.planning/phases/16-stack/16-02-SUMMARY.md

<interfaces>
<!-- Field derivation: 16-RESEARCH.md Pattern 3 (lines 324-331). Analog: MergeStatsCard. -->
Existing in StatsView.swift (verified): stackRecords: [GameRecord] (line 132), stackBestScores: [BestScore] (line 135) — both @Query already present; Stack placeholder block at lines 221-230; .merge section pattern at lines 172-177; MergeStatsCard at lines 253-294; MergeModeStatsRow at 296-339.
Mode keys (from 16-02): GameStats.stackEndlessMode == "endless", GameStats.stackPerfectStreakMode == "perfectStreak".
StackStatsCard signature: struct StackStatsCard: View { let theme: Theme; let records: [GameRecord]; let bestScores: [BestScore] }
Derivation:
  highScore  = bestScores.first { $0.difficultyRaw == "endless" }?.score
  bestStreak = bestScores.first { $0.difficultyRaw == "perfectStreak" }?.score
  runsPlayed = records.count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StackStatsCard props-only card with empty state</name>
  <files>gamekit/gamekit/Screens/StackStatsCard.swift</files>
  <read_first>
    - 16-PATTERNS.md "StackStatsCard.swift" section (lines 176-198)
    - 16-RESEARCH.md Pattern 3 read path (lines 324-331)
    - gamekit/gamekit/Screens/StatsView.swift lines 253-339 (MergeStatsCard + MergeModeStatsRow — props-only, empty-state, monoNumber, a11y combine)
    - CLAUDE.md §8.2 (data-driven not data-fetching) + §8.3 (explicit empty state)
  </read_first>
  <action>
    Create a new file `Screens/StackStatsCard.swift` with `struct StackStatsCard: View { let theme: Theme; let records: [GameRecord]; let bestScores: [BestScore] }`. Mirror MergeStatsCard layout (StatsView.swift:253-294). Empty state FIRST (§8.3): when `records.isEmpty`, render explicit copy via `String(localized:)` (e.g. "No Stack games played yet.") — no blank screen. Otherwise show exactly THREE metrics (D-10, minimal): High Score (bestScores endless row score, "—" if nil), Runs Played (records.count), Best Perfect Streak (bestScores perfectStreak row score, "—" if nil). Filter via `difficultyRaw == GameStats.stackEndlessMode` / `== GameStats.stackPerfectStreakMode` (use the constants, not bare literals, where reachable). Numbers use `theme.typography.monoNumber.monospacedDigit()` (MergeModeStatsRow.swift:326-328); all spacing/radii/colors from tokens. Add `.accessibilityElement(children: .combine)` + a combined label. This is a props-only view — NEVER declare `@Query` here. Keep file < 200 lines. Do NOT write raw color inits / system color names (token discipline does not apply to Screens/ via the hook, but DESIGN.md §2 + CLAUDE.md §1 still hold — use tokens).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -f gamekit/gamekit/Screens/StackStatsCard.swift && test -z "$(grep -n '@Query' gamekit/gamekit/Screens/StackStatsCard.swift)" && grep -q 'struct StackStatsCard' gamekit/gamekit/Screens/StackStatsCard.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `struct StackStatsCard: View` with the props-only signature; NO `@Query` (grep empty).
    - Empty-state branch present with localized copy.
    - Exactly three metrics rendered (high score, runs played, best perfect streak).
    - High score / streak derived from BestScore rows filtered by mode key; runs = records.count.
    - `wc -l` < 200.
  </acceptance_criteria>
  <done>StackStatsCard renders three minimal metrics + empty state, props-only, its own file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire StackStatsCard into StatsView (replace placeholder)</name>
  <files>gamekit/gamekit/Screens/StatsView.swift</files>
  <read_first>
    - 16-PATTERNS.md "Screens/StatsView.swift" section (lines 220-231)
    - gamekit/gamekit/Screens/StatsView.swift lines 172-177 (.merge section shape), 221-230 (Stack placeholder to replace), 123-135 (existing stack @Query pairs)
  </read_first>
  <action>
    Replace the Phase 15 Stack placeholder block (StatsView.swift ~lines 221-230) with the real section mirroring the .merge shape (lines 172-177): inside the `if shows(.stack)` branch, emit the section header when `focusedKind == nil`, then a `DKCard(theme: theme)` wrapping `StackStatsCard(theme: theme, records: stackRecords, bestScores: stackBestScores)`. Use the existing `stackRecords` / `stackBestScores` `@Query` pairs (lines 132/135) — do NOT add a new query. Keep the Snake placeholder untouched (Phase 17). Net line change should be near-zero (StatsView stays under the §8.5 hard cap — confirm `wc -l` does not increase materially).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'StackStatsCard(theme:' gamekit/gamekit/Screens/StatsView.swift && xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `StackStatsCard(theme: theme, records: stackRecords, bestScores: stackBestScores)` call site present in the `shows(.stack)` branch.
    - Old "No Stack games yet." placeholder block removed.
    - No new `@Query` added; Snake placeholder unchanged.
    - `xcodebuild build` succeeds.
    - `wc -l StatsView.swift` did not materially grow (placeholder swapped, not added on top).
  </acceptance_criteria>
  <done>Stats screen renders the real Stack section from persisted data via the existing queries; app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted rows → Stats display | Local/CloudKit BestScore + GameRecord read into a props-only view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Tampering | unknown persisted mode raw values | accept | Rows that don't match "endless"/"perfectStreak" filters are ignored; nil-safe "—" fallback rendered |
| T-16-11 | Information Disclosure | stats content | accept | No PII; scores are local non-sensitive gameplay data |
</threat_model>

<verification>
- App builds with the Stack section wired.
- Card is props-only (no @Query); three metrics + empty state present.
- StatsView under §8.5 cap.
</verification>

<success_criteria>
Stats screen shows a minimal, accurate Stack section (high score + runs played + best perfect streak) sourced from persisted data, via a props-only card in its own file.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-06-SUMMARY.md` when done.
</output>
