---
phase: 16-stack
plan: 07
type: execute
wave: 3
depends_on: ["16-05", "16-06"]
files_modified:
  - .planning/phases/16-stack/16-VERIFICATION.md
  - Docs/releases/v1.4.md
autonomous: false
requirements: [STACK-05, STACK-06]
user_setup: []

must_haves:
  truths:
    - "Stack Canvas is legible on Classic (Chrome Diner) AND one Loud/Moody preset (Voltage or Dracula) — blocks, overhang trim, score/streak chip all read (§8.12, SC4)"
    - "Reduce Motion ON makes blocks jump-cut, pulse->instant flash, slow-mo->instant cut; gameplay mechanics unchanged (SC5)"
    - "No disk I/O during active play; the high score saves exactly once on game-over; the loop is paused (zero CPU) after the banner (SC3)"
    - "DESIGN.md §12.5 new-game done checklist passes for Stack"
    - "The release log for the current MARKETING_VERSION records the Stack feature"
  artifacts:
    - path: ".planning/phases/16-stack/16-VERIFICATION.md"
      provides: "Signed §8.12 + Reduce Motion + Instruments audit record"
      contains: "8.12"
  key_links:
    - from: "Stack gameplay"
      to: "DESIGN.md §12.5 + CLAUDE.md §8.12"
      via: "manual audit on Classic + Voltage/Dracula + Reduce Motion + Instruments"
      pattern: "§8.12"
---

<objective>
Close Phase 16: run the mandatory manual gates that cannot be automated — the §8.12 theme legibility audit (Classic + one Loud/Moody preset), the Reduce Motion jump-cut recipe (SC5), the Instruments no-disk-I/O / save-once / loop-paused check (SC3), and the DESIGN.md §12.5 new-game checklist — then append the Stack feature to the current release log (§8.14).

Purpose: SC3/SC4/SC5 are visual / profiling / accessibility judgments that require a human + Instruments; this plan records the sign-off and lands the release-log wrap-up for the multi-commit Stack feature.
Output: `.planning/phases/16-stack/16-VERIFICATION.md`, release-log entry in `Docs/releases/v{MARKETING_VERSION}.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-VALIDATION.md
@.planning/phases/16-stack/16-RESEARCH.md
@DESIGN.md
@CLAUDE.md

<interfaces>
Manual-only verifications: 16-VALIDATION.md "Manual-Only Verifications" table (lines 66-73).
Token-discipline grep gate (must be empty across the whole game folder):
  grep -rn "Color(red:|Color(hex:|Color(white:|\.green\b|\.red\b|\.blue\b|\.orange\b" gamekit/gamekit/Games/Stack/
Engine purity grep (must be empty):
  grep -rn "import SwiftUI|import UIKit|import SwiftData|modelContext" gamekit/gamekit/Games/Stack/StackEngine.swift
File-cap gate: wc -l gamekit/gamekit/Games/Stack/*.swift gamekit/gamekit/Screens/StackStatsCard.swift  (all < 400)
Release log: pull MARKETING_VERSION from gamekit/gamekit.xcodeproj/project.pbxproj (currently 1.4);
  append to Docs/releases/v{that}.md (create from Docs/releases/TEMPLATE.md only if the file is missing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated phase gates — token discipline, engine purity, file caps, full suite</name>
  <files>.planning/phases/16-stack/16-VERIFICATION.md</files>
  <read_first>
    - 16-VALIDATION.md (full) — per-task map + sign-off checklist
    - 16-RESEARCH.md Common Pitfalls greps (lines 413-436)
  </read_first>
  <action>
    Run and record the automated gates in a new `16-VERIFICATION.md`: (1) token-discipline grep over `Games/Stack/` returns empty (NOTE: comments must not contain the literal banned tokens — if the grep trips on a comment, fix the comment, not the gate); (2) engine-purity grep over `StackEngine.swift` returns empty; (3) `wc -l` for every Stack file + `StackStatsCard.swift` all < 400; (4) `git status` shows no `?? *2.swift` Finder-dupe; (5) full `gamekitTests` suite green (`xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15'`). Record each command + PASS/FAIL in the doc. If any fails, STOP and report — do not proceed to the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -z "$(grep -rn 'Color(red:\|Color(hex:\|Color(white:\|\.green\b\|\.red\b\|\.blue\b\|\.orange\b' gamekit/gamekit/Games/Stack/)" && test -z "$(grep -rn 'import SwiftUI\|import UIKit\|import SwiftData\|modelContext' gamekit/gamekit/Games/Stack/StackEngine.swift)" && echo GATES_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Token-discipline grep over Games/Stack/ empty; engine-purity grep over StackEngine.swift empty (command prints GATES_CLEAN).
    - All Stack files + StackStatsCard.swift < 400 lines (recorded in doc).
    - No `* 2.swift` dupes.
    - Full `gamekitTests` suite passes (TEST SUCCEEDED) — recorded in doc.
  </acceptance_criteria>
  <done>Automated phase gates recorded green in 16-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual §8.12 + Reduce Motion + Instruments sign-off</name>
  <files>.planning/phases/16-stack/16-VERIFICATION.md</files>
  <action>
    This is a blocking human-verification checkpoint — Claude has already automated everything verifiable (Task 1 gates green, the game is built and runnable). Present the verification recipe below to the user, wait for "approved", and record the PASS/FAIL results (with the preset names used and any gaps) in 16-VERIFICATION.md. Do not write code in this task.
  </action>
  <what-built>Fully playable Stack: tap-to-drop tower with accent-palette Canvas, visible combo, gated haptics, cinematic game-over banner, Reduce Motion path, and a minimal Stats section. All automated gates are green (Task 1).</what-built>
  <how-to-verify>
    1. §8.12 THEME AUDIT (SC4): Build/run on a simulator. Play Stack on the Classic preset (Chrome Diner) — confirm tower blocks, the current sliding block, the overhang trim piece, and the score + combo chips are all legible and visually distinct (adjacent blocks distinguishable). Switch to a Loud/Moody preset (Voltage OR Dracula) via Settings and replay — confirm the same legibility (no token bleedthrough, blocks still distinguishable, combo counter readable). Spot-check colorblind distinguishability (adjacent blocks differ in brightness, not just hue).
    2. REDUCE MOTION (SC5): Simulator → Settings → Accessibility → Motion → Reduce Motion ON. Play a run — confirm blocks JUMP-CUT to position each tick (no slide/spring), the perfect-drop pulse becomes an instant color flash, the combo bump is an instant number change, and game-over is an INSTANT CUT to the banner (no slow-mo, no color-drain, NO screen shake). Confirm gameplay mechanics + speed ramp are unchanged vs RM off.
    3. INSTRUMENTS (SC3): Run with Time Profiler + the Core Data (SwiftData) instrument (or os_signpost). During active play confirm ZERO disk-write spikes; on game-over confirm exactly ONE write (the recordStackRun save); after the banner appears confirm the loop is paused (~0 CPU on the game view).
    4. DESIGN.md §12.5 new-game done checklist: walk the checklist for Stack and confirm each item.
    Record results (PASS/FAIL per item, preset names used, any gap) in 16-VERIFICATION.md.
  </how-to-verify>
  <verify>
    <human-check>User confirms §8.12 legibility (Classic + Voltage/Dracula), Reduce Motion jump-cut + instant-cut game-over (no shake), Instruments no-per-frame-I/O + save-once + loop-paused, and DESIGN §12.5 checklist all PASS; results recorded in 16-VERIFICATION.md.</human-check>
  </verify>
  <resume-signal>Type "approved" if §8.12 + Reduce Motion + Instruments + §12.5 all PASS, or describe the gaps to address.</resume-signal>
  <done>Manual SC3/SC4/SC5 + §12.5 sign-off recorded in 16-VERIFICATION.md; any gaps logged for closure.</done>
</task>

<task type="auto">
  <name>Task 3: Release-log append for the Stack feature (§8.14)</name>
  <files>Docs/releases/v1.4.md</files>
  <read_first>
    - CLAUDE.md §0.3 + §8.14 (release-log convention)
    - Docs/releases/TEMPLATE.md (section shape)
    - gamekit/gamekit.xcodeproj/project.pbxproj (read the current MARKETING_VERSION before writing)
  </read_first>
  <action>
    Read the current `MARKETING_VERSION` from `gamekit/gamekit.xcodeproj/project.pbxproj` (currently `1.4`). Append a brief Stack entry to `Docs/releases/v{MARKETING_VERSION}.md` (create from `Docs/releases/TEMPLATE.md` only if that version file does not yet exist). Under the appropriate sections (Summary / User-facing changes / Internal changes): note the new playable Stack endless game (tap-to-drop tower, combo/streak width recovery, accent-palette Canvas, Reduce Motion path, high score + best perfect streak persisted, minimal Stats section). Keep it bullet-point brief — what + why, no per-file lists. If `MARKETING_VERSION` has been bumped to a new value since this plan was written, target that file instead and note the actual path in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && V=$(grep -m1 'MARKETING_VERSION' gamekit/gamekit.xcodeproj/project.pbxproj | grep -o '[0-9.]*') && grep -qi 'stack' "Docs/releases/v${V}.md" && echo "LOGGED v${V}"</automated>
  </verify>
  <acceptance_criteria>
    - The release-log file for the current MARKETING_VERSION contains a Stack entry (command prints LOGGED v{version}).
    - Entry is brief (bullets), mentions the playable game + persistence + Reduce Motion + Stats section.
    - No per-file dumps; no edits to a shipped version's file other than the current one.
  </acceptance_criteria>
  <done>Stack feature recorded in the current release log per §8.14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification + documentation only; no new runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-15 | Information Disclosure / legibility regression | Loud-preset Canvas | mitigate | §8.12 human audit on Classic + Voltage/Dracula gates the phase; documented Wong-safe fallback if a preset fails |
| T-16-16 | Denial of Service (self) | loop running after game-over / per-frame I/O | mitigate | Instruments check confirms zero CPU after banner + single save on game-over |
</threat_model>

<verification>
- Automated gates green (token discipline, engine purity, file caps, full suite).
- Human sign-off on §8.12 + Reduce Motion + Instruments + §12.5 recorded in 16-VERIFICATION.md.
- Release log updated for the current MARKETING_VERSION.
</verification>

<success_criteria>
SC3 (save-once / no per-frame I/O / loop paused), SC4 (§8.12 legibility on Classic + Loud), and SC5 (Reduce Motion jump-cut) are human-verified and recorded; the Stack feature is logged in the current release notes.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-07-SUMMARY.md` when done.
</output>
