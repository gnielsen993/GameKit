---
phase: 16-stack
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - gamekit/gamekit/Core/GameStats.swift
  - gamekit/gamekitTests/Core/GameStatsTests.swift
autonomous: true
requirements: [STACK-04]
user_setup: []

must_haves:
  truths:
    - "recordStackRun writes exactly ONE GameRecord (gameKind .stack, mode 'endless') per run so runs-played stays honest"
    - "recordStackRun evaluates TWO BestScore rows in one save(): mode 'endless' (high score) and mode 'perfectStreak' (best streak)"
    - "Both BestScore rows are higher-only — a lower later run does not overwrite"
    - "No new @Model, no migration, no schemaVersion bump (CloudKit-safe, D-11)"
    - "resetAll() already clears BestScore rows so Stack score + streak clear for free"
  artifacts:
    - path: "gamekit/gamekit/Core/GameStats.swift"
      provides: "recordStackRun(score:perfectStreak:) + permanent mode-key constants"
      contains: "func recordStackRun"
    - path: "gamekit/gamekitTests/Core/GameStatsTests.swift"
      provides: "recordStackRunWritesStreakWithoutSchemaChange test"
      contains: "func recordStackRunWritesStreakWithoutSchemaChange"
  key_links:
    - from: "recordStackRun"
      to: "evaluateBestScore (private, same file)"
      via: "two calls (endless + perfectStreak) before one modelContext.save()"
      pattern: "evaluateBestScore.*stack"
---

<objective>
Add the single additive `GameStats.recordStackRun(score:perfectStreak:)` write path (D-11) and its locked persistence test. Best-perfect-streak persists as a second `BestScore` row under a distinct mode key — no schema change. Wave 0 test-first foundation: method + test land in the SAME commit (CLAUDE.md §5).

Purpose: Give the VM a single, CloudKit-safe, write-once-on-game-over persistence call that keeps runs-played honest and tracks the streak without a new SwiftData model.
Output: edited `Core/GameStats.swift`, edited `gamekitTests/Core/GameStatsTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md

<interfaces>
<!-- recordStackRun body: 16-RESEARCH.md Pattern 3 (lines 295-321). evaluateBestScore is PRIVATE -->
<!-- (GameStats.swift:261) so recordStackRun MUST be added inside Core/GameStats.swift -->
<!-- (a method or same-file extension), NOT a separate file. -->
Existing (Core/GameStats.swift, verified):
  func record(gameKind:mode:outcome:score:)        // ~line 113 (score-game record path)
  private func evaluateBestScore(gameKind:mode:score:) throws   // line 261, higher-only
  func resetAll() throws                            // line 175, deletes GameRecord + BestScore
New:
  static let stackEndlessMode = "endless"           // permanent serialization key
  static let stackPerfectStreakMode = "perfectStreak"
  func recordStackRun(score: Int, perfectStreak: Int) throws
GameRecord init shape (existing): GameRecord(gameKind:difficulty:outcome:durationSeconds:playedAt:score:)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recordStackRun + permanent mode-key constants in GameStats</name>
  <files>gamekit/gamekit/Core/GameStats.swift</files>
  <read_first>
    - 16-RESEARCH.md Pattern 3 (lines 295-341) — recordStackRun body + D-11 candidate rationale
    - 16-PATTERNS.md "Core/GameStats.swift" section (lines 202-211)
    - gamekit/gamekit/Core/GameStats.swift lines 113-144 (existing score record path) + 175-195 (resetAll) + 261-289 (evaluateBestScore)
    - gamekit/gamekit/Core/BestScore.swift (the gameKindRaw/difficultyRaw/score keying — difficultyRaw stores the mode)
  </read_first>
  <action>
    Add `static let stackEndlessMode = "endless"` and `static let stackPerfectStreakMode = "perfectStreak"` to GameStats. Add `func recordStackRun(score: Int, perfectStreak: Int) throws` INSIDE Core/GameStats.swift (method or same-file extension — it must reach the private evaluateBestScore). Body per research Pattern 3: insert ONE GameRecord (gameKind .stack, difficulty Self.stackEndlessMode, outcome .loss, durationSeconds 0, playedAt .now, score) FIRST; then if score greater than 0 call evaluateBestScore(gameKind: .stack, mode: Self.stackEndlessMode, score:) wrapped in best-effort do/catch (logger.error on failure, do not rethrow); then if perfectStreak greater than 0 call evaluateBestScore(gameKind: .stack, mode: Self.stackPerfectStreakMode, score: perfectStreak) in the same best-effort guard; then try modelContext.save() exactly ONCE. Mirror the existing score-path ordering (GameStats.swift:119-143). These mode keys are PERMANENT serialization keys — renaming is a data break (comment as such). Additive only — NO new @Model, NO migration, NO schemaVersion bump. Confirm resetAll() (line 175) already deletes all BestScore + GameRecord rows; Stack writes nothing to UserDefaults this phase, so no addition to the resetAll clear list is needed (confirm and note in SUMMARY). Keep file under 400 lines (currently 290).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'func recordStackRun' gamekit/gamekit/Core/GameStats.swift && grep -q 'stackPerfectStreakMode' gamekit/gamekit/Core/GameStats.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `recordStackRun(score:perfectStreak:)` and both `static let` mode-key constants present.
    - Exactly one `modelContext.save()` call inside recordStackRun (`grep -c "modelContext.save" ` within the method body == 1).
    - Two `evaluateBestScore(... .stack ...)` call sites (one endless, one perfectStreak).
    - No new `@Model` and no `schemaVersion` increment introduced: `grep -c "@Model" Core/GameStats.swift` unchanged from baseline; no `schemaVersion` raised.
    - `wc -l gamekit/gamekit/Core/GameStats.swift` < 400.
  </acceptance_criteria>
  <done>recordStackRun added in GameStats.swift, reuses higher-only evaluateBestScore for two mode rows, one save, no schema change. Commit TOGETHER with Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: recordStackRunWritesStreakWithoutSchemaChange persistence test</name>
  <files>gamekit/gamekitTests/Core/GameStatsTests.swift</files>
  <read_first>
    - 16-RESEARCH.md Code Examples persistence test (lines 483-501) — full body
    - 16-PATTERNS.md "GameStatsTests.swift" section (lines 250-252)
    - gamekit/gamekitTests/Core/GameStatsTests.swift lines 24-59 (`@MainActor @Suite` + makeStats() in-memory container helper)
  </read_first>
  <action>
    Add `@Test("recordStackRun: one GameRecord (endless) + two higher-only BestScore rows") func recordStackRunWritesStreakWithoutSchemaChange() throws` to the existing `@MainActor @Suite` (ModelContext is non-Sendable so the suite stays @MainActor, NOT nonisolated). Body per research lines 487-500: call `makeStats()`; `try stats.recordStackRun(score: 42, perfectStreak: 7)`; fetch GameRecord where gameKindRaw == "stack" → `#expect count == 1`; fetch BestScore where gameKindRaw == "stack" → `#expect count == 2`; `#expect` the "endless" row score == 42 and the "perfectStreak" row score == 7; then call `recordStackRun(score: 10, perfectStreak: 3)` and `#expect` the perfectStreak row is STILL 7 (higher-only). Use `FetchDescriptor` with `#Predicate` filters on `gameKindRaw` / `difficultyRaw`.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && xcodebuild test -scheme gamekit -only-testing:gamekitTests/GameStatsTests -destination 'platform=iOS Simulator,name=iPhone 15' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `recordStackRunWritesStreakWithoutSchemaChange` present in the `@MainActor @Suite`.
    - Test asserts: exactly 1 GameRecord, exactly 2 BestScore rows, endless==42, perfectStreak==7, and higher-only (perfectStreak stays 7 after a lower run).
    - `xcodebuild test -only-testing:gamekitTests/GameStatsTests` passes (TEST SUCCEEDED).
    - If a test-runner crash mentions NSStagedMigrationManager, follow CLAUDE.md §8.9 (`xcrun simctl uninstall <id> com.lauterstar.gamekit`) and retry — not a code bug.
  </acceptance_criteria>
  <done>Persistence test green; D-11 streak path proven CloudKit-safe with no schema change. Method + test committed together (CLAUDE.md §5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted SwiftData row → app read | Mode/GameKind raw strings loaded from local store (and CloudKit mirror) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering (local) | corrupt/unknown persisted GameKind or mode raw value | mitigate | Existing safe-fallback accessors (BestScore.gameKind); unknown mode rows simply fail the "endless"/"perfectStreak" filters and are ignored |
| T-16-04 | Denial of Service (self) | stale SwiftData store after schema drift | accept | This phase adds NO new @Model → no migration risk; CLAUDE.md §8.9 runbook covers stale-simulator crashes |
| T-16-05 | Repudiation | best-effort do/catch swallows evaluateBestScore error | accept | logger.error records the failure; GameRecord still flushes so runs-played is never lost |
</threat_model>

<verification>
- `xcodebuild test -only-testing:gamekitTests/GameStatsTests` passes.
- One GameRecord + two BestScore rows per run; higher-only confirmed.
- GameStats.swift < 400 lines; no schema bump.
</verification>

<success_criteria>
recordStackRun ships in GameStats.swift with permanent mode keys; persistence test green; CloudKit-safe, no schema change (D-11). Method and test in one commit.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-02-SUMMARY.md` when done.
</output>
