---
phase: 16-stack
plan: 03
type: execute
wave: 1
depends_on: ["16-01", "16-02"]
files_modified:
  - gamekit/gamekit/Games/Stack/StackViewModel.swift
autonomous: true
requirements: [STACK-01, STACK-03, STACK-04]
user_setup: []

must_haves:
  truths:
    - "VM owns the fixed-timestep accumulator; tick(dt:) runs the engine in a while accumulator >= fixedDt loop and adds NO second dt clamp"
    - "pendingDrop is latched inside the while loop and cleared so a tap fires on exactly one engine step"
    - "perfectCount and dropCount increment on the matching StackEvent so the view can drive counter-trigger haptics"
    - "On the engine's gameOver transition the VM sets state .gameOver and calls gameStats?.recordStackRun(...) exactly once (never per-tick)"
    - "VM imports Foundation only (SwiftData firewall — holds GameStats?, never imports SwiftData)"
  artifacts:
    - path: "gamekit/gamekit/Games/Stack/StackViewModel.swift"
      provides: "@Observable @MainActor loop bridge + counters + persistence call + lifecycle"
      contains: "final class StackViewModel"
  key_links:
    - from: "StackViewModel.tick(dt:)"
      to: "StackEngine.step(dt:input:)"
      via: "while accumulator >= fixedDt loop with latched pendingDrop"
      pattern: "while accumulator"
    - from: "StackViewModel gameOver transition"
      to: "GameStats.recordStackRun"
      via: "single call on .running -> .gameOver"
      pattern: "recordStackRun"
---

<objective>
Build `StackViewModel` — the `@Observable @MainActor` bridge between the arcade loop and the pure `StackEngine`. It owns the fixed-timestep accumulator, latches tap input, surfaces chrome state (score/streak/counters) separately from the per-frame render snapshot, and fires the single save-on-game-over persistence call.

Purpose: Isolate the loop/lifecycle/persistence wiring from both the pure engine and the Canvas render, so the view stays dumb and the engine stays Foundation-pure.
Output: `Games/Stack/StackViewModel.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md
@.planning/phases/16-stack/16-01-SUMMARY.md
@.planning/phases/16-stack/16-02-SUMMARY.md

<interfaces>
<!-- Consumes the locked StackEngine contract (16-01) + recordStackRun (16-02). -->
From StackEngine (16-01): mutating func step(dt:input:) -> StackFrame; StackInput(drop:);
  StackFrame { currentCenterX, currentWidth: Double; score, streak, bestStreak: Int; gameOver: Bool; event: StackEvent }
From GameStats (16-02): func recordStackRun(score: Int, perfectStreak: Int) throws
From Core/ArcadeGameState.swift: enum ArcadeGameState { idle, running, paused, gameOver }
Accumulator/lifecycle analog: Games/Stack/StackHarnessView.swift StackHarnessVM (lines 28-71)
GameStats firewall + counters analog: Games/Merge/MergeViewModel.swift (lines 16-45, 79-83, 213-221)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VM skeleton — accumulator, engine drive, lifecycle</name>
  <files>gamekit/gamekit/Games/Stack/StackViewModel.swift</files>
  <read_first>
    - 16-PATTERNS.md "StackViewModel.swift" section (lines 60-98)
    - gamekit/gamekit/Games/Stack/StackHarnessView.swift lines 28-71 (StackHarnessVM accumulator + start/pause/resume/stop)
    - gamekit/gamekit/Games/Merge/MergeViewModel.swift lines 16-45 (@Observable @MainActor class decl, private(set) discipline)
    - gamekit/gamekit/Core/ArcadeGameState.swift (lifecycle enum)
  </read_first>
  <action>
    Create `import Foundation` only (SwiftData firewall). Declare `@Observable @MainActor final class StackViewModel`. State (all `private(set)` where externally read): `state: ArcadeGameState = .idle`, the `StackEngine` instance, `accumulator: Double = 0`, `let fixedDt = 1.0/60.0` (or read from StackConfig), `pendingDrop: Bool` (settable from the view's tap), plus the latest `StackFrame` snapshot the Canvas reads. Copy the accumulator + `start()/pause()/resume()/stop()` shape verbatim from StackHarnessVM (StackHarnessView.swift:40-70). Implement `tick(dt:)`: guard `state == .running`; `accumulator += dt`; `while accumulator >= fixedDt { let input = StackInput(drop: pendingDrop); pendingDrop = false; let frame = engine.step(dt: fixedDt, input: input); accumulator -= fixedDt; <handle frame in Task 2> }`. Do NOT add a second dt clamp (ArcadeLoopDriver already clamps min(rawDt, 0.1)). Latch pendingDrop INSIDE the while loop and clear it so a tap maps to exactly one step.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && test -z "$(grep -rn 'import SwiftData\|import SwiftUI' gamekit/gamekit/Games/Stack/StackViewModel.swift)" && grep -q 'while accumulator' gamekit/gamekit/Games/Stack/StackViewModel.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `@Observable @MainActor final class StackViewModel` present; imports Foundation only (no SwiftUI/SwiftData) — grep returns empty.
    - `tick(dt:)` uses a `while accumulator >= fixedDt` fixed-timestep loop; pendingDrop latched and cleared inside the loop.
    - No `min(.*0.1)` or other dt clamp in the VM (`grep -c "0.1)" StackViewModel.swift` for a clamp == 0).
    - Externally-read state is `private(set)`.
  </acceptance_criteria>
  <done>VM drives the engine via a fixed-timestep accumulator with single-step tap latching; Foundation-only; lifecycle methods present.</done>
</task>

<task type="auto">
  <name>Task 2: Event counters, save-on-game-over, GameStats attach, restart</name>
  <files>gamekit/gamekit/Games/Stack/StackViewModel.swift</files>
  <read_first>
    - 16-RESEARCH.md Pattern 5 feedback table (lines 365-376) — perfectCount / dropCount, no game-over counter (banner owns .error)
    - 16-PATTERNS.md "StackViewModel.swift" counter + persistence notes (lines 89-97)
    - gamekit/gamekit/Games/Merge/MergeViewModel.swift lines 31-37 (counters), 79-83 (attachGameStats one-shot guard), 149-157 (restart), 213-221 (recordTerminal single save)
  </read_first>
  <action>
    Add `private(set) var perfectCount = 0` and `private(set) var dropCount = 0` (mirror mergeCount/terminalCount). In the tick while-loop's frame handler: on `frame.event == .perfect` bump `perfectCount`; on `frame.event == .trim` bump `dropCount` (normal-drop impact). Do NOT add a game-over haptic counter — VideoModeBanner fires `.error` itself (VideoModeBanner.swift:126-129). On `frame.gameOver` becoming true (the .running -> .gameOver transition): set `state = .gameOver`, stop the loop, and call `gameStats?.recordStackRun(score: frame.score, perfectStreak: frame.bestStreak)` exactly ONCE via best-effort `try?` (mirror recordTerminal, MergeViewModel.swift:213-221). NEVER call recordStackRun per-tick (Pitfall 12). Add `gameStats: GameStats?` (private(set)) and `func attachGameStats(_:)` with a one-shot `didAttach` guard (MergeViewModel.swift:79-83). Add `func restart()`: reset engine to a fresh `StackEngine`, `accumulator = 0`, `perfectCount = 0`, `dropCount = 0`, `pendingDrop = false`, set `state = .running` (or `.idle` if the idle screen re-shows — Claude's Discretion; document choice). Keep file < 200 lines.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'recordStackRun' gamekit/gamekit/Games/Stack/StackViewModel.swift && grep -q 'perfectCount' gamekit/gamekit/Games/Stack/StackViewModel.swift && grep -q 'attachGameStats' gamekit/gamekit/Games/Stack/StackViewModel.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `perfectCount` and `dropCount` increment on `.perfect` / `.trim` events respectively.
    - No game-over haptic counter exists (banner owns .error) — `grep -c "gameOverCount\|terminalCount\|errorCount" StackViewModel.swift` == 0.
    - Exactly one `recordStackRun` call site, on the gameOver transition (not inside per-step body other than the transition guard). `grep -c "recordStackRun" StackViewModel.swift` == 1.
    - `attachGameStats(_:)` has a one-shot guard; `restart()` resets engine + counters + accumulator.
    - VM still imports Foundation only; `wc -l` < 200.
    - `xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 15'` succeeds (VM compiles against locked engine + recordStackRun).
  </acceptance_criteria>
  <done>VM surfaces counters, fires the single save-on-game-over call, attaches GameStats once, and restarts cleanly; builds against Wave 0 contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view tap → VM.pendingDrop | Main-actor boolean; no Sendable concern |
| VM → GameStats (SwiftData) | VM holds GameStats? and never imports SwiftData (firewall) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Denial of Service (self) | per-frame disk write | mitigate | recordStackRun called only on the gameOver transition; one call site enforced by acceptance grep (Pitfall 12) |
| T-16-07 | Tampering | architecture drift (SwiftData leaking into VM) | mitigate | Foundation-only import grep gate; VM holds GameStats? opaque reference |
</threat_model>

<verification>
- App builds against locked StackEngine + recordStackRun.
- Foundation-only grep empty; single recordStackRun call site; no second dt clamp.
- `wc -l StackViewModel.swift` < 200.
</verification>

<success_criteria>
StackViewModel bridges loop↔engine with a fixed-timestep accumulator, surfaces counters for haptics, and persists once on game-over — all Foundation-only.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-03-SUMMARY.md` when done.
</output>
