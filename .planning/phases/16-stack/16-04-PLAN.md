---
phase: 16-stack
plan: 04
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - gamekit/gamekit/Games/Stack/StackPalette.swift
  - gamekit/gamekit/Games/Stack/StackBoardCanvas.swift
autonomous: true
requirements: [STACK-05, STACK-06]
user_setup: []

must_haves:
  truths:
    - "Every block color comes from DesignKit semantic tokens only (theme.charts.chart1...6 cycled by block index)"
    - "A placed block's color is fixed by its index and never recolors as the tower scrolls (D-06)"
    - "The Canvas draws the tower, current sliding block, and overhang trim via GraphicsContext fills of token colors"
    - "Camera/scroll lives in the view layer and interpolates with the accumulator remainder; under Reduce Motion the offset and block position SNAP (jump-cut), no slide/bounce"
    - "No raw color initializers or SwiftUI system color names anywhere in Games/Stack/"
  artifacts:
    - path: "gamekit/gamekit/Games/Stack/StackPalette.swift"
      provides: "Token-only accent-derived block-color ramp helper"
      contains: "enum StackPalette"
    - path: "gamekit/gamekit/Games/Stack/StackBoardCanvas.swift"
      provides: "Immediate-mode Canvas board render with RM jump-cut gate"
      contains: "Canvas"
  key_links:
    - from: "StackBoardCanvas draw"
      to: "StackPalette.color(forIndex:theme:)"
      via: "ctx.fill(path, with: .color(token)) per placed block"
      pattern: "StackPalette.color"
    - from: "StackBoardCanvas camera"
      to: "accessibilityReduceMotion"
      via: "snap offset when reduceMotion true, interpolate otherwise"
      pattern: "reduceMotion"
---

<objective>
Build the Stack render layer: `StackPalette` (the token-only accent-derived per-layer gradient, D-05/06/07) and `StackBoardCanvas` (the immediate-mode `Canvas` that draws the tower, sliding block, and overhang trim with camera scroll and the Reduce Motion jump-cut gate, STACK-06).

Purpose: Deliver the visual signature — the tower-as-preset-palette — entirely through DesignKit tokens, and the first real-time `Canvas` board in the repo, while keeping all motion in the view layer (engine never reads geometry or Reduce Motion).
Output: `Games/Stack/StackPalette.swift`, `Games/Stack/StackBoardCanvas.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stack/16-RESEARCH.md
@.planning/phases/16-stack/16-PATTERNS.md
@.planning/phases/16-stack/16-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- StackPalette + Canvas contracts: 16-RESEARCH.md Pattern 4 (lines 343-363), 16-PATTERNS.md (lines 160-172, 150-156). -->
DesignKit tokens (public, accent-derived): theme.charts.chart1 ... chart6, theme.colors.background,
  theme.colors.surface, theme.colors.danger, theme.colors.textSecondary, theme.colors.accentPrimary
StackPalette: enum StackPalette { static func color(forIndex i: Int, theme: Theme) -> Color }
From StackEngine (16-01): the StackFrame snapshot + placed: [PlacedBlock] (centerX, width in normalized [0,1] units)
GraphicsContext.Shading.color(_:) accepts a SwiftUI.Color directly (DesignKit tokens feed it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StackPalette token-only accent ramp helper</name>
  <files>gamekit/gamekit/Games/Stack/StackPalette.swift</files>
  <read_first>
    - 16-RESEARCH.md Pattern 4 (lines 343-363) + Open Question 1 (lines 539-542) + Alternatives (lines 96-101)
    - 16-PATTERNS.md "StackPalette.swift" section (lines 160-172)
    - CLAUDE.md §2 (available tokens) + §1 "no hard-coded colors" + DESIGN.md §2 (color semantics)
  </read_first>
  <action>
    Create `enum StackPalette` with `static func color(forIndex i: Int, theme: Theme) -> Color`. Build a ramp array from `theme.charts.chart1 ... chart6` and return `ramp[i % ramp.count]` (D-06: color fixed by index, cycles every 6). These chart tokens are public + accent-derived (DesignKit ColorDerivation.derivedCharts rotates hue around the accent AND varies brightness), which satisfies D-05 (tower becomes the preset palette) and D-07 (low-hue presets still get visible lightness steps — no special-casing needed). Cycle length 6 is a tuning constant — comment it. CRITICAL token discipline (D-07): use DesignKit semantic tokens ONLY. Do NOT write any raw color initializer or SwiftUI system color name. In comments, paraphrase the prohibition ("no raw color initializers") rather than writing the literal banned tokens, so the absence-grep gate stays clean. Keep file < 80 lines.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'theme.charts.chart' gamekit/gamekit/Games/Stack/StackPalette.swift && test -z "$(grep -rn 'Color(red:\|Color(hex:\|Color(white:\|\.green\b\|\.red\b\|\.blue\b\|\.orange\b' gamekit/gamekit/Games/Stack/StackPalette.swift)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `StackPalette.color(forIndex:theme:)` returns `ramp[i % ramp.count]` over `theme.charts.chart1...6`.
    - Token-discipline grep over StackPalette.swift returns empty (no raw color inits / system color names, including in comments).
    - `wc -l` < 80.
  </acceptance_criteria>
  <done>StackPalette returns accent-derived, index-cycled, token-only block colors; placed blocks have stable colors by index.</done>
</task>

<task type="auto">
  <name>Task 2: StackBoardCanvas immediate-mode render + Reduce Motion gate</name>
  <files>gamekit/gamekit/Games/Stack/StackBoardCanvas.swift</files>
  <read_first>
    - 16-RESEARCH.md Pattern 4 camera/scroll notes (lines 363-375) + Open Question 2 (lines 544-547) + Anti-Patterns (lines 378-384)
    - 16-PATTERNS.md "StackBoardCanvas.swift" NO-analog guidance (lines 150-156) + "Reduce Motion / animation gating" (lines 266-268)
    - DESIGN.md §10 (animation = causality; Reduce Motion gate; §10.3 timing)
    - 16-01-SUMMARY.md (the StackFrame / PlacedBlock snapshot shape this view reads)
  </read_first>
  <action>
    Create a props-only SwiftUI view `StackBoardCanvas` that takes the engine snapshot (the placed blocks + current block centerX/width), the active `theme`, the `reduceMotion` Bool, and the accumulator remainder (interpolation alpha). Render with `Canvas { ctx, size in ... }`: map normalized [0,1] block coordinates to `size`, draw each placed block as a filled `Path` rect via `ctx.fill(path, with: .color(StackPalette.color(forIndex: i, theme: theme)))`, draw the current sliding block, and draw the overhang trim piece (short fall + fade when animations on; vanish instantly under reduceMotion). Camera/scroll stays HERE in the view layer (NOT the engine): camera offset = f(towerHeight) keeping the current block in the upper third; render only the visible window (~12-16 top blocks per Open Question 2). Interpolate the camera offset and the sliding-block position using the accumulator-remainder alpha (Gaffer). Reduce Motion gate (STACK-06): when `reduceMotion` is true, SNAP the camera offset and jump-cut the block position each tick — no interpolation, no trim fall. NEVER use `.animation()` on tower/block state (Pitfall 18) — all interpolation happens inside the Canvas draw. Use board-surface tokens (`theme.colors.background` / `theme.colors.surface`) for backdrop; danger token only if a narrow-block danger cue is added (Claude's Discretion). Token discipline applies — no raw color inits / system color names (paraphrase in comments). Keep file < 250 lines.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/GameKit && grep -q 'Canvas' gamekit/gamekit/Games/Stack/StackBoardCanvas.swift && grep -q 'reduceMotion' gamekit/gamekit/Games/Stack/StackBoardCanvas.swift && test -z "$(grep -rn 'Color(red:\|Color(hex:\|Color(white:\|\.animation(' gamekit/gamekit/Games/Stack/StackBoardCanvas.swift)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Uses `Canvas { ctx, size in ... }` and `ctx.fill(_, with: .color(StackPalette.color(...)))` for blocks.
    - Reads `reduceMotion` and snaps camera + block position when true (jump-cut); interpolates with the accumulator remainder otherwise.
    - No `.animation(` on board state anywhere in the file (Pitfall 18) — grep returns empty.
    - Token-discipline grep over StackBoardCanvas.swift returns empty (incl. comments).
    - `wc -l` < 250; `xcodebuild build` succeeds.
  </acceptance_criteria>
  <done>StackBoardCanvas renders the tower/block/trim immediate-mode with token-only colors, view-layer camera, and a Reduce Motion jump-cut path; engine untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none meaningful | Render layer reads in-memory engine snapshot + theme tokens; no external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure / legibility regression | Canvas under Loud presets | mitigate | Token-only colors via accent-derived charts; §8.12 audit (Plan 16-07) on Classic + Voltage/Dracula; documented Wong-safe fallback if a preset fails |
| T-16-09 | Denial of Service (self) | per-frame view-tree churn | mitigate | Immediate-mode Canvas draws from a snapshot; no `.animation()` on board state (Pitfall 18) |
</threat_model>

<verification>
- Token-discipline grep empty across both files.
- Canvas + reduceMotion snap path present; no `.animation()` on board state.
- Files within caps (Palette < 80, Canvas < 250); app builds.
</verification>

<success_criteria>
Tower renders as the active preset's accent palette via tokens only; Canvas board is the first real-time render in the repo with a working Reduce Motion jump-cut path.
</success_criteria>

<output>
Create `.planning/phases/16-stack/16-04-SUMMARY.md` when done.
</output>
