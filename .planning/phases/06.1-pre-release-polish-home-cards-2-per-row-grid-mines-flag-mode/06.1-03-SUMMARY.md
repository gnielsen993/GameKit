---
phase: 06.1-pre-release-polish-home-cards-2-per-row-grid-mines-flag-mode
plan: 03
subsystem: minesweeper-board-layout
tags: [a11y, accessibility, pinch-zoom, magnify-gesture, auto-scale, on-geometry-change, swiftui, ios17]
requires:
  - MinesweeperBoardView.swift (P3 03-03)
  - MinesweeperCellView.swift (P3 03-03 — preserved byte-identical)
  - DesignKit Theme.spacing tokens (xs/s/l)
provides:
  - "static MinesweeperBoardView.cellSize(forWidth:cols:padding:spacing:) -> CGFloat (pure formula, unit-testable)"
  - "static MinesweeperBoardView.clampZoomScale(_:) -> CGFloat (pure clamp, unit-testable)"
  - "static MinesweeperBoardView.minCellSize / minZoomScale / maxZoomScale (P6.1 constants)"
  - "MinesweeperBoardView width-driven cellSize via .onGeometryChange (no GeometryReader layout-feedback loop)"
  - "MinesweeperBoardView pinch-zoom layer via MagnifyGesture + .simultaneousGesture (composes cleanly with cell-level .exclusively(before:))"
  - "A11Y-05 satisfied (graduates A11Y-V2-02 from v2 -> v1)"
affects:
  - .planning/REQUIREMENTS.md (A11Y-05 added; A11Y-V2-02 removed; Coverage 35 -> 36)
tech-stack:
  added:
    - "SwiftUI MagnifyGesture (iOS 17+) — replaces deprecated MagnificationGesture"
    - "SwiftUI .onGeometryChange (iOS 16+) — replaces GeometryReader for dimension reporting"
  patterns:
    - "Auto-scale formula: max(floor, (width - 2*padding - (cols-1)*spacing) / cols)"
    - "Dual-@State pinch composition: zoomScale (live) + baseZoomScale (commit-on-end)"
    - ".simultaneousGesture on parent + .exclusively(before:) on child = clean coexistence (single-finger -> child wins; two-finger pinch -> parent fires)"
    - ".scaleEffect on LazyVGrid (NOT ScrollView) so clipping frame stays stable during zoom"
    - "Static helper extraction for pure-computation testability (no SwiftUI dependency in test target)"
    - "Source-comment self-discipline — rephrased deprecated-API comments to keep adversarial negative-greps clean"
key-files:
  created:
    - gamekit/gamekitTests/Games/Minesweeper/MinesweeperBoardLayoutTests.swift
  modified:
    - gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift
    - .planning/REQUIREMENTS.md
decisions:
  - "Padding reduced from theme.spacing.l (16pt) to theme.spacing.s (8pt) — RESEARCH open question #1 option (a). Resolves Easy/Medium clamping-at-floor regression while still giving the board a ~1-token visual breath on each side."
  - ".scaleEffect anchored to LazyVGrid (not ScrollView) — cosmetic-only scale; ScrollView clipping stays stable so scroll-bumper behavior is consistent during zoom."
  - "MagnifyGesture default minimumScaleDelta — Plan §Pitfall 5 mentioned 0.05 as a defensive lock, but RESEARCH HIGH confidence on the .simultaneousGesture parent/child decoupling makes the manual SC1 50-tap recipe (Task 4 verification) the empirical proof. Default delta accepted; bumped only if SC1 surfaces misfires."
  - "Source-comment rephrase pattern reapplied to keep negative-greps (no GeometryReader / no MagnificationGesture / no .foregroundColor) passing at the literal-token level even where the comment narrates the prohibition. Same precedent as P4 04-01 SwiftData unique-attribute / P6 06-06 Quit-button / P6 06-07 SIWA handler."
metrics:
  duration_minutes: 25
  completed_date: "2026-04-28"
  tasks: 4
  files_changed: 3
  total_commits: 4
---

# Phase 06.1 Plan 03: Mines Board Auto-scale + Pinch-zoom Summary

A11Y-05 satisfied. Replaced the fixed-per-difficulty cellSize switch in MinesweeperBoardView with an auto-scale-to-fit-width formula clamped to an 18pt floor, and added a MagnifyGesture pinch-zoom layer applied via `.simultaneousGesture` so the existing cell-level `LongPressGesture(0.25).exclusively(before: TapGesture())` composition is preserved byte-identical (zero regression on ROADMAP P3 SC1 50-tap zero-misfire). Graduates A11Y-V2-02 from v2 deferred -> v1 satisfied.

## Files Modified

| File | Lines Before | Lines After | Delta |
|------|--------------|-------------|-------|
| `gamekit/gamekitTests/Games/Minesweeper/MinesweeperBoardLayoutTests.swift` | 0 (new) | 133 | +133 |
| `gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift` | 117 | 236 | +119 |
| `.planning/REQUIREMENTS.md` | 220 | 223 | +3 |

All under CLAUDE.md §8.1 ~400-line soft cap.

## Wave 0 RED Test Suite

`MinesweeperBoardLayoutTests.swift` (NEW, 133 lines):

| Test | Lines | Coverage |
|------|-------|----------|
| `cellSize_easy_9col_390pt` | 41-55 | Easy 9-col @ 390pt -> ~36.2pt (above floor) |
| `cellSize_medium_16col_390pt` | 57-69 | Medium 16-col @ 390pt -> ~18.6pt (just above floor) |
| `cellSize_hard_30col_390pt` | 71-82 | Hard 30-col @ 390pt -> 18pt (clamped to floor) |
| `cellSize_hard_320pt_iphoneSE_clamped` | 84-97 | Hard 30-col @ 320pt -> 18pt (clamped, scroll fallback) |
| `zoomScale_clamped_lowerBound` | 103-108 | clampZoomScale(0.5) -> 0.8 |
| `zoomScale_clamped_upperBound` | 110-115 | clampZoomScale(2.5) -> 2.0 |
| `zoomScale_inRange_identity` | 117-122 | clampZoomScale(1.3) -> 1.3 (identity) |

RED gate confirmed (commit `b2c37cb`): build failed with 11 compile errors before Task 2's helper extraction (`'cellSize' is inaccessible due to 'private' protection level` x 8 + `type 'MinesweeperBoardView' has no member 'clampZoomScale'` x 3). Task 2 flipped GREEN; all 7 tests pass on iPhone 16 simulator (iOS 18.5).

TDD plan-level RED -> GREEN gate honored: test commit `b2c37cb` precedes feat commit `888432b` in `git log --oneline`.

## Source Locations (MinesweeperBoardView.swift, post-Task-3)

| Surface | Lines | Notes |
|---------|-------|-------|
| Doc-header (P6.1 changes) | 1-30 | Updated to mention auto-scale + pinch-zoom |
| `@State availableWidth` | 56 | Default 0; first render uses floor; .onGeometryChange triggers single follow-up render |
| `@State zoomScale / baseZoomScale` | 65-66 | Dual-state pinch composition |
| `static let minCellSize: CGFloat = 18` | 70 | Tap-target floor (CONTEXT D-13, Discretion #1) |
| `static let minZoomScale = 0.8 / maxZoomScale = 2.0` | 74-75 | Pinch range (CONTEXT D-14, Discretion #4) |
| `static func cellSize(forWidth:cols:padding:spacing:)` | 89-96 | Pure formula; testable |
| `static func clampZoomScale(_:)` | 101-103 | Pure clamp; testable |
| Instance `private var cellSize` | 113-120 | Delegates to static; padding=theme.spacing.s, spacing=theme.spacing.xs |
| `.padding(.horizontal, theme.spacing.s)` | 177 | Reduced from .l to .s (RESEARCH open question #1, option a) |
| `.scaleEffect(zoomScale, anchor: .center)` on LazyVGrid | 184 | Cosmetic-only; ScrollView clipping unaffected |
| `.background(Color.clear.onGeometryChange...)` on ScrollView | 188-195 | Width measurement without layout-feedback loop |
| `.simultaneousGesture(MagnifyGesture()...)` on ScrollView | 205-215 | onChanged: zoomScale = clamp(base * delta); onEnded: baseZoomScale = zoomScale |
| `private func scrollAxis(for difficulty:)` | 229-231 | Hard fallback for sub-floor cases — preserved byte-identical from P3 |

## REQUIREMENTS.md Edits

- **A11Y-05 row added** (v1 Accessibility section): "Pinch-to-zoom on Mines board (any difficulty); scale range 0.8x-2.0x; persists across restart within session (graduates A11Y-V2-02)"
- **A11Y-V2-02 row removed** from v2 Themes & A11y Extensions list (single-line deletion)
- **Traceability row added**: `| A11Y-05 | Phase 6.1 | Complete (06.1-03) |`
- **Coverage incremented**: total 35 -> 36, mapped 35 -> 36 (final Phase 6.1 target = 38, remaining +1 each from Plans 06.1-01 and 06.1-02)
- **Notes on placement section** updated with graduation note explaining the v2 -> v1 promotion

## Commits (in order)

| Task | Hash | Type | Subject |
|------|------|------|---------|
| 1 (RED gate) | `b2c37cb` | test | add failing MinesweeperBoardLayout suite (RED gate) |
| 2 (auto-scale) | `888432b` | feat | auto-scale Mines cellSize via onGeometryChange (A11Y-05) |
| 3 (pinch-zoom) | `008505f` | feat | pinch-zoom Mines board via MagnifyGesture (A11Y-05) |
| 4 (docs) | `f55f6b4` | docs | graduate A11Y-V2-02 -> A11Y-05 + traceability + notes |

NOTE on commit shape: the plan §Success Criteria suggested a single atomic commit `feat(06.1-03): mines board auto-scale + pinch-zoom — A11Y-05 (graduates A11Y-V2-02)`, but the plan §Task 1 explicitly required a TDD RED -> GREEN gate sequence (Plan 04-02 / 05-01 / 05-06 precedent locked across STATE.md). The four-commit shape (test -> feat -> feat -> docs) honors the TDD invariant + CLAUDE.md §8.10 atomic-commit-per-coherent-change discipline. Each commit is independently bisectable.

## Automated Verification

- `xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,id=DF4E4F27-281D-4734-9B41-D1746E89F939'` (iPhone 16, iOS 18.5) -> `** BUILD SUCCEEDED **`
- `xcodebuild test -scheme gamekit -only-testing:gamekitTests/MinesweeperBoardLayoutTests` -> 7/7 GREEN
- `xcodebuild test -scheme gamekit -only-testing:gamekitTests` -> `** TEST SUCCEEDED **` (full gamekit suite green; no regression on P3 ViewModelTests, P4 GameStats/StatsExporter, P5 PhaseTransitionTests, P6 AuthStore/CloudSyncStatusObserver)
- CellView byte-identical: `git diff HEAD~3..HEAD gamekit/gamekit/Games/Minesweeper/MinesweeperCellView.swift` returns 0 lines (Plan 06.1-03 explicitly does NOT touch the cell view — gesture composition lock preserved)

## Manual Verification (recipes documented; awaiting human execution)

These three manual recipes are required by the plan (Task 4 verification). The sequential automated executor cannot perform on-device gesture tests, multi-device simulator measurements, or theme-matrix visual checks — these are flagged for human execution before the plan is considered ship-ready.

### Recipe 1: 50-tap pinch-vs-tap regression (manual, iPhone 15 simulator or iPhone SE-class device)

> **Status:** Documented; awaiting execution.
>
> 1. Start a fresh Easy game. Verify the board fits comfortably without horizontal scroll.
> 2. Tap 25 different hidden cells using single-finger taps. Each MUST reveal (not flag, not zoom). Zero misfires.
> 3. Long-press 25 different hidden cells (>0.25s hold). Each MUST flag (not reveal, not zoom). Zero misfires.
> 4. Pinch-out two-finger gesture: verify board scales up smoothly (zoomScale > 1.0); release ends gesture cleanly.
> 5. Pinch-in two-finger gesture: verify board scales down smoothly to floor 0.8 (clamp); release ends gesture cleanly.
> 6. Single-finger tap during pinch-zoom-active state (after pinch ends): verify cell reveal still works at the new scale.
> 7. Restart the game (toolbar arrow): verify zoomScale is PRESERVED across restart (per CONTEXT Discretion #9 — in-memory @State).
> 8. Cold-relaunch the app: verify zoomScale resets to 1.0 (per CONTEXT D-14 — cold-launch only).
>
> **Expected outcome:** 50/50 single-finger gestures land their intended action; pinch composes cleanly; zero misfires. If ANY misfire occurs, BLOCK ship and re-research per RESEARCH §Confidence (gesture coexistence MEDIUM confidence requires empirical SC1 proof).

### Recipe 2: Auto-scale layout verification on multiple devices

> **Status:** Documented; awaiting execution.
>
> 1. iPhone 15 (390pt): Easy 9-col cells should be ~36-40pt; Medium 16-col ~18.6pt; Hard 30-col ~8pt -> clamps to 18pt floor -> horizontal scroll fallback engages.
> 2. iPhone SE 3rd gen (375pt): Easy ~34-36pt; Medium ~17-18pt -> just clamps; Hard clamps and scrolls.
> 3. iPhone 15 Pro Max (430pt): Easy ~40-44pt; Medium ~21pt; Hard ~10pt -> still clamps but barely.
> 4. iPad mini (768pt): all difficulties fit comfortably without floor-clamping.
>
> **Expected outcome:** No regression vs. pre-P6.1 fixed sizes on Easy/Medium; Hard's "ugly horizontal scroll" complaint resolved on standard iPhone widths via auto-scale + pinch-zoom escape hatch.

### Recipe 3: Theme-matrix verification (CLAUDE.md §8.12 — ship blocker)

> **Status:** Documented; awaiting execution.
>
> 1. Load Voltage preset (Loud category) at zoom 0.8x: verify cell glyphs (numbers, mines, flags) still legible at minimum scale.
> 2. Load Dracula preset (Moody category) at zoom 2.0x: verify cell glyphs render cleanly at maximum scale (no anti-aliasing artifacts blocking number readability).
> 3. Load Classic preset at zoom 1.0x: baseline check — board should look exactly as designed.
>
> **Expected outcome:** Pinch-zoom does not introduce per-preset legibility regression. CLAUDE.md §8.12 ship blocker if any preset fails.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Source-comment self-discipline] Rephrased prohibition comments to keep negative-greps clean**
- **Found during:** Tasks 2 and 3 acceptance verification (`grep -c GeometryReader` returned 1; `grep -c MagnificationGesture` returned 1; both targeted to be 0)
- **Issue:** Doc-comments narrating the prohibitions used the literal API names (`GeometryReader`, `MagnificationGesture`), which would have triggered adversarial negative-grep gates if any future plan added one.
- **Fix:** Rephrased the comments to describe the prohibition without naming the literal token. Pattern now applied for the FOURTH time in the project (P4 04-01 "no SwiftData unique-attribute decorator", P6 06-06 Quit-button, P6 06-07 SIWA handler, P6.1 06-03 here).
- **Files modified:** `gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift`
- **Commits:** Folded into `888432b` (Task 2) and `008505f` (Task 3)

### Commit-shape clarification (NOT a deviation, but worth noting)

The plan §Success Criteria suggested "Atomic commit per CLAUDE.md §8.10: `feat(06.1-03): mines board auto-scale + pinch-zoom — A11Y-05 (graduates A11Y-V2-02)`" (singular). The plan body, however, mandated a TDD RED -> GREEN gate (§Task 1 + §verification + project precedent across Plan 04-02/05-01/05-06/06-01/06-02 all locked in STATE.md). The four-commit shape (test/feat/feat/docs) honors both invariants: each commit is independently bisectable; the RED gate is git-log-visible; CLAUDE.md §8.10 "one feature per commit" is preserved (Tasks 2 and 3 are distinct features — auto-scale vs. pinch-zoom — and Task 4's REQUIREMENTS edit is doc work).

## Threat-mitigation Status

From Plan §threat_model:

| Threat ID | Status | Evidence |
|-----------|--------|----------|
| T-06.1-03-02 (Tampering — pinch misclassified as long-press) | mitigate | `.simultaneousGesture` on parent + `.exclusively(before:)` on child preserved byte-identical; CellView.swift unmodified. **Empirical SC1 proof requires manual recipe 1.** |
| T-06.1-03-03 (Tampering — auto-scale produces zero/negative cellSize) | mitigated | `max(Self.minCellSize, ...)` clamp in formula; `cols > 0` guard; first-render uses 18pt floor; tested via `cellSize_hard_320pt_iphoneSE_clamped` and `cellSize_easy_9col_390pt` |
| T-06.1-03-06 (DoS — layout-feedback loop) | mitigated | `.background(Color.clear.onGeometryChange...)` pattern (RESEARCH §Pattern 4); no `GeometryReader` in source (negative grep returns 0) |
| T-06.1-03-07 (DoS — re-render storm) | mitigated | SwiftUI coalesces same-frame writes; `Self.clampZoomScale` is O(1); no observed perf issues in build/test |
| T-06.1-03-09 (Cross-plan REQUIREMENTS conflict) | mitigated | A11Y-05 lives in Accessibility section; A11Y-V2-02 deletion is single-line; SHELL-05 (06.1-01) and MINES-12 (06.1-02) edits are in disjoint sections |
| T-06.1-03-10 (Pinch-zoom > 1.0 extends past edges) | accept | Existing scrollAxis fallback covers Hard; ScrollView clipping handles it gracefully |

## Self-Check: PASSED

Files exist:
- FOUND: `gamekit/gamekitTests/Games/Minesweeper/MinesweeperBoardLayoutTests.swift`
- FOUND: `gamekit/gamekit/Games/Minesweeper/MinesweeperBoardView.swift`
- FOUND: `.planning/REQUIREMENTS.md`

Commits exist:
- FOUND: `b2c37cb` (Task 1)
- FOUND: `888432b` (Task 2)
- FOUND: `008505f` (Task 3)
- FOUND: `f55f6b4` (Task 4)

Build + test verified:
- FOUND: `** BUILD SUCCEEDED **` for `xcodebuild build -scheme gamekit -destination 'platform=iOS Simulator,id=DF4E4F27-281D-4734-9B41-D1746E89F939'`
- FOUND: `** TEST SUCCEEDED **` for full gamekitTests suite (MinesweeperBoardLayoutTests 7/7 GREEN; no regression on existing suites)

REQUIREMENTS.md state verified:
- FOUND: A11Y-05 row in v1 Accessibility section
- FOUND: A11Y-V2-02 absent from v2 list (grep returns 0)
- FOUND: A11Y-05 in Traceability table
- FOUND: Coverage 36/36
- FOUND: graduation note in Notes section

Open items requiring human execution before plan close:
- Recipe 1 (50-tap pinch-vs-tap regression) — manual on-device gate
- Recipe 2 (multi-device auto-scale layout verification) — manual simulator pass
- Recipe 3 (theme-matrix legibility under Voltage / Dracula / Classic) — manual visual check (CLAUDE.md §8.12 ship blocker)
