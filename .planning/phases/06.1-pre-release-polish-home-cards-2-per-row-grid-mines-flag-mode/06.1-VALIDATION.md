---
phase: 06.1
slug: pre-release-polish-home-cards-2-per-row-grid-mines-flag-mode
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Generated from RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Swift Testing (Apple, no external package) |
| **Config file** | none — uses auto-discovery via `gamekitTests/` target |
| **Quick run command** | `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:gamekitTests/MinesweeperViewModelTests -quiet 2>&1 \| tail -5` |
| **Full suite command** | `xcodebuild test -scheme gamekit -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 \| tail -5` |
| **Estimated runtime** | ~30 seconds (quick) / ~90 seconds (full) on M-series Mac |

---

## Sampling Rate

- **After every task commit:** Run quick run command (`MinesweeperViewModelTests` only)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds (quick) / 90 seconds (full)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-03-W0 | 03 | 0 | A11Y-05 | T-06.1-03-cellSize / T-06.1-03-zoom-clamp | cellSize formula correctness; zoom clamped [0.8, 2.0] | unit | `xcodebuild test ... -only-testing:gamekitTests/MinesweeperBoardLayoutTests` | ❌ Wave 0 | ⬜ pending |
| 06.1-03-01 | 03 | 1 | A11Y-05 | — | auto-scale fits Easy/Medium without scroll on 390pt; floor 18pt | unit | `xcodebuild test ... -only-testing:gamekitTests/MinesweeperBoardLayoutTests/cellSizeFormula` | ✅ (after W0) | ⬜ pending |
| 06.1-03-02 | 03 | 1 | A11Y-05 | T-06.1-03-pinch-conflict | MagnifyGesture composes with cell `.exclusively(before:)` | manual | 50-tap zero-misfire recipe on iPhone SE | N/A | ⬜ pending |
| 06.1-03-03 | 03 | 1 | A11Y-05 | — | zoomScale persists across restart() | manual | pinch zoom, tap restart, observe scale | N/A | ⬜ pending |
| 06.1-02-W0 | 02 | 0 | MINES-12 | T-06.1-02-mode-routing | handleTap/handleLongPress route correctly per mode | unit | `xcodebuild test ... -only-testing:gamekitTests/MinesweeperViewModelTests/InteractionMode` | ❌ Wave 0 | ⬜ pending |
| 06.1-02-01 | 02 | 2 | MINES-12 | — | VM `interactionMode` + `toggleInteractionMode()` + `handleTap` + `handleLongPress` + `restart()` reset | unit | same | ✅ (after W0) | ⬜ pending |
| 06.1-02-02 | 02 | 2 | MINES-12 | T-06.1-02-gesture-misfire | gesture composition unchanged byte-identical; FAB tap toggles mode; FAB hidden post-terminal | unit + manual | tests + 40-tap manual recipe (10 reveal-tap, 10 reveal-longpress, 10 flag-tap, 10 flag-longpress) | N/A (manual) | ⬜ pending |
| 06.1-02-03 | 02 | 2 | MINES-12 | — | FAB renders correctly under Voltage + Dracula theme presets | manual | theme matrix legibility check (CLAUDE.md §8.12) | N/A | ⬜ pending |
| 06.1-01-01 | 01 | 2 | SHELL-05 | — | HomeView LazyVGrid 2-col with Mines + Upcoming square tiles | manual | visual check on iPhone 16 simulator | N/A | ⬜ pending |
| 06.1-01-02 | 01 | 2 | SHELL-05 | — | Upcoming sheet lists 8 placeholder games + tap fires ComingSoonOverlay | manual | tap each row in sheet | N/A | ⬜ pending |
| 06.1-01-03 | 01 | 2 | SHELL-05 | — | REQUIREMENTS.md SHELL-05 row added; Coverage incremented by 1 | grep | `grep -E "^\\| SHELL-05" .planning/REQUIREMENTS.md` | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

**Plan 06.1-02 Wave 0:**
- [ ] Add `@Suite("InteractionMode")` block to `gamekitTests/Games/Minesweeper/MinesweeperViewModelTests.swift` covering 7 tests:
  1. `handleTap` routes to `reveal` when `interactionMode == .reveal`
  2. `handleTap` routes to `toggleFlag` when `interactionMode == .flag`
  3. `handleLongPress` routes to `toggleFlag` when `interactionMode == .reveal` (opposite of mode)
  4. `handleLongPress` routes to `reveal` when `interactionMode == .flag` (opposite of mode)
  5. `toggleInteractionMode()` cycles `.reveal → .flag → .reveal`
  6. `restart()` resets `interactionMode` to `.reveal` AND `modeToggleCount` to 0
  7. `modeToggleCount` bumps by 1 per `toggleInteractionMode()` call (idempotency check: 0 → 1 → 2 → ...)
- [ ] Tests must pass RED (compile error: cannot find `interactionMode` / `handleTap` / `handleLongPress` / `toggleInteractionMode` / `modeToggleCount` in scope) before production code in Plan 06.1-02 Task 1 lands. TDD gate per project precedent (Plan 04-02 / 05-01 / 05-06 RED→GREEN sequence locked).

**Plan 06.1-03 Wave 0:**
- [ ] Create `gamekitTests/Games/Minesweeper/MinesweeperBoardLayoutTests.swift` (NEW file) covering:
  1. `cellSize` formula returns correct value for Easy 9-col @ 390pt (~ standard iPhone)
  2. `cellSize` formula returns correct value for Medium 16-col @ 390pt
  3. `cellSize` formula clamps to 18pt floor for Hard 30-col @ 390pt
  4. `cellSize` formula honors 18pt floor on iPhone SE 320pt for all difficulties
  5. Zoom scale clamping: input 0.5 → output 0.8 (lower clamp)
  6. Zoom scale clamping: input 2.5 → output 2.0 (upper clamp)
  7. Zoom scale clamping: input 1.3 → output 1.3 (in-range identity)
- [ ] Tests must pass RED before Plan 06.1-03 Task 1 production code lands. Pure-computation tests (no SwiftUI dependency); the formula and clamp can be extracted to file-private static funcs in `MinesweeperBoardView.swift` and tested directly.

**Plan 06.1-01 Wave 0:** None — HomeView is pure layout, no automated tests per CLAUDE.md §5 ("UI tests minimal unless explicitly requested"). Manual visual + 40-tap functional verification only.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 2-col grid layout renders correctly on iPhone 16 simulator | SHELL-05 | UI layout — no XCTest UITest infrastructure in project | Run app → land on Home → verify 2 square cards side-by-side (Mines + Upcoming) |
| Upcoming sheet displays 8 games with ComingSoonOverlay on tap | SHELL-05 | Sheet presentation + overlay timing | Tap Upcoming tile → verify sheet opens with 8 rows → tap each row → ComingSoonOverlay appears + dismisses 1.8s |
| FAB toggles between reveal/flag modes with icon + color swap | MINES-12 | Visual + animation | Start game → observe FAB icon `cursorarrow.click` accentPrimary tint → tap FAB → icon swaps to `flag.fill`, color swaps to danger |
| FAB hidden post-win/loss | MINES-12 | View-state visibility | Win or lose game → verify FAB has `.opacity(0)` and is non-interactive |
| 40-tap mode-routing zero-misfire | MINES-12 | Gesture-misfire regression test | 10 reveal-mode taps + 10 reveal-mode long-presses + 10 flag-mode taps + 10 flag-mode long-presses — all 40 must produce expected action with zero misfires |
| 50-tap MagnifyGesture coexistence | A11Y-05 | RESEARCH-MEDIUM-confidence empirical gate | iPhone SE-class device, follow 03-04-PLAN.md 50-tap recipe with `MagnifyGesture()` active; expect zero misfires; if any misfire occurs, BLOCK ship and re-research |
| Pinch-zoom scale persists across restart() within session | A11Y-05 | View state, not unit-testable | Pinch-zoom to 1.5x → tap Restart → verify board still rendered at 1.5x |
| Theme matrix legibility under Voltage + Dracula presets | SHELL-05 + MINES-12 + A11Y-05 (CLAUDE.md §8.12) | Per-preset visual check | Switch theme to Voltage → check Home grid + game FAB + board zoom; switch to Dracula → repeat. No legibility regressions on either preset |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (Plan 02 + Plan 03 Wave 0 covers MINES-12 + A11Y-05; Plan 01 has no automated verify per project §5 carve-out)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (Plan 01 has 3 tasks with manual-only verify — accepted per CLAUDE.md §5 UI tests minimal carve-out)
- [ ] Wave 0 covers all MISSING references (✅ Wave 0 task in Plan 02 + Plan 03)
- [ ] No watch-mode flags (✅ all `xcodebuild test` invocations are one-shot, not `-watch`)
- [ ] Feedback latency < 90s (✅ full suite ~90s)
- [ ] `nyquist_compliant: true` set in frontmatter (pending — flips after Wave 0 + plan checker re-verification both pass)

**Approval:** pending
